diff --git a/lib/screens/today/timeline_geometry.dart b/lib/screens/today/timeline_geometry.dart
index a54accf..948725b 100644
--- a/lib/screens/today/timeline_geometry.dart
+++ b/lib/screens/today/timeline_geometry.dart
@@ -9,6 +9,20 @@ import '../../utils/time_format.dart';
 /// `build()`'s single `nowDt` sample. This file only does arithmetic on
 /// already-known `int`s; it has no opinion about what time it is.
 
+/// SPIKE 001 ONLY (Phase 27, True Grid) — selects which live-row treatment
+/// this build compiles. `baseline` is the shipped behaviour; `a` and `b` are
+/// the two candidate answers to "a true grid gives the live row a 100dp slot,
+/// so what happens to the card?". Set with
+/// `flutter build web --dart-define=SPIKE_VARIANT=a`.
+///
+/// THIS CONSTANT IS NOT SHIPPING CODE. It exists so all three builds come
+/// from one working tree and the screenshots differ only in the variant.
+/// The chosen variant becomes unconditional; this is deleted with the spike.
+const String kSpikeVariant = String.fromEnvironment(
+  'SPIKE_VARIANT',
+  defaultValue: 'baseline',
+);
+
 /// Pixels per minute of the timeline's vertical scale (CAL-01).
 ///
 /// **PD-1 (26-01-PLAN.md "Locked planner decisions").** Supersedes
@@ -251,7 +265,13 @@ class TimelineGeometry {
     }
 
     double liveExtraPx = 0.0;
-    if (liveStartMinutes != null && liveEndMinutes != null) {
+    // SPIKE 001: both candidate variants delete the exception term outright —
+    // that single `if` is the whole of "make the grid true". Everything else
+    // either variant does is about what the live CARD becomes once its slot
+    // stops growing to fit it.
+    if (kSpikeVariant == 'baseline' &&
+        liveStartMinutes != null &&
+        liveEndMinutes != null) {
       final liveDurationPx =
           (liveEndMinutes - liveStartMinutes) * kPixelsPerMinute;
       final extra = kLiveRowReservedHeight - liveDurationPx;
diff --git a/lib/screens/today/today_screen.dart b/lib/screens/today/today_screen.dart
index d30b0b7..00ccb81 100644
--- a/lib/screens/today/today_screen.dart
+++ b/lib/screens/today/today_screen.dart
@@ -740,6 +740,35 @@ class _TodayScreenState extends State<TodayScreen> with WidgetsBindingObserver {
             child: SizedBox.shrink(),
           );
         }
+        if (isLive && kSpikeVariant != 'baseline') {
+          // SPIKE 001: the live row now takes a duration-exact slot like
+          // every other row — same ClipRect + OverflowBox treatment, no
+          // exception. Variant (a) hands LiveRowCard the slot height so it
+          // can pick a density tier that fits; variant (b) hands it nothing
+          // and lets the existing card be clipped, which is what "just delete
+          // the term" literally looks like.
+          final liveSlot = geometry.heightFor(start, chunk.durationMinutes);
+          return Positioned(
+            top: geometry.yFor(start),
+            left: 0,
+            right: 0,
+            height: liveSlot,
+            child: ClipRect(
+              child: OverflowBox(
+                alignment: Alignment.topCenter,
+                minHeight: 0,
+                maxHeight: double.infinity,
+                child: _buildLiveRow(
+                  context,
+                  chunk,
+                  nowState,
+                  secondsRemaining,
+                  slotHeight: kSpikeVariant == 'a' ? liveSlot : null,
+                ),
+              ),
+            ),
+          );
+        }
         if (isLive) {
           // NO height: the live row swells to its natural size within
           // TimelineGeometry's reserved liveExtraPx (PD-2/PD-10) — every
@@ -878,8 +907,12 @@ class _TodayScreenState extends State<TodayScreen> with WidgetsBindingObserver {
     BuildContext context,
     ScheduledChunk chunk,
     NowState nowState,
-    int? secondsRemaining,
-  ) {
+    int? secondsRemaining, {
+    // SPIKE 001 variant (a) only: the pixel height this row actually has, so
+    // the card can pick a density tier that fits it — the same slot-height-
+    // drives-density rule every other row already follows.
+    double? slotHeight,
+  }) {
     final title = _liveTitle(context, chunk);
     final start = chunk.displayStartMinutes;
     final end = start != null ? start + chunk.durationMinutes : null;
@@ -934,6 +967,7 @@ class _TodayScreenState extends State<TodayScreen> with WidgetsBindingObserver {
       progress: progress,
       nextLine: nextLine,
       showActions: chunk.chunkType == ChunkType.work,
+      slotHeight: slotHeight,
     );
   }
 
diff --git a/lib/screens/today/widgets/live_row_card.dart b/lib/screens/today/widgets/live_row_card.dart
index 1e5f3f8..298377b 100644
--- a/lib/screens/today/widgets/live_row_card.dart
+++ b/lib/screens/today/widgets/live_row_card.dart
@@ -25,6 +25,7 @@ class LiveRowCard extends StatelessWidget {
     required this.progress,
     this.nextLine,
     this.showActions = true,
+    this.slotHeight,
   });
 
   /// The chunk this card acts on — Complete/Skip always target exactly this
@@ -51,12 +52,30 @@ class LiveRowCard extends StatelessWidget {
   /// hides both buttons entirely.
   final bool showActions;
 
+  /// SPIKE 001 variant (a) ONLY — the duration-exact pixel height this row has
+  /// under a true grid. Non-null switches this card into the slot-fitting
+  /// layouts below; null keeps the shipped swelling layout. Deleted with the
+  /// spike once a variant is chosen.
+  final double? slotHeight;
+
   @override
   Widget build(BuildContext context) {
     final theme = Theme.of(context);
     final colorScheme = theme.colorScheme;
     final clampedProgress = progress.clamp(0.0, 1.0);
 
+    final slot = slotHeight;
+    if (slot != null) {
+      // Same rule every other row already follows: slot height picks the
+      // density tier. A standard 25-minute chunk gets 100dp and can carry the
+      // compact tier; a live 5-minute break gets 20dp and can only carry one
+      // line. There is no height at which the shipped layout fits, which is
+      // the whole point of the variant.
+      return slot >= _kCompactLiveMinHeight
+          ? _buildCompact(context, theme, colorScheme, clampedProgress)
+          : _buildSingleLine(context, theme, colorScheme);
+    }
+
     // "Let now break the grid" (22-UI-SPEC.md "The live row", amended
     // 2026-08-08 after UAT, then three times more as the now-line landed).
     // The horizontal half of that treatment is gone: this row is flush with
@@ -176,4 +195,177 @@ class LiveRowCard extends StatelessWidget {
       ),
     );
   }
+
+  // ── SPIKE 001 variant (a) layouts ──────────────────────────────────────
+  // Everything below is spike-only and goes away with the spike.
+
+  /// Compact tier: title + countdown + progress + icon-only actions, sized to
+  /// clear a 25-minute chunk's 100dp slot. Keeps every affordance the shipped
+  /// card has except the "Next ·" line, which a true grid makes redundant —
+  /// the next row is literally drawn immediately below this one.
+  Widget _buildCompact(
+    BuildContext context,
+    ThemeData theme,
+    ColorScheme colorScheme,
+    double clampedProgress,
+  ) {
+    return Card(
+      margin: const EdgeInsets.only(
+        top: 2,
+        bottom: 2,
+        left: kCardLeftInset,
+        right: kTimelineRowInset,
+      ),
+      color: colorScheme.primaryContainer,
+      elevation: 6,
+      shadowColor: colorScheme.shadow,
+      shape: const RoundedRectangleBorder(borderRadius: BorderRadius.zero),
+      child: Padding(
+        padding: const EdgeInsets.fromLTRB(14, 8, 6, 8),
+        child: Column(
+          crossAxisAlignment: CrossAxisAlignment.start,
+          mainAxisSize: MainAxisSize.min,
+          children: [
+            Row(
+              crossAxisAlignment: CrossAxisAlignment.center,
+              children: [
+                Expanded(
+                  child: Column(
+                    crossAxisAlignment: CrossAxisAlignment.start,
+                    mainAxisSize: MainAxisSize.min,
+                    children: [
+                      Text(
+                        kicker.toUpperCase(),
+                        maxLines: 1,
+                        overflow: TextOverflow.ellipsis,
+                        style: theme.textTheme.labelSmall?.copyWith(
+                          color: colorScheme.onPrimaryContainer.withValues(
+                            alpha: 0.72,
+                          ),
+                          fontWeight: FontWeight.w600,
+                          letterSpacing: 0.5,
+                        ),
+                      ),
+                      Text(
+                        title,
+                        maxLines: 1,
+                        overflow: TextOverflow.ellipsis,
+                        style: theme.textTheme.titleMedium?.copyWith(
+                          fontWeight: FontWeight.w700,
+                          color: colorScheme.onPrimaryContainer,
+                        ),
+                      ),
+                    ],
+                  ),
+                ),
+                if (showActions) ...[
+                  IconButton(
+                    tooltip: 'Complete',
+                    visualDensity: VisualDensity.compact,
+                    constraints: const BoxConstraints.tightFor(
+                      width: 36,
+                      height: 36,
+                    ),
+                    padding: EdgeInsets.zero,
+                    icon: const Icon(Icons.check_circle_outline),
+                    color: colorScheme.primary,
+                    onPressed: () =>
+                        context.read<ScheduleNotifier>().markComplete(chunkId),
+                  ),
+                  IconButton(
+                    tooltip: 'Skip',
+                    visualDensity: VisualDensity.compact,
+                    constraints: const BoxConstraints.tightFor(
+                      width: 36,
+                      height: 36,
+                    ),
+                    padding: EdgeInsets.zero,
+                    icon: const Icon(Icons.skip_next_outlined),
+                    color: colorScheme.error,
+                    onPressed: () =>
+                        context.read<ScheduleNotifier>().markSkipped(chunkId),
+                  ),
+                ],
+              ],
+            ),
+            const SizedBox(height: 4),
+            Padding(
+              padding: const EdgeInsets.only(right: 8),
+              child: Text(
+                remainingLabel,
+                maxLines: 1,
+                overflow: TextOverflow.ellipsis,
+                style: theme.textTheme.bodySmall?.copyWith(
+                  color: colorScheme.onPrimaryContainer.withValues(alpha: 0.82),
+                  fontFeatures: const [FontFeature.tabularFigures()],
+                ),
+              ),
+            ),
+            const SizedBox(height: 6),
+            Padding(
+              padding: const EdgeInsets.only(right: 8),
+              child: ClipRRect(
+                borderRadius: BorderRadius.circular(3),
+                child: LinearProgressIndicator(
+                  value: clampedProgress,
+                  minHeight: 4,
+                  valueColor: AlwaysStoppedAnimation<Color>(
+                    colorScheme.primary,
+                  ),
+                  backgroundColor: colorScheme.onPrimaryContainer.withValues(
+                    alpha: 0.16,
+                  ),
+                ),
+              ),
+            ),
+          ],
+        ),
+      ),
+    );
+  }
+
+  /// Single-line tier: for a slot too short for anything else — a live
+  /// 5-minute break has 20dp. Fill and the now-line carry the emphasis.
+  Widget _buildSingleLine(
+    BuildContext context,
+    ThemeData theme,
+    ColorScheme colorScheme,
+  ) {
+    return Card(
+      margin: const EdgeInsets.only(
+        top: 1,
+        bottom: 1,
+        left: kCardLeftInset,
+        right: kTimelineRowInset,
+      ),
+      color: colorScheme.primaryContainer,
+      elevation: 4,
+      shadowColor: colorScheme.shadow,
+      shape: const RoundedRectangleBorder(borderRadius: BorderRadius.zero),
+      child: Padding(
+        padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 2),
+        child: Row(
+          children: [
+            Expanded(
+              child: Text(
+                '$title · $remainingLabel',
+                maxLines: 1,
+                overflow: TextOverflow.ellipsis,
+                style: theme.textTheme.labelMedium?.copyWith(
+                  fontWeight: FontWeight.w700,
+                  color: colorScheme.onPrimaryContainer,
+                ),
+              ),
+            ),
+          ],
+        ),
+      ),
+    );
+  }
 }
+
+/// SPIKE 001 variant (a): the slot height at or above which the compact tier
+/// fits. Derived to be measured in a real browser, not asserted — the compact
+/// tier's own natural height is what this has to clear, and that number comes
+/// out of the screenshots, not out of this file.
+const double _kCompactLiveMinHeight = 60.0;
