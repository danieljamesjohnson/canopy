---
phase: 02-goals-and-commitments
plan: 06
type: execute
wave: 5
depends_on: [02-05]
files_modified: []
autonomous: false
requirements: [goal-types, commitment-blocks]

must_haves:
  truths:
    - "User can complete onboarding start-to-finish including skipping all optional screens in under 90 seconds"
    - "User can create a goal of each type (time-target, outcome, habit) and see it in the goals list with correct visual type indicator"
    - "User can create a CommitmentBlock and see it persist across app restart"
    - "User can archive a goal — it disappears from main list and appears in Archived screen"
    - "Onboarding-complete flag persists — onboarding does not re-appear after completion"
    - "No internal enum labels ('timeTarget', 'outcome', 'habit') appear anywhere in the UI"
  artifacts:
    - path: "lib/screens/goals/goals_screen.dart"
      provides: "Goals list screen"
    - path: "lib/screens/onboarding/onboarding_screen.dart"
      provides: "3-screen onboarding flow"
    - path: "lib/screens/commitments/commitments_screen.dart"
      provides: "Commitments list screen"
  key_links:
    - from: "lib/screens/onboarding/onboarding_screen.dart"
      to: "lib/providers/settings_notifier.dart"
      via: "setOnboardingComplete(true) called last after all saves"
      pattern: "setOnboardingComplete"
---

<objective>
Human verification of the complete Phase 2 feature set on a running device or simulator.

Purpose: Automated analysis confirms code compiles; this checkpoint confirms the user experience flows correctly end-to-end, including the 90-second onboarding constraint and cross-restart persistence.
Output: User approval or specific issues to fix.
</objective>

<execution_context>
@C:/Users/Dan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final analysis and test run</name>
  <files></files>
  <action>
Run the full analysis and test suite to confirm zero issues before the human checkpoint:

```bash
flutter analyze
flutter test
```

If any issues exist, fix them before presenting the checkpoint.

Also run `flutter run -d <device>` and confirm the app launches without console errors.
  </action>
  <verify>
    <automated>flutter analyze && flutter test</automated>
  </verify>
  <done>flutter analyze zero issues; flutter test all passing; app launches without errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Phase 2 flows end-to-end on device</name>
  <files></files>
  <action>Verify the complete Phase 2 feature set by running through all user flows on a physical or simulated device.</action>
  <what-built>
Complete Phase 2 feature set:
- Goal model expanded with 8 new fields; TypeAdapter regenerated; schemaVersion bumped to 2
- GoalsNotifier (load, save, archive, reorder), CommitmentsNotifier (load, save, delete), SettingsNotifier (persisted to Hive AppSettings)
- Goals list screen: three plain-language sections, colored-border cards, type icons, FAB, overflow menu
- Goal form sheet: DraggableScrollableSheet, type picker, name field, type-specific fields, archive action
- Archived goals screen
- CommitmentsScreen: card list with delete; CommitmentFormSheet: day chips + time pickers
- Three-screen onboarding: GoalTypePicker, commitment block form, habit setup; skip affordances; step dots; no back nav
  </what-built>
  <how-to-verify>
Run the app on Android, iOS simulator, or Web. Complete each check:

**Onboarding flow (under 90 seconds):**
1. Fresh launch (or clear app data) — confirm onboarding appears
2. Screen 1: select any goal type, enter a name, tap Continue
3. Screen 2: tap Skip — confirm it's instant (no confirmation dialog)
4. Screen 3: tap Skip — confirm instant skip
5. Confirm you land on the Goals screen and see the goal you created
6. Hot restart — confirm onboarding does NOT reappear

**Goals list:**
7. Tap FAB "Add goal" — confirm DraggableScrollableSheet opens
8. Select each goal type — confirm plain-language descriptions (no "timeTarget", "outcome", "habit" text)
9. Create one goal of each type — confirm each appears in correct section with correct icon and left-border color
10. Tap a goal card — confirm edit sheet opens pre-populated
11. From edit sheet: tap Archive — confirm goal disappears from main list
12. Overflow menu → "View archived" — confirm archived goal appears there

**Commitment blocks:**
13. Overflow menu → "Commitment blocks" — confirm navigation to CommitmentsScreen
14. Tap FAB — add a commitment block (name + days + times), tap Save
15. Confirm card shows name, day abbreviations, and time range (e.g. "9am – 5pm · Mon–Fri")
16. Hot restart — confirm commitment block still appears (persistence check)
17. Delete a commitment block — confirm it disappears
  </how-to-verify>
  <verify>
    <manual>All 17 verification steps completed without issues; user types "approved"</manual>
  </verify>
  <done>User confirms all Phase 2 acceptance criteria pass on a running device</done>
  <resume-signal>Type "approved" to proceed, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 2 acceptance criteria (from ROADMAP.md):
1. User can create a goal of each type through the plain-language picker and the goal appears with a visual type indicator ✓
2. User can create a CommitmentBlock with name, days of week, and time window, and it persists across app restarts ✓
3. User can edit and archive any goal; archived goals accessible in "Archived" section ✓
4. Three-screen onboarding can be completed start-to-finish including skipping in under 90 seconds ✓
5. Completing or skipping onboarding sets the onboarding-complete flag so the flow does not re-appear ✓
</verification>

<success_criteria>
All five ROADMAP acceptance criteria for Phase 2 are confirmed by user on a running device. Phase 3 (Schedule Generation) can begin.
</success_criteria>

<output>
After completion, create `.planning/phases/02-goals-and-commitments/02-06-SUMMARY.md`
</output>
