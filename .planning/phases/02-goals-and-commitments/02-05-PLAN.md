---
phase: 02-goals-and-commitments
plan: 05
type: execute
wave: 4
depends_on: [02-03, 02-04]
files_modified:
  - lib/screens/onboarding/onboarding_screen.dart
autonomous: true
requirements: [goal-types, commitment-blocks]

must_haves:
  truths:
    - "Onboarding completes in under 90 seconds including all optional screens"
    - "Screen 1 collects first goal using the same GoalTypePicker vertical card stack as the Goals screen"
    - "Screen 2 (commitment block) and Screen 3 (first habit) are skippable with a single prominent tap — no confirmation dialog"
    - "Step indicator dots at top show current position; no back navigation possible during onboarding"
    - "Completing or skipping all screens navigates to /goals (not /home) so user sees their created goal"
    - "setOnboardingComplete(true) is called last, after all saves are awaited, to prevent router redirect race"
    - "onboardingComplete flag persists to Hive — onboarding does not re-appear on next app launch"
  artifacts:
    - path: "lib/screens/onboarding/onboarding_screen.dart"
      provides: "3-screen PageView onboarding with step dots, no back nav"
      min_lines: 150
  key_links:
    - from: "lib/screens/onboarding/onboarding_screen.dart"
      to: "lib/providers/settings_notifier.dart"
      via: "context.read<SettingsNotifier>().setOnboardingComplete(true)"
      pattern: "setOnboardingComplete"
    - from: "lib/screens/onboarding/onboarding_screen.dart"
      to: "lib/screens/goals/widgets/goal_type_picker.dart"
      via: "GoalTypePicker widget reused on Screen 1"
      pattern: "GoalTypePicker"
---

<objective>
Build the three-screen onboarding flow as a PageView with step indicator dots, no back navigation, and prominent skip affordances on Screens 2 and 3.

Purpose: Onboarding seeds the app with the user's first goal and optionally first commitment block and first habit. It must complete in under 90 seconds and land on the Goals screen so the user sees immediate value. The flow reuses GoalTypePicker from Plan 03 for consistency.
Output: Fully functional onboarding_screen.dart replacing the Phase 1 stub.
</objective>

<execution_context>
@C:/Users/Dan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-goals-and-commitments/02-03-SUMMARY.md
@.planning/phases/02-goals-and-commitments/02-04-SUMMARY.md
@.planning/phases/02-goals-and-commitments/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build three-screen onboarding flow</name>
  <files>
    lib/screens/onboarding/onboarding_screen.dart
  </files>
  <action>
Replace the Phase 1 stub with a full `OnboardingScreen extends StatefulWidget`.

**State fields:**
- `final _controller = PageController();`
- `int _currentPage = 0;`
- `GoalType? _screen1Type;`
- `final _screen1NameController = TextEditingController();`
- `CommitmentBlock? _screen2Block;` (null if Screen 2 skipped)
- `Goal? _screen3Habit;` (null if Screen 3 skipped)
- `bool _isSaving = false;` (prevent double-tap on final complete button)

**Navigation methods:**
- `_nextPage()`: `_controller.nextPage(duration: Duration(milliseconds: 350), curve: Curves.easeInOut)`
- `_skipToComplete()`: calls `_completeOnboarding()` directly — instant, no animation, no confirmation
- `_completeOnboarding()`: async, sets `_isSaving = true`. Awaits in order: (1) if Screen 1 goal filled: `context.read<GoalsNotifier>().saveGoal(goal)` where goal = `Goal(name: _screen1NameController.text.trim(), goalTypeIndex: _screen1Type!.index, color: context.read<GoalsNotifier>().autoColor())`. (2) if Screen 2 block filled: `context.read<CommitmentsNotifier>().saveBlock(_screen2Block!)`. (3) if Screen 3 habit filled: `context.read<GoalsNotifier>().saveGoal(_screen3Habit!)`. (4) await `context.read<SettingsNotifier>().setOnboardingComplete(true)`. The setOnboardingComplete call is ALWAYS last — it triggers the router redirect. Do not call it before saves complete.

**Build method:** `Scaffold(backgroundColor: colorScheme.surface, body: SafeArea(child: Column([_StepDots(currentPage: _currentPage, totalPages: 3), Expanded(child: PageView(controller: _controller, physics: NeverScrollableScrollPhysics(), onPageChanged: (p) => setState(() => _currentPage = p), children: [_Screen1(), _Screen2(), _Screen3()]))])))` — no AppBar, no back button.

**_StepDots widget** (private to this file): Row of 3 `AnimatedContainer` dots centered, margin EdgeInsets.symmetric(horizontal: 4, vertical: 16). Active dot: width 24, height 8, color `colorScheme.primary`. Inactive: width 8, height 8, color `colorScheme.outlineVariant`. BorderRadius.circular(4). Duration 200ms.

**_Screen1** (private StatefulWidget — needs its own state for GoalTypePicker selection):
- Conversational tone header: "What's one thing you most want to make time for?"
- Subtext: "We'll build your schedule around it."
- GoalTypePicker (from goals/widgets/goal_type_picker.dart) for type selection
- SizedBox(height: 16)
- TextField for goal name (hint: "Give it a name", autofocus: false — let user tap)
- Spacer
- ElevatedButton "Continue" — disabled if name empty or type not selected. On tap: set parent state `_screen1Type` and `_screen1NameController`, then call `_nextPage()`. Use a callback: the Screen1 widget calls `widget.onNext(selectedType, nameController.text)` callback, and the parent stores the values before calling `_nextPage()`.

Actually: since Screen 1 is a private StatefulWidget, use a simpler approach — pass `_screen1NameController` and a `ValueChanged<GoalType>` callback from the parent. The parent `OnboardingScreen`'s `_screen1NameController` is shared directly with the Screen 1 text field. The type selection callback sets `_screen1Type` on the parent.

**_Screen2** (can be StatefulWidget or StatelessWidget with callbacks):
- Header: "Do you have a regular job or fixed commitment?"
- Subtext: "We'll always schedule around it, whatever your mood."
- A simplified inline CommitmentBlock form: name TextField + day chips (S M T W T F S FilterChips) + start/end time InkWell tiles (same pattern as CommitmentFormSheet). State managed locally.
- Bottom: Row with `TextButton("Skip", onPressed: widget.onSkip)` (prominent, full-width or at least clearly visible) and `ElevatedButton("Continue", onPressed: widget.onNext)` where onNext passes the created block or null.
- Skip text button style: use `TextButton` with normal text size — not a small link. Place it in a Row: `Row(children: [Expanded(child: TextButton("Skip", ...)), SizedBox(8), ElevatedButton("Add it", ...)])`.

**_Screen3** (similar pattern):
- Header: "Is there anything you do every morning we should protect?"
- Subtext: "Like exercise, journaling, or coffee time."
- Two fields: habit name TextField + frequency selector (simple Row of ChoiceChip: "Daily", "Weekdays", "3× / week", "Custom"). For simplicity in onboarding, just offer "Daily" (frequencyPerWeek=7) and "Weekdays" (frequencyPerWeek=5) and "3× / week" (frequencyPerWeek=3). Store selection in local int state, default "Daily".
- Bottom: same Skip+Continue row pattern as Screen 2. Complete button says "Let's go" (not "Continue"). onComplete callback passes habit Goal or null.

**Router note:** After `setOnboardingComplete(true)`, the go_router redirect fires: `onboardingDone == true && onOnboarding == true → redirect to '/home'`. The CONTEXT.md decision says to land on Goals screen. Update router.dart redirect to navigate to `/goals` instead of `/home` when onboarding is just completed. Change: `if (onboardingDone && onOnboarding) return '/goals';`
  </action>
  <verify>
    <automated>flutter analyze lib/screens/onboarding/onboarding_screen.dart lib/router.dart</automated>
    <manual>Confirm PageView uses NeverScrollableScrollPhysics; setOnboardingComplete is called last in _completeOnboarding; skip buttons are prominent (TextButton, not small link)</manual>
  </verify>
  <done>onboarding_screen.dart compiles; router redirect after onboarding goes to /goals; flutter analyze zero issues; no back navigation possible (no AppBar, NeverScrollableScrollPhysics)</done>
</task>

</tasks>

<verification>
After task completion:
- `flutter analyze` zero issues
- `flutter test` passes
- OnboardingScreen has 3 PageView children
- PageView physics: NeverScrollableScrollPhysics (verified by code inspection)
- `setOnboardingComplete(true)` appears exactly once, at the end of `_completeOnboarding()`, after all await save calls
- Router redirect `onboardingDone && onOnboarding` returns `/goals` not `/home`
</verification>

<success_criteria>
A fresh user sees onboarding, creates their first goal on Screen 1, optionally adds a commitment block on Screen 2 and a habit on Screen 3 (or skips both), completes onboarding in under 90 seconds, and lands on the Goals screen seeing their created goal. Onboarding does not re-appear on subsequent launches.
</success_criteria>

<output>
After completion, create `.planning/phases/02-goals-and-commitments/02-05-SUMMARY.md`
</output>
