---
phase: 12-home-as-landing-schedule-as-plan
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - lib/screens/home/widgets/active_chunk_card.dart
  - lib/screens/home/home_screen.dart
  - test/screens/active_chunk_card_test.dart
autonomous: true
requirements: [NAV-02]
must_haves:
  truths:
    - "Home leads with a 'Now' section showing the current chunk (goal name, clock-time range, actions)"
    - "Home shows a compact 'Next' section for the second unresolved work chunk"
    - "Home exposes a 'See full schedule' link that navigates to /schedule"
    - "When all work chunks are resolved, Home shows 'All done today!' in the Now slot"
    - "ActiveChunkCard exposes always-visible Complete and Skip buttons (no hover)"
  artifacts:
    - path: "lib/screens/home/widgets/active_chunk_card.dart"
      provides: "ActiveChunkCard widget — current chunk with actions"
      contains: "class ActiveChunkCard"
    - path: "lib/screens/home/home_screen.dart"
      provides: "Now/Next sections + See full schedule link (NAV-02)"
      contains: "ActiveChunkCard"
    - path: "test/screens/active_chunk_card_test.dart"
      provides: "NAV-02 widget test (Now section + See full schedule)"
  key_links:
    - from: "lib/screens/home/widgets/active_chunk_card.dart"
      to: "ScheduleNotifier.markComplete / markSkipped"
      via: "context.read in button onPressed"
      pattern: "markComplete|markSkipped"
    - from: "lib/screens/home/home_screen.dart"
      to: "/schedule"
      via: "context.go in See full schedule TextButton"
      pattern: "context.go\\('/schedule'\\)"
    - from: "lib/screens/home/widgets/active_chunk_card.dart"
      to: "ScheduledChunk.displayStartMinutes"
      via: "clock-time range rendering"
      pattern: "displayStartMinutes"
---

<objective>
Make Home the live-day landing surface (NAV-02): a "Now" section with an `ActiveChunkCard` (current chunk, clock-time range, always-visible Complete/Skip), a compact "Next" section for the following chunk, and a "See full schedule" link to `/schedule`. This plan owns only `home/` files; the schedule/router work is plan 12-02 (file-disjoint, same wave). Both depend only on the persisted `displayStartMinutes` from 12-01.

Purpose: The dogfood found Home merely duplicating Schedule as a static "Up next" card with no actions, forcing the user to Schedule just to answer "what am I doing now." This plan splits current vs. next, adds inline actions, and makes the Home↔Schedule relationship explicit via the link.

Output: new `ActiveChunkCard` widget, refactored `HomeScreen` Now/Next layout with the "See full schedule" link, and a NAV-02 widget test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-home-as-landing-schedule-as-plan/12-RESEARCH.md
@.planning/phases/12-home-as-landing-schedule-as-plan/12-PATTERNS.md
@.planning/phases/12-home-as-landing-schedule-as-plan/12-UI-SPEC.md
@.planning/phases/12-home-as-landing-schedule-as-plan/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ActiveChunkCard widget</name>
  <read_first>
    - lib/screens/home/widgets/end_of_day_card.dart (role analog — StatelessWidget + Card + colorScheme in the same directory)
    - lib/screens/schedule/widgets/chunk_card.dart (source of _formatTimeRange/_formatMinutes/hexToColor and the Card + left-bar + action-row shape; goal color/name lookup pattern)
    - lib/data/models/scheduled_chunk.dart (displayStartMinutes getter from 12-01)
    - lib/providers/schedule_notifier.dart (markComplete / markSkipped)
    - lib/providers/goals_notifier.dart (goals list for color/name lookup)
    - .planning/phases/12-home-as-landing-schedule-as-plan/12-PATTERNS.md (§ active_chunk_card.dart — full widget source incl. lookups, "Now" badge, action row)
    - .planning/phases/12-home-as-landing-schedule-as-plan/12-UI-SPEC.md (§ Component Inventory #3 ActiveChunkCard; § Chunk Action Contract; § Accessibility Contract)
  </read_first>
  <behavior>
    - ActiveChunkCard is a StatelessWidget taking a required ScheduledChunk
    - Renders the goal name (titleMedium w600), a clock-time range "START – END · N min" when displayStartMinutes is set, else the "N min" duration fallback
    - Renders a left color bar (4dp) using the goal color, falling back to colorScheme.primary
    - Renders a "Now" badge
    - Renders always-visible FilledButton.icon "Complete" and OutlinedButton.icon "Skip" (tooltips matching labels); Complete → markComplete(chunk.id), Skip → markSkipped(chunk.id)
    - Uses only Theme.of(context).colorScheme colors (no hardcoded colors)
  </behavior>
  <action>
    Create lib/screens/home/widgets/active_chunk_card.dart following the full source in 12-PATTERNS.md § active_chunk_card.dart. Reuse `_formatTimeRange`/`_formatMinutes`/`hexToColor` rather than re-implementing AM/PM formatting (RESEARCH "Don't Hand-Roll"). Per 12-PATTERNS.md, these currently live as top-level functions in chunk_card.dart; to avoid duplication, extract them into a shared `lib/utils/time_format.dart` (exposing `formatMinutes`, `formatTimeRange`, `hexToColor`) and import that from both `chunk_card.dart` (12-02 already touches it; this plan does NOT modify chunk_card.dart — instead import the shared util here and, if extraction would require editing chunk_card.dart, fall back to duplicating the two tiny formatters locally in active_chunk_card.dart to preserve file-disjoint parallelism with plan 12-02). Copy the goal color/name lookup via `context.read<GoalsNotifier>().goals`. Build the Card with the 4dp left color bar, the goal-name + clock-time column, the "Now" badge (primary background, onPrimary "Now" label), and the always-visible action row: FilledButton.icon (Icons.check_circle_outline, "Complete", tooltip, compact, → markComplete) and OutlinedButton.icon (Icons.skip_next_outlined, "Skip", tooltip, compact, error-colored, → markSkipped). Ensure each button meets the 44dp minimum tap target (UI-SPEC Accessibility).

    Note on parallelism: to keep this plan's files disjoint from plan 12-02 (which owns chunk_card.dart), prefer the local-duplication fallback for the two formatters unless a shared util can be created without editing chunk_card.dart. Document the choice in the SUMMARY.
  </action>
  <verify>
    <automated>/home/dan/development/flutter/bin/flutter analyze lib/screens/home/widgets/active_chunk_card.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'class ActiveChunkCard' lib/screens/home/widgets/active_chunk_card.dart` returns 1
    - `grep -c 'FilledButton.icon' lib/screens/home/widgets/active_chunk_card.dart` returns at least 1
    - `grep -c 'OutlinedButton.icon' lib/screens/home/widgets/active_chunk_card.dart` returns at least 1
    - `grep -c 'displayStartMinutes' lib/screens/home/widgets/active_chunk_card.dart` returns at least 1
    - `grep -c 'Colors\\.' lib/screens/home/widgets/active_chunk_card.dart` returns 0 (no hardcoded colors except grey-resolved states already provided by the analog, which use colorScheme)
    - `flutter analyze` reports zero issues on the new file
  </acceptance_criteria>
  <done>ActiveChunkCard renders the current chunk with goal name, clock-time range, "Now" badge, and always-visible Complete/Skip buttons wired to ScheduleNotifier, using only theme colors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor HomeScreen to Now/Next + See full schedule link (NAV-02) + widget test</name>
  <read_first>
    - lib/screens/home/home_screen.dart (the file being refactored — nextChunk computation ~lines 95-100; "Up next" label ~line 149; all-done text ~lines 156-163; _lookupGoalName ~line 247; hasScheduleToday branch ~line 84)
    - lib/screens/home/widgets/active_chunk_card.dart (the new widget from Task 1)
    - .planning/phases/12-home-as-landing-schedule-as-plan/12-PATTERNS.md (§ home_screen.dart — current/next split, section labels, See full schedule link, all-done state)
    - .planning/phases/12-home-as-landing-schedule-as-plan/12-UI-SPEC.md (§ Component Inventory #1 HomeScreen layout; § States and Transitions HomeScreen states; § Navigation Contract)
    - test/screens/home_screen_breathing_pulse_test.dart (analog for pumping HomeScreen with providers in a test)
  </read_first>
  <behavior>
    - When a schedule exists with unresolved work: Home shows a "Now" label + ActiveChunkCard(currentChunk), a "Next" label + compact next-chunk row, the mood row (relocated below Now/Next), and a "See full schedule" TextButton
    - currentChunk = first unresolved work chunk; nextChunk = second unresolved work chunk
    - When all work chunks are resolved (currentChunk == null): Home shows "All done today!" in the Now slot
    - When no schedule exists: the existing empty state ("Start your day" CTA) is unchanged
    - "See full schedule" calls context.go('/schedule')
  </behavior>
  <action>
    In lib/screens/home/home_screen.dart refactor per 12-PATTERNS.md § home_screen.dart: replace the single `nextChunk` computation with `unresolvedWork` (filter chunkType==work && !isCompleted && !isSkipped, toList), then `currentChunk = unresolvedWork.firstOrNull` and `nextChunk = unresolvedWork.length > 1 ? unresolvedWork[1] : null`. Render a "Now" section label (labelMedium, onSurfaceVariant, letterSpacing 0.8 — mirroring the existing "Up next" label) followed by `ActiveChunkCard(chunk: currentChunk)` when currentChunk != null, else the existing "All done today!" text. Render a "Next" section label + a compact row for nextChunk (goal name via existing `_lookupGoalName` + clock-time range + duration) when nextChunk != null. Relocate the existing mood row below the Now/Next sections (same widget code, new position). Add a "See full schedule" TextButton calling `context.go('/schedule')` per NAV-02 / Navigation Contract. Leave the `!hasScheduleToday` empty-state branch and ReviewBanner/EndOfDayCard/ScheduleProgressBar wiring unchanged. Import the new ActiveChunkCard widget.

    Create test/screens/active_chunk_card_test.dart covering NAV-02: pump HomeScreen (or ActiveChunkCard directly with the necessary GoalsNotifier/ScheduleNotifier providers, mirroring home_screen_breathing_pulse_test.dart) with a schedule containing at least two unresolved work chunks; assert the "Now" label and ActiveChunkCard render, the "Next" label renders, and a "See full schedule" widget is found. Add an all-resolved case asserting "All done today!" renders and no ActiveChunkCard is shown. Do not add new packages.
  </action>
  <verify>
    <automated>/home/dan/development/flutter/bin/flutter test test/screens/active_chunk_card_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ActiveChunkCard' lib/screens/home/home_screen.dart` returns at least 2 (import + usage)
    - `grep -c "context.go('/schedule')" lib/screens/home/home_screen.dart` returns at least 1
    - `grep -c 'See full schedule' lib/screens/home/home_screen.dart` returns at least 1
    - `grep -c 'currentChunk' lib/screens/home/home_screen.dart` returns at least 2
    - `flutter test test/screens/active_chunk_card_test.dart` passes
    - Full suite `/home/dan/development/flutter/bin/flutter test` is green
  </acceptance_criteria>
  <done>HomeScreen leads with Now/Next sections + actions + a working "See full schedule" link, shows "All done today!" when resolved, keeps the empty state unchanged, and the NAV-02 widget test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user tap (Complete/Skip on Home) → ScheduleNotifier mutation | Home actions mutate persisted schedule state via the same notifier methods as Schedule |
| "See full schedule" tap → go_router navigation | Branch-switch navigation to /schedule |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering | ActiveChunkCard markComplete/markSkipped | accept | Reuses the existing idempotent, guarded notifier methods (chunk-null/already-resolved guard per RESEARCH Security Domain); no new mutation path beyond what ChunkCard/swipe already use |
| T-12-06 | Information Disclosure | goal name/color rendered on Home | accept | Goal name is user's own data on a local single-user device; no network or cross-user surface |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No packages added this phase; ActiveChunkCard built from Material 3 primitives already in pubspec |

Spoofing, Repudiation, DoS, and Elevation of Privilege are N/A — local-first single-user Home refactor with no auth, session, or network surface.
</threat_model>

<verification>
- `flutter analyze` clean on the new widget and refactored HomeScreen
- `flutter test test/screens/active_chunk_card_test.dart` green
- Full suite green at wave merge
- Manual (per 12-VALIDATION.md): confirm Complete/Skip on the Home ActiveChunkCard render without hover on both mouse and touch surfaces
</verification>

<success_criteria>
- Home leads with Now/Next sections and inline Complete/Skip actions (NAV-02)
- "See full schedule" navigates to /schedule
- "All done today!" shows when all chunks resolved; empty state unchanged when no schedule
- NAV-02 widget test passes
</success_criteria>

<output>
Create `.planning/phases/12-home-as-landing-schedule-as-plan/12-03-SUMMARY.md` when done. Record: whether the formatters were shared via lib/utils/time_format.dart or duplicated locally (and why), and confirmation the mood row and empty state were preserved.
</output>

## Artifacts this phase produces (Plan 03 contribution)

- New `ActiveChunkCard` StatelessWidget (`lib/screens/home/widgets/active_chunk_card.dart`) — current chunk with "Now" badge, clock-time range, and always-visible Complete/Skip buttons
- `HomeScreen` Now/Next refactor: `currentChunk`/`nextChunk` split, "Now"/"Next" section labels, relocated mood row, and a "See full schedule" TextButton → `context.go('/schedule')` (NAV-02)
- Optional `lib/utils/time_format.dart` shared formatters (`formatMinutes`, `formatTimeRange`, `hexToColor`) — only if extracted without editing chunk_card.dart
- `test/screens/active_chunk_card_test.dart` NAV-02 widget test
