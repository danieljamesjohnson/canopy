---
phase: 09-an-engine-that-budgets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/services/schedule_generator.dart
  - lib/data/repositories/in_memory_completion_log_repository.dart
  - test/services/schedule_generator_test.dart
autonomous: true
requirements: [ENGINE-01, ENGINE-02, ENGINE-03, ENGINE-04, ENGINE-05]
must_haves:
  truths:
    - "At mood 4–5 with three time-target goals, the schedule contains more than three discretionary work chunks (capacity is filled, not one-per-goal)."
    - "A time-target goal most behind its weekly budget receives more chunks than one that is ahead, computed from CompletionLog, most-behind first."
    - "A habit set to 3x/week does not appear in the schedule on a day it is not due (floor-div weekday spread)."
    - "An outcome goal with a near deadline is scheduled ahead of one with a far deadline; the `chunksRemaining = 2.0` placeholder is gone."
    - "Toggling lighter-day (mood 3–5) reduces the discretionary chunk count by dropping one mood tier."
  artifacts:
    - path: "lib/services/schedule_generator.dart"
      provides: "Rewritten Steps 1–4 allocation honoring budget/frequency/deadline/priority/lighter-day; Steps A–E untouched"
      contains: "completionLogs"
    - path: "lib/data/repositories/in_memory_completion_log_repository.dart"
      provides: "Shared in-memory CompletionLogRepository test seam under lib/"
      contains: "class InMemoryCompletionLogRepository"
    - path: "test/services/schedule_generator_test.dart"
      provides: "T-09-01..T-09-06 plus mechanically-updated existing calls"
      contains: "T-09-01"
  key_links:
    - from: "lib/services/schedule_generator.dart generate()"
      to: "completionLogs parameter"
      via: "new named param threaded into Steps 2–4 demand/streak helpers"
      pattern: "List<CompletionLog> completionLogs"
---

<objective>
Rewrite the allocation steps (Steps 1–4) of `ScheduleGeneratorService.generate()` so the engine honors the goal fields it already stores — weekly hour budget, habit frequency, deadline pressure, priority — and so the lighter-day flag reduces capacity. The Phase 8 ordering/break pass (Steps A–E) is correct and MUST NOT be touched.

Purpose: This is the core of ENGINE-01..05. The engine currently emits one chunk per goal, ignores frequency, uses a `chunksRemaining = 2.0` placeholder, and never sees completion history. After this plan the engine produces a real, data-driven schedule.

Output: A rewritten `schedule_generator.dart` (Steps 1–4 only), a new shared `InMemoryCompletionLogRepository` test seam, and 6 new deterministic unit tests (T-09-01..06) plus mechanically-updated existing tests — written test-first (Wave 0) before the rewrite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-an-engine-that-budgets/09-RESEARCH.md
@.planning/phases/09-an-engine-that-budgets/09-PATTERNS.md
@.planning/phases/09-an-engine-that-budgets/09-VALIDATION.md

# Source-of-truth files for this plan
@lib/services/schedule_generator.dart
@lib/data/models/goal.dart
@lib/data/models/completion_log.dart
@lib/data/models/scheduled_chunk.dart
@lib/data/repositories/completion_log_repository.dart
@lib/data/repositories/in_memory_app_settings_repository.dart
@test/services/schedule_generator_test.dart
</context>

## Artifacts this phase produces (Plan 01 portion)

NEW symbols created by this plan:
- `lib/data/repositories/in_memory_completion_log_repository.dart` — `class InMemoryCompletionLogRepository implements CompletionLogRepository` (methods: `getAll`, `getById`, `append`, `getByDate`, `getByGoalId`).
- On `ScheduleGeneratorService`:
  - new `generate()` named params: `List<CompletionLog> completionLogs = const []`, `bool lighterDay = true`.
  - new private helpers: `_effectiveCap(int moodIndex, bool lighterDay)`, `_computeDueWeekdays(int freq)`, `_computeStreak(String goalId, Set<int> dueWeekdays, List<CompletionLog> allLogs)`, `_weekStart(DateTime date)`, `_completedChunksThisWeek(String goalId, List<CompletionLog> logs, DateTime today)`, `_remainingHours(Goal goal, List<CompletionLog> logs, DateTime date)`, `_demandForTimeTarget(Goal goal, List<CompletionLog> logs, DateTime date)`, `_habitRationale(Goal goal, int streak)`, `_outcomeRationale(Goal goal, DateTime date)`, `_timeTargetRationale(Goal goal, List<CompletionLog> logs, DateTime date)`.
- New test names: `T-09-01`, `T-09-02`, `T-09-03a`, `T-09-04`, `T-09-05`, `T-09-06` in `test/services/schedule_generator_test.dart`, plus helpers `makeTimeTarget(...)` and `makeLog(...)`.

NOTE: `_computeStreak` and `_computeDueWeekdays` must be reachable from `ScheduleNotifier` in Plan 03 for streak write-back. Expose them as **public static** methods (e.g. `ScheduleGeneratorService.computeDueWeekdays`, `ScheduleGeneratorService.computeStreak`) so the notifier can call them without duplicating logic. Use these public static methods internally from `generate()` too. This is the contract Plan 03 consumes.

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — add the in-memory test seam + write failing tests for the new generate() signature and ENGINE-01..06</name>
  <files>lib/data/repositories/in_memory_completion_log_repository.dart, test/services/schedule_generator_test.dart</files>
  <read_first>
    - lib/data/repositories/completion_log_repository.dart (the interface to implement — has getAll, getById, append, getByDate, getByGoalId)
    - lib/data/repositories/in_memory_app_settings_repository.dart (the in-memory-fake pattern to mirror, including the lib/-not-test/ doc comment)
    - lib/data/models/completion_log.dart (CompletionLog ctor + CompletionEvent enum + `event` getter)
    - test/services/schedule_generator_test.dart (existing helpers makeHabit/makeOutcome/makeBlock/workCount/workChunksOf; the 17 existing `sut.generate(...)` call sites that need a mechanical param add)
    - .planning/phases/09-an-engine-that-budgets/09-PATTERNS.md (test/seam sections; exact helper shapes for makeTimeTarget/makeLog)
    - .planning/phases/09-an-engine-that-budgets/09-RESEARCH.md (Phase Requirements → Test Map; week-boundary and floor-div formulas)
  </read_first>
  <action>
    Create `lib/data/repositories/in_memory_completion_log_repository.dart` implementing `CompletionLogRepository` (all 5 methods: `getAll`, `getById`, `append`, `getByDate`, `getByGoalId`) backed by a private `List<CompletionLog>`, mirroring `InMemoryAppSettingsRepository`'s doc-comment style (published under lib/ so multiple test files share it). `getById` returns the matching entry or null; `getByDate`/`getByGoalId` filter the list.

    In `test/services/schedule_generator_test.dart`: (1) add `makeTimeTarget({name, weeklyHourBudget, priorityWeight})` and `makeLog({required goalId, required dateYmd, event = CompletionEvent.completed})` helpers following the existing factory style (import `completion_log.dart`). (2) Mechanically add `completionLogs: []` to ALL existing `sut.generate(...)` call sites (17 sites) — `lighterDay` is omitted (defaults true). (3) Add the six new tests as separate `test()` calls, each tied to one success criterion, all of which FAIL until the engine is rewritten in Task 2:
      - T-09-01 (ENGINE-01): mood 4, three time-target goals each `weeklyHourBudget: 10`, Monday, `completionLogs: []` → `workChunksOf(result) > 3`.
      - T-09-02 (ENGINE-02): two time-target goals, equal budgets; goal A has prior completed logs this week, goal B has none → B (most behind) gets >= A's chunk count, and strictly more in a setup where A is on-track.
      - T-09-03a (ENGINE-03): a 3x/week habit (`frequencyPerWeek: 3`) on a NON-due weekday (e.g. Tuesday=weekday 2, since freq=3 → due {1,3,5}) produces zero work chunks for that habit; assert it appears on a due day (Monday).
      - T-09-04 (ENGINE-04): two outcome goals, deadlines 3 days vs 90 days out, equal priority → the near-deadline goal's chunk precedes the far one in discretionary order. Also assert the source no longer contains `chunksRemaining = 2.0` (covered by Task 2 verify, but add a behavioral ordering assertion here).
      - T-09-05 (ENGINE-05): same inputs, `moodIndex: 5`, compare `lighterDay: false` vs `lighterDay: true` → lighter true yields strictly fewer discretionary work chunks (cap drops from 11 to 9).
      - T-09-06 (ENGINE-06): one remaining capacity slot contested by a high-priority (0.75) and low-priority (0.25) time-target goal with equal remaining budget → the high-priority goal's chunk is the one scheduled (priority tiebreaker).
    Do NOT implement engine logic in this task. Tests are expected RED.
  </action>
  <verify>
    <automated>cd /home/dan/CodeProjects/canopy && /home/dan/development/flutter/bin/flutter analyze lib/data/repositories/in_memory_completion_log_repository.dart test/services/schedule_generator_test.dart 2>&1 | grep -v '^Analyzing' | grep -iE 'error' | grep -v '^#' | wc -l | grep -qx 0</automated>
    <automated>cd /home/dan/CodeProjects/canopy && for t in T-09-01 T-09-02 T-09-03a T-09-04 T-09-05 T-09-06; do grep -q "$t" test/services/schedule_generator_test.dart || { echo "missing $t"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - `lib/data/repositories/in_memory_completion_log_repository.dart` exists and `flutter analyze` reports zero errors for it (it correctly implements all 5 `CompletionLogRepository` methods).
    - `test/services/schedule_generator_test.dart` contains the literal test names `T-09-01`, `T-09-02`, `T-09-03a`, `T-09-04`, `T-09-05`, `T-09-06` and the helpers `makeTimeTarget` and `makeLog`.
    - Every `sut.generate(` call site in the test file passes a `completionLogs:` argument (grep: `grep -c 'sut.generate(' == grep -c 'completionLogs:'` for generate-call lines).
    - The file compiles against the NOT-YET-rewritten engine (the new tests fail on assertions, not on missing parameters — so the generate() signature change must be coordinated; if the signature does not yet accept `completionLogs`, this task may leave the new tests referencing the new signature and they will not compile until Task 2 — acceptable, but the analyze gate above must still pass for the repo file). Prefer: make the `generate()` signature change a shared first edit so the test file compiles and the 6 new tests run RED on assertions.
  </acceptance_criteria>
  <done>The shared in-memory log repository exists under lib/; the test file has the 6 new ENGINE tests and the two new helpers; all existing generate call sites pass `completionLogs: []`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite Steps 1–4 of generate() to honor budget/frequency/deadline/priority/lighter-day; keep Steps A–E untouched</name>
  <files>lib/services/schedule_generator.dart</files>
  <behavior>
    - generate() accepts `completionLogs` and `lighterDay`; cap is computed via `_effectiveCap(moodIndex, lighterDay)`.
    - Habits scheduled only on due weekdays via floor-div spread `i * 7 ~/ freq + 1`; streak from logs; rationale dynamic.
    - Outcomes ordered by `urgency = (priorityWeight ?? 0.5) / max(1, daysRemaining)` — NO `2.0` factor; mood-3–5 include all, mood 1–2 lighter-ON only deadline-today, lighter-OFF all with deadlines.
    - Time-targets multi-chunk: demand `ceil(remainingHrs*60/25/daysLeft).clamp(0,4)`, sorted most-behind-first, priority as tiebreaker, mood 3–5 only.
    - All six new tests (T-09-01..06) and all existing tests pass.
  </behavior>
  <read_first>
    - lib/services/schedule_generator.dart (full file — note the boundary comment `// Ordering + break insertion pass (READ-02).` at ~line 153; everything below it through end of `_assignSyntheticStartTimes` is FROZEN)
    - .planning/phases/09-an-engine-that-budgets/09-PATTERNS.md (the exact replacement code blocks for Steps 2/3/4 and every new helper; the `_moodCap` reuse; the urgency formula)
    - .planning/phases/09-an-engine-that-budgets/09-RESEARCH.md (allocation policy, week-boundary pitfall, round-vs-floor-div pitfall, daysLeft formula, streak walk)
    - lib/data/models/goal.dart and lib/data/models/completion_log.dart (field names: weeklyHourBudget, frequencyPerWeek, deadline, priorityWeight, streakCount; CompletionEvent / `event` getter)
  </read_first>
  <action>
    Add `import 'package:canopy/data/models/completion_log.dart';` at the top. Extend the `generate()` named-parameter list with `List<CompletionLog> completionLogs = const []` and `bool lighterDay = true` (after `date`). Replace `final int cap = _moodCap[moodIndex] ?? 8;` with `final int cap = _effectiveCap(moodIndex, lighterDay);`.

    Add public static helpers `computeDueWeekdays(int freq)` (returns `{ for (int i=0; i<freq; i++) i * 7 ~/ freq + 1 }` — floor-div ONLY; never the round-based form) and `computeStreak(String goalId, Set<int> dueWeekdays, List<CompletionLog> allLogs)` (walk goal's logs newest-first by `dateYmd`; skip non-due weekdays without breaking; increment on `CompletionEvent.completed`; break on any other event on a due day). Add private `_effectiveCap`, `_weekStart`, `_completedChunksThisWeek`, `_remainingHours`, `_demandForTimeTarget`, `_habitRationale`, `_outcomeRationale`, `_timeTargetRationale` per the PATTERNS code blocks.

    Rewrite Step 2 (habits): gate by `computeDueWeekdays(goal.frequencyPerWeek ?? 7).contains(date.weekday)`; rationale `_habitRationale(goal, computeStreak(...))`. Rewrite Step 3 (outcomes): urgency `= (g.priorityWeight ?? 0.5) / max(1, daysRemaining)` (the `chunksRemaining = 2.0` line and the `* 2.0` factor are DELETED entirely); inclusion logic by mood/lighterDay per PATTERNS; rationale `_outcomeRationale`. Rewrite Step 4 (time-target): sort most-behind-first via `_remainingHours` with `priorityWeight` tiebreaker; inner loop emits `_demandForTimeTarget(goal,...)` chunks while `discretionaryCount < cap`; rationale `_timeTargetRationale`. Do NOT add `async`, repository access, or any `streakCount` write in the service. Do NOT modify anything from the `// Ordering + break insertion pass (READ-02).` comment onward.
  </action>
  <verify>
    <automated>cd /home/dan/CodeProjects/canopy && /home/dan/development/flutter/bin/flutter test test/services/schedule_generator_test.dart 2>&1 | tail -3 | grep -qiE 'All tests passed'</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/services/schedule_generator_test.dart` exits 0 with all tests passing (existing 13 + new 6).
    - `schedule_generator.dart` no longer contains the substring `chunksRemaining = 2.0` and no longer contains `* 2.0` in the urgency function (`grep -c 'chunksRemaining = 2.0' lib/services/schedule_generator.dart` == 0).
    - `schedule_generator.dart` contains `i * 7 ~/ freq + 1` (floor-div spread) and does NOT contain a `.round() % 7` weekday spread.
    - The region from `// Ordering + break insertion pass (READ-02).` through the end of `_assignSyntheticStartTimes` is byte-identical to its pre-edit content (no diff below that comment).
    - The service has no `async` keyword and no repository import.
  </acceptance_criteria>
  <done>All service-layer ENGINE tests pass; the `2.0` placeholder is gone; floor-div spread is used; Steps A–E are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider → Service | `List<Goal>` and `List<CompletionLog>` flow in-process from trusted notifiers into the pure-Dart engine. No external/network input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Denial of Service | `_demandForTimeTarget` / `_remainingHours` | mitigate | Divide-by-zero on `daysLeft`: use `(7 - date.weekday + 1).clamp(1, 7)` so the divisor is never 0. `remaining` clamped to `[0, budget]`. |
| T-09-02 | Denial of Service | `urgencyScore` (outcome) | mitigate | `max(1, daysRemaining)` guards a past/today deadline producing a 0 or negative divisor. |
| T-09-03 | Tampering | `computeStreak` over malformed `dateYmd` | mitigate | `dateYmd` is produced internally by `DateFormat('yyyy-MM-dd')`; `DateTime.parse` is total over that format. Empty/`null` goalId logs (commitment chunks) are filtered by `l.goalId == goalId` where goalId is a non-empty UUID, so `''` logs never match. |
| T-09-04 | Denial of Service | `computeDueWeekdays(freq)` | mitigate | `assert(freq in [1,7])`; callers pass `goal.frequencyPerWeek ?? 7`. A `freq` of 0 would yield an empty set (habit silently never due) rather than a crash; defensive clamp acceptable. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Phase 9 installs ZERO new packages (pure Dart, existing stack). No package legitimacy surface; nothing to verify. |

All dispositions are input-robustness / defensive-arithmetic, proportional to a local offline rule-based engine. No network, auth, or external-input threats exist in this phase.
</threat_model>

<verification>
- `flutter test test/services/schedule_generator_test.dart` — all engine tests green (existing + T-09-01..06).
- `grep -c 'chunksRemaining = 2.0' lib/services/schedule_generator.dart` returns 0.
- `flutter analyze` clean for the modified/created files.
</verification>

<success_criteria>
- ENGINE-01: mood 4–5 with 3 goals yields >3 discretionary chunks (T-09-01).
- ENGINE-02: most-behind time-target goal gets more chunks (T-09-02).
- ENGINE-03 (scheduling half): 3x/week habit absent on non-due days (T-09-03a); streak helper computed from logs.
- ENGINE-04: near-deadline outcome precedes far; `2.0` placeholder removed (T-09-04 + grep).
- ENGINE-05 (cap half): lighter-day reduces discretionary chunks (T-09-05).
- Steps A–E untouched; all pre-existing tests still pass.
</success_criteria>

<output>
Create `.planning/phases/09-an-engine-that-budgets/09-01-SUMMARY.md` when done. Record: the public static method names exposed for Plan 03 (`computeDueWeekdays`, `computeStreak`), the final `generate()` signature, and confirmation that Steps A–E were not modified.
</output>
