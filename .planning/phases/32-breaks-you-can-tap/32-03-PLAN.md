---
phase: 32-breaks-you-can-tap
plan: 03
type: execute
wave: 3
depends_on: ["32-02"]
autonomous: false
requirements: [TAPBREAK-01, TAPBREAK-02, TAPBREAK-03]
files_modified:
  - .planning/phases/32-breaks-you-can-tap/32-UAT.md

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The bytes the owner looks at are provably the bytes just built: the sha256 of the built bundle equals the sha256 of the served bundle, and the served bundle greps non-zero for a symbol this phase introduced."
    - "No server predating this build holds port 8143 — the port is checked and any prior listener killed BEFORE the sha256 comparison is taken. A stale server has been found squatting this port on two of the last three UATs on this project, and a third was still listening at research time."
    - "The UAT's own first instruction is a mandatory tap of the Re-check-in control, marked mandatory and recorded with a date, before any item is judged."
    - "The compact break tier's real content is measured in a real browser at the smallest reachable slot, not inferred from a widget test — this project's placeholder font has produced a wrong constant three times."
    - "D-31-07 is re-asked. LiveRowCard's compact-tier Skip button is code-complete and test-proven and has never been confirmed by a human on a device across two prior UAT rounds, and this phase changes the surface beside it."
    - "The Up next transition is surfaced as an open question, not a pass/fail item: when a live break is skipped the now-state resolver's pre-existing advance-past-resolved loop delists it, so the header switches to the next chunk while the now-line does not move. The owner has not ruled on whether that is right."
    - "The owner is asked directly whether the Skip rail reads as ONE tappable unit rather than two zones — the defect class that ended Phase 31 was an element that was legible, rendered and structurally correct and meant the wrong thing to a human."
    - "The owner is asked whether the full-height rail reads sensibly on a 180dp long break, since applying one shape to both tiers is a reasoned planner default rather than an owner ruling."
    - "The owner is told that free-time regions still render dashed while breaks now render as filled cards, and asked whether that divergence is acceptable — a foreseeable side effect an earlier UAT had specifically chosen to avoid."
    - "Any FAIL is routed explicitly to a gap-closure plan or a new phase, never noted and left."
    - statement: "The compact tier's real content — one small-body line, zero margin, inside a bordered card at 30dp — fits without clipping and without visually crowding the border, with real font metrics rather than the test harness's placeholder font."
      verification: backstop
    - statement: "The redesigned break card reads as a section of the day rather than a hairline, and reads as a break rather than as a work chunk — a perceptual judgment structurally identical to the one Phase 29 named and the one Phase 31 round two failed."
      verification: backstop
  prohibitions:
    - "MUST NOT judge any item before a Re-check-in is recorded with a date. An already-generated day is never regenerated on load, so a scale change is invisible in the running app even though a grep on the bundle will correctly confirm the code shipped. This exact omission produced a false failure on 2026-08-21 and let a real defect survive three more days."
    - "MUST NOT trust the port. Check what is listening and kill it before serving, and record the killed process and its start time."
    - "MUST NOT serve a release build on this port, and MUST NOT use the incremental web-server run command. Port 8143 has only ever served debug builds; a release service worker left on this origin would intercept and serve a cached shell against a mismatched server."
    - "MUST NOT quietly substitute a different grep string when a presence probe returns zero. A zero can mean the compiler folded the symbol away rather than that the change is missing — pick a symbol that survives compilation and record which one was used and why the first returned zero."
    - "MUST NOT treat a green suite, however large, as grounds to skip or shorten this checkpoint. Three green suites have been contradicted by a thumb on this project, and one defect was of a kind no assertion could ever catch."
    - "MUST NOT record a verdict on the owner's behalf, and MUST NOT read silence on the open questions as consent either way."
  artifacts:
    - path: ".planning/phases/32-breaks-you-can-tap/32-UAT.md"
      provides: "the UAT script and the recorded per-item verdicts, with a Summary block and a Gaps block a later gap-closure run can consume"
      contains: "Re-check-in"
  key_links:
    - "CLAUDE.md trap #4 — the schedule notifier's load path reads today's schedule straight from local storage and the generator runs only at check-in with silent replace, so a non-zero grep on the bundle proves the CODE shipped and never that the DATA on screen came from it."
    - "CLAUDE.md trap #3 — the stock Python http server sends no cache-control header, so the repo's own serve script is mandatory."
    - "CLAUDE.md trap #1 — one port per build type; port 8143 is the debug port for this project."
    - "32-VALIDATION.md § Manual-Only Verifications — the five things flutter test structurally cannot settle for this phase, which are exactly the items this plan exists to close."
---

<objective>
Close the three things `flutter test` structurally cannot settle: whether the redesigned card's real
content fits at 30dp with real font metrics, whether it reads as a section of the day, and whether the
Skip rail reads as one tappable unit under a thumb.

Purpose: this is the fourth consecutive phase to end in a human checkpoint, with the strongest
precedent in the project. Phase 27 failed two of three human items after sixteen of seventeen
automated ones. Phase 29 shipped 587-green while the owner saw no breaks at all. Phase 31 went
625-green, then 639-green, and was contradicted by a thumb twice — the second time by a defect (an
icon that meant the wrong verb) that no assertion in the suite could ever have caught. That defect is
the direct cause of this phase existing.

Output: a byte-verified debug build on port 8143, a real-browser measurement of the compact tier, and
`32-UAT.md` with a recorded verdict per item.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/32-breaks-you-can-tap/32-VALIDATION.md
@.planning/phases/32-breaks-you-can-tap/32-UI-SPEC.md
@.planning/phases/32-breaks-you-can-tap/32-01-SUMMARY.md
@.planning/phases/32-breaks-you-can-tap/32-02-SUMMARY.md
@.planning/phases/31-breaks-you-can-skip/31-GAPS-UAT.md
</context>

## Artifacts this phase produces

See `32-01-PLAN.md` § "Artifacts this phase produces" for the consolidated created / restored /
retired tables. This plan creates one artifact of its own:

| Artifact | Path | Kind |
|---|---|---|
| `32-UAT.md` | `.planning/phases/32-breaks-you-can-tap/` | the human UAT script and its recorded verdicts (named to match the UAT glob the tooling greps) |
| A running debug build | `http://danserver:8143/` | byte-verified serve, not a committed artifact |

## Coverage lift

See `32-01-PLAN.md` § "Coverage lift". This plan carries the two backstop edge predicates — the
30dp fit and the "reads as a section" judgment — both of which abstain to a human rather than
passing silently.

<tasks>

<task type="auto">
  <name>Task 1: Reclaim the port, build, serve, byte-verify, measure the card, and write the UAT</name>
  <precondition>`flutter` resolves on PATH (prepend `/home/dan/development/flutter/bin` if `flutter --version` fails), the working tree contains the merged commits from 32-01 and 32-02, and `flutter test` is green on that merged tree. If this task runs inside a git worktree that is force-removed on return, the build, serve and byte-verification steps must instead be performed by the orchestrator from the main working tree after merge — record honestly which agent produced the numbers, exactly as 31-05 and 31-08 did.</precondition>
  <files>.planning/phases/32-breaks-you-can-tap/32-UAT.md</files>
  <read_first>
    - `CLAUDE.md` § "Local hosting for UAT" and all four traps, in full. Trap #4 cost the owner a round
      trip and let a real defect survive three extra days; trap #3 is why the repo's own serve script
      is mandatory; trap #1 is why this port must never serve a release build.
    - `.planning/phases/31-breaks-you-can-skip/31-GAPS-UAT.md` in full — round two of the previous
      phase's UAT. Its structure, its pre-flight block, and the wording of the item that was judged a
      FAIL on meaning are what this document must be comparable against.
    - `.planning/phases/32-breaks-you-can-tap/32-VALIDATION.md` § "Manual-Only Verifications" — the
      five rows there are the items this UAT must cover, with the reason each is manual stated in the
      document rather than asserted.
    - `.planning/phases/32-breaks-you-can-tap/32-UI-SPEC.md` § "Verification" — including the new
      question this phase's own design choice raises about the tall rail on a long break.
    - `32-01-SUMMARY.md` and `32-02-SUMMARY.md` — the exact new symbol names, needed for the
      served-bytes presence probe.
    - `tools/serve-uat.py` — confirm it still sends a no-store cache-control header and strips
      conditional-request headers.
    - `lib/screens/settings/settings_screen.dart` § the time-travel controls — the affordance the UAT
      tells the owner to use to reach a live break without waiting for one.
  </read_first>
  <action>
**Reclaim the port before building anything.** Run `lsof -i :8143` (or `ss -ltnp | grep 8143`). A
server from the previous phase's UAT, started 2026-08-26, was still listening at research time and is
expected to still be there. Kill whatever holds it and record the process id and its start time in the
UAT document. Do not skip this because a previous agent already did it — that assumption is exactly
what produced the near-miss twice before.

Build with exactly `flutter build web --debug --source-maps --pwa-strategy=none`. Never the
incremental web-server run command, whose module fan-out never finishes loading over tailscale
latency. Never a release build on this port. Serve with exactly
`python3 tools/serve-uat.py 8143 --dir build/web`; never the stock Python http server, which sends no
cache-control header.

Run the byte-verification pre-flight and paste every command with its verbatim output into the UAT:
the sha256 of the built bundle, the sha256 of the served bundle, a presence grep for a symbol this
phase introduced, and the served cache-control header. The two hashes must be identical and the grep
must be non-zero. If the grep returns zero, do not proceed and do not quietly swap in a different
string: a zero can mean the compiler folded the symbol away rather than that the change is missing —
this exact false negative fired on the previous phase's pre-flight, where a constant and an icon both
grepped zero on a bundle that definitely contained them. Pick a symbol that survives compilation, re-run,
and record which one you used and why the first returned zero.

Immediately after the pre-flight block, state in the document's own words that **a non-zero grep
proves the code shipped and not that the data on screen was produced by it.** That is trap #4, it is
what Step 0 exists for, and an agent who has just run a clean grep is the one most likely to skip
Step 0 because the grep felt like proof.

**Then measure the card in a real browser.** Render the served page headless with
`--use-gl=swiftshader --enable-unsafe-swiftshader`, capture the timeline, and measure the 5-minute
break card: its painted height, whether its single label line is clipped at top or bottom, and the
distance between the text's ink extent and the card's border. Paste the measured numbers and attach or
reference the screenshot. Judge fit against the reasoned expectation the UI-SPEC records — roughly
16-18dp of line box plus a 1dp border inside 30dp — and say plainly whether the measurement confirms
or contradicts it. If it contradicts it, stop and record that as a blocking finding for a gap-closure
plan rather than asking a human to judge a clipped card. A widget test asserting no overflow exception
was thrown passes happily while real text crowds a border; no exception is thrown for ugly.

Then write `.planning/phases/32-breaks-you-can-tap/32-UAT.md` containing, in order:

1. **A header** naming this as Phase 32's UAT, stating what shipped in one paragraph, and stating that
   two items are inherited from Phase 31 rather than raised by this phase.

2. **An honesty note about who ran what** — if the build, serve and verification numbers were produced
   by the orchestrator from the main tree rather than by this executor, say so. Do not present another
   agent's output as this one's.

3. **The pre-flight block**, including the killed process id and start time, both hashes, the presence
   grep and which symbol it used, and the cache-control header.

4. **The real-browser measurement block** — the numbers, the verdict on fit, and the screenshot.

5. **STEP 0 — MANDATORY, DO THIS FIRST: tap the Re-check-in control.** First, before any item, marked
   mandatory, with the reason stated rather than asserted: the schedule loads from local storage and
   the generator runs only at check-in with silent replace, so an already-generated day is never
   regenerated on load and a scale change is invisible until then. Name the 2026-08-21 incident by
   date. Include a line for the owner to record that Step 0 was done and on what date, and state that
   an unrecorded Step 0 invalidates every item below and must be re-run rather than assumed.

6. **A "do this on a phone or tablet" instruction**, with the reason: a mouse cursor is one pixel and
   the entire question is what happens when the input is roughly 40dp of fingertip.

7. **Item 1 — does a 5-minute break read as a section of the day? (TAPBREAK-03.)** Ask whether the
   short break now reads as a real, small section rather than a line between two blocks; whether it
   still reads as a *break* rather than as another work block, now that it wears the same card
   container work chunks do; and whether the label is comfortably legible inside it rather than
   crowding the border. State the accepted cost up front: the whole day is 50% taller to scroll, an
   8-hour day going from about 1920dp to about 2880dp, and ask whether that trade feels right in the
   hand rather than only on paper.

8. **Item 2 — does the Skip rail read as ONE tappable thing, and can a thumb hit it? (TAPBREAK-01,
   D-32-03.)** This is the item that carries the Phase 31 defect class. Ask directly, in these terms:
   does the rail read as a single button, or as two separate zones — an icon zone and a word zone?
   Does the icon plus the word say "skip this break", or does it say something else? Then ask him to
   skip five short breaks with a thumb at a natural placement and count how many landed first time.
   State the geometry honestly: the rail is about 64dp wide by 30dp tall, roughly 1920 square dp
   against the 2304 of the usual 48-by-48 guideline — deliberately under, because every pixel of it is
   painted rather than invisible, and because the previous approach met the number with an invisible
   target and failed a thumb twice.

9. **Item 3 — the long break's rail (a planner default, not a ruling).** Ask whether the full-height
   rail on a 30-minute break, 64dp wide by 180dp tall, reads sensibly or looks odd. Say plainly that
   applying one shape to both tiers was a reasoned default chosen for simplicity, that the owner's
   ruling was derived against the short break, and that a "looks odd" answer here is new evidence
   rather than a contradiction of anything settled.

10. **Item 4 — re-ask D-31-07, which has never been judged by a human.** Give a practical route to a
    live break rather than asking him to catch a five-minute window by luck: note a break's start time,
    use the Settings time-travel control to set the simulated clock inside that window, return to
    Today, and reset the clock afterwards. Warn him to stay inside the same calendar day. Then ask: on
    a 30-minute break that is running right now, is there a Skip and no Complete, and does it work? And
    on a 5-minute break that is running right now, is there a Skip rail at all — which is new this
    phase and did not exist in any previous round?

11. **Two open questions, explicitly not pass/fail items.** (a) The Up next transition: when a live
    break is skipped, the now-state resolver's pre-existing advance-past-resolved loop delists it, so
    the header switches to the next chunk while the now-line does not move. Describe exactly what he
    will see and ask whether that is what he wants — it was flagged in the previous round and is still
    unanswered, and it is pre-existing behaviour rather than something this phase introduced. (b) Free
    time still renders as a dashed region while breaks now render as filled cards. An earlier UAT
    deliberately made those two match; this phase's own instruction to make breaks look like work has
    pulled them apart again. Ask whether that divergence is acceptable or wants its own phase.

12. **Two carried-forward notes, not questions.** Skipped-break legibility at half opacity and
    "skipping a break does not hand the minutes back" both passed human UAT and are settled; ask only
    that he say something if either now looks *different*, since this phase rebuilt the same rows.
    Break cards are still not tappable, by his own standing instruction, enforced by a test.

13. **A resume signal**, and a **remedies** section stating that any FAIL routes explicitly to a
    gap-closure plan or a new phase, never noted and left — a recorded failure with no route is how an
    earlier phase's gap survived a full plan cycle.

14. **A `## Summary` block** with total / passed / issues / pending / skipped / blocked counts, and an
    empty `## Gaps` section carrying the YAML comment marker a gap-closure run consumes, so a further
    round can be planned mechanically.

Do not record a verdict for any item yourself. This task writes the document and stands up the build;
Task 2 is where a human answers it.
  </action>
  <verify>
    <automated>test -f .planning/phases/32-breaks-you-can-tap/32-UAT.md &amp;&amp; [ "$(curl -s http://danserver:8143/main.dart.js | sha256sum | cut -d' ' -f1)" = "$(sha256sum build/web/main.dart.js | cut -d' ' -f1)" ] &amp;&amp; curl -sI http://danserver:8143/main.dart.js | grep -qi 'no-store'</automated>
    <human-check>
      On a phone or tablet at http://danserver:8143/ — tap the Re-check-in control FIRST (mandatory;
      an already-generated day is never regenerated on load, so nothing below is meaningful without
      it). Then judge, recording each answer in 32-UAT.md: (1) does a 5-minute break read as a small
      section of the day, and still as a break rather than a work block? (2) does the Skip rail read
      as ONE tappable thing rather than two zones, and can a thumb hit it five times out of five?
      (3) does the full-height rail read sensibly on a 30-minute break? (4) D-31-07, never yet judged
      by a human: on a running break is there a Skip and no Complete, on both the 30-minute and the
      5-minute case? Plus two open questions, not pass/fail: what should happen to "Up next" when a
      live break is skipped, and is it acceptable that free time still renders dashed while breaks
      are now filled cards?
    </human-check>
  </verify>
  <acceptance_criteria>
    - `lsof -i :8143` shows exactly one listener whose start time is later than the built bundle's mtime; the killed process id and its start time are recorded in the UAT.
    - The built and served bundle sha256 digests are identical and both are pasted verbatim into the UAT.
    - The served-bytes presence grep returns non-zero, and the UAT records which symbol was used and, if the first choice returned zero, why.
    - `curl -sI http://danserver:8143/main.dart.js | grep -i cache-control` shows no-store, pasted verbatim.
    - The UAT contains the sentence distinguishing "the code shipped" from "the data on screen came from it", positioned immediately after the pre-flight block.
    - The UAT's first item-level heading is Step 0, ahead of every numbered item, and it is marked mandatory and carries a date field.
    - The real-browser measurement block contains actual measured numbers for the 5-minute break card's painted height and its text-to-border clearance, plus a fit verdict — not an estimate restated.
    - The UAT contains an item asking whether the rail reads as one tappable unit rather than two zones, in those terms.
    - The UAT re-asks D-31-07 explicitly and covers both the 30-minute and the 5-minute live cases.
    - The UAT surfaces the Up-next transition and the free-time visual divergence as questions, not as pass/fail items.
    - The UAT contains a `## Summary` block and a `## Gaps` section with the YAML consumption marker.
    - `flutter test` and `flutter analyze` are re-run from the merged tree, both clean, totals pasted into the UAT.
  </acceptance_criteria>
  <done>A debug build of the merged phase is live on port 8143, provably byte-identical to what was just built, on a port no stale server holds; the compact break card has been measured in a real browser rather than inferred; and `32-UAT.md` asks the questions automation cannot answer, with a mandatory Step 0 in front of them.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: The owner's thumb — blocking UAT</name>
  <precondition>A real touch device (phone or tablet) on the tailnet, and the server from Task 1 still listening on port 8143. There is no automated fallback and there cannot be one: a widget test can prove the rail is mechanically one ink response, and cannot prove a human perceives it as one button.</precondition>
  <files>.planning/phases/32-breaks-you-can-tap/32-UAT.md</files>
  <read_first>
    - `.planning/phases/32-breaks-you-can-tap/32-UAT.md` as written by Task 1.
    - `.planning/phases/31-breaks-you-can-skip/31-GAPS-UAT.md` — the previous round, for the exact
      wording of the item that failed on meaning rather than on visibility.
    - `CLAUDE.md` trap #4 in full.
  </read_first>
  <what-built>
Breaks are now tapped, not swiped, and the whole timeline is 50% taller so a 5-minute break is a real
row rather than a hairline.

**The scale.** Minutes-to-pixels went from 4.0 to 6.0 (D-32-01). Every row still renders at exactly
its own duration — a 5-minute break is 30dp, a 25-minute work block is 150dp, a 30-minute break is
180dp — so nothing lies about how long it takes. The accepted cost: the day is 50% taller to scroll,
about 1920dp to about 2880dp for an 8-hour day. That was the trade chosen precisely because the
alternatives all bought a bigger break by making some row lie.

**The card.** A break is now a real bordered card with a background, the same container language a
work block uses — your own words, "a small section similar to work." The hairline-between-two-dividers
treatment is gone entirely, along with the grip glyph that read as "drag and drop."

**The button.** Every break carries a visible, labelled Skip rail on its right-hand side: about 64dp
wide by the full height of the row, filled, with a skip icon above the word Skip. On a 5-minute break
that is 64 by 30. It is deliberately a little under the usual 48-by-48 area guideline, and every pixel
of it is painted rather than invisible — the previous approach met that number with an invisible
target and failed your thumb twice.

**The swipe is gone from breaks, and only from breaks.** Work chunks still swipe exactly as before.
Breaks and work no longer share a gesture vocabulary; you were shown that trade and took it.

**New this phase and never seen before:** a break that is running *right now* — including a
5-minute one, which previously had no button at all — carries the same Skip rail.

A DEBUG web build is live and byte-verified at **`http://danserver:8143/`**. Task 1 compared the
served bundle's sha256 against the built one and grepped it for this phase's own new symbol, so what
is in front of you is provably this code.
  </what-built>
  <how-to-verify>
Run this on a **phone or tablet**, not a desktop pointer. A mouse cursor is one pixel; the entire
question is what happens when the input is roughly 40dp of fingertip. Record a verdict per item
directly in `32-UAT.md`. Do not mark this task complete until a verdict exists for every item.

---

**STEP 0 — MANDATORY, DO THIS FIRST. Tap the Re-check-in control.**

Not optional and not a formality. Today's schedule is read straight from local storage and the
generator only runs at check-in, so an already-generated day is never rebuilt on load. If today's
schedule was generated before this code landed, everything below would be judged against a
4.0-scale day while the new code sat unused in a bundle that was just proven present. That exact
omission happened on 2026-08-21: a UAT judged a pre-fix day, reported a false failure, and let the
real defect survive three more days. Re-check-in first, and record the date in the file.

---

**Item 1 — does a 5-minute break read as a section of the day? (TAPBREAK-03)**

- (a) Does the short break read as a real, small section — or still as a line between two blocks?
- (b) Does it still read as a *break*, now that it wears the same card container a work block does?
- (c) Is the label comfortably legible inside the card, or does it crowd the border?
- (d) The whole day is now 50% taller to scroll. Does that trade feel right in the hand?

**Item 2 — does the Skip rail read as ONE thing, and can a thumb hit it? (TAPBREAK-01, D-32-03)**

This is the item that carries the defect that ended the last round — an element that was perfectly
legible and meant the wrong verb.

- (a) Does the rail read as a single button, or as two separate zones (an icon zone and a word zone)?
- (b) Does the icon plus the word say "skip this break", or does it say something else?
- (c) Skip five short breaks with a thumb at a natural placement. How many landed first time?
- (d) Did skipping a break ever accidentally hit the work block above or below it?

**Item 3 — the long break's rail (a planner default, not your ruling)**

On a 30-minute break the rail is the same 64dp wide but 180dp tall. Does that read sensibly, or does
it look odd? Applying one shape to both sizes was chosen for simplicity; your ruling was derived
against the short break. "Looks odd" here is new evidence, not a contradiction of anything settled.

**Item 4 — a break that is running RIGHT NOW (D-31-07, never yet judged by a human)**

You ruled this in on 2026-08-26 and never reached it in the last round. To get to a live break without
waiting: note a break's start time on the timeline, go to **Settings**, set the simulated clock to a
minute inside that window, and return to Today. Stay inside the same calendar day. Reset the clock
when you are done.

- (a) On a running **30-minute** break, is there a Skip and no Complete, and does tapping it work?
- (b) On a running **5-minute** break, is there a Skip rail at all? This is new — until this phase that
  row had no button of any kind, and its only skip was the swipe that has now been removed.
- (c) When you skip a running break, does the row stay where it was, and does the red now-line stay put?

---

**Two open questions — answer them, but they are not pass/fail.**

- **Up next.** When you skip a break that is running, the header stops calling it "now" and switches to
  the next chunk, while the red now-line stays exactly where it is. That is pre-existing behaviour, not
  something this phase added, and nobody has ruled on whether it is right. What should happen?
- **Free time still looks different.** Free/gap regions still render as dashed outlines while breaks are
  now filled cards. An earlier UAT deliberately made those two match; this phase's "make breaks look
  like work" instruction has pulled them apart again. Acceptable, or does it want its own phase?

**Two things carried forward, not questions.** Skipped-break legibility at half opacity, and "skipping
a break does not hand the minutes back", both passed and are settled — this phase rebuilt the same
rows, so say something only if either now looks *different*. And break cards are still not tappable, by
your own standing instruction, enforced by a test rather than by convention.
  </how-to-verify>
  <resume-signal>Type "approved" if Items 1-4 all pass. Otherwise describe what happened per item — for Item 1 say whether it reads as a section and whether it still reads as a break; for Item 2 give the five-attempt count and say whether the rail read as one button or two zones, and what the icon-plus-word said to you; for Item 3 say whether the tall rail looks right or odd; for Item 4 say whether each of the 30-minute and 5-minute live cases offered a Skip. Answer both open questions in your own words. If either carried-forward item now looks different, say that too.</resume-signal>
  <acceptance_criteria>
    - `32-UAT.md` records "Step 0 performed" with a date, written by the human who tapped it — not inferred.
    - Every item (1, 2, 3, 4) carries an explicit PASS or FAIL verdict with the owner's own words quoted.
    - Item 2 records the five-attempt count and an explicit answer to "one button or two zones", so it is comparable with the previous round's defect.
    - Both open questions carry a recorded answer, or are explicitly recorded as still unanswered rather than being read as consent either way.
    - The `## Summary` block's counts match the recorded verdicts.
    - Any FAIL is written into the `## Gaps` YAML block with truth, status, reason, severity, root_cause, artifacts and missing fields, so a gap-closure run can consume it mechanically.
    - `STATE.md` is updated with the verdict and, if any item failed, an explicit route to a gap-closure plan or a new phase.
  </acceptance_criteria>
  <done>A human verdict exists for all four items, recorded in `32-UAT.md` in the owner's own words, with Item 2's one-button-or-two answer and its five-attempt count on the record — or the phase is explicitly left open as deferred-human rather than closed on a green suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built bytes → served bytes → browser | Three places a stale artifact can be substituted for the one under test: a stale server on the port, a missing cache-control header, and a browser cache entry predating the switch |
| served code → data on screen | An already-generated day rendered by new code without having been produced by it — the trap a passing byte-verification most encourages an agent to ignore |
| test harness metrics → device metrics | The placeholder font has produced a wrong constant three times on this project; a widget test cannot settle whether real text fits |
| rendered element → human meaning | A legible, structurally correct element that communicates the wrong thing — the defect class that ended the previous phase and no assertion can catch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-03-01 | Spoofing | a stale server on port 8143 impersonating this build | high | mitigate | The port is checked and any prior listener killed BEFORE the sha256 comparison; the killed process id and start time are recorded in the UAT. |
| T-32-03-02 | Tampering | a browser cache serving a previous bundle | high | mitigate | The repo's own serve script only, with its no-store header asserted in the pre-flight; the stock Python server is prohibited. |
| T-32-03-03 | Repudiation | a byte-verified bundle judged against a pre-change generated day | critical | mitigate | Step 0 is mandatory, dated, and recorded by the human; the UAT states in its own words that a non-zero grep proves the code shipped and not that the screen's data came from it. |
| T-32-03-04 | Tampering | a presence probe silently changing its own success criterion | medium | mitigate | A zero grep halts the pre-flight; substituting a symbol requires recording which one and why the first returned zero. |
| T-32-03-05 | Information Disclosure | a debug build with source maps exposed beyond the tailnet | low | accept | This host is reachable only over Tailscale and LAN with no public exposure, and a debug build is the deliberate choice at this stage per `CLAUDE.md`. |
| T-32-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | This phase installs no packages — it runs an existing build command and an existing repo-local server script. No package-manager step exists to gate. |
</threat_model>

<verification>
- Port 8143 held by exactly one listener, started after the build.
- Built and served bundle sha256 identical, both pasted into the UAT.
- Served bundle greps non-zero for a symbol this phase introduced; no-store header present.
- The 5-minute break card measured in a real browser, with numbers and a fit verdict recorded.
- `32-UAT.md` leads with a mandatory, dated Step 0 and carries a verdict per item.
- Both open questions answered or explicitly recorded as unanswered.
- Any FAIL routed into the `## Gaps` YAML block.
</verification>

<success_criteria>
The three things no test can settle get settled: whether the card fits at 30dp with real font metrics,
whether it reads as a section of the day, and whether the Skip rail reads as one button under a thumb.
A PASS closes Phase 32. A FAIL is routed, not noted.
</success_criteria>

<output>
Create `.planning/phases/32-breaks-you-can-tap/32-03-SUMMARY.md` when done.
Record in it: the killed process id and its start time, both sha256 digests, the presence-grep count
and which symbol was used, the real-browser measurements and the fit verdict, whether the build and
serve steps were run by this executor or by the orchestrator, the per-item verdicts, and the answers
to both open questions.
</output>
