---
phase: 31-breaks-you-can-skip
plan: 08
type: execute
wave: 3
depends_on: ["31-06", "31-07"]
gap_closure: true
autonomous: false
requirements: [SKIPBREAK-01, SKIPBREAK-02]
files_modified:
  - .planning/phases/31-breaks-you-can-skip/31-GAPS-UAT.md

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A real thumb, on a real touch device, can reliably GRAB a 5-minute break's row — re-asked in the same five-attempts form the owner already answered on 2026-08-26, so the before/after is directly comparable."
    - "The grip glyph is findable by eye: the owner is asked whether he can see what to grab, which is the specific thing that was missing when 52dp cleared both platform minimums and a thumb still failed."
    - "A break that is currently running can be skipped, and the now-line does not move when it is (D-31-07)."
    - "The bytes the owner is looking at are provably the bytes just built: sha256 of built main.dart.js equals sha256 of served main.dart.js, and the served bundle greps non-zero for a symbol this gap closure introduced."
    - "No server predating this build is left holding port 8143 — the port is checked and any prior listener killed BEFORE the sha256 comparison is taken."
    - "The UAT's own Step 0 is a mandatory tap of the Re-check-in control, recorded with a date, before any item is judged."
    - "Any FAIL is routed explicitly to a gap-closure plan or a new phase, never noted and left."
  artifacts:
    - ".planning/phases/31-breaks-you-can-skip/31-GAPS-UAT.md — the round-two UAT document with a recorded verdict per item"
  key_links:
    - "CLAUDE.md trap #4: ScheduleNotifier._loadToday() reads Hive directly and ScheduleGeneratorService.generate() runs only at check-in with silent-replace, so an already-generated day is never regenerated on load — a non-zero grep proves the CODE shipped, never that the DATA on screen came from it."
    - "CLAUDE.md trap #3: python3 -m http.server sends no Cache-Control, so tools/serve-uat.py is mandatory."
    - "CLAUDE.md trap #1: port 8143 has served debug builds only across Phases 29/30/31 — keep it that way; never serve a release build on it."
    - "Settings carries the Phase 25 DevClock time-travel controls in a debug build — that is what makes the live-break item practically reachable instead of requiring the owner to catch a 5-minute window by luck."
---

<objective>
The blocking human UAT for the Phase 31 gap closure. Both D-31-06 (a bigger, findable acquisition
target) and D-31-07 (a live break can be skipped) ship together specifically so **one** re-run of the
owner's thumb covers both.

Purpose: this is the third time in this project a green suite has been contradicted by a physical
device — Phase 27 scored 16/17 automated then failed 2 of 3 human items; Phase 29 went 587-green
while the owner looked at a screen showing no breaks at all; Phase 31 went 625-green and 12/12
verified and a thumb still could not grab the row. And **the specific defect being fixed here is
invisible to automation by construction**: a widget test fires a synthetic drag at an exact
coordinate and cannot distinguish a 52dp band from a 68dp one. Nothing in `flutter test` can close
this gap. Only a thumb can.

Output: `31-GAPS-UAT.md` with a recorded verdict per item, produced against a byte-verified debug
build on port 8143.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/31-breaks-you-can-skip/31-UAT.md
@.planning/phases/31-breaks-you-can-skip/31-CONTEXT.md
@.planning/phases/31-breaks-you-can-skip/31-06-SUMMARY.md
@.planning/phases/31-breaks-you-can-skip/31-07-SUMMARY.md
</context>

## Artifacts this phase produces

| Artifact | Path | Kind |
|---|---|---|
| `31-GAPS-UAT.md` | `.planning/phases/31-breaks-you-can-skip/` | round-two human UAT document (named to match the `*-UAT.md` glob the GSD tooling greps) |
| A running debug build | `http://danserver:8143/` | byte-verified serve, not a committed artifact |

<tasks>

<task type="auto">
  <name>Task 1: Build, reclaim port 8143, serve, byte-verify, and write the round-two UAT</name>
  <files>.planning/phases/31-breaks-you-can-skip/31-GAPS-UAT.md</files>
  <precondition>`flutter` resolves on PATH (prepend `/home/dan/development/flutter/bin` if not), and the working tree contains the merged commits from plans 31-06 and 31-07. If this task runs inside a git worktree that will be force-removed on return, see the honesty note in the action below — the build, serve and byte-verification steps must then be performed by the orchestrator from the main working tree after merge, exactly as happened in 31-05.</precondition>
  <read_first>
    - `CLAUDE.md` § "Local hosting for UAT" and all four traps, in full. Trap #4 is the one that cost
      the owner a round trip on 2026-08-21 and let a real defect survive three more days; trap #3 is
      why `tools/serve-uat.py` is mandatory; trap #1 is why port 8143 must never serve a release
      build.
    - `.planning/phases/31-breaks-you-can-skip/31-UAT.md` in full — this is round one. Item 1's
      wording, its five-attempts form, and the recorded verdict are what round two must be comparable
      against. Also read its pre-flight block and the note recording that port 8143 was found held by
      a two-day-old server that was killed before the sha256 comparison was taken.
    - `31-06-SUMMARY.md` and `31-07-SUMMARY.md` — the shipped values and the exact new symbol names,
      needed for the served-bytes grep.
    - `tools/serve-uat.py` — confirm it still sends `Cache-Control: no-store` and strips
      conditional-request headers.
    - `lib/screens/settings/settings_screen.dart` § the Phase 25 time-travel controls — the shift /
      jump / set-absolute affordances the UAT's live-break item tells the owner to use.
  </read_first>
  <action>
**Reclaim the port first, before building anything.** Run `lsof -i :8143` (or `ss -ltnp | grep 8143`).
A server from the round-one UAT is expected to still be listening — at planning time it was, and in
round one a server two days older than the build was found squatting the same port and nearly
produced a false result. Kill whatever holds it and record the PID and its start time in the UAT
document. Do not skip this because the last agent already did it; that is precisely the assumption
that produced the near-miss.

Build the debug bundle with exactly:
`flutter build web --debug --source-maps --pwa-strategy=none`
Never `flutter run -d web-server` (its DDC output fans into ~866 module files and never finishes
loading over tailscale latency), and never a release build on this port.

Serve with exactly:
`python3 tools/serve-uat.py 8143 --dir build/web`
Never `python3 -m http.server`, which sends no `Cache-Control` and lets a browser keep serving a
stale bundle.

Then run the byte-verification pre-flight and paste every command with its verbatim output into the
UAT document:

- `sha256sum build/web/main.dart.js`
- `curl -s http://danserver:8143/main.dart.js | sha256sum`
- `curl -s http://danserver:8143/main.dart.js | grep -c SwipeableRowShell`
- `curl -s http://danserver:8143/main.dart.js | grep -c showComplete`
- `curl -sI http://danserver:8143/main.dart.js | grep -i cache-control`

The two hashes must be identical and both greps must be non-zero. If either grep returns zero, **do
not proceed and do not quietly substitute a different string**: a zero count may mean the symbol was
tree-shaken or renamed rather than that the change is missing. Pick another symbol from the
"Artifacts this phase produces" tables in `31-06-PLAN.md` / `31-07-PLAN.md` that does survive
compilation, re-run the grep, and record in the UAT which symbol you used and why the first one
returned zero. A pre-flight that quietly changes its own success criterion is worse than no
pre-flight.

Immediately after the pre-flight block, state plainly and in the UAT's own words that **a non-zero
grep proves the code shipped and NOT that the data on screen was produced by it** — that is trap #4,
it is exactly what Step 0 exists for, and an agent who has just run this grep is the agent most
likely to skip Step 0 because the grep felt like proof.

Then write `.planning/phases/31-breaks-you-can-skip/31-GAPS-UAT.md` containing, in order:

1. **A header** naming this as round two of the Phase 31 UAT, closing the gaps from round one
   (Item 1 FAIL, plus the D-31-07 ruling), and stating that Items 2 and 3 of round one already
   PASSED and are not re-asked except where a change could have disturbed them.

2. **The honesty note about who ran what.** If this task executes inside a git worktree that is
   force-removed on return, and given `build/web` is gitignored, the build / serve /
   byte-verification steps may have to be performed by the orchestrator from the main working tree
   after merge — as happened in 31-05. Say which agent actually produced the numbers above. Do not
   present orchestrator output as this executor's own.

3. **The pre-flight block** described above, including the killed-server PID and start time.

4. **STEP 0 — MANDATORY, DO THIS FIRST: tap ⟳ Re-check-in.** Marked mandatory, before any item is
   judged, with the reason stated rather than asserted: `ScheduleNotifier._loadToday()` reads today's
   schedule straight from Hive and `ScheduleGeneratorService.generate()` only runs at check-in with
   silent-replace, so **an already-generated day is never regenerated on load**. Name the 2026-08-21
   incident by date: a UAT that omitted this judged a pre-fix day, reported a false failure, and let
   the real defect survive three more days until the owner reported the identical symptom again on
   2026-08-24. Include a line for the owner to record that Step 0 was performed and on what date, and
   state that an unrecorded Step 0 invalidates the items below and must be re-run rather than
   assumed. The orchestrator cannot perform this step; it requires tapping the control in the running
   app.

5. **A "do this on a phone or tablet" instruction**, with the reason: a mouse cursor is one pixel and
   the entire question is what happens when the input is roughly 40dp of fingertip.

6. **Item 1 — can a thumb GRAB a 5-minute break now? (SKIPBREAK-01, D-31-06)**, re-asked in the same
   five-attempts form as round one so the before/after is directly comparable:
   - (a) Try it five times at a natural thumb placement. Count how many attempts grabbed the break
     versus the work block above or below it. Round one's answer was "hard to do this with a thumb";
     this is the number that has to move.
   - (b) Did the swipe complete — a red panel with a right-pointing arrow, and the break marked
     skipped?
   - (c) Did skipping the break ever accidentally complete or skip the work chunk next to it?
   State the new geometry honestly: the reachable band is now 24dp above and 24dp below the visible
   hairline, roughly 68dp against a 20dp painted row, up from roughly 52dp. State that the ceiling is
   real — the neighbouring 25-minute work chunk retains 52dp at this value and would drop under the
   48dp minimum at 32dp of slop — so "make it bigger again" is not an available answer if this still
   fails, and say what would be tried instead.

7. **Item 2 — can you SEE what to grab? (D-31-06 part 2)** This is the new question and it is the one
   that addresses the actual root cause. Ask whether a small grip glyph is visible at the left end of
   the `Short break` label, whether it reads as "grab here" rather than as decoration or as an
   artifact, and whether it is findable at arm's length without hunting. Note that it deliberately
   does not appear on a break that is already skipped, and ask whether that reads as correct.
   Explain why this question exists at all: 52dp already cleared both Material's 48dp and iOS's 44pt
   **on paper**, and those minimums assume a target the user can see — SKIPBREAK-02 forbids painting
   into the slop, so before this change the thumb had to land within ±16dp of a 20dp hairline by feel
   alone.

8. **Item 3 — can a break that is RUNNING RIGHT NOW be skipped, and does the now-line stay put?
   (D-31-07)** Give the owner a practical route to a live break rather than asking him to catch a
   five-minute window by luck: in this debug build, Settings carries the Phase 25 time-travel
   controls. Tell him to note a break's start time from the timeline, then use Settings to set the
   simulated clock to a minute inside that break's window, and return to Today. Warn explicitly that
   he should stay within the same calendar day — crossing midnight changes the "today" key and he
   would be looking at a different (or empty) schedule. Then ask:
   - (a) On the running break's card, is there a **Skip** action and **no Complete** action? (A
     30-minute long break shows a Skip icon; a 5-minute break's row is too short for a button, so
     there the gesture is the same leftward swipe as everywhere else — say this plainly rather than
     letting him hunt for a button that cannot fit.)
   - (b) Skip it. Does the row stay exactly where it was on the timeline, at the same height?
   - (c) Does the red now-line stay in the same place, or does it jump?
   - (d) Is there any visible sign the skip landed — the title struck through?
   Tell him to reset the dev clock in Settings when done.

9. **Two carried-forward notes, not questions.** (i) Round one's Items 2 and 3 (skipped-break
   legibility at `Opacity(0.5)`, and nothing else moving when a break is skipped) both PASSED on
   2026-08-26 and are settled; the uniform 0.5 stands and D-31-03's "mark it skipped and move on" is
   confirmed. Ask only that he says something if either now looks *different*, since this closure
   touched the same rows. (ii) Break cards are still not tappable, by his own 2026-08-21 instruction,
   and that is enforced by a test rather than by convention.

10. **A resume signal** ("approved" if every item passes; otherwise a per-item description), and a
    **remedies** section stating that any FAIL routes explicitly to a gap-closure plan in this phase
    or to a new phase, never noted and left — a recorded failure with no route is how Phase 24's
    DayComplete gap survived a full plan cycle.

11. **A `## Summary` block** with `total` / `passed` / `issues` / `pending` / `skipped` / `blocked`
    counts, and an empty `## Gaps` section carrying the YAML comment marker for
    `/gsd-plan-phase 31 --gaps` consumption, so a further round can be planned from it mechanically
    if needed.

Do not record a verdict for any item yourself. This task writes the document and stands up the
build; Task 2 is where a human answers it.
  </action>
  <verify>
    <automated>curl -s http://danserver:8143/main.dart.js | sha256sum | cut -d' ' -f1 | grep -qxF "$(sha256sum build/web/main.dart.js | cut -d' ' -f1)" && curl -s http://danserver:8143/main.dart.js | grep -c SwipeableRowShell</automated>
  </verify>
  <acceptance_criteria>
    - `lsof -i :8143` shows exactly one listener, and its start time is later than the build's `main.dart.js` mtime.
    - `sha256sum build/web/main.dart.js` and `curl -s http://danserver:8143/main.dart.js | sha256sum` produce identical digests, both pasted verbatim into the UAT.
    - `curl -s http://danserver:8143/main.dart.js | grep -c SwipeableRowShell` returns a non-zero count, pasted verbatim (or a documented substitute symbol with the reason recorded).
    - `curl -sI http://danserver:8143/main.dart.js | grep -i cache-control` shows `no-store`.
    - `.planning/phases/31-breaks-you-can-skip/31-GAPS-UAT.md` exists and its first item-level heading is Step 0, ahead of every numbered item.
    - The UAT contains the sentence distinguishing "the code shipped" from "the data on screen came from it", positioned immediately after the pre-flight block.
    - The UAT names who produced the build/serve/verification output (executor or orchestrator).
    - The UAT contains a `## Summary` block and a `## Gaps` section with the YAML consumption marker.
    - `flutter test` and `flutter analyze` are re-run from the merged tree and both are clean, with the totals pasted into the UAT.
  </acceptance_criteria>
  <done>A debug build of the merged gap closure is live on port 8143, provably byte-identical to what was just built and provably containing this closure's own symbols, on a port no stale server holds — and `31-GAPS-UAT.md` asks the three questions automation cannot answer, with a mandatory Step 0 in front of them.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: The owner's thumb, round two — blocking UAT</name>
  <files>.planning/phases/31-breaks-you-can-skip/31-GAPS-UAT.md</files>
  <precondition>A real touch device (phone or tablet) on the tailnet, and the server from Task 1 still listening on port 8143. There is no automated fallback for this and there cannot be one — a widget test fires a synthetic drag at an exact coordinate and cannot distinguish a 52dp acquisition band from a 68dp one.</precondition>
  <read_first>
    - `.planning/phases/31-breaks-you-can-skip/31-GAPS-UAT.md` as written by Task 1.
    - `.planning/phases/31-breaks-you-can-skip/31-UAT.md` — round one, for the exact wording of Item 1
      and the recorded verdict this round must be comparable against.
    - `CLAUDE.md` trap #4 in full.
  </read_first>
  <what-built>
Two changes ship together, both ruled by the owner on 2026-08-26 after round one of this UAT failed
its first item.

**D-31-06 — the acquisition fix, both halves.** The invisible band a thumb can grab a 5-minute break
by grew from roughly 52dp to roughly 68dp (24dp above and below the 20dp painted hairline). That
number has a hard ceiling: the break *wins* the contested band, so every dp comes out of the
neighbouring work chunk, which retains 52dp at this value and would fall under the 48dp minimum at
32. Alongside it, a small grip glyph now leads the `Short break` label — inside the existing 20dp
row, costing exactly zero painted pixels. That second half is the one that answers what actually
went wrong: 52dp already cleared both platform minimums, but those minimums assume a target the user
can see, and this one was invisible by design.

**D-31-07 — a break that is currently running can now be skipped.** Skip only, never Complete —
a break can never be "completed" in this app. On a long break that is a Skip icon on the live card;
on a 5-minute break, whose row is too short for any button, it is the same leftward swipe used
everywhere else. The row keeps its slot, stays on the timeline, and the now-line does not move.

A DEBUG web build is live and byte-verified at **`http://danserver:8143/`** — Task 1 compared the
served `main.dart.js` sha256 against the built one and grepped it for this closure's own new symbol,
so the bundle in front of you is provably this code.
  </what-built>
  <how-to-verify>
Run this on a **phone or tablet**, not a desktop pointer. A mouse cursor is one pixel; the entire
question is what happens when the input is roughly 40dp of fingertip. Record a verdict per item
directly in `31-GAPS-UAT.md`. Do not mark this task complete until a verdict exists for every item.

---

**STEP 0 — MANDATORY, DO THIS FIRST. Tap ⟳ Re-check-in.**

Not optional, not a formality. `ScheduleNotifier._loadToday()` reads today's schedule straight from
Hive and the generator only runs at check-in with silent-replace, so **an already-generated day is
never regenerated on load**. If today's schedule was built before this code landed, every item below
would be judged against a pre-change day while the new code sits unused in the bundle that was just
proven present. That exact omission happened on 2026-08-21: a UAT judged a pre-fix day, reported a
false failure, and let the real defect survive three more days until the identical symptom was
reported again on 2026-08-24. Re-check-in first, and record the date in the file.

---

**Item 1 — Can a thumb GRAB a 5-minute break now? (SKIPBREAK-01 / D-31-06)**

Find a 5-minute break — the thin hairline row labelled `Short break` between two work blocks. With a
thumb, not a fingernail and not a stylus, swipe it leftward.

- (a) Try five times at a natural thumb placement. How many of the five grabbed the break, versus
  grabbing the work block above or below? Round one's answer was "hard to do this with a thumb" —
  this is the number that has to move.
- (b) Did the swipe complete? A red panel with a right-pointing arrow, and the break marked skipped.
- (c) Did it ever accidentally complete or skip the work chunk next to it?

If (a) is still unreliable, say roughly how it fails — "it grabs the block above" and "nothing
happens at all" are different defects with different fixes. Note that "make the band bigger again"
is not available: at 32dp of slop the neighbouring work chunk drops below the 48dp minimum and the
defect just moves next door.

**Item 2 — Can you SEE what to grab? (D-31-06 part 2)**

- (a) Is there a small grip glyph at the left end of the `Short break` label?
- (b) Does it read as "grab here", or as decoration / an artifact / a rendering glitch?
- (c) At arm's length, can you find the break's grab point without hunting for it?
- (d) It deliberately disappears once a break is skipped. Does that read as right?

This is the question round one could not ask, and it is the one that matters most — the previous
target met every published touch-size minimum and still failed, because those minimums assume you
can see what you are aiming at.

**Item 3 — Can a break that is RUNNING RIGHT NOW be skipped? (D-31-07)**

You ruled on this on 2026-08-26: fold it in, Skip only. Here it is.

To reach a live break without waiting for one: note a break's start time on the timeline, go to
**Settings**, use the time-travel control to set the simulated clock to a minute inside that break's
window, and come back to Today. **Stay within the same calendar day** — crossing midnight changes
which day's schedule is loaded. Reset the dev clock when you are done.

- (a) On the running break's card, is there a **Skip** action and **no Complete** action? (A
  30-minute long break shows a Skip icon. A 5-minute break's row is too short for any button, so
  there it is the same leftward swipe as everywhere else — don't hunt for a button that cannot fit.)
- (b) Skip it. Does the row stay exactly where it was, at the same height?
- (c) Does the red now-line stay put, or jump?
- (d) Is there any visible sign the skip landed — the title struck through?

---

**Two things carried forward, not questions.** Round one's Items 2 and 3 — skipped-break legibility
at half opacity, and nothing else moving when a break is skipped — both PASSED and are settled. This
closure touched the same rows, so say something only if either now looks *different*. And break cards
are still not tappable, by your own 2026-08-21 instruction, enforced by a test rather than by
convention.

**Resume signal:** type "approved" if all three items pass. Otherwise describe what happened per
item. If you are unavailable, this records `verification_deferred_human` in `STATE.md` and the phase
stays open rather than closing on a green suite — which is exactly what Phases 29 and 30 did before
being judged honestly a working day later.
  </how-to-verify>
  <resume-signal>Type "approved" if all three items pass. Otherwise describe what happened per item — for Item 1 give the five-attempt count and say roughly how it still fails ("it grabs the block above" and "nothing happens at all" are different defects with different fixes); for Item 2 say whether you can see the grip at all and whether it reads as something to grab; for Item 3 say whether the running break offered a Skip, whether the row moved, and whether the now-line jumped. If either of round one's already-passed items now looks different, say that too.</resume-signal>
  <acceptance_criteria>
    - `31-GAPS-UAT.md` records "Step 0 performed" with a date, written by the human who tapped it — not inferred.
    - Every item (1, 2, 3) carries an explicit PASS or FAIL verdict with the owner's own words quoted.
    - Item 1 records the five-attempt count, so it is numerically comparable with round one.
    - The `## Summary` block's counts match the recorded verdicts.
    - Any FAIL is written into the `## Gaps` YAML block with `truth`, `status`, `reason`, `severity`, `root_cause`, `artifacts` and `missing` fields, so `/gsd-plan-phase 31 --gaps` can consume it mechanically.
    - `STATE.md` is updated with the verdict and, if any item failed, an explicit route.
  </acceptance_criteria>
  <done>A human verdict exists for all three items, recorded in `31-GAPS-UAT.md` in the owner's own words, with Item 1's five-attempt count comparable to round one — or the phase is explicitly left open as `verification_deferred_human` rather than closed on a green suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built bytes → served bytes → browser | Three places a stale artifact can be substituted for the one under test: a stale server on the port, a missing `Cache-Control`, and a browser cache entry predating the switch |
| served code → data on screen | An already-generated Hive day is rendered by new code without having been produced by it — the trap a passing byte-verification most encourages an agent to ignore |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-08-01 | Spoofing | A stale server on port 8143 impersonating this build | high | mitigate | `lsof -i :8143` checked and any prior listener killed BEFORE the sha256 comparison; PID and start time recorded in the UAT |
| T-31-08-02 | Tampering | Browser cache serving a previous bundle | high | mitigate | `tools/serve-uat.py` only (`Cache-Control: no-store`, conditional headers stripped); the header is asserted in the pre-flight |
| T-31-08-03 | Repudiation | A byte-verified bundle judged against a pre-change generated day | critical | mitigate | Step 0 is mandatory, dated, and recorded by the human; the UAT states in its own words that a non-zero grep proves the code shipped and not that the screen's data came from it |
| T-31-08-04 | Information Disclosure | A debug build with source maps exposed beyond the tailnet | low | accept | `danserver` is reachable only over Tailscale and LAN with no public exposure; a debug build is the deliberate choice at this stage per `CLAUDE.md` ("diagnostics over speed") |
| T-31-08-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no packages — it runs an existing build command and an existing repo-local server script. No package-manager step exists to gate. |
</threat_model>

<verification>
- Port 8143 held by exactly one listener, started after the build.
- Built and served `main.dart.js` sha256 identical, both pasted into the UAT.
- Served bundle greps non-zero for a symbol this closure introduced.
- `Cache-Control: no-store` present.
- `31-GAPS-UAT.md` leads with a mandatory, dated Step 0 and carries a verdict per item.
- Any FAIL routed into the `## Gaps` YAML block.
</verification>

<success_criteria>
The question that three green suites could not answer gets answered by a thumb, against a build
proven to be this one, on a day proven to have been generated by this code. A PASS closes Phase 31.
A FAIL is routed, not noted.
</success_criteria>

<output>
Create `.planning/phases/31-breaks-you-can-skip/31-08-SUMMARY.md` when done.
Record in it: the killed PID and its start time, both sha256 digests, the grep counts and which
symbol was used, whether the build/serve steps were run by this executor or by the orchestrator, and
the per-item verdicts.
</output>
