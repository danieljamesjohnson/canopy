---
phase: 31-breaks-you-can-skip
plan: 05
type: execute
wave: 4
depends_on: ["31-01", "31-02", "31-03", "31-04"]
files_modified:
  - .planning/phases/31-breaks-you-can-skip/31-UAT.md
autonomous: false
requirements: [SKIPBREAK-01, SKIPBREAK-02]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SKIPBREAK-01 (human): a real thumb can reliably initiate AND complete a skip swipe on a 5-minute break, on a real touch device, without also triggering the adjacent work chunk's own swipe. flutter test fires synthetic drags at exact coordinates and does not model finger size, so no assertion in this repository settles this."
    - "D-31-04 (human): the skipped visual state is legible at every tier, sub-compact included, at Opacity(0.5) against an already low-contrast onSurfaceVariant label. If it is not, the fix is to raise the opacity for that tier only, with the new value and its reason documented."
    - "D-31-03 (human): skipping a break visibly moves and resizes nothing else on the timeline — the work chunk after the break still starts at exactly the time it started at before the skip."
    - "The UAT judges the build the owner is actually looking at: the served main.dart.js is byte-identical to the built one (sha256 compared) and contains this phase's own new string, before the owner is asked to look at anything. Phase 29/30 closed this way and it is the reason their verdicts are trustworthy."
    - "PD-31-06 recorded, not assumed away: a break that is currently LIVE renders through LiveRowCard and has no skip affordance, before or after this phase. This is a pre-existing gap, deliberately left out of scope, and it is put in front of the owner in this UAT rather than discovered by him later."
    - statement: "A break that is currently in progress (the now-line falls inside its slot) can also be skipped, and the live-row treatment must compose with the skipped treatment rather than fight it — the row keeps its slot height, stays on the timeline, and does not move the now-line."
      verification: backstop
    - statement: "This phase adds no text length at any tier — strikethrough is a decoration flag, and the full-tier swap shortens the string. Inherited large-text-scale behaviour at the 20dp sub-compact tier is Phase 29's surface and is unchanged here."
      verification: backstop
  artifacts:
    - ".planning/phases/31-breaks-you-can-skip/31-UAT.md — the UAT script with a mandatory Step 0 re-check-in, the served-bytes proof, and the recorded verdict per item"
  key_links:
    - "CLAUDE.md trap #4 -> Step 0: an already-generated day is never regenerated on load, so any item judging scheduling-engine output must re-check-in FIRST. Omitting it once already cost the owner a round trip and let a real defect survive three extra days."
    - "CLAUDE.md trap #1 -> port 8143: never swap build types on one origin. 8143 has only ever served this project's debug builds; do not serve a release build on it."
    - "CLAUDE.md trap #3 -> tools/serve-uat.py: python3 -m http.server sends no Cache-Control, so the browser can serve a two-day-old bundle while the server has the right bytes."
  prohibitions:
    - "This UAT must never be judged without the re-check-in step being performed first — a pre-fix day judged against post-fix code produces a false verdict, which has already happened once in this project."
    - "Skipping a break must never pull the day forward — no subsequent chunk's start time may move as a result of a skip (out of scope by the owner's decision; it is an engine change of a different size)."
---

<objective>
Put the phase in front of a thumb.

Everything before this plan is green. That is exactly the condition under which this project has
twice shipped a wrong answer: Phase 27 scored 16 of 17 automated items and then failed 2 of 3
human ones; Phase 29's suite went 587-green and three pixel measurements deep while the owner was
looking at a screen with no breaks on it at all. "Can a thumb skip this break" is a physical
question about an *invisible* 52dp band whose edges the user cannot see, and no assertion in this
repository settles it.

Purpose: close Phase 31 on evidence rather than on a green suite, and surface the two things
planning deliberately left unresolved — sub-compact legibility at half opacity, and the live-break
skip gap — to the one person who can rule on them.

Output: a served, byte-verified debug build on port 8143, a UAT script whose Step 0 is a mandatory
re-check-in, and a recorded per-item verdict in `31-UAT.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/31-breaks-you-can-skip/31-VALIDATION.md
@.planning/phases/31-breaks-you-can-skip/31-UI-SPEC.md
@.planning/phases/31-breaks-you-can-skip/31-01-SUMMARY.md
@.planning/phases/31-breaks-you-can-skip/31-02-SUMMARY.md
@.planning/phases/31-breaks-you-can-skip/31-03-SUMMARY.md
@.planning/phases/31-breaks-you-can-skip/31-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Phase-wide list (repeated in every plan of Phase 31). This plan owns `31-UAT.md`.

| Artifact | Kind | Where |
|---|---|---|
| `kBreakHitSlop` = `16.0` | `const double` | `lib/screens/today/timeline_geometry.dart` (plan 31-01) |
| `kMinBreakDragTarget` = `48.0` | `const double` | `lib/screens/today/timeline_geometry.dart` (plan 31-01) |
| `SwipeableChunkCard.visualHeight` | new optional `double?` param | `lib/screens/schedule/widgets/swipeable_chunk_card.dart` (plan 31-01) |
| `_needsSlop(ScheduledChunk, TimelineGeometry)` | new private predicate | `lib/screens/today/today_screen.dart` (plan 31-01) |
| Layer 1b Stack pass | new `for` loop | `lib/screens/today/today_screen.dart` (plan 31-01) |
| `_SubCompactRow.isSkipped` | new `bool` param | `lib/screens/schedule/widgets/chunk_card.dart` (plan 31-02) |
| compact-tier `Semantics` wrapper | new widget | `lib/screens/schedule/widgets/chunk_card.dart` (plan 31-02) |
| `if (next.isSkipped) return null;` | new guard | `lib/providers/schedule_notifier.dart` (plan 31-04) |
| hit-test + painted-grid cases | new test cases | `test/screens/today_screen_test.dart` (plans 31-01, 31-03) |
| skipped-state cases in `break densities` | new test cases | `test/screens/today_row_widgets_test.dart` (plan 31-02) |
| new cases in `G-05 no-op guards` + streak inertness | new test cases | `test/providers/schedule_notifier_break_extension_test.dart` (plan 31-04) |
| `31-RED-tracer.txt`, `31-RED-d3105.txt` | captured RED evidence | `.planning/phases/31-breaks-you-can-skip/` |
| **`31-UAT.md`** | **human UAT script + recorded verdict** | **`.planning/phases/31-breaks-you-can-skip/`** |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Build, serve, and prove the served bytes are the built bytes</name>
  <files>.planning/phases/31-breaks-you-can-skip/31-UAT.md</files>
  <precondition>Port 8143 is free (nothing already bound to it) and `tools/serve-uat.py` exists at the repository root. Port 8143 has only ever served this project's DEBUG builds — do not serve a release build on it (CLAUDE.md trap #1).</precondition>
  <read_first>
    - `CLAUDE.md` § "Local hosting for UAT" in full, including all four traps. Traps #1, #3 and #4
      each independently determine a step below.
    - `tools/serve-uat.py` — confirm it still sends `Cache-Control: no-store` and strips
      conditional-request headers.
    - `.planning/STATE.md` § "Resolved Verification" — how Phase 29/30's UAT was closed
      (sha256-compared served bytes, plus a grep of `main.dart.js` for the phase's own new
      symbol). Reproduce that recipe, do not invent a new one.
    - `.planning/phases/29-breaks-you-can-see/tools/` — the existing harness, reusable as-is.
  </read_first>
  <action>
Confirm the phase is actually ready to be looked at first: run the full suite and the analyzer,
and abort if either is not clean. There is no point spending the owner's attention on a build that
does not pass its own tests.

Build the debug web bundle with exactly the command `CLAUDE.md` prescribes:
`flutter build web --debug --source-maps --pwa-strategy=none`. Do not use
`flutter run -d web-server` — that is the DDC incremental compiler, which fans the app into ~866
module files and turns tailscale latency into minutes of serial round-trips. Do not build release:
release minifies every stack frame to `main.dart.js:<n>`, and early UAT wants diagnostics over
speed. `--pwa-strategy=none` registers no service worker, so this build cannot create a trap-#1
cache collision on its own.

Prove the served bytes are the built bytes, before the owner is asked to look at anything:
1. `sha256sum build/web/main.dart.js` and record the digest.
2. Serve with `python3 tools/serve-uat.py 8143 --dir build/web` in the background. Never
   `python3 -m http.server` — it sends no `Cache-Control`, so the browser is free to keep serving
   a cached bundle and the owner judges a two-day-old build (trap #3, which cost a round trip in
   Phase 25's UAT).
3. `curl -s http://danserver:8143/main.dart.js | sha256sum` and confirm the digest matches step 1.
4. `curl -s http://danserver:8143/main.dart.js | grep -c ", skipped"` and confirm a non-zero
   count. That string literal is introduced by this phase's sub-compact and compact semantics
   labels, so a non-zero count proves this phase's code is in the bundle being served — not merely
   that some bundle is.

Record all four results, the digest, and the exact command lines in a new
`.planning/phases/31-breaks-you-can-skip/31-UAT.md`, with a `Status: awaiting owner` header and
the three-item script Task 2 defines. Then write the UAT script itself into that file, with the
re-check-in as Step 0 (Task 2 gives its wording).

Note in the file that a non-zero grep count proves the *code* shipped and NOT that the *data on
screen* was produced by it — that distinction is trap #4, it is what Step 0 exists for, and an
agent who has just run this grep is exactly the agent most likely to skip Step 0 because the grep
felt like proof.

Finally, hand over the URL `http://danserver:8143/` and note that the check must be done on a
phone or tablet, not a desktop pointer. A mouse cursor is a single pixel; the entire question this
UAT exists to answer is what happens when the input is roughly 40dp of fingertip.
  </action>
  <acceptance_criteria>
    - `PATH="$PATH:/home/dan/development/flutter/bin" flutter test` green and `flutter analyze`
      clean, both captured in `31-UAT.md`.
    - `build/web/main.dart.js` exists and was produced by a `--debug --source-maps
      --pwa-strategy=none` build — the command line is recorded verbatim in `31-UAT.md`.
    - `curl -s http://danserver:8143/main.dart.js | sha256sum` matches
      `sha256sum build/web/main.dart.js`; both digests recorded in `31-UAT.md`.
    - `curl -s http://danserver:8143/main.dart.js | grep -c ", skipped"` returns a non-zero count,
      recorded in `31-UAT.md`.
    - `31-UAT.md` exists with `Status: awaiting owner`, the three UAT items, and Step 0 as the
      first instruction in the document body.
    - `pgrep -af "serve-uat.py 8143"` shows exactly one server, and no release build has ever been
      served on that port.
  </acceptance_criteria>
  <verify>
    <automated>test "$(sha256sum build/web/main.dart.js | cut -d' ' -f1)" = "$(curl -s http://danserver:8143/main.dart.js | sha256sum | cut -d' ' -f1)" && curl -s http://danserver:8143/main.dart.js | grep -c ", skipped"</automated>
  </verify>
  <done>A debug build is serving on 8143, its served bytes are proven identical to the built bytes, this phase's own new string is proven present in the bundle, and `31-UAT.md` records all of it.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: The owner's thumb — blocking UAT</name>
  <files>.planning/phases/31-breaks-you-can-skip/31-UAT.md</files>
  <precondition>A real touch device (phone or tablet) on the tailnet, and the server from Task 1 still running on port 8143. There is no automated fallback for this — it is the reason this phase cannot close on `flutter test`.</precondition>
  <read_first>
    - `.planning/phases/31-breaks-you-can-skip/31-UAT.md` as written by Task 1.
    - `CLAUDE.md` trap #4 in full.
    - `.planning/phases/31-breaks-you-can-skip/31-VALIDATION.md` § "Manual-Only Verifications" —
      the three rows below are that table, made into instructions.
  </read_first>
  <what-built>
Phase 31 makes a break skippable by the same left-swipe gesture that skips a work chunk, at every
density including the 20dp 5-minute one. A break's touch target is grown to roughly 52dp by an
invisible 16dp band above and below its painted row; nothing painted moved. A skipped break renders
muted and struck through at all three tiers. Skipping a break changes nothing else about the day.

A DEBUG web build is already running and byte-verified at **`http://danserver:8143/`** — Task 1
sha256-compared the served `main.dart.js` against the built one and grepped it for this phase's own
new string, so the bundle in front of you is provably this phase's code.
  </what-built>
  <how-to-verify>
Run the following on a **phone or tablet**, not a desktop pointer. A mouse cursor is one pixel; the
entire question this check exists to answer is what happens when the input is roughly 40dp of
fingertip. Record a verdict per item in `31-UAT.md`. Do not mark this task complete until a verdict
exists for every item. If the owner is unavailable, record `verification_deferred_human` and leave
the phase open — that is what Phases 29 and 30 did, and both were judged honestly a working day
later rather than quietly passed.

---

**STEP 0 — MANDATORY, DO THIS FIRST. Tap ⟳ Re-check-in.**

This is not optional and it is not a formality. `ScheduleNotifier._loadToday()` reads today's
schedule straight from Hive, and the generator only runs at check-in. **An already-generated day
is never regenerated on load**, so if today's schedule was built before this phase's code landed,
every item below would be judged against a pre-change day while the new code sits unused in the
bundle. This exact omission happened on 2026-08-21: a UAT judged a pre-fix day, reported a false
failure, and let the real defect survive three more days until the owner reported the identical
symptom again. Re-check-in first. Item 3 in particular is meaningless without it.

---

**Item 1 — Can a thumb skip a 5-minute break? (SKIPBREAK-01)**

Find a 5-minute break on the timeline — the thin hairline row with `Short break` between two work
blocks. Using a thumb, not a fingernail and not a stylus, swipe it **leftward**. Then judge:

- (a) Did the swipe start reliably? Try it five times at a natural thumb placement. Count how many
  attempts grabbed the break versus grabbed the work block above or below it.
- (b) Did the swipe complete? A left swipe should reveal a red panel with a right-pointing arrow
  and mark the break skipped.
- (c) Did skipping the break ever accidentally complete or skip the work chunk next to it?

The target is an invisible band 16dp above and 16dp below the visible hairline, so the reachable
area is roughly 52dp against a 20dp visible row. If (a) is unreliable, say roughly how it fails —
"it grabs the block above" and "nothing happens at all" are different defects with different
fixes.

**Item 2 — Is a skipped break legible? (D-31-04)**

Look at a skipped 5-minute break next to an unresolved one, at arm's length.

- (a) Can you tell at a glance which one is skipped?
- (b) Is the struck-through label still readable, or has half opacity on an already-grey label
  pushed it past readable?

If (b) fails, the planned fix is to raise the opacity **for the sub-compact tier only** and
document the new value — do not raise it everywhere. This was flagged in advance as a plausible
risk that could not be settled from a desk.

Also glance at a **30-minute long break** and skip one: it should read as skipped too, with its
trailing duration text replaced by the word `skipped`.

**Item 3 — Did anything else move? (D-31-03) — requires Step 0**

Before skipping anything, note the start time of the work chunk immediately AFTER a break. Skip
that break. Then check that same start time again.

It must be unchanged. Skipping a break does **not** hand those minutes back — the day is
duration-exact and time-anchored, so the next work chunk still starts exactly when it always did.
This is deliberate and it is counter-intuitive; if it reads as wrong to you, say so, because
pulling the day forward is a real option that was explicitly deferred rather than dismissed and it
would need your decision to plan.

---

**Two things planning deliberately did NOT do — flagging them so you find out from us, not later:**

1. **A break that is currently running still has no skip affordance.** While the now-line is
   inside a break's own slot, that row renders through the live-row card, which shows its
   Complete/Skip buttons for work chunks only. That was already true before this phase and this
   phase did not change it (PD-31-06). If you want a running break to be skippable too, say so —
   it is a small, separate change, not a defect in this one.
2. **Break cards are still not tappable.** No detail sheet, at any density. That was your own
   instruction on 2026-08-21 and it is enforced by a test, not by convention.
  </how-to-verify>
  <resume-signal>Type "approved" if all three items pass. Otherwise describe what happened per item — for Item 1 say roughly how the swipe failed ("it grabs the block above" and "nothing happens at all" are different defects with different fixes); for Item 2 say whether the label was unreadable at sub-compact; for Item 3 say whether the following chunk's start time moved. Also say what you want done about the two deliberate exclusions.</resume-signal>
  <acceptance_criteria>
    - `31-UAT.md` contains an explicit PASS / FAIL / PARTIAL verdict for each of Items 1, 2 and 3,
      each with the owner's own words attached rather than a paraphrase.
    - `31-UAT.md` records whether Step 0 (⟳ Re-check-in) was actually performed, and the date.
      An unrecorded Step 0 invalidates Item 3 and must be re-run, not assumed.
    - The two deliberate exclusions are recorded as acknowledged by the owner, with any routing
      decision he makes about them captured (a new phase, a seed, or "leave it").
    - Any FAIL is routed explicitly — to a gap-closure plan in this phase, or to a new phase —
      rather than being noted and left. A recorded failure with no route is how Phase 24's
      DayComplete gap survived a full plan cycle.
    - `31-UAT.md` `Status:` is updated from `awaiting owner` to the resulting state.
  </acceptance_criteria>
  <verify>
    <human-check>The owner has walked all three items on a real touch device against the port-8143 build, with Step 0 performed first, and a verdict per item is recorded in `31-UAT.md`.</human-check>
  </verify>
  <done>A per-item human verdict exists in `31-UAT.md`, Step 0 is recorded as performed, both deliberate exclusions are acknowledged, and every FAIL has a route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tailnet -> `serve-uat.py` on 8143 | A static file server bound to all interfaces, reachable only over Tailscale and the local LAN. No public exposure. |
| finger -> the running app | The gesture surface under test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-09 | Information Disclosure | `serve-uat.py` bound to `0.0.0.0` on 8143 | low | accept | The box is reachable only over Tailscale and the local LAN, with no public exposure; the served artifact is a debug build of a local-first app containing no secrets. Accepted for the duration of the UAT; stop the server when the verdict is recorded. |
| T-31-10 | Spoofing | judging a stale bundle or a stale day as if it were the change under test | high | mitigate | The whole verdict becomes false. Mitigated three ways: `--pwa-strategy=none` (no service worker, trap #1), `tools/serve-uat.py`'s `no-store` plus stripped conditional headers (trap #3), a served-vs-built sha256 comparison and a grep of the bundle for this phase's own new string (Task 1), and Step 0's mandatory re-check-in (trap #4). Rated high because it has already produced a false verdict in this project once. |
| T-31-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install occurs in this phase. |
</threat_model>

<verification>
- Full suite green and `flutter analyze` clean, captured in `31-UAT.md` before the owner is asked
  to look.
- Served-vs-built sha256 match recorded.
- A non-zero grep count for this phase's new string in the served `main.dart.js`, recorded.
- A per-item human verdict recorded, with Step 0 confirmed performed.
</verification>

<success_criteria>
- A thumb can reliably start and complete a skip swipe on a 5-minute break, or the failure mode is
  described precisely enough to fix.
- The skipped state is legible at every tier, or the sub-compact opacity is corrected with a
  documented value.
- Nothing else on the day moved when a break was skipped.
- Both deliberate exclusions were surfaced to the owner rather than discovered by him.
</success_criteria>

<output>
Create `.planning/phases/31-breaks-you-can-skip/31-05-SUMMARY.md` when done, and finalize
`.planning/phases/31-breaks-you-can-skip/31-UAT.md` with the recorded verdicts. If the owner is
unavailable, record `verification_deferred_human` and say so plainly in `STATE.md` — do not close
the phase on a green suite alone.
</output>
