---
phase: 31-breaks-you-can-skip
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [SKIPBREAK-01, SKIPBREAK-02]
files_modified:
  - lib/screens/today/timeline_geometry.dart
  - lib/screens/schedule/widgets/chunk_card.dart
  - test/screens/today_screen_test.dart
  - test/screens/today_row_widgets_test.dart
  - .planning/phases/31-breaks-you-can-skip/31-RED-slop24.txt
  - .planning/phases/31-breaks-you-can-skip/31-RED-grip.txt

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A 5-minute break's acquisition band is 68dp tall (20dp painted + 24dp above + 24dp below), and a drag started 22dp above the painted top edge — a point that was OUTSIDE the shipped 16dp band — resolves to that break (D-31-06 part 1, SKIPBREAK-01)."
    - "A 25-minute work chunk with a break on both sides retains at least kMinBreakDragTarget (48dp) of its own uncontested acquisition band at the shipped slop value, so the fix does not move the defect next door (D-31-06 ceiling)."
    - "An unresolved sub-compact break row paints a visible grip glyph leading its label, so the acquisition target is findable by eye rather than by feel alone (D-31-06 part 2 — the half that answers the root cause the UAT actually found)."
    - "The grip changes the sub-compact row's natural, unclipped height by exactly zero pixels: an unresolved sub-compact break row and a skipped one (which renders no grip) measure the identical height in the same harness (SKIPBREAK-02)."
    - "A skipped break renders no grip — the glyph is an affordance signal for a swipe that is still available, not decoration (consistent with SwipeableChunkCard giving a resolved chunk DismissDirection.none)."
    - "kBreakHitSlop's doc comment states the D-31-06 arithmetic re-derived at the shipped value — the break's own resulting band, the neighbouring work chunk's retained band, the value at which that neighbour would drop under 48dp — and states whether the omitted per-neighbour clamp's never-binds claim still holds, with the binding threshold named in minutes."
    - "dismissThresholds is unchanged anywhere in lib/ — the owner reported acquisition difficulty, not completion difficulty, and the evidence for a threshold change still does not exist."
    - "Every new behavioural assertion in this plan has an executed RED capture on disk proving it can fail, and the production file it was proven against is restored byte-identical before the commit."
    - statement: "This phase adds no text length at any tier — strikethrough is a decoration flag, and the full-tier swap shortens the string. Inherited large-text-scale behaviour at the 20dp sub-compact tier is Phase 29's surface and is unchanged here. The grip glyph is a fixed-size box and does not scale with text, so it cannot introduce new text-driven growth."
      verification: backstop
  artifacts:
    - "lib/screens/today/timeline_geometry.dart — kBreakHitSlop raised to 24.0 with re-derived doc comment"
    - "lib/screens/schedule/widgets/chunk_card.dart — kSubCompactGripSize and the grip glyph inside _SubCompactRow"
    - "test/screens/today_screen_test.dart — the literal-coordinate 68dp band case and the ceiling guard"
    - "test/screens/today_row_widgets_test.dart — the grip presence/absence cases and the zero-extent proof"
    - ".planning/phases/31-breaks-you-can-skip/31-RED-slop24.txt"
    - ".planning/phases/31-breaks-you-can-skip/31-RED-grip.txt"
  key_links:
    - "_needsSlop (today_screen.dart:697) gates slop on heightFor(...) < kMinBreakDragTarget — it reads kBreakHitSlop nowhere, so raising the constant must NOT require editing it, and Layer 1a's exclusion / Layer 1b's inclusion must keep agreeing on it (31-RESEARCH.md Pitfall 1)."
    - "SwipeableChunkCard.visualHeight confines every painted pixel to the slot via Align(center) + SizedBox — the grip is inside that confinement, which is exactly why the screen-level ClipRect measurement CANNOT prove the grip's zero-extent claim and the widget-level unbounded harness must."
    - "_SubCompactRow's Divider(height: 1) comment names an unzeroed default box height as the class of miss that broke the compact tier in Phase 29 — the grip is that same hazard in a new costume."
---

<objective>
Close **Gap 1** of the Phase 31 human UAT: the owner, on a real phone, could not reliably **grab**
a 5-minute break's row with a thumb. Not complete the swipe — grab it.

D-31-06 (LOCKED, owner, 2026-08-26) rules that the fix is **both** halves, and that shipping only
the first does not satisfy it:

1. `kBreakHitSlop` 16.0 → 24.0 — the acquisition band goes 52dp → 68dp.
2. A **visible grip glyph** inside the existing 20dp label row of `_SubCompactRow`.

Purpose: 625 green tests and 12/12 verified code truths were contradicted by a thumb, for the third
time in this project. The root cause is not that 52dp was under a platform minimum — it cleared
both. It is that **those minimums assume a target the user can see**, and SKIPBREAK-02 forbids
painting into the slop, so the band is invisible by construction. Part 1 makes the target bigger;
part 2 makes it findable. Only part 2 addresses what the owner actually experienced.

Output: a 68dp acquisition band with its ceiling arithmetic re-derived and documented, a grip glyph
that costs exactly zero painted pixels, and two executed RED captures proving the new assertions can
fail.

**Explicitly NOT in this plan** (all offered to the owner and all declined): a slop-only fix, an
affordance in the left time-gutter outside the grid, long-press-to-skip, and any change to
`dismissThresholds`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/31-breaks-you-can-skip/31-CONTEXT.md
@.planning/phases/31-breaks-you-can-skip/31-UAT.md
@.planning/phases/31-breaks-you-can-skip/31-RESEARCH.md
@.planning/phases/31-breaks-you-can-skip/31-03-SUMMARY.md
</context>

## Artifacts this phase produces

New or changed symbols, files and test groups created by this plan. A later plan or a reviewer
looking for "what did the gap closure add" should find every one of these:

| Symbol / artifact | File | Kind |
|---|---|---|
| `kBreakHitSlop` = `24.0` (was `16.0`) | `lib/screens/today/timeline_geometry.dart` | changed constant + rewritten doc comment |
| `kSubCompactGripSize` = `14.0` | `lib/screens/schedule/widgets/chunk_card.dart` | new `const double` |
| grip glyph (`Icons.drag_indicator`) inside `_SubCompactRow` | `lib/screens/schedule/widgets/chunk_card.dart` | new widget child |
| group `D-31-06 — a bigger, findable acquisition band` | `test/screens/today_screen_test.dart` (nested inside the existing `Phase 31 — SKIPBREAK: breaks you can skip` group) | new test group |
| group `D-31-06 — the sub-compact grip glyph` | `test/screens/today_row_widgets_test.dart` (nested inside the existing Phase 31/SEEBREAK break-card group) | new test group |
| `skipped` parameter on `_pumpBreakCardUnbounded` | `test/screens/today_row_widgets_test.dart` | new optional test-helper parameter |
| `31-RED-slop24.txt` | `.planning/phases/31-breaks-you-can-skip/` | RED capture artefact |
| `31-RED-grip.txt` | `.planning/phases/31-breaks-you-can-skip/` | RED capture artefact |

<tasks>

<task type="auto">
  <name>Task 1: Raise kBreakHitSlop to 24.0, re-derive its arithmetic, and pin the 68dp band with a literal-coordinate drag</name>
  <files>lib/screens/today/timeline_geometry.dart, test/screens/today_screen_test.dart, .planning/phases/31-breaks-you-can-skip/31-RED-slop24.txt</files>
  <precondition>`flutter` resolves on PATH. If it does not, prepend `/home/dan/development/flutter/bin` to PATH before running any verify command in this plan — it is not on the default non-login shell PATH on this machine.</precondition>
  <read_first>
    - `lib/screens/today/timeline_geometry.dart` in full — specifically `kBreakHitSlop`'s existing
      doc comment (its PD-31-01 paragraph, the symmetric-slop justification, and its explicit claim
      that the omitted `clamp(kBreakHitSlop, 0, precedingRowSlot / 2)` guard "can never bind"), plus
      `kMinBreakDragTarget` and `kPixelsPerMinute`.
    - `lib/screens/today/today_screen.dart` lines ~689-706 (`_needsSlop`), ~814-881 (the break arm of
      `_buildPositionedRow` and the comment block above it), and ~1460-1522 (the Layer 1a exclusion
      and the Layer 1b pass). You are changing a constant these read; confirm none of them needs
      editing and that the Layer 1a/1b agreement on `_needsSlop` is untouched.
    - `.planning/phases/31-breaks-you-can-skip/31-RESEARCH.md` Pitfall 1 — the z-order mechanism the
      Layer 1b pass exists to provide.
    - `test/screens/today_screen_test.dart` lines 2377-2700 — the whole existing
      `Phase 31 — SKIPBREAK: breaks you can skip` group: `skipTracerFixture()`, the top-band tracer,
      the vacuity guard, the bottom-band case, the negative/no-theft case, and the below-threshold
      case. Every existing origin in that group is computed *symbolically* from `kBreakHitSlop`; note
      that this is precisely why none of them can distinguish a 52dp band from a 68dp one.
    - `.planning/phases/31-breaks-you-can-skip/31-03-SUMMARY.md` — the executed non-vacuity RED
      protocol (delete the fix from the *production* file, run, capture the named failure verbatim,
      restore byte-identical, confirm `git diff` empty). Reuse it exactly.
  </read_first>
  <action>
Change `kBreakHitSlop` from `16.0` to `24.0` in `lib/screens/today/timeline_geometry.dart`, per
D-31-06 part 1. Change no other constant, and change nothing in `today_screen.dart` — `_needsSlop`
gates on `kMinBreakDragTarget`, not on the slop value, so the Layer 1a/1b split needs no edit;
confirm that by reading, and say so in the summary rather than assuming it.

Rewrite `kBreakHitSlop`'s doc comment so the number is **derived in place, not restated**. It must
now carry, computed at 24.0 rather than quoted from the old text:

- The break's own resulting acquisition band for a 5-minute break: its 20dp painted slot plus 24dp
  above and 24dp below.
- What that costs the neighbour, and why that is the binding constraint: the Layer 1b pass makes the
  break *win* the contested band, so every dp of slop is taken from the neighbouring work chunk's
  own effective target. A 25-minute work chunk is 100dp at `kPixelsPerMinute`; with a break on both
  sides it loses twice the slop. State what it retains at 16, at 24, and at 32, and state that the
  32 case falls below `kMinBreakDragTarget` — which would push the *work* chunk under the minimum
  and simply move the defect next door. State the resulting ceiling of roughly 24-26dp explicitly,
  as the reason this value cannot be raised again without re-deriving the whole paragraph.
- **Re-examine, do not restate, the PD-31-01 "can never bind" claim.** The omitted per-neighbour
  clamp would bind whenever a neighbour's slot is under twice the slop value. Work out what
  neighbour duration that corresponds to at `kPixelsPerMinute`, state it in minutes, and then
  actually check whether today's generator can emit a chunk that short — read
  `lib/services/schedule_generator.dart` (the discretionary packing loop, `buildCommitmentChunks`,
  and the trailing-trim step) before writing the sentence. If the claim still holds, say so and name
  the new margin. If it no longer holds unconditionally, say that instead and name the condition;
  do NOT preserve an absolute claim you could not confirm.
- Why the value moved at all: cite the 2026-08-26 human UAT finding by name — a platform minimum met
  by an *invisible* target is a weaker guarantee than one met by a visible target — and record that
  D-31-06 pairs this increase with the grip glyph in `_SubCompactRow`, so a future reader who deletes
  the glyph knows they have removed half of a two-part fix.

Then extend `test/screens/today_screen_test.dart`'s existing
`Phase 31 — SKIPBREAK: breaks you can skip` group with a new nested group named
`D-31-06 — a bigger, findable acquisition band`, reusing `skipTracerFixture()` and the group's
established `_FakeScheduleNotifierWithSchedule` recording fake and 9:00 AM DayComplete clock (the
existing comment explains why 9:00 and not 18:00 — the fixture must stay inside the test viewport).

Add two cases:

**Case A — the band is genuinely 68dp.** Locate the break's painted band the way the existing cases
do (the innermost `ClipRect` ancestor of the `Short break` text — never the enclosing `Positioned`,
which is legitimately taller). Start a leftward drag from a point **22 logical pixels above the
painted top edge**, written as a bare numeric literal `22`, NOT as an expression involving
`kBreakHitSlop`. Assert the recording fake's `lastSkippedId` is `b1`. Add a `reason` that states
plainly why the literal is deliberate: an origin computed from the constant moves with the constant
and therefore cannot discriminate a 52dp band from a 68dp one — which is exactly how a suite stayed
green through a defect a thumb found. Also assert `lastSkippedId` is not `w1`, so a failure names
the thief rather than merely reporting a miss.

**Case B — the neighbour keeps its own minimum.** A pure-constant ceiling guard, no pump: assert
that a 25-minute work chunk's slot (`25 * kPixelsPerMinute`) minus twice `kBreakHitSlop` is greater
than or equal to `kMinBreakDragTarget`, and assert `kBreakHitSlop` is less than or equal to `26.0`.
Give both a `reason` naming D-31-06's ceiling and stating that raising the slop past it moves the
defect to the adjacent work chunk instead of fixing it. This case is the guard that makes a future
"just bump it again" fail loudly.

Do not modify any existing case in the group. Do not touch `dismissThresholds` — it does not appear
in the swipe wrapper today and must not appear after this plan; the owner reported acquisition
difficulty, not completion difficulty, and the evidence for a completion-threshold change still does
not exist.

**Then run the RED experiment before finalising**, per 31-03's protocol: temporarily set
`kBreakHitSlop` back to `16.0` in the production file, run the group, and confirm Case A fails with
the preceding work chunk named as the thief. Capture the run's output verbatim — including the
expected-vs-actual lines and the reason string — to
`.planning/phases/31-breaks-you-can-skip/31-RED-slop24.txt`, with a short header stating what was
temporarily changed and that it was reverted. Restore `24.0`, confirm `git diff` on
`timeline_geometry.dart` shows only the intended change, and only then commit. The RED capture and
the restored file land in the same commit; the experiment itself must never be a commit of its own.
  </action>
  <verify>
    <automated>flutter test test/screens/today_screen_test.dart --plain-name "SKIPBREAK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'const double kBreakHitSlop = 24.0;' lib/screens/today/timeline_geometry.dart` returns `1`.
    - `grep -c 'kBreakHitSlop = 16.0' lib/screens/today/timeline_geometry.dart` returns `0`.
    - `git diff --name-only` does NOT list `lib/screens/today/today_screen.dart` — the Layer 1a/1b machinery needed no edit.
    - `git status --porcelain lib/screens/schedule/widgets/swipeable_chunk_card.dart` is empty — the file that owns swipe completion is untouched by this plan.
    - `flutter test test/screens/today_screen_test.dart --plain-name "SKIPBREAK"` passes, and its case count is strictly greater than the pre-change count for the same filter (record both numbers in the summary).
    - `test/screens/today_screen_test.dart` contains the bare literal `22` as Case A's vertical drag offset, and Case A's origin expression does NOT reference `kBreakHitSlop`.
    - `.planning/phases/31-breaks-you-can-skip/31-RED-slop24.txt` exists, is non-empty, and contains the verbatim failure line naming `w1` as the actual value where `b1` was expected.
    - `kBreakHitSlop`'s doc comment contains a duration in minutes naming the neighbour size at which the omitted clamp would begin to bind, and an explicit statement of whether the never-binds claim still holds — checked by reading, and quoted in the summary.
    - `flutter analyze` reports no issues.
  </acceptance_criteria>
  <done>A drag started 22dp above a 5-minute break's painted top edge resolves to that break; the same drag against the old 16.0 constant demonstrably resolves to the preceding work chunk, captured on disk; the neighbouring work chunk provably retains at least 48dp; and the constant's doc comment derives all of it at 24.0 with the never-binds claim re-checked rather than restated.</done>
</task>

<task type="auto">
  <name>Task 2: Add the visible grip glyph to _SubCompactRow at exactly zero painted cost</name>
  <files>lib/screens/schedule/widgets/chunk_card.dart, test/screens/today_row_widgets_test.dart, .planning/phases/31-breaks-you-can-skip/31-RED-grip.txt</files>
  <read_first>
    - `lib/screens/schedule/widgets/chunk_card.dart` lines ~340-450 — `_SubCompactRow` in full,
      including its class doc comment and **especially** the in-body comment explaining that
      `Divider.height: 1` is load-bearing because `Divider`'s own box height defaults to Material 3's
      16.0 `DividerThemeData.space`, and that omitting it is "the same class of miss" that broke the
      compact tier in Phase 29. The glyph you are adding is that identical hazard wearing a different
      costume: any widget with an unpinned default box height inflates this row.
    - `lib/screens/schedule/widgets/chunk_card.dart` lines ~149-200 — `_buildBreak`'s density
      `if`-chain and the `subCompact` branch that constructs `_SubCompactRow`, including the
      `isSkipped` / `', skipped'` semantics wiring from D-31-04.
    - `lib/screens/schedule/widgets/swipeable_chunk_card.dart` — `_confineContent` and
      `_confineReveal`. Understand that the painted content is wrapped in
      `Align(center) + SizedBox(height: visualHeight) + ClipRect + OverflowBox`, so **the screen-level
      `ClipRect` is always exactly `visualHeight` tall no matter how tall its child is**. That is why
      the existing `SKIPBREAK-02 — the grid is unchanged` screen test cannot prove anything about
      this glyph, and why this task's proof must be a widget-level unbounded measurement instead.
    - `test/screens/today_row_widgets_test.dart` lines ~130-165 (`_pumpBreakCardUnbounded` and its
      doc comment explaining why a bounded `Scaffold(body:)` makes any height comparison
      meaningless), lines ~722-756 (the `SEEBREAK-01: sub-compact renders shorter than compact` case
      and its PD-29-06 note that only *relative* height claims are trustworthy in this harness), and
      lines ~791-900 (the D-31-04 skipped-break cases and the `pumpWithMood` helper usage).
    - `.planning/phases/31-breaks-you-can-skip/31-03-SUMMARY.md` — the RED protocol, reused here.
  </read_first>
  <action>
Add a named constant `kSubCompactGripSize` with the value `14.0` to
`lib/screens/schedule/widgets/chunk_card.dart`, declared above `_SubCompactRow` with a doc comment
that records: it is D-31-06 part 2; it is deliberately smaller than the `bodySmall` label's own line
box so it can never become the row's tallest child; and it is named rather than inlined because it is
the single number standing between this row and the SKIPBREAK-02 violation that
`_SubCompactRow`'s `Divider.height` comment already warns about.

Add the glyph inside `_SubCompactRow.build`'s `Row`, positioned per D-31-06 **between the leading
`Expanded(Divider)` and the `Padding`/`Text`** — leading the label, inside the existing 20dp slot,
painting into no part of the invisible slop band.

Concrete shape:

- Use `Icons.drag_indicator` — the Material six-dot grab affordance. It is direction-neutral and
  reads as "grab this", which is the acquisition problem the owner reported. Do not use a chevron or
  an arrow; those assert a direction and would re-open a design question the owner did not rule on.
- Give the `Icon` an explicit `size: kSubCompactGripSize`, and wrap it in a `SizedBox` whose `width`
  and `height` are both `kSubCompactGripSize`. The `SizedBox` is belt-and-braces against a future
  `IconTheme` change silently restoring the 24.0 default box; state that in a short comment that
  cross-references the `Divider.height` warning already in this widget.
- Colour it `theme.colorScheme.onSurfaceVariant` — the same colour the label `Text` already uses, so
  the glyph reads as part of the same element rather than as separate chrome. Do NOT use
  `outlineVariant` (the divider colour): the entire finding is that this target was not findable, and
  the faintest colour in the row is the wrong answer to that.
- Render the glyph **only when the row is not skipped**. A resolved chunk gets
  `DismissDirection.none` from `SwipeableChunkCard`, so a grip on a skipped break would advertise a
  gesture that no longer exists. Gate it on `_SubCompactRow`'s existing `isSkipped` field.
- Add no `Semantics` and no `semanticLabel`. `_SubCompactRow` already wraps everything in
  `Semantics(excludeSemantics: true)` with the D-31-04 label; a labelled icon inside would either be
  swallowed silently or perturb an announcement the owner already passed. Note this in the comment
  so it reads as a decision rather than an omission.

Do not touch the `compact` or `full` break tiers. D-31-06 names `_SubCompactRow` specifically, and
those tiers' painted slots (a 30-minute break is 120dp) already clear `kMinBreakDragTarget` on their
own without any slop at all — a grip there would be decoration answering no reported problem.

Then add a new nested group `D-31-06 — the sub-compact grip glyph` to
`test/screens/today_row_widgets_test.dart`, inside the existing break-card group alongside the
D-31-04 cases. First extend `_pumpBreakCardUnbounded` with an optional `bool skipped = false`
parameter forwarded into `_breakChunk`, so the same unbounded harness can pump both states — the
existing call sites keep their current behaviour untouched.

Add three cases:

**Case A — presence.** Pump an unresolved sub-compact break and assert exactly one
`Icon` with `icon == Icons.drag_indicator` is a descendant of the `ChunkCard`.

**Case B — absence when skipped.** Pump a *skipped* sub-compact break and assert that same finder
matches nothing, with a `reason` stating that a resolved chunk is not swipeable so the grip must not
advertise a gesture that no longer exists.

**Case C — the SKIPBREAK-02 zero-extent proof.** This is the load-bearing one and it must be
measured on the **natural, unclipped** height, never through a `ClipRect`. Using
`_pumpBreakCardUnbounded`, measure `tester.getSize(find.byType(ChunkCard)).height` for the
unresolved (grip-bearing) sub-compact break, then unmount and measure it again for the skipped
(grip-free) one, and assert the two are **exactly equal**. Because the grip is present in one and
absent in the other, an inflating glyph makes these two numbers differ — the comparison is
self-contained, needs no recorded literal, and cancels the placeholder-font bias PD-29-06 warns
about, exactly like the neighbouring `SEEBREAK-01 renders shorter than compact` case does. Give it a
`reason` naming SKIPBREAK-02 and stating both measured values so a failure is diagnosable. Add a
second, weaker assertion in the same case that the measured unresolved height is not greater than
`5 * kPixelsPerMinute`, flagged in its `reason` as a harness bound rather than a device requirement,
per this file's standing caveat.

Add a short comment at the top of the new group recording why the proof lives here and not in
`today_screen_test.dart`'s `SKIPBREAK-02 — the grid is unchanged` group: that group measures the
confined `ClipRect`, whose height is `visualHeight` by construction regardless of its child, so it
would pass unchanged even if this glyph inflated the row by 10dp. A future agent must not mistake
that green test for this glyph's proof.

**Then run the RED experiment**, per 31-03's protocol: temporarily change the production `Icon`'s
`size` to `24.0` and delete the pinning `SizedBox` in `chunk_card.dart`, run the new group, and
confirm Case C fails with the two heights differing. Capture the output verbatim — including the
expected-vs-actual numbers — to `.planning/phases/31-breaks-you-can-skip/31-RED-grip.txt` with a
header naming what was temporarily changed. Restore the file, confirm `git diff` shows only the
intended change, and commit the restored file together with the capture.
  </action>
  <verify>
    <automated>flutter test test/screens/today_row_widgets_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'const double kSubCompactGripSize = 14.0;' lib/screens/schedule/widgets/chunk_card.dart` returns `1`.
    - `grep -c 'Icons.drag_indicator' lib/screens/schedule/widgets/chunk_card.dart` returns `1`.
    - `grep -c 'kSubCompactGripSize' lib/screens/schedule/widgets/chunk_card.dart` returns at least `3` (declaration, `Icon.size`, and the pinning `SizedBox`).
    - `flutter test test/screens/today_row_widgets_test.dart` passes, including the three new cases.
    - `flutter test test/screens/today_screen_test.dart` passes with no change to any existing SKIPBREAK-02 case.
    - `.planning/phases/31-breaks-you-can-skip/31-RED-grip.txt` exists, is non-empty, and contains a verbatim failure showing two unequal heights for the grip-bearing and grip-free rows.
    - `git diff lib/screens/schedule/widgets/chunk_card.dart` shows no change to the `compact` or `full` branches of `_buildBreak`.
    - `flutter analyze` reports no issues.
  </acceptance_criteria>
  <done>An unresolved 5-minute break paints a visible grip leading its label; a skipped one paints none; the two rows measure the identical natural height; and a deliberately oversized glyph is proven on disk to break that equality.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression, RED artefact audit, and the honest coverage note</name>
  <files>test/screens/today_screen_test.dart, test/screens/today_row_widgets_test.dart</files>
  <read_first>
    - The two RED captures written by Tasks 1 and 2.
    - `.planning/phases/31-breaks-you-can-skip/31-VERIFICATION.md` — the truth table, so this plan's
      summary can state which truths it newly makes provable and which it leaves to plan 31-07.
    - `.planning/STATE.md` § "Carry-Forward Invariants" — specifically "Regression tests must be
      proven RED" and the test-harness caveat about placeholder-font measurements.
  </read_first>
  <action>
Run the whole suite and `flutter analyze`, and reconcile the result against the pre-change baseline
of 625 passing tests. Record the new total and the delta in the summary. Any test that changed from
passing to failing is a real regression from this plan and must be fixed here, not deferred — with
one exception to check for explicitly rather than assume away: the existing Phase 31 drag cases
compute their origins symbolically from `kBreakHitSlop`, so they should track the new value without
edits. If any of them did need editing, that is a finding worth stating plainly in the summary,
because it would mean an origin was not as symbolic as the group's own comments claim.

Audit both RED captures for the property that actually matters: each must show the **named** failure
the corresponding fix protects against, not merely a non-zero exit code. `31-RED-slop24.txt` must
name `w1` as the actual value; `31-RED-grip.txt` must show two unequal heights. If either capture
shows only a generic failure, the experiment did not prove what it claims and must be re-run.

Confirm both production files are restored: `git diff` on `timeline_geometry.dart` and
`chunk_card.dart` must show only this plan's intended changes, with no residue of either temporary
experiment.

Finally, add a short dated comment to the existing `SKIPBREAK-02 — the grid is unchanged` group in
`test/screens/today_screen_test.dart` recording what that group does and does not prove after this
plan: it measures the confined `ClipRect`, which is `visualHeight` tall by construction, so it proves
the *slot* never grew but is structurally incapable of detecting an inflated *child*. Point the
reader at the widget-level zero-extent case added in Task 2 as the glyph's actual proof. This is a
comment-only edit; add no assertion to that group.
  </action>
  <verify>
    <automated>flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test` reports all tests passing, with a total strictly greater than 625; both numbers recorded in the summary.
    - `flutter analyze` reports no issues.
    - Both RED capture files exist, are non-empty, and each contains the specific named failure described above (quote the naming line from each in the summary).
    - `git diff lib/screens/today/timeline_geometry.dart lib/screens/schedule/widgets/chunk_card.dart` contains no leftover experiment edit — no `16.0` slop value and no `24.0` icon size.
    - The `SKIPBREAK-02 — the grid is unchanged` group carries the new dated comment and no new assertion.
  </acceptance_criteria>
  <done>The full suite is green above its prior baseline, both RED captures name their specific thief, both production files are byte-clean, and the one screen-level test that looks like it covers the glyph carries an in-file note saying it does not.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user touch → timeline row hit-test | An imprecise physical input crosses into a gesture dispatch that mutates persisted schedule state |
| widget-test harness → device reality | Assertions taken under a placeholder font and synthetic exact-coordinate drags are treated as evidence about a fingertip |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-06-01 | Tampering | `kBreakHitSlop` grown band vs. neighbouring work chunk | medium | mitigate | Task 1 Case B pins the neighbour's retained band at ≥ `kMinBreakDragTarget` as a pure-constant guard, so a future raise past ~26dp fails loudly instead of silently moving the defect next door |
| T-31-06-02 | Repudiation | A green assertion that cannot fail | high | mitigate | Both new behavioural claims carry an executed RED capture naming the specific thief; Task 3 audits the captures for that naming rather than for a non-zero exit code |
| T-31-06-03 | Denial of Service | Grip glyph inflating the 20dp row and re-clipping the sub-compact tier | medium | mitigate | Pinned `SizedBox` at `kSubCompactGripSize`, plus a widget-level natural-height equality proof measured outside any `ClipRect`, proven RED with an oversized glyph |
| T-31-06-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no packages — it edits two Dart source files and two Dart test files. No package-manager step exists to gate. |
</threat_model>

<verification>
- `flutter test` green, total above the 625 baseline.
- `flutter analyze` clean.
- `31-RED-slop24.txt` and `31-RED-grip.txt` both on disk, both naming their specific failure.
- `kBreakHitSlop` is 24.0 with a doc comment that derives the band, the neighbour's retained target,
  the ceiling, and a re-checked verdict on the omitted clamp's never-binds claim.
- `dismissThresholds` appears nowhere in `lib/`, and `swipeable_chunk_card.dart` is untouched.
- The grip is present on an unresolved sub-compact break, absent on a skipped one, and costs zero
  painted pixels.
</verification>

<success_criteria>
Both halves of D-31-06 are shipped and independently proven: the acquisition band is 68dp with its
ceiling documented and guarded, and the target is visible for the first time. Neither half was
shipped alone. Nothing about swipe *completion* changed.
</success_criteria>

<output>
Create `.planning/phases/31-breaks-you-can-skip/31-06-SUMMARY.md` when done.
Record in it: the pre/post test totals, the quoted naming line from each RED capture, the verdict on
the never-binds claim (with the neighbour duration in minutes), and whether any existing Phase 31
drag case needed editing.
</output>
