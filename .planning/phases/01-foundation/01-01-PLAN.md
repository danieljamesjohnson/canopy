---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/main.dart
autonomous: true
requirements:
  - foundation-implicit
must_haves:
  truths:
    - "flutter pub get runs without errors"
    - "All required packages are present in pubspec.yaml with correct versions"
    - "Project directory structure matches the architecture defined in RESEARCH.md"
    - "lib/main.dart compiles without errors after removing the counter demo"
  artifacts:
    - path: "pubspec.yaml"
      provides: "All Phase 1 dependencies declared"
      contains: "hive_ce"
    - path: "lib/main.dart"
      provides: "Minimal app entry point (no counter demo code)"
      min_lines: 10
    - path: "lib/data/models/"
      provides: "Directory for Hive entity models"
    - path: "lib/data/repositories/"
      provides: "Directory for repository interfaces"
    - path: "lib/data/database/"
      provides: "Directory for database init and migration runner"
    - path: "lib/providers/"
      provides: "Directory for ChangeNotifier classes"
    - path: "lib/screens/home/"
      provides: "Directory for home screen"
    - path: "lib/screens/onboarding/"
      provides: "Directory for onboarding screen"
    - path: "lib/screens/goals/"
      provides: "Directory for goals screen"
    - path: "lib/screens/schedule/"
      provides: "Directory for schedule screen"
    - path: "lib/screens/quarterly_review/"
      provides: "Directory for quarterly review screen"
    - path: "lib/screens/settings/"
      provides: "Directory for settings screen"
  key_links:
    - from: "pubspec.yaml"
      to: "flutter pub get"
      via: "dependency resolution"
      pattern: "hive_ce.*2\\.19"
---

<objective>
Add all Phase 1 package dependencies to pubspec.yaml, create the project directory structure defined in RESEARCH.md, and replace the Flutter counter demo in lib/main.dart with a minimal placeholder that compiles cleanly.

Purpose: Every subsequent plan in this phase needs the packages available and the directory structure in place before writing any files into those directories. This is the one plan that must run before all others.

Output: Updated pubspec.yaml with all dependencies; clean directory scaffold under lib/; minimal lib/main.dart placeholder; build.yaml for hive_ce_generator.
</objective>

<execution_context>
@C:/Users/Dan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@lib/main.dart
@pubspec.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add all Phase 1 dependencies to pubspec.yaml and create build.yaml</name>
  <files>pubspec.yaml, build.yaml</files>
  <action>
Replace the dependencies section of pubspec.yaml with the full stack from RESEARCH.md. The exact versions to use:

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  hive_ce: ^2.19.3
  hive_ce_flutter: ^2.3.4
  go_router: ^17.1.0
  provider: ^6.1.5+1
  shared_preferences: ^2.5.4
  uuid: ^4.5.3
  intl: ^0.20.2
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  hive_ce_generator: ^1.11.1
  build_runner: ^2.4.13

Keep the existing `name`, `description`, `publish_to`, `version`, `environment`, and `flutter` sections unchanged. Only the dependencies and dev_dependencies blocks change.

Also create build.yaml at project root to configure hive_ce_generator:

```yaml
targets:
  $default:
    builders:
      hive_ce_generator|hive_ce_generator:
        enabled: true
        options:
          use_null_safety: true
```

After editing, run: flutter pub get

Verify pub get succeeds (exit code 0) before proceeding.
  </action>
  <verify>
    <automated>cd D:/FlutterApps/canopy && flutter pub get 2>&amp;1 | tail -5</automated>
    <manual>Output ends with "Got dependencies." or similar success message; no resolution errors.</manual>
  </verify>
  <done>flutter pub get exits 0; pubspec.yaml contains hive_ce, go_router, provider, shared_preferences, uuid, intl, path_provider, hive_ce_generator, build_runner at the specified versions.</done>
</task>

<task type="auto">
  <name>Task 2: Create project directory structure and minimal main.dart placeholder</name>
  <files>
    lib/main.dart,
    lib/data/models/.gitkeep,
    lib/data/repositories/.gitkeep,
    lib/data/database/.gitkeep,
    lib/providers/.gitkeep,
    lib/screens/home/.gitkeep,
    lib/screens/onboarding/.gitkeep,
    lib/screens/goals/.gitkeep,
    lib/screens/schedule/.gitkeep,
    lib/screens/quarterly_review/.gitkeep,
    lib/screens/settings/.gitkeep
  </files>
  <action>
Create the directory structure from RESEARCH.md by placing a .gitkeep file in each leaf directory. The directories to create under lib/:
  - lib/data/models/
  - lib/data/repositories/
  - lib/data/database/
  - lib/providers/
  - lib/screens/home/
  - lib/screens/onboarding/
  - lib/screens/goals/
  - lib/screens/schedule/
  - lib/screens/quarterly_review/
  - lib/screens/settings/

Replace lib/main.dart entirely with a minimal placeholder that:
1. Imports only package:flutter/material.dart
2. Has a main() that calls runApp(const CanopyApp())
3. Has a CanopyApp StatelessWidget returning a MaterialApp with title 'Canopy', Material 3 theme using ColorScheme.fromSeed(seedColor: Colors.deepOrangeAccent), and home: Scaffold(body: Center(child: Text('Canopy')))
4. Has NO counter demo code (no MyHomePage, no _counter, no FloatingActionButton)

The MaterialApp in this placeholder will be replaced by MaterialApp.router in plan 01-04. Using MaterialApp temporarily here is intentional — go_router is wired in plan 01-04 after all its dependencies exist.

Run flutter analyze after creating files to verify zero issues.
  </action>
  <verify>
    <automated>cd D:/FlutterApps/canopy && flutter analyze 2>&amp;1 | tail -10</automated>
    <manual>flutter analyze reports "No issues found!" or only info-level messages (no errors, no warnings).</manual>
  </verify>
  <done>All 11 directories exist under lib/; lib/main.dart compiles cleanly showing a minimal Scaffold; flutter analyze reports zero issues.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `flutter pub get` exits 0 — all packages resolved
2. `flutter analyze` reports zero issues
3. `grep -r "hive_ce" pubspec.yaml` confirms hive_ce dependency present
4. Directory structure matches RESEARCH.md architecture exactly
5. lib/main.dart contains no counter demo code
</verification>

<success_criteria>
- pubspec.yaml has all 10 runtime dependencies and 3 dev dependencies from RESEARCH.md at the specified versions
- build.yaml exists at project root with hive_ce_generator configuration
- All 11 sub-directories exist under lib/ (data/models, data/repositories, data/database, providers, screens/home, screens/onboarding, screens/goals, screens/schedule, screens/quarterly_review, screens/settings)
- lib/main.dart is a minimal placeholder with no counter demo; flutter analyze clean
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` with:
- What was done (deps added, structure created)
- Package versions confirmed installed
- Any issues encountered during pub get
</output>
