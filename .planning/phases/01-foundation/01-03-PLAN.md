---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - lib/router.dart
  - lib/screens/home/home_screen.dart
  - lib/screens/onboarding/onboarding_screen.dart
  - lib/screens/goals/goals_screen.dart
  - lib/screens/schedule/schedule_screen.dart
  - lib/screens/quarterly_review/quarterly_review_screen.dart
  - lib/screens/settings/settings_screen.dart
  - lib/providers/goals_notifier.dart
  - lib/providers/commitments_notifier.dart
  - lib/providers/schedule_notifier.dart
  - lib/providers/settings_notifier.dart
autonomous: true
requirements:
  - foundation-implicit
must_haves:
  truths:
    - "go_router routes between at least two placeholder screens without errors"
    - "StatefulShellRoute.indexedStack wraps the four main tab screens (home, goals, schedule, settings)"
    - "Onboarding route exists outside the shell (no bottom nav bar shown)"
    - "Quarterly review route exists outside the shell (no bottom nav bar shown)"
    - "Four ChangeNotifier stub classes exist and compile"
    - "NavigationBar widget used for bottom navigation (Material 3)"
  artifacts:
    - path: "lib/router.dart"
      provides: "GoRouter instance with all 6 screen routes"
      contains: "StatefulShellRoute"
    - path: "lib/screens/home/home_screen.dart"
      provides: "HomeScreen stub widget"
      contains: "HomeScreen"
    - path: "lib/screens/onboarding/onboarding_screen.dart"
      provides: "OnboardingScreen stub widget"
      contains: "OnboardingScreen"
    - path: "lib/screens/goals/goals_screen.dart"
      provides: "GoalsScreen stub widget"
      contains: "GoalsScreen"
    - path: "lib/screens/schedule/schedule_screen.dart"
      provides: "ScheduleScreen stub widget"
      contains: "ScheduleScreen"
    - path: "lib/screens/quarterly_review/quarterly_review_screen.dart"
      provides: "QuarterlyReviewScreen stub widget"
      contains: "QuarterlyReviewScreen"
    - path: "lib/screens/settings/settings_screen.dart"
      provides: "SettingsScreen stub widget"
      contains: "SettingsScreen"
    - path: "lib/providers/goals_notifier.dart"
      provides: "GoalsNotifier ChangeNotifier stub"
      contains: "GoalsNotifier"
    - path: "lib/providers/commitments_notifier.dart"
      provides: "CommitmentsNotifier ChangeNotifier stub"
      contains: "CommitmentsNotifier"
    - path: "lib/providers/schedule_notifier.dart"
      provides: "ScheduleNotifier ChangeNotifier stub"
      contains: "ScheduleNotifier"
    - path: "lib/providers/settings_notifier.dart"
      provides: "SettingsNotifier ChangeNotifier stub"
      contains: "SettingsNotifier"
  key_links:
    - from: "lib/router.dart"
      to: "lib/screens/*/screen.dart"
      via: "GoRoute builder imports"
      pattern: "import.*screens.*screen\\.dart"
    - from: "lib/router.dart"
      to: "lib/providers/settings_notifier.dart"
      via: "refreshListenable for onboarding redirect"
      pattern: "refreshListenable"
---

<objective>
Create the go_router configuration with StatefulShellRoute.indexedStack for bottom navigation, six stub screen widgets, and four ChangeNotifier stubs. This plan runs in parallel with plan 01-02 (models) since it touches different files.

Purpose: go_router must be in place before main.dart is wired in plan 01-04. The stub screens are the source of truth for all screens that will exist — retrofitting routing after multiple screens exist is painful especially for Web URL support per ROADMAP.md.

Output: lib/router.dart with full route table; 6 stub screen widgets; 4 ChangeNotifier stubs.
</objective>

<execution_context>
@C:/Users/Dan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create six stub screen widgets and four ChangeNotifier stubs</name>
  <files>
    lib/screens/home/home_screen.dart,
    lib/screens/onboarding/onboarding_screen.dart,
    lib/screens/goals/goals_screen.dart,
    lib/screens/schedule/schedule_screen.dart,
    lib/screens/quarterly_review/quarterly_review_screen.dart,
    lib/screens/settings/settings_screen.dart,
    lib/providers/goals_notifier.dart,
    lib/providers/commitments_notifier.dart,
    lib/providers/schedule_notifier.dart,
    lib/providers/settings_notifier.dart
  </files>
  <action>
Create stub screen widgets. Each screen is a StatelessWidget returning a Scaffold with an AppBar showing the screen name and a centered Text placeholder. No business logic.

--- lib/screens/home/home_screen.dart ---
```dart
import 'package:flutter/material.dart';

class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Home')),
      body: const Center(child: Text('Home — coming in Phase 3')),
    );
  }
}
```

Repeat this pattern for:
- lib/screens/onboarding/onboarding_screen.dart — class OnboardingScreen, title 'Onboarding', body 'Onboarding — coming in Phase 2'
- lib/screens/goals/goals_screen.dart — class GoalsScreen, title 'Goals', body 'Goals — coming in Phase 2'
- lib/screens/schedule/schedule_screen.dart — class ScheduleScreen, title 'Schedule', body 'Schedule — coming in Phase 3'
- lib/screens/quarterly_review/quarterly_review_screen.dart — class QuarterlyReviewScreen, title 'Quarterly Review', body 'Quarterly Review — coming in Phase 5'
- lib/screens/settings/settings_screen.dart — class SettingsScreen, title 'Settings', body 'Settings — coming in Phase 4'

Create the four ChangeNotifier stubs. Each is an empty ChangeNotifier — no state, no methods, no business logic. Phase 2+ will add real state.

--- lib/providers/goals_notifier.dart ---
```dart
import 'package:flutter/foundation.dart';

class GoalsNotifier extends ChangeNotifier {
  // Phase 1 stub — Phase 2 adds goal state and CRUD methods.
}
```

--- lib/providers/commitments_notifier.dart ---
```dart
import 'package:flutter/foundation.dart';

class CommitmentsNotifier extends ChangeNotifier {
  // Phase 1 stub — Phase 2 adds commitment block state and methods.
}
```

--- lib/providers/schedule_notifier.dart ---
```dart
import 'package:flutter/foundation.dart';

class ScheduleNotifier extends ChangeNotifier {
  // Phase 1 stub — Phase 3 adds schedule generation and state.
}
```

--- lib/providers/settings_notifier.dart ---
```dart
import 'package:flutter/foundation.dart';

class SettingsNotifier extends ChangeNotifier {
  // Phase 1 stub. Holds onboarding completion flag for router redirect.
  // Will be wired to shared_preferences in plan 01-04.
  bool _onboardingComplete = false;
  bool get onboardingComplete => _onboardingComplete;

  void setOnboardingComplete(bool value) {
    _onboardingComplete = value;
    notifyListeners(); // triggers go_router redirect re-evaluation
  }
}
```

SettingsNotifier gets minimal state NOW because it is needed by go_router's refreshListenable in the next task. The other three notifiers are fully empty stubs.
  </action>
  <verify>
    <automated>cd D:/FlutterApps/canopy &amp;&amp; dart analyze lib/screens/ lib/providers/ 2>&amp;1 | tail -5</automated>
    <manual>6 screen files and 4 provider files exist; dart analyze reports no issues on those directories.</manual>
  </verify>
  <done>6 stub screen .dart files and 4 ChangeNotifier .dart files exist; dart analyze on lib/screens/ and lib/providers/ reports zero issues.</done>
</task>

<task type="auto">
  <name>Task 2: Create go_router configuration with StatefulShellRoute</name>
  <files>lib/router.dart</files>
  <action>
Create lib/router.dart with the GoRouter instance. Use StatefulShellRoute.indexedStack for the four main tab screens. Onboarding and quarterly review are outside the shell (no bottom nav).

```dart
import 'package:flutter/material.dart';
import 'package:go_router/go_router.dart';

import 'providers/settings_notifier.dart';
import 'screens/home/home_screen.dart';
import 'screens/onboarding/onboarding_screen.dart';
import 'screens/goals/goals_screen.dart';
import 'screens/schedule/schedule_screen.dart';
import 'screens/quarterly_review/quarterly_review_screen.dart';
import 'screens/settings/settings_screen.dart';

GoRouter createRouter(SettingsNotifier settingsNotifier) {
  return GoRouter(
    initialLocation: '/home',
    refreshListenable: settingsNotifier,
    redirect: (context, state) {
      final onboardingDone = settingsNotifier.onboardingComplete;
      final onOnboarding = state.matchedLocation == '/onboarding';

      if (!onboardingDone && !onOnboarding) return '/onboarding';
      if (onboardingDone && onOnboarding) return '/home';
      return null;
    },
    routes: [
      StatefulShellRoute.indexedStack(
        builder: (context, state, navigationShell) {
          return _ScaffoldWithNavBar(navigationShell: navigationShell);
        },
        branches: [
          StatefulShellBranch(
            routes: [
              GoRoute(
                path: '/home',
                builder: (context, state) => const HomeScreen(),
              ),
            ],
          ),
          StatefulShellBranch(
            routes: [
              GoRoute(
                path: '/goals',
                builder: (context, state) => const GoalsScreen(),
              ),
            ],
          ),
          StatefulShellBranch(
            routes: [
              GoRoute(
                path: '/schedule',
                builder: (context, state) => const ScheduleScreen(),
              ),
            ],
          ),
          StatefulShellBranch(
            routes: [
              GoRoute(
                path: '/settings',
                builder: (context, state) => const SettingsScreen(),
              ),
            ],
          ),
        ],
      ),
      // Onboarding is outside the shell — no bottom nav shown.
      GoRoute(
        path: '/onboarding',
        builder: (context, state) => const OnboardingScreen(),
      ),
      // Quarterly review is outside the shell — full-screen experience.
      GoRoute(
        path: '/review',
        builder: (context, state) => const QuarterlyReviewScreen(),
      ),
    ],
  );
}

class _ScaffoldWithNavBar extends StatelessWidget {
  const _ScaffoldWithNavBar({required this.navigationShell});

  final StatefulNavigationShell navigationShell;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: navigationShell,
      bottomNavigationBar: NavigationBar(
        selectedIndex: navigationShell.currentIndex,
        onDestinationSelected: (index) {
          navigationShell.goBranch(
            index,
            initialLocation: index == navigationShell.currentIndex,
          );
        },
        destinations: const [
          NavigationDestination(icon: Icon(Icons.home_outlined), label: 'Home'),
          NavigationDestination(icon: Icon(Icons.flag_outlined), label: 'Goals'),
          NavigationDestination(icon: Icon(Icons.calendar_today_outlined), label: 'Schedule'),
          NavigationDestination(icon: Icon(Icons.settings_outlined), label: 'Settings'),
        ],
      ),
    );
  }
}
```

IMPORTANT: GoRouter is created via a factory function `createRouter(SettingsNotifier)`, NOT as a top-level final variable. This is because the notifier must be constructed from the Provider tree first, and then passed in. The router is wired up in plan 01-04's main.dart rewrite. Do NOT initialize it as a global — the RESEARCH.md pitfall #3 explains why: GoRouter initialized before the Provider tree fails when redirect reads Provider state.

Do NOT add `refreshListenable` pointing to a Listenable you are constructing inside the GoRouter constructor — the SettingsNotifier instance must come from outside.
  </action>
  <verify>
    <automated>cd D:/FlutterApps/canopy &amp;&amp; dart analyze lib/router.dart 2>&amp;1 | tail -5</automated>
    <manual>lib/router.dart exists; dart analyze reports no issues; file contains StatefulShellRoute, NavigationBar, and redirect logic.</manual>
  </verify>
  <done>lib/router.dart created with GoRouter factory function; StatefulShellRoute.indexedStack with 4 branches; onboarding and review routes outside shell; NavigationBar with 4 destinations; SettingsNotifier passed in as parameter for refreshListenable; dart analyze clean.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `dart analyze lib/screens/ lib/providers/ lib/router.dart` reports zero issues
2. lib/router.dart contains "StatefulShellRoute" and "refreshListenable"
3. All 6 screen files exist
4. All 4 provider files exist
5. SettingsNotifier has onboardingComplete getter and setOnboardingComplete method
</verification>

<success_criteria>
- 6 stub screen widgets created (home, onboarding, goals, schedule, quarterly_review, settings)
- 4 ChangeNotifier stubs created (goals, commitments, schedule, settings)
- lib/router.dart has createRouter(SettingsNotifier) factory with StatefulShellRoute, onboarding redirect, and NavigationBar
- dart analyze on all new files reports zero issues
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` with:
- Route table (path → screen class)
- Navigation structure decisions (bottom nav with 4 tabs; onboarding/review outside shell)
- SettingsNotifier state added for router redirect
- Any go_router version-specific API observations
</output>
