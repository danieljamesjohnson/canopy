---
phase: 06-desktop-and-web-polish
plan: 06
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - test/providers/theme_notifier_test.dart
  - test/screens/responsive_layout_test.dart
  - test/screens/chunk_card_hover_test.dart
  - test/screens/goal_card_hover_test.dart
  - test/screens/goal_card_drag_handle_test.dart
  - test/screens/home_screen_breathing_pulse_test.dart
  - test/platform/window_setup_test.dart
  - test/screens/router_redirect_test.dart
  - test/screens/quarterly_review_test.dart
autonomous: true
requirements: [AC-1, AC-2, AC-3, AC-4, AC-5, AC-6]
threat_refs: [T-router-1]

must_haves:
  truths:
    - "ThemeNotifier._modulateHsl peaks at noon and troughs at midnight (verified with synthetic DateTime)"
    - "ThemeNotifier.setMoodSeed triggers notifyListeners"
    - "ThemeNotifier uses curiousSeed when _moodSeed is null"
    - "On AppLifecycleState.paused the ticker cancels; on .resumed it restarts and notifyListeners fires"
    - "_resetIfDayChanged sets _moodSeed = null when persisted lastMoodSetYmd != today"
    - "ResponsiveShell shows NavigationBar at 480dp and 720dp inclusive boundary shows NavigationRail"
    - "ResponsiveShell shows NavigationRail at 1200dp"
    - "MouseRegion.onEnter on ChunkCard reveals checkbox + skip (opacity 1); onExit hides (opacity 0)"
    - "tester.drag on ChunkCard does NOT make hover icons appear (touch ≠ pointer-enter)"
    - "InkWell.onHover on GoalCard reveals edit + archive"
    - "Drag handle is hidden on Android/iOS and visible at 0.6 opacity on macOS via defaultTargetPlatform override"
    - "Breathing pulse animates when isPreCheckin is true; stops when false"
    - "Breathing pulse respects MediaQuery.disableAnimations (controller not animating, set to value 0.5)"
    - "setupDesktopWindow() completes without throwing on the test host"
    - "go_router redirect respects onboardingComplete on direct URL load (router_redirect_test)"
    - "quarterly_review_test.dart is migrated to pumpWithMood and still passes"
  artifacts:
    - path: test/providers/theme_notifier_test.dart
      provides: "Unit tests for HSL modulation, setMoodSeed/resetToCurious notifyListeners, ticker lifecycle, curiousSeed default, daily rollover seam"
    - path: test/screens/responsive_layout_test.dart
      provides: "Widget tests for LayoutBuilder breakpoints at 480/720/1200dp"
    - path: test/screens/chunk_card_hover_test.dart
      provides: "Widget tests for MouseRegion hover reveal + Dismissible non-conflict"
    - path: test/screens/goal_card_hover_test.dart
      provides: "Widget tests for InkWell.onHover reveal of edit + archive icons"
    - path: test/screens/goal_card_drag_handle_test.dart
      provides: "Widget tests for platform-gated drag handle visibility (defaultTargetPlatform)"
    - path: test/screens/home_screen_breathing_pulse_test.dart
      provides: "Widget tests for breathing pulse on/off + disableAnimations respect"
    - path: test/platform/window_setup_test.dart
      provides: "Unit test that setupDesktopWindow completes without throwing"
    - path: test/screens/router_redirect_test.dart
      provides: "Widget tests verifying go_router redirect honors onboardingComplete on direct URL load"
    - path: test/screens/quarterly_review_test.dart
      provides: "MIGRATED — existing test file now uses pumpWithMood helper instead of bare MaterialApp(home:...)"
  key_links:
    - from: "test/screens/responsive_layout_test.dart"
      to: "lib/widgets/responsive_shell.dart"
      via: "import 'package:canopy/widgets/responsive_shell.dart' + tester.view.physicalSize at 480/720/1200"
      pattern: "ResponsiveShell"
    - from: "test/providers/theme_notifier_test.dart"
      to: "lib/providers/theme_notifier.dart"
      via: "Constructs ThemeNotifier with InMemoryAppSettingsRepository fake + synthetic DateTime + timeModulationEnabled flag"
      pattern: "ThemeNotifier\\("
---

<objective>
Write all Wave 4 tests in one parallel-safe plan: 8 new test files plus one migration of the existing `quarterly_review_test.dart` to the new `pumpWithMood` helper. These tests provide the Nyquist Dimension 8 coverage documented in `06-VALIDATION.md` — each acceptance criterion (AC-1 through AC-6) gets at least one automated row.

Purpose: AC-5 explicitly requires "All existing widget tests pass; new LayoutBuilder breakpoint tests pass at 480dp, 720dp, and 1200dp." This plan satisfies that AC and provides defense-in-depth tests for every other AC (hover reveals, drag handle visibility, breathing pulse, router redirect, window setup) and AC-6's unit-testable ThemeNotifier surface.

Output: 8 new test files + 1 modified test file. All `flutter test` runs green. Plan 07 then handles the manual UAT checklist + ROADMAP update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-desktop-and-web-polish/06-CONTEXT.md
@.planning/phases/06-desktop-and-web-polish/06-RESEARCH.md
@.planning/phases/06-desktop-and-web-polish/06-PATTERNS.md
@.planning/phases/06-desktop-and-web-polish/06-UI-SPEC.md
@.planning/phases/06-desktop-and-web-polish/06-VALIDATION.md
@.planning/phases/06-desktop-and-web-polish/06-05-SUMMARY.md
@lib/providers/theme_notifier.dart
@lib/widgets/responsive_shell.dart
@lib/platform/window_setup.dart
@lib/router.dart
@lib/screens/schedule/widgets/chunk_card.dart
@lib/screens/goals/widgets/goal_card.dart
@lib/screens/goals/goals_screen.dart
@lib/screens/home/home_screen.dart
@lib/screens/schedule/checkin_screen.dart
@test/test_helpers/mood_pump.dart
@test/test_helpers/viewport.dart
@test/screens/quarterly_review_test.dart
@test/services/notification_service_test.dart
@test/repositories/goal_repository_test.dart

<interfaces>
From `lib/providers/theme_notifier.dart` (Plan 02 product — testable surface):
- Constructor: `ThemeNotifier({AppSettingsRepository? repository, DateTime Function() now = DateTime.now, bool timeModulationEnabled = true})`
- Static: `static const Map<int, Color> moodSeeds`, `static const Color curiousSeed = Color(0xFF7A8FA3)`
- Public: `Future<void> init()`, `Future<void> setMoodSeed(Color seed)`, `Future<void> resetToCurious()`, `bool get isPreCheckin`, `ThemeData get currentTheme`
- `static Color _modulateHsl(Color base, DateTime now)` — private but reachable via `currentTheme` or by exposing as `@visibleForTesting` (preferred: tests work via `currentTheme` + comparison against expected HSL math, OR rebuild the same `cos(2*pi*(min/1440-0.5))` formula in the test and compare. Choice: add `@visibleForTesting static Color modulateHsl(...)` wrapper or test through `currentTheme` + manual HSL extraction. Simpler: add `@visibleForTesting` annotation on `_modulateHsl` to make it accessible — Flutter test infrastructure honors this.)

From `lib/widgets/responsive_shell.dart` (Plan 04 product):
- `class ResponsiveShell extends StatelessWidget`
- Constructor: `const ResponsiveShell({super.key, required this.navigationShell})`
- The `navigationShell` parameter is a `StatefulNavigationShell` — constructing one in tests requires running `createRouter(settingsNotifier)` from `lib/router.dart`. PATTERNS.md flags this: "The harness decision belongs to the planner." Choice: build a test harness MaterialApp.router with the actual createRouter factory + a mock SettingsNotifier with onboardingComplete=true so the redirect goes to `/goals` (which has a NavigationShell inside the StatefulShellRoute).

From `test/services/notification_service_test.dart` (analog for `completes` smoke-test):
```
test('does not throw the macOS-settings ArgumentError on the test host', () async {
  try { await NotificationService.initialize(); } catch (e) { ... }
});
```
Use `expectLater(future, completes)` for window_setup_test.

From `test/repositories/goal_repository_test.dart` (analog for in-memory repository fake):
```
class InMemoryGoalRepository implements GoalRepository {
  final Map<String, Goal> _store = {};
  ...
}
```
Sibling `InMemoryAppSettingsRepository` for theme_notifier_test:
```
class InMemoryAppSettingsRepository implements AppSettingsRepository {
  AppSettings? _stored;
  @override
  Future<AppSettings?> getSettings() async => _stored;
  @override
  Future<void> saveSettings(AppSettings settings) async => _stored = settings;
}
```

From RESEARCH.md §Code Examples (verbatim test sketches):
- HSL modulation noon peak test (lines 735-742): `final modulated = modulateForTime(base, DateTime(2026, 5, 12, 12, 0)); expect(modHsl.lightness, closeTo(baseHsl.lightness + 0.05, 0.01)); expect(modHsl.saturation, closeTo(baseHsl.saturation + 0.10, 0.01));`
- HSL modulation midnight trough test (lines 744-751): analogous with `-0.05` and `-0.10`
- MouseRegion hover-reveal test (PATTERNS.md lines 933-945): `final gesture = await tester.createGesture(kind: PointerDeviceKind.mouse); await gesture.addPointer(location: Offset.zero); addTearDown(gesture.removePointer); await gesture.moveTo(tester.getCenter(find.byType(ChunkCard))); await tester.pumpAndSettle(); expect(find.byIcon(Icons.check_circle_outline), findsOneWidget);`
- Drag handle visibility test (PATTERNS.md lines 988-1006): `debugDefaultTargetPlatformOverride = TargetPlatform.android; addTearDown(() => debugDefaultTargetPlatformOverride = null); await pumpWithMood(tester, ...); expect(find.byIcon(Icons.drag_handle), findsNothing);`
- LayoutBuilder breakpoint test (RESEARCH.md lines 572-598): `setViewport(tester, const Size(480, 800)); ... expect(find.byType(NavigationBar), findsOneWidget); expect(find.byType(NavigationRail), findsNothing);`
- Breathing pulse test (PATTERNS.md lines 1015-1027): assert `AnimationController.isAnimating == true` when `isPreCheckin == true`; mock ThemeNotifier with `isPreCheckin == false` and assert stopped; wrap in `MediaQuery(data: MediaQueryData(disableAnimations: true), ...)` and assert controller is not animating
- Window setup smoke (PATTERNS.md lines 1050-1062): `expectLater(setupDesktopWindow(), completes)`
- Router redirect (PATTERNS.md lines 1075-1088): `await tester.pumpWidget(MaterialApp.router(theme: ..., routerConfig: createRouter(settings)..go('/schedule'))); ... expect(find.byType(ScheduleScreen), findsOneWidget);`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ThemeNotifier unit tests + visibility annotation</name>
  <files>lib/providers/theme_notifier.dart, test/providers/theme_notifier_test.dart, lib/data/repositories/in_memory_app_settings_repository.dart</files>
  <behavior>
    - Test 1: `setMoodSeed(seed)` calls notifyListeners exactly once and persists `moodSeedArgb` to the injected repository (verified via `await repo.getSettings()` returning the seed value)
    - Test 2: `resetToCurious()` calls notifyListeners and clears `moodSeedArgb` in the repository
    - Test 3: `isPreCheckin` is true after construction with no persisted seed; false after `setMoodSeed(...)`
    - Test 4: `currentTheme` uses `curiousSeed` (#7A8FA3) when `_moodSeed` is null
    - Test 5: `currentTheme` uses the mood seed when `_moodSeed` is set
    - Test 6: `_modulateHsl` peaks at noon — for base `#4A8C7A`, modulated lightness is `baseHsl.lightness + 0.05` (within 0.01), modulated saturation is `baseHsl.saturation + 0.10` (within 0.01)
    - Test 7: `_modulateHsl` troughs at midnight — modulated L is `base - 0.05`, S is `base - 0.10`
    - Test 8: `_modulateHsl` returns base color (approximately) at sunrise (6:00) — t = cos(2pi*(360/1440 - 0.5)) = cos(-pi/2) = 0
    - Test 9: Ticker cancels on `AppLifecycleState.paused` — verify by checking that calling `didChangeAppLifecycleState(AppLifecycleState.paused)` does NOT throw and that subsequent calls don't trigger ticker fires (use the `notifyListeners`-call-count technique: a listener counter incremented from `addListener`)
    - Test 10: Ticker restarts on `.resumed` and calls `notifyListeners` immediately (RESEARCH.md Pattern 2 line 375)
    - Test 11: `init()` with persisted `lastMoodSetYmdInt = yesterday` resets `_moodSeed` to null without writing to Hive (D-10 enforcement)
    - Test 12: `init()` with persisted `lastMoodSetYmdInt = today` preserves the persisted seed (no rollover triggered)
  </behavior>
  <read_first>
    - lib/providers/theme_notifier.dart (Plan 02 product — every public surface)
    - lib/data/models/app_settings.dart (post-Plan-02 with both new fields)
    - lib/data/repositories/app_settings_repository.dart (abstract)
    - test/repositories/goal_repository_test.dart (analog for the in-memory fake pattern)
    - .planning/phases/06-desktop-and-web-polish/06-RESEARCH.md §Code Examples §Time-of-day modulation lines 718-751 (HSL test math)
    - .planning/phases/06-desktop-and-web-polish/06-PATTERNS.md `test/providers/theme_notifier_test.dart (NEW)` section (lines 873-902)
  </read_first>
  <action>
    First, add `@visibleForTesting` annotation to `ThemeNotifier._modulateHsl` in `lib/providers/theme_notifier.dart`. Either:
    (a) Rename `_modulateHsl` → `modulateHsl` and add `@visibleForTesting` annotation, OR
    (b) Keep the underscore name but add `@visibleForTesting` (Dart allows this — analyzer permits test-only access to the underscore-prefixed symbol when annotated and called from test files).
    Choice: (a) — rename to `modulateHsl` with `@visibleForTesting` annotation. The `@visibleForTesting` is imported from `package:flutter/foundation.dart`. This is the cleaner public-API contract for tests.

    Create `lib/data/repositories/in_memory_app_settings_repository.dart` (NEW — sibling of `InMemoryGoalRepository`, but lives under `lib/` so tests can import it without test-test imports):
    - Implements `AppSettingsRepository`
    - `AppSettings? _stored;`
    - `Future<AppSettings?> getSettings() async => _stored;`
    - `Future<void> saveSettings(AppSettings settings) async => _stored = settings;`
    - Doc comment: "In-memory implementation for tests. Mirrors HiveAppSettingsRepository contract without disk I/O."

    Create `test/providers/theme_notifier_test.dart`:
    - Imports: `package:canopy/data/models/app_settings.dart`, `package:canopy/data/repositories/in_memory_app_settings_repository.dart`, `package:canopy/providers/theme_notifier.dart`, `package:flutter/material.dart`, `package:flutter/widgets.dart`, `package:flutter_test/flutter_test.dart`.
    - At top: `TestWidgetsFlutterBinding.ensureInitialized();` (required because ThemeNotifier addObserver uses WidgetsBinding.instance).
    - Group "ThemeNotifier construction and persistence" with tests 1-5 above. Each test constructs `final repo = InMemoryAppSettingsRepository();` and `final notifier = ThemeNotifier(repository: repo, now: () => DateTime(2026, 5, 12, 12, 0), timeModulationEnabled: false);`. Call `await notifier.init();` before assertions. Use `final listener = MockListener(); notifier.addListener(listener.call);` to verify notifyListeners (where MockListener is `class MockListener { int count = 0; void call() => count++; }`).
    - Group "Time-of-day modulation" with tests 6-8 (call `ThemeNotifier.modulateHsl(seed, syntheticNow)` directly via the @visibleForTesting accessor).
    - Group "Lifecycle ticker" with tests 9-10. Manually call `notifier.didChangeAppLifecycleState(AppLifecycleState.paused)` and `.resumed`. Verify the listener count: a resume should fire one notifyListeners synchronously (RESEARCH.md Pattern 2 line 375).
    - Group "Daily rollover seam" with tests 11-12. Set up the repository with `AppSettings()..moodSeedArgb = 0xFF4A8C7A..lastMoodSetYmdInt = 20260511` (yesterday) for test 11; assert that after `init()` `notifier.isPreCheckin == true` (memory state cleared) AND `(await repo.getSettings())?.moodSeedArgb == 0xFF4A8C7A` (Hive NOT touched — per RESEARCH.md Open Question 1 the reset is in-memory only until next mood tap). For test 12 use `lastMoodSetYmdInt = 20260512` (today, matches the injected `now`) — assert `notifier.isPreCheckin == false` and the moodSeed is the persisted one.
  </action>
  <verify>
    <automated>flutter test test/providers/theme_notifier_test.dart -r expanded 2>&amp;1 | tail -30 &amp;&amp; flutter analyze 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `test/providers/theme_notifier_test.dart` exists with at least 12 `testWidgets` or `test()` blocks (one per behavior above)
    - File `lib/data/repositories/in_memory_app_settings_repository.dart` exists with `class InMemoryAppSettingsRepository implements AppSettingsRepository`
    - `@visibleForTesting` annotation on `modulateHsl` (renamed from `_modulateHsl`) in theme_notifier.dart
    - All tests pass: `flutter test test/providers/theme_notifier_test.dart` exits 0
    - `flutter analyze` 0 issues
  </acceptance_criteria>
  <done>
    ThemeNotifier has full unit coverage for HSL modulation, persistence, lifecycle ticker, and daily rollover. The InMemoryAppSettingsRepository is published under lib/ for cross-test reuse.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widget tests for responsive layout, chunk card hover, goal card hover, drag handle visibility</name>
  <files>test/screens/responsive_layout_test.dart, test/screens/chunk_card_hover_test.dart, test/screens/goal_card_hover_test.dart, test/screens/goal_card_drag_handle_test.dart</files>
  <behavior>
    - responsive_layout_test:
      - At 480dp width: `find.byType(NavigationBar)` is `findsOneWidget`; `find.byType(NavigationRail)` is `findsNothing`
      - At 720dp width: `find.byType(NavigationRail)` is `findsOneWidget`; `find.byType(NavigationBar)` is `findsNothing` (720 is the INCLUSIVE lower bound for rail mode)
      - At 1200dp width: `find.byType(NavigationRail)` is `findsOneWidget`
      - No `OverflowError` exceptions captured (AC-1 sub-row)
    - chunk_card_hover_test:
      - `MouseRegion.onEnter` reveals `Icons.check_circle_outline` and `Icons.skip_next_outlined` (both `findsOneWidget`, opacity 1)
      - `MouseRegion.onExit` returns icons to opacity 0 (use `find.byType(AnimatedOpacity)` to inspect the `opacity` property)
      - `tester.drag(find.byType(ChunkCard), const Offset(300, 0))` (simulating swipe) does NOT cause hover icons to appear (the touch-drag is via the existing Dismissible — Phase 4)
    - goal_card_hover_test:
      - `InkWell.onHover(true)` reveals `Icons.edit_outlined` and `Icons.archive_outlined` (when trailing is null)
      - `InkWell.onHover(false)` returns icons to opacity 0
      - When `trailing` is non-null (e.g., drag handle), hover icons are NOT shown (the trailing slot takes precedence per PATTERNS.md guard)
    - goal_card_drag_handle_test:
      - With `debugDefaultTargetPlatformOverride = TargetPlatform.android`: `find.byIcon(Icons.drag_handle)` is `findsNothing` (trailing is null, drag handle hidden)
      - With `debugDefaultTargetPlatformOverride = TargetPlatform.macOS`: `find.byIcon(Icons.drag_handle)` is `findsOneWidget`; the wrapping `AnimatedOpacity.opacity` is `0.6`
      - Always reset `debugDefaultTargetPlatformOverride = null` via `addTearDown`
  </behavior>
  <read_first>
    - test/test_helpers/mood_pump.dart (Plan 01 product — pumpWithMood signature)
    - test/test_helpers/viewport.dart (Plan 01 product — setViewport)
    - lib/widgets/responsive_shell.dart (Plan 04 product)
    - lib/screens/schedule/widgets/chunk_card.dart (Plan 05 product with MouseRegion)
    - lib/screens/goals/widgets/goal_card.dart (Plan 05 product with InkWell.onHover)
    - lib/screens/goals/goals_screen.dart (Plan 05 product with defaultTargetPlatform-gated drag handle)
    - .planning/phases/06-desktop-and-web-polish/06-RESEARCH.md §Pattern 6 (lines 562-598 — responsive layout test verbatim sketch)
    - .planning/phases/06-desktop-and-web-polish/06-PATTERNS.md §"test/screens/chunk_card_hover_test.dart + goal_card_hover_test.dart" (lines 930-960) and §"test/screens/goal_card_drag_handle_test.dart" (lines 964-1011)
    - test/screens/quarterly_review_test.dart (existing widget test pattern — analog)
  </read_first>
  <action>
    Create `test/screens/responsive_layout_test.dart`:
    - Imports: `package:canopy/widgets/responsive_shell.dart`, the test_helpers, `package:flutter/material.dart`, `package:flutter_test/flutter_test.dart`, `package:go_router/go_router.dart` (for StatefulNavigationShell construction in the harness), `package:provider/provider.dart`, `package:canopy/providers/settings_notifier.dart`, `package:canopy/router.dart`.
    - The harness widget creates a real `createRouter(settingsNotifier)` with `settingsNotifier.onboardingComplete = true` (set via the in-memory repository if needed). The router exposes a `StatefulNavigationShell` inside the StatefulShellRoute branches. Wrap in `MaterialApp.router(routerConfig: router, ...)` and let it settle on `/home`.
    - Group "Responsive shell layout" with 3 tests at 480, 720, 1200 dp. Each uses `setViewport(tester, Size(W, 800))` before `pumpAndSettle`.
    - For OverflowError detection: `final errors = <FlutterErrorDetails>[]; FlutterError.onError = (details) { errors.add(details); }; ... expect(errors, isEmpty);` (in the `tearDown`).

    Create `test/screens/chunk_card_hover_test.dart`:
    - Imports: `package:canopy/screens/schedule/widgets/chunk_card.dart`, the test_helpers, `package:flutter/gestures.dart` (for PointerDeviceKind), `package:flutter/material.dart`, `package:flutter_test/flutter_test.dart`.
    - Test 1: pump a ChunkCard work variant via pumpWithMood. Create mouse gesture: `final gesture = await tester.createGesture(kind: PointerDeviceKind.mouse); await gesture.addPointer(location: Offset.zero); addTearDown(gesture.removePointer);`. Move to card center: `await gesture.moveTo(tester.getCenter(find.byType(ChunkCard)));`. Pump and settle. Expect `Icons.check_circle_outline` and `Icons.skip_next_outlined` to be visible.
    - Test 2 (onExit): move gesture away with `await gesture.moveTo(Offset.zero);`. Pump and settle. Verify the AnimatedOpacity opacity is 0.0 (inspect via `final ao = tester.widget<AnimatedOpacity>(find.ancestor(of: find.byIcon(Icons.check_circle_outline), matching: find.byType(AnimatedOpacity)).first); expect(ao.opacity, 0.0);` — or assert findsNothing if the icon is not painted at opacity 0).
    - Test 3 (touch-drag invariant per Pitfall 5): pumpWithMood the ChunkCard, then `await tester.drag(find.byType(ChunkCard), const Offset(300, 0));` and pumpAndSettle. Assert hover icons never appeared (use `find.byIcon(Icons.check_circle_outline)` — if MouseRegion correctly filters non-pointer events, it remains hidden).

    Create `test/screens/goal_card_hover_test.dart`:
    - Imports: `package:canopy/screens/goals/widgets/goal_card.dart`, test_helpers, etc.
    - Construct a stub Goal: small helper inside the test file (analog from `quarterly_review_test.dart` `_stubGoal` at lines 169-184).
    - Test 1: pumpWithMood with `GoalCard(goal: stub, onTap: () {}, onEdit: () {}, onArchive: () {})` (no trailing). Create mouse gesture, move to card. Assert `Icons.edit_outlined` and `Icons.archive_outlined` findsOneWidget.
    - Test 2 (onExit): move gesture away. Assert AnimatedOpacity.opacity returns to 0.
    - Test 3 (trailing non-null guard): pumpWithMood with `GoalCard(... trailing: SizedBox(width: 20))` and hover. Assert hover icons NOT shown (trailing takes precedence).

    Create `test/screens/goal_card_drag_handle_test.dart`:
    - Imports: `package:canopy/screens/goals/goals_screen.dart`, test_helpers, etc.
    - Per PATTERNS.md analog (lines 988-1006): use `debugDefaultTargetPlatformOverride = TargetPlatform.android` for the mobile test and `TargetPlatform.macOS` for the desktop test. Always `addTearDown(() => debugDefaultTargetPlatformOverride = null);`.
    - For the mobile test: pumpWithMood a GoalsScreen (or a minimal ReorderableListView with GoalCard items) and assert `find.byIcon(Icons.drag_handle), findsNothing`.
    - For the desktop test: pumpWithMood the same widget under macOS override, assert `findsOneWidget`, then inspect `tester.widget<AnimatedOpacity>(find.ancestor(of: find.byIcon(Icons.drag_handle), matching: find.byType(AnimatedOpacity)).first).opacity == 0.6`.
    - Construct test goals via a stub helper (or reuse from a test_helpers if available).
  </action>
  <verify>
    <automated>flutter test test/screens/responsive_layout_test.dart test/screens/chunk_card_hover_test.dart test/screens/goal_card_hover_test.dart test/screens/goal_card_drag_handle_test.dart -r expanded 2>&amp;1 | tail -30 &amp;&amp; flutter analyze 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All 4 test files exist and each contains at least 2 `testWidgets` blocks
    - `flutter test test/screens/responsive_layout_test.dart` exits 0 with 3 tests passing (480/720/1200dp)
    - `flutter test test/screens/chunk_card_hover_test.dart` exits 0 with 3 tests passing (onEnter, onExit, touch-drag invariant)
    - `flutter test test/screens/goal_card_hover_test.dart` exits 0 with 3 tests passing (onHover true, onHover false, trailing non-null)
    - `flutter test test/screens/goal_card_drag_handle_test.dart` exits 0 with 2 tests passing (mobile hidden, desktop opacity 0.6)
    - `flutter analyze` 0 issues
    - All tests honor the `addTearDown(tester.view.reset)` / `addTearDown(() => debugDefaultTargetPlatformOverride = null)` pattern (Pitfall 4)
  </acceptance_criteria>
  <done>
    Responsive shell, ChunkCard hover, GoalCard hover, and drag handle visibility have full automated coverage. The touch-drag-no-hover invariant from RESEARCH.md Pitfall 5 is asserted in test.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Breathing pulse + window setup + router redirect tests + quarterly_review migration</name>
  <files>test/screens/home_screen_breathing_pulse_test.dart, test/platform/window_setup_test.dart, test/screens/router_redirect_test.dart, test/screens/quarterly_review_test.dart</files>
  <behavior>
    - home_screen_breathing_pulse_test:
      - When `ThemeNotifier.isPreCheckin == true`, the `_BreathingPulseButton`'s AnimationController IS animating (verify via `tester.binding.hasScheduledFrame == true` after pumping a single frame, or by inspecting the BoxShadow blurRadius across two frames — blur must change)
      - When `ThemeNotifier.isPreCheckin == false`, the pulse stops (controller.isAnimating == false; blur is static)
      - When wrapped in `MediaQuery(data: MediaQueryData(accessibleNavigation: true, disableAnimations: true), child: ...)`, the controller does NOT animate; the BoxShadow blur is at the midpoint (12px, computed from `8.0 + 8.0 * 0.5`)
    - window_setup_test:
      - `expectLater(setupDesktopWindow(), completes)` — the function returns Future<void> without throwing on the test host (which lacks the window_manager platform plugin)
    - router_redirect_test:
      - With `settingsNotifier.onboardingComplete = false`, navigating to `/schedule` triggers the redirect to `/onboarding` (verified by `find.byType(OnboardingScreen)` findsOneWidget)
      - With `settingsNotifier.onboardingComplete = true`, navigating to `/schedule` loads `ScheduleScreen` (verified by `find.byType(ScheduleScreen)` findsOneWidget)
      - With `settingsNotifier.onboardingComplete = true`, navigating to `/goals` loads `GoalsScreen` findsOneWidget
    - quarterly_review_test migration:
      - The existing `_wrap(Widget child) => MaterialApp(home: Scaffold(body: child));` helper is replaced with `pumpWithMood(tester, child, extraProviders: [ChangeNotifierProvider<GoalsNotifier>(create: (_) => GoalsNotifier())])` at each call site
      - All existing tests continue to pass unchanged (no `colorScheme.primary` assertions exist in this file per RESEARCH.md AC-5 row)
  </behavior>
  <read_first>
    - lib/screens/home/home_screen.dart (Plan 05 product with `_BreathingPulseButton`)
    - lib/platform/window_setup.dart (Plan 03 product)
    - lib/router.dart (Plan 04 product with ResponsiveShell + redirect)
    - lib/providers/settings_notifier.dart (existing)
    - test/screens/quarterly_review_test.dart (the 33 `_wrap(...)` call sites to migrate)
    - test/services/notification_service_test.dart (analog for the `completes` smoke-test)
    - .planning/phases/06-desktop-and-web-polish/06-PATTERNS.md §"test/platform/window_setup_test.dart" (lines 1030-1067) and §"test/screens/router_redirect_test.dart" (lines 1071-1097) and §"test/screens/home_screen_breathing_pulse_test.dart" (lines 1014-1028)
  </read_first>
  <action>
    Create `test/screens/home_screen_breathing_pulse_test.dart`:
    - Imports: `package:canopy/screens/home/home_screen.dart`, `package:canopy/providers/theme_notifier.dart`, test_helpers, `package:flutter/material.dart`, `package:flutter_test/flutter_test.dart`, `package:provider/provider.dart`.
    - Use `pumpWithMood` with `extraProviders: [ChangeNotifierProvider<ThemeNotifier>(create: (_) => ThemeNotifier(repository: InMemoryAppSettingsRepository(), now: () => DateTime(2026, 5, 12, 12, 0), timeModulationEnabled: false))]`. Need to also seed other providers HomeScreen reads (ScheduleNotifier, SettingsNotifier) — use minimal stubs.
    - Test 1: pump HomeScreen with `isPreCheckin == true` (default for a fresh ThemeNotifier). Pump one frame, capture BoxShadow.blurRadius from the BoxDecoration; pump another frame after 100ms; capture again. Assert the two values differ (the pulse is animating).
    - Test 2: pump with a ThemeNotifier where `setMoodSeed(...)` was called (so `isPreCheckin == false`). Assert blur is static across frames.
    - Test 3: wrap in `MediaQuery(data: const MediaQueryData(disableAnimations: true), child: ...)` and pump. Assert blur is 12px (midpoint) and does NOT change across frames.

    Create `test/platform/window_setup_test.dart` (per PATTERNS.md lines 1050-1067):
    - `TestWidgetsFlutterBinding.ensureInitialized();` at top of main.
    - Imports: `package:canopy/platform/window_setup.dart`, `package:flutter_test/flutter_test.dart`.
    - Group "setupDesktopWindow" with one test: `await expectLater(setupDesktopWindow(), completes);`. Since the test host lacks the window_manager platform channel registration, the io branch may throw a MissingPluginException — wrap in a try/catch that ignores MissingPluginException specifically. PATTERNS.md analog (notification_service_test.dart line 39): use a `try ... catch (e) { expect(e, isNot(isA<SomeUnexpectedError>())); }` shape. Choice: simpler approach — set `debugDefaultTargetPlatformOverride = TargetPlatform.linux;` to force the io branch to RUN but the `windowManager.ensureInitialized()` call would still fail in test mode; instead, just call `setupDesktopWindow()` and `expectLater(..., completes)` — the function's `kIsWeb`+`Platform.is*` gating in test runs against TargetPlatform.android (default test platform) → both gates fail → function returns early without calling windowManager. This is a no-throw smoke test verifying the EARLY-RETURN path on a non-desktop test host. Add a doc comment explaining the test exercises the kIsWeb=false + Platform.isAndroid=true gate path.

    Create `test/screens/router_redirect_test.dart`:
    - Imports: `package:canopy/router.dart`, `package:canopy/providers/settings_notifier.dart`, `package:canopy/screens/onboarding/onboarding_screen.dart`, `package:canopy/screens/schedule/schedule_screen.dart`, `package:canopy/screens/goals/goals_screen.dart`, plus test_helpers + the InMemoryAppSettingsRepository.
    - Construct a `SettingsNotifier` with the in-memory repository, set `onboardingComplete` via direct field manipulation (the existing setOnboardingComplete is async; tests can either await it or use a test-only constructor parameter — choice: use the existing async setter `await settings.setOnboardingComplete(true);`).
    - Test 1: with `onboardingComplete = false`, build `MaterialApp.router(routerConfig: createRouter(settings)..go('/schedule'))`. Pump and settle. Assert `find.byType(OnboardingScreen), findsOneWidget`.
    - Test 2: with `onboardingComplete = true`, build the router and navigate to `/schedule`. Assert `find.byType(ScheduleScreen), findsOneWidget`.
    - Test 3: with `onboardingComplete = true`, navigate to `/goals`. Assert `find.byType(GoalsScreen), findsOneWidget`.

    Migrate `test/screens/quarterly_review_test.dart`:
    1. Add import `import '../test_helpers/mood_pump.dart';`.
    2. Replace each `await tester.pumpWidget(_wrap(child));` call site with `await pumpWithMood(tester, child);`. There are 33 call sites per PATTERNS.md.
    3. Delete the local `Widget _wrap(Widget child) => MaterialApp(home: Scaffold(body: child));` helper at line 31 (no longer referenced).
    4. For tests that wrap in `ChangeNotifierProvider<GoalsNotifier>` (line ~215-219), use the `extraProviders` knob: `await pumpWithMood(tester, child, extraProviders: [ChangeNotifierProvider<GoalsNotifier>(create: (_) => GoalsNotifier())]);`.
    5. Run `flutter test test/screens/quarterly_review_test.dart` and verify all 9-ish tests still pass. No `colorScheme.primary` assertions need updating per RESEARCH.md AC-5 row.
  </action>
  <verify>
    <automated>flutter test test/screens/home_screen_breathing_pulse_test.dart test/platform/window_setup_test.dart test/screens/router_redirect_test.dart test/screens/quarterly_review_test.dart -r expanded 2>&amp;1 | tail -40 &amp;&amp; flutter analyze 2>&amp;1 | tail -10 &amp;&amp; flutter test 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All 3 new test files exist (`home_screen_breathing_pulse_test.dart`, `window_setup_test.dart`, `router_redirect_test.dart`) with their respective tests passing
    - `quarterly_review_test.dart` migrated: no `_wrap(` calls remain; `pumpWithMood` is the wrap helper; all existing tests still pass
    - `flutter test` full suite exits 0 (entire repository)
    - `flutter analyze` 0 issues
    - The breathing pulse test verifies all three behaviors: isPreCheckin true → animating, isPreCheckin false → static, disableAnimations true → midpoint 12px and not animating
    - The window_setup test exercises the `kIsWeb=false + Platform.is*-not-desktop` early-return path
    - The router_redirect test covers both the redirect-to-onboarding path and the direct-load paths for `/schedule` and `/goals` (T-router-1 mitigation verification)
  </acceptance_criteria>
  <done>
    All Wave 4 tests pass. The full `flutter test` suite is green. Every AC has at least one automated row except AC-3 manual macOS resize (Plan 07 UAT), AC-4 manual Web URL bar (Plan 07 UAT), and AC-6 manual visual timing (Plan 07 UAT). Quarterly review test file is migrated to the shared `pumpWithMood` helper without behavior change.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → production code | Tests import production widgets and Notifiers. `@visibleForTesting` annotation on `modulateHsl` is the only test-only access surface; everything else uses the regular public API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-router-1 | Elevation of Privilege | Direct URL load bypasses onboarding redirect | mitigate | `test/screens/router_redirect_test.dart` provides automated verification of the redirect for the three sensitive routes (`/schedule`, `/goals`, `/onboarding`). T-router-1 is the threat first surfaced in Plan 04; this plan adds the defense-in-depth automated test. |
| T-06-06-1 | Tampering | A flaky test viewport state leaks to neighbouring tests | mitigate | `setViewport` always pairs with `addTearDown(tester.view.reset)` (Plan 01 product); all `debugDefaultTargetPlatformOverride` settings pair with `addTearDown(... = null)` (RESEARCH.md Pitfall 4). |
| T-06-06-2 | Information Disclosure | Test mocks accidentally expose real Hive box state | mitigate | All theme_notifier tests use `InMemoryAppSettingsRepository` (Task 1 product) — no Hive disk I/O in tests. |

No new HIGH-severity threats introduced. The test suite verifies T-router-1 mitigation.
</threat_model>

<verification>
- `flutter test` full suite exits 0 (Wave 0/1/2/3/4 inclusive)
- `flutter analyze` 0 issues
- Test count grows from ~13 (pre-Phase-6) to ~13 + 12 + 3 + 3 + 3 + 2 + 3 + 1 + 3 = ~43+ tests
- Wave 0 VALIDATION.md gap list (10 new files) fully closed
</verification>

<success_criteria>
- 8 new test files exist + 1 migrated test file
- Every AC-1 through AC-6 row in VALIDATION.md marked ❌ (automated) is now covered (status flips to ✅ on green run)
- The breathing pulse, window setup, router redirect, and quarterly_review migration are all verified
- `flutter test` exits 0 across the full repository
- Plan 07 can now mark VALIDATION.md `nyquist_compliant: true` and finalize the ROADMAP update
</success_criteria>

<output>
After completion, create `.planning/phases/06-desktop-and-web-polish/06-06-SUMMARY.md` listing:
- 8 new test files (with brief description of each)
- 1 migrated test file (quarterly_review_test.dart — call-site count of 33 `_wrap` → `pumpWithMood`)
- Full test suite count before/after Phase 6 (~13 → ~43+)
- Confirmation that `@visibleForTesting` was added to `modulateHsl`
- Note that Plan 07 handles the manual UAT checklist (AC-3 macOS resize, AC-4 web URL bar, AC-6 visual timing) and the ROADMAP update
</output>
