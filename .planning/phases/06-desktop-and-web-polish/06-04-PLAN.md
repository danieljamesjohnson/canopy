---
phase: 06-desktop-and-web-polish
plan: 04
type: execute
wave: 2
depends_on: [02, 03]
files_modified:
  - lib/widgets/responsive_shell.dart
  - lib/main.dart
  - lib/router.dart
autonomous: true
requirements: [AC-1, AC-3, AC-4, AC-6]
threat_refs: [T-router-1, T-platform-1]

must_haves:
  truths:
    - "The app launches without errors on Android, iOS, Web, Windows, macOS, and Linux"
    - "On a window with width < 720dp the NavigationBar at the bottom is shown"
    - "On a window with width >= 720dp the NavigationRail on the left is shown and there is no bottom NavigationBar"
    - "MaterialApp.router.theme is sourced from ThemeNotifier.currentTheme via a Consumer<ThemeNotifier> wrapper"
    - "Theme changes cross-fade over 500ms with easeOutCubic"
    - "setupDesktopWindow runs before runApp on every platform, no-op on mobile/web"
    - "go_router redirect chain (onboardingComplete) is unaffected — direct URL load on Web honors the redirect"
  artifacts:
    - path: lib/widgets/responsive_shell.dart
      provides: "Public ResponsiveShell widget wrapping LayoutBuilder + NavigationRail/NavigationBar swap at 720dp"
      exports: ["ResponsiveShell"]
      min_lines: 60
    - path: lib/main.dart
      provides: "ThemeNotifier construction, Consumer<ThemeNotifier> wrap, themeAnimationDuration/Curve, setupDesktopWindow call before runApp"
      contains: "ThemeNotifier"
    - path: lib/router.dart
      provides: "StatefulShellRoute builder using ResponsiveShell instead of _ScaffoldWithNavBar"
      contains: "ResponsiveShell(navigationShell: navigationShell)"
  key_links:
    - from: "lib/main.dart"
      to: "lib/platform/window_setup.dart"
      via: "await setupDesktopWindow() after WidgetsFlutterBinding.ensureInitialized"
      pattern: "await setupDesktopWindow"
    - from: "lib/main.dart"
      to: "lib/providers/theme_notifier.dart"
      via: "ThemeNotifier construction + Consumer<ThemeNotifier> wrap around MaterialApp.router"
      pattern: "Consumer<ThemeNotifier>"
    - from: "lib/router.dart"
      to: "lib/widgets/responsive_shell.dart"
      via: "StatefulShellRoute builder returns ResponsiveShell(navigationShell: navigationShell)"
      pattern: "ResponsiveShell\\(navigationShell:"
    - from: "lib/widgets/responsive_shell.dart"
      to: "Flutter framework"
      via: "LayoutBuilder branching at constraints.maxWidth >= 720"
      pattern: "constraints\\.maxWidth >= 720"
---

<objective>
Wire the three Phase 6 foundations into the app at the integration seam: build the responsive shell (NavigationBar vs NavigationRail at 720dp), and modify `lib/main.dart` + `lib/router.dart` so the app actually uses the new ThemeNotifier (Plan 02), the desktop window minimum (Plan 03), and the adaptive layout (this plan).

Purpose: After Plan 04, AC-1 (two-column at >=720dp), AC-3 (window minimum), AC-4 (Web URLs continue to work because the route table is unchanged), and the AC-6 600ms cross-fade transition are all functionally in place. The hover affordances and breathing pulse are added in Plan 05; tests are added in Plan 06.

Output: One new file (`lib/widgets/responsive_shell.dart`) and two existing files modified (`lib/main.dart`, `lib/router.dart`). After this plan, `flutter run -d macos` and `flutter run -d chrome` both launch successfully and a 1280x800 desktop window renders the NavigationRail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-desktop-and-web-polish/06-CONTEXT.md
@.planning/phases/06-desktop-and-web-polish/06-RESEARCH.md
@.planning/phases/06-desktop-and-web-polish/06-PATTERNS.md
@.planning/phases/06-desktop-and-web-polish/06-UI-SPEC.md
@.planning/phases/06-desktop-and-web-polish/06-02-SUMMARY.md
@.planning/phases/06-desktop-and-web-polish/06-03-SUMMARY.md
@lib/main.dart
@lib/router.dart
@lib/providers/theme_notifier.dart
@lib/screens/onboarding/onboarding_screen.dart

<interfaces>
From lib/main.dart (current state, pre-Plan-04) — see PATTERNS.md lines 656-688.

From lib/router.dart `_ScaffoldWithNavBar` lines 117-143 — the current single-column body that ResponsiveShell will replace structurally. Same 4 destinations (Home/Goals/Schedule/Settings) with `Icons.*_outlined`. Same `goBranch(index, initialLocation: index == navigationShell.currentIndex)` semantics.

From RESEARCH.md Pattern 4 (lines 444-514) — the verbatim ResponsiveShell body. Key elements:
- `LayoutBuilder(builder: (context, constraints) { ... })`
- Branch on `constraints.maxWidth >= 720` (inclusive — UI-SPEC + RESEARCH.md line 601)
- Rail branch: `Scaffold(body: Row(children: [NavigationRail(...), VerticalDivider(thickness: 1, width: 1), Expanded(child: navigationShell)]))`
- NavigationRail: `labelType: NavigationRailLabelType.all`, `selectedIndex: navigationShell.currentIndex`, `onDestinationSelected: _goBranch`
- Bar branch: existing Scaffold with bottom NavigationBar (1-to-1 with current `_ScaffoldWithNavBar`)

From RESEARCH.md Pattern 1 (MaterialApp.router theme animation wiring, lines 245-279):
- `themeAnimationDuration: const Duration(milliseconds: 500)`
- `themeAnimationCurve: Curves.easeOutCubic`
- `Consumer<ThemeNotifier>` wraps `MaterialApp.router` (NOT `AnimatedTheme` — anti-pattern per RESEARCH.md line 604-606)

From UI-SPEC §Mood Warming Transition (D-09 locked):
- Duration: 500ms (within 400-600ms budget)
- Curve: `Curves.easeOutCubic`
- Wrapper: `AnimatedTheme` at MaterialApp.router theme slot — UI-SPEC predates RESEARCH; RESEARCH supersedes with `themeAnimationDuration`/`themeAnimationCurve` on MaterialApp.router (Flutter 3.19+ idiom). Both achieve the same locked 500ms easeOutCubic visual outcome. Use the MaterialApp.router properties, NOT `AnimatedTheme`, per RESEARCH.md Standard Stack alternatives table.

From PATTERNS.md cross-cutting landmines: "`_ScaffoldWithNavBar` is private — untestable" → resolution: move to public `lib/widgets/responsive_shell.dart` for Plan 06 testability.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the public ResponsiveShell widget with LayoutBuilder at 720dp</name>
  <files>lib/widgets/responsive_shell.dart</files>
  <read_first>
    - lib/router.dart lines 117-143 (existing `_ScaffoldWithNavBar` to migrate structurally)
    - lib/screens/onboarding/onboarding_screen.dart lines 567-588 (the only existing LayoutBuilder pattern)
    - .planning/phases/06-desktop-and-web-polish/06-RESEARCH.md Pattern 4 (lines 444-514) — copy verbatim, rename class to ResponsiveShell
    - .planning/phases/06-desktop-and-web-polish/06-PATTERNS.md "lib/widgets/responsive_shell.dart" section (lines 222-318)
    - .planning/phases/06-desktop-and-web-polish/06-UI-SPEC.md §Two-Column Adaptive Layout (D-11 locked values)
  </read_first>
  <action>
    Create `lib/widgets/responsive_shell.dart` (NEW directory `lib/widgets/`). Class MUST be PUBLIC (no leading underscore) so Plan 06's `responsive_layout_test.dart` can import and test it.

    Imports: `package:flutter/material.dart`, `package:go_router/go_router.dart`.

    Class signature: `class ResponsiveShell extends StatelessWidget` with `const ResponsiveShell({super.key, required this.navigationShell});` and `final StatefulNavigationShell navigationShell;`.

    Static const `_destinations` (4 records): Home/`Icons.home_outlined`, Goals/`Icons.flag_outlined`, Schedule/`Icons.calendar_today_outlined`, Settings/`Icons.settings_outlined`. These MUST match existing router.dart `NavigationDestination` destinations verbatim per UI-SPEC §Design System icon library lock.

    Private method `void _goBranch(int index)`: `navigationShell.goBranch(index, initialLocation: index == navigationShell.currentIndex);` (preserves existing router.dart line 128-133 semantics).

    `build` returns a `LayoutBuilder` with `builder: (context, constraints) { ... }`. Inside:
    - If `constraints.maxWidth >= 720` (D-11 inclusive threshold): return `Scaffold(body: Row(children: [NavigationRail(...), const VerticalDivider(thickness: 1, width: 1), Expanded(child: navigationShell)]))`. NavigationRail uses `labelType: NavigationRailLabelType.all` (UI-SPEC always-show labels), `selectedIndex: navigationShell.currentIndex`, `onDestinationSelected: _goBranch`, and 4 `NavigationRailDestination(icon: Icon(d.icon), label: Text(d.label))` from the static destinations list.
    - Else: return `Scaffold(body: navigationShell, bottomNavigationBar: NavigationBar(selectedIndex: navigationShell.currentIndex, onDestinationSelected: _goBranch, destinations: [for (final d in _destinations) NavigationDestination(icon: Icon(d.icon), label: d.label)]))`.

    Material 3 defaults: do NOT override nav rail width (80dp default per UI-SPEC), surface color (`surfaceContainer` default per UI-SPEC), or selected indicator color (`secondaryContainer` default per UI-SPEC).

    Doc comment on the class: cite D-11 and UI-SPEC §Two-Column Adaptive Layout. List the 720dp inclusive threshold as the lock.
  </action>
  <verify>
    <automated>flutter analyze 2>&amp;1 | tail -10 &amp;&amp; test -f lib/widgets/responsive_shell.dart &amp;&amp; grep -c "^class ResponsiveShell extends StatelessWidget" lib/widgets/responsive_shell.dart | grep -q "^1$" &amp;&amp; grep -q "constraints.maxWidth >= 720" lib/widgets/responsive_shell.dart &amp;&amp; grep -q "NavigationRail" lib/widgets/responsive_shell.dart &amp;&amp; grep -q "NavigationBar" lib/widgets/responsive_shell.dart &amp;&amp; grep -q "NavigationRailLabelType.all" lib/widgets/responsive_shell.dart</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/widgets/responsive_shell.dart` exists with public class `ResponsiveShell extends StatelessWidget`
    - Source assertions:
      - `grep "constraints.maxWidth >= 720"` exits 0 (inclusive D-11 threshold)
      - `grep "NavigationRail"` exits 0 (rail branch present)
      - `grep "NavigationBar"` exits 0 (bar branch preserved)
      - `grep "NavigationRailLabelType.all"` exits 0 (UI-SPEC always-show labels)
      - All 4 icon literals present: `Icons.home_outlined`, `Icons.flag_outlined`, `Icons.calendar_today_outlined`, `Icons.settings_outlined`
      - `goBranch(index, initialLocation:` present (preserves existing semantics)
    - File length >= 60 lines (per must_haves.artifacts.min_lines)
    - `flutter analyze` reports 0 issues
  </acceptance_criteria>
  <done>
    `ResponsiveShell` is a public widget at `lib/widgets/responsive_shell.dart`. The 720dp LayoutBuilder branch is encoded and testable from outside the router file. Material 3 defaults preserved (no overrides). Plan 06 can `import 'package:canopy/widgets/responsive_shell.dart'` and test the rail-vs-bar swap.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire ThemeNotifier and window_setup into main.dart; switch router.dart to use ResponsiveShell</name>
  <files>lib/main.dart, lib/router.dart</files>
  <read_first>
    - lib/main.dart (current state — 76 lines, MaterialApp.router with hardcoded `ColorScheme.fromSeed(Color(0xFF3D6B4F))`)
    - lib/router.dart lines 36-39 (StatefulShellRoute builder) and lines 117-143 (`_ScaffoldWithNavBar` to delete)
    - lib/providers/theme_notifier.dart (just created in Plan 02 — uses `ThemeNotifier({...})` constructor with optional named params; for production wiring no params needed)
    - lib/platform/window_setup.dart (just created in Plan 03 — exposes `setupDesktopWindow()`)
    - .planning/phases/06-desktop-and-web-polish/06-PATTERNS.md `lib/main.dart` section (lines 656-744) and `lib/router.dart` section (lines 748-763) — exact Phase 6 changes
    - .planning/phases/06-desktop-and-web-polish/06-RESEARCH.md Pattern 1 (MaterialApp.router themeAnimationDuration wiring)
    - .planning/phases/06-desktop-and-web-polish/06-UI-SPEC.md §Mood Warming Transition (500ms, easeOutCubic locked)
  </read_first>
  <action>
    Modify `lib/main.dart`:
    1. Add imports near the existing import block:
       - `import 'platform/window_setup.dart';`
       - `import 'providers/theme_notifier.dart';`
       - `import 'package:provider/provider.dart';` is already present.
    2. In `main()`, immediately after `WidgetsFlutterBinding.ensureInitialized();`, add `await setupDesktopWindow();` (safe on every platform — stub on web, no-op on mobile, real on desktop).
    3. After the existing `await scheduleNotifier.init();` line, construct ThemeNotifier:
       - `final themeNotifier = ThemeNotifier();`
       - `await themeNotifier.init();`
    4. Modify the `runApp(CanopyApp(...))` call to pass `themeNotifier: themeNotifier` as a new named parameter.
    5. In `CanopyApp`:
       - Add `final ThemeNotifier themeNotifier;` field.
       - Update the `const CanopyApp({...})` constructor to accept `required this.themeNotifier`.
       - In `build`, add `ChangeNotifierProvider<ThemeNotifier>.value(value: themeNotifier)` to the `providers:` list in `MultiProvider` (use `.value` since the notifier was constructed before `runApp` — matches the existing pattern for `scheduleNotifier` and `settingsNotifier`).
       - Replace the existing `child: MaterialApp.router(...)` with `child: Consumer<ThemeNotifier>(builder: (context, theme, _) => MaterialApp.router(...))`. The Consumer<ThemeNotifier> narrows the rebuild scope to JUST `MaterialApp.router`, preventing unnecessary rebuilds of MultiProvider on every 20-min tick (RESEARCH.md anti-pattern line 607).
       - Inside the `MaterialApp.router(...)` call:
         - Replace `theme: ThemeData(colorScheme: ColorScheme.fromSeed(seedColor: Color(0xFF3D6B4F)), useMaterial3: true)` with `theme: theme.currentTheme` (where `theme` is the Consumer's `ThemeNotifier` argument).
         - Add `themeAnimationDuration: const Duration(milliseconds: 500),` (D-09 / UI-SPEC §Mood Warming Transition locked 500ms).
         - Add `themeAnimationCurve: Curves.easeOutCubic,` (UI-SPEC locked curve).
         - Keep `routerConfig: createRouter(settingsNotifier)` unchanged.
         - Keep `title: 'Canopy'` unchanged.

    Modify `lib/router.dart`:
    1. Add import: `import 'widgets/responsive_shell.dart';`
    2. In the StatefulShellRoute.indexedStack builder (line 37-39), change `return _ScaffoldWithNavBar(navigationShell: navigationShell);` to `return ResponsiveShell(navigationShell: navigationShell);`.
    3. DELETE the entire `_ScaffoldWithNavBar` class (lines 117-143). Its body is now superseded by the new `ResponsiveShell` widget which handles both layout branches.
    4. The route table (lines 35-113), the `redirect:` logic (lines 27-34), the `refreshListenable: settingsNotifier` (line 26), and the `rootNavigatorKey` export (lines 18-19) all REMAIN UNCHANGED per AC-4 (route table is the source of truth for Web URLs and is already correct).

    Per D-XX traceability: this task implements D-01 (full mood theming via ColorScheme.fromSeed at MaterialApp level — via `theme.currentTheme`), D-02 (no per-route overrides — only one `theme:` slot in MaterialApp.router), D-09 (500ms easeOutCubic warming transition — via themeAnimationDuration/Curve), and D-11 (720dp layout swap via ResponsiveShell + 480x640 window minimum via setupDesktopWindow).
  </action>
  <verify>
    <automated>flutter analyze 2>&amp;1 | tail -20 &amp;&amp; grep -q "await setupDesktopWindow" lib/main.dart &amp;&amp; grep -q "final themeNotifier = ThemeNotifier" lib/main.dart &amp;&amp; grep -q "await themeNotifier.init" lib/main.dart &amp;&amp; grep -q "ChangeNotifierProvider<ThemeNotifier>" lib/main.dart &amp;&amp; grep -q "Consumer<ThemeNotifier>" lib/main.dart &amp;&amp; grep -q "themeAnimationDuration: const Duration(milliseconds: 500)" lib/main.dart &amp;&amp; grep -q "themeAnimationCurve: Curves.easeOutCubic" lib/main.dart &amp;&amp; grep -q "theme: theme.currentTheme" lib/main.dart &amp;&amp; ! grep -q "ColorScheme.fromSeed(seedColor: Color(0xFF3D6B4F))" lib/main.dart &amp;&amp; grep -q "ResponsiveShell(navigationShell: navigationShell)" lib/router.dart &amp;&amp; ! grep -q "_ScaffoldWithNavBar" lib/router.dart &amp;&amp; flutter test 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `lib/main.dart` source assertions:
      - Imports `platform/window_setup.dart` and `providers/theme_notifier.dart`
      - `await setupDesktopWindow();` present in `main()` after `WidgetsFlutterBinding.ensureInitialized();`
      - `final themeNotifier = ThemeNotifier();` and `await themeNotifier.init();` present in `main()`
      - `ChangeNotifierProvider<ThemeNotifier>.value(value: themeNotifier)` in MultiProvider providers list
      - `Consumer<ThemeNotifier>(builder: ...)` wraps `MaterialApp.router`
      - `theme: theme.currentTheme` replaces the previous hardcoded ColorScheme
      - `themeAnimationDuration: const Duration(milliseconds: 500)` present (literal `500`)
      - `themeAnimationCurve: Curves.easeOutCubic` present
      - The old `ColorScheme.fromSeed(seedColor: Color(0xFF3D6B4F))` line is REMOVED (grep returns nothing)
    - `lib/router.dart` source assertions:
      - Imports `widgets/responsive_shell.dart`
      - `return ResponsiveShell(navigationShell: navigationShell);` replaces `_ScaffoldWithNavBar`
      - The `_ScaffoldWithNavBar` class declaration is fully DELETED (grep returns nothing)
      - Route table unchanged (still has GoRoutes for `/home`, `/goals`, `/goals/archived`, `/schedule`, `/schedule/checkin`, `/settings`, `/settings/past-reviews`, `/onboarding`, `/review`, `/commitments`, `/summary`)
    - `flutter analyze` reports 0 issues
    - `flutter test` passes the existing `quarterly_review_test.dart` (note: this test still uses the bare `MaterialApp(home: ...)` wrap — it does not exercise ThemeNotifier directly. Plan 06 migrates it to `pumpWithMood`. For now it must still pass.)
    - Build smoke: `flutter build web --no-tree-shake-icons` exits 0 (proves the conditional-import stub branch compiles — AC-3 manual gate row from VALIDATION.md "Conditional import compiles on web")
  </acceptance_criteria>
  <done>
    main.dart constructs ThemeNotifier and calls setupDesktopWindow before runApp. CanopyApp registers ThemeNotifier in MultiProvider and wraps MaterialApp.router in Consumer<ThemeNotifier>. MaterialApp.router uses theme.currentTheme with the locked 500ms easeOutCubic theme animation. router.dart uses ResponsiveShell. The deprecated `_ScaffoldWithNavBar` is gone. Web build compiles. Existing tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web URL bar → go_router | Untrusted user-typed URL crosses into the app. go_router redirect logic (router.dart lines 27-34) is the gate. Phase 6 does NOT modify the redirect — it remains the only path that can be bypassed if broken. |
| Desktop window-manager FFI → app launch | `setupDesktopWindow()` runs before runApp; failure modes are: (a) plugin not yet initialized (Flutter handles via `windowManager.ensureInitialized()`), (b) macOS NSWindow incompatibility (Plan 01 verified). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-router-1 | Elevation of Privilege | Direct URL load of `/goals` or `/schedule` bypassing the onboarding redirect | mitigate | The existing `redirect: (context, state) { ... if (!onboardingDone && !onOnboarding) return '/onboarding'; }` (router.dart line 30) re-evaluates on every URL change, including direct loads. Phase 6 does NOT touch this code. Plan 06 adds `test/screens/router_redirect_test.dart` as defense-in-depth verification. |
| T-platform-1 | Tampering | Desktop window resize below 480x640 | accept | window_manager constraint covered in Plan 03 threat model. Plan 04 just wires the call into main.dart. |
| T-06-04-1 | Denial of Service | ThemeNotifier rebuild storm via 20-min ticker triggering full MaterialApp tree rebuilds | mitigate | `Consumer<ThemeNotifier>` narrows the rebuild scope to MaterialApp.router only (NOT the MultiProvider node). Verified by source assertion `grep "Consumer<ThemeNotifier>"`. RESEARCH.md anti-pattern line 607 explicitly forbids the broader rebuild scope. |
| T-06-04-2 | Information Disclosure | A previous user's mood state leaking via shared user account (in a multi-user OS context) | accept | PROJECT.md establishes "local storage only" / "no backend" — there is no remote attacker model and the app is explicitly single-user-per-OS-account. Hive box is in the app's local sandboxed storage. |

ASVS L1 review: no new auth surface. Existing redirect logic is the only access-control mechanism and is untouched by Plan 04.
</threat_model>

<verification>
- `flutter analyze` 0 issues
- `flutter test` passes existing tests (note: quarterly_review_test.dart still uses bare MaterialApp wrap; Plan 06 will migrate to pumpWithMood — for now it must still pass because no widget contract changed)
- `flutter build web --no-tree-shake-icons` exits 0 (proves the conditional-import stub branch compiles — VALIDATION.md AC-3 smoke row)
- Manual smoke verification (deferred to Plan 07 verification phase): `flutter run -d macos` opens a window that refuses to resize below 480px; `flutter run -d chrome` opens with the mobile-style NavigationBar at narrow widths; resizing wide shows the NavigationRail at 720dp.
</verification>

<success_criteria>
- ResponsiveShell exists and is testable (public class)
- main.dart wires ThemeNotifier (Plan 02 product) and window_setup (Plan 03 product) into the running app
- Theme cross-fade is set to 500ms easeOutCubic per D-09 + UI-SPEC lock
- router.dart uses ResponsiveShell at the StatefulShellRoute builder seam
- The dead `_ScaffoldWithNavBar` class is removed
- Existing AC-4 (Web URL routing) verified by the unchanged route table (no edits to GoRoute definitions; redirect logic intact)
- `flutter build web --no-tree-shake-icons` succeeds (verifies the conditional-import stub branch compiles end-to-end)
</success_criteria>

<output>
After completion, create `.planning/phases/06-desktop-and-web-polish/06-04-SUMMARY.md` listing:
- New file `lib/widgets/responsive_shell.dart` (line count)
- Modified `lib/main.dart` (added setupDesktopWindow call, ThemeNotifier construction, Consumer<ThemeNotifier> wrap, themeAnimationDuration/Curve, removed hardcoded 0xFF3D6B4F seed)
- Modified `lib/router.dart` (deleted `_ScaffoldWithNavBar`, uses ResponsiveShell)
- Confirmation that the web build smoke passes
- Note that the breathing pulse (Plan 05) and hover affordances (Plan 05) are next, then tests (Plan 06)
</output>
