---
phase: 06-desktop-and-web-polish
plan: 05
type: execute
wave: 3
depends_on: [02, 04]
files_modified:
  - lib/screens/schedule/widgets/chunk_card.dart
  - lib/screens/goals/widgets/goal_card.dart
  - lib/screens/goals/goals_screen.dart
  - lib/screens/commitments/commitments_screen.dart
  - lib/screens/home/home_screen.dart
  - lib/screens/schedule/checkin_screen.dart
  - lib/screens/quarterly_review/sections/adjustments_section.dart
autonomous: true
requirements: [AC-2, AC-6]
threat_refs: []

clarifications:
  - "AC-2 'drag handle on chunk cards' wording: ChunkCard is NOT inside a ReorderableListView. AC-2's 'drag handle' clause refers exclusively to GoalCard + adjustments_section (the existing ReorderableListView consumers) per UI-SPEC §Drag Handle Visibility table. ChunkCard hover reveals only checkbox + skip per UI-SPEC §Mouse/Touch Interaction Parity Map row 1. Chunk reorder is not in scope for Phase 6 — treat as deferred."

must_haves:
  truths:
    - "Hovering over a ChunkCard reveals checkbox + skip icons via MouseRegion (opacity 0 -> 1, 120ms easeOut)"
    - "Clicking the hover-revealed checkbox calls ScheduleNotifier.markComplete(chunk.id); clicking skip calls markSkipped(chunk.id) — same notifier methods Phase 4 Dismissible uses"
    - "Hovering over a GoalCard reveals edit + archive icons via InkWell.onHover (opacity 0 -> 1, 120ms easeOut)"
    - "Hovering over a commitment list row reveals edit + delete icons via InkWell.onHover"
    - "On mobile (Android/iOS) the hover-revealed icons stay at opacity 0 (pointer-only callbacks never fire) preserving swipe gestures"
    - "On desktop the drag handle for ReorderableListView is always visible at opacity 0.6; on mobile it is hidden (opacity 0)"
    - "HomeScreen check-in CTA pulses (shadow blur 8 -> 16px, 2400ms easeInOut) when ThemeNotifier.isPreCheckin is true; pulse stops when a mood is set"
    - "The breathing pulse is implemented as a standalone public widget BreathingPulseCta (or equivalent extracted widget) so Plan 06 Task 3 can pump it in isolation without HomeScreen's full provider tree"
    - "Tapping a mood at checkin_screen calls ThemeNotifier.setMoodSeed(ThemeNotifier.moodSeeds[mood]!) in addition to the existing mood state write"
    - "Pre-existing duplicate _moodColors maps in home_screen.dart and checkin_screen.dart are removed; both screens reference ThemeNotifier.moodSeeds as the single source of truth"
  artifacts:
    - path: lib/screens/schedule/widgets/chunk_card.dart
      provides: "MouseRegion wrapping the work-variant card content; hover-revealed checkbox + skip IconButtons via AnimatedOpacity. Wired directly to ScheduleNotifier via context.read inside the internal _HoverableChunkContent StatefulWidget — same path Phase 4 Dismissible (swipeable_chunk_card.dart) uses."
      contains: "MouseRegion"
    - path: lib/screens/goals/widgets/goal_card.dart
      provides: "InkWell.onHover callback driving AnimatedOpacity reveal of edit + archive icons in trailing slot when trailing is null"
      contains: "onHover"
    - path: lib/screens/goals/goals_screen.dart
      provides: "ReorderableListView with platform-gated drag handle (opacity 0.6 desktop / 0.0 mobile via defaultTargetPlatform)"
      contains: "defaultTargetPlatform"
    - path: lib/screens/commitments/commitments_screen.dart
      provides: "InkWell.onHover wrap; AnimatedOpacity-wrapped edit + delete icons (delete preserved on mobile via platform check)"
      contains: "onHover"
    - path: lib/screens/home/home_screen.dart
      provides: "Public BreathingPulseCta StatefulWidget (extracted for testability per W-3) wrapping the Start your day OutlinedButton with AnimatedBuilder + BoxShadow blur 8->16; reads ThemeNotifier.isPreCheckin; removes duplicate _moodColors map"
      contains: "class BreathingPulseCta"
    - path: lib/screens/schedule/checkin_screen.dart
      provides: "On mood tap calls context.read<ThemeNotifier>().setMoodSeed(ThemeNotifier.moodSeeds[mood]!); removes duplicate _moodColors map"
      contains: "ThemeNotifier.moodSeeds"
    - path: lib/screens/quarterly_review/sections/adjustments_section.dart
      provides: "Platform-gated drag handle (matches goals_screen pattern)"
      contains: "defaultTargetPlatform"
  key_links:
    - from: "lib/screens/schedule/widgets/chunk_card.dart"
      to: "lib/providers/schedule_notifier.dart"
      via: "context.read<ScheduleNotifier>().markComplete(chunk.id) / markSkipped(chunk.id) from inside _HoverableChunkContent IconButton onPressed"
      pattern: "context\\.read<ScheduleNotifier>\\(\\)\\.markComplete"
    - from: "lib/screens/schedule/checkin_screen.dart"
      to: "lib/providers/theme_notifier.dart"
      via: "context.read<ThemeNotifier>().setMoodSeed on mood tap"
      pattern: "context\\.read<ThemeNotifier>\\(\\)\\.setMoodSeed"
    - from: "lib/screens/home/home_screen.dart"
      to: "lib/providers/theme_notifier.dart"
      via: "context.watch<ThemeNotifier>().isPreCheckin drives pulse animation on/off (inside BreathingPulseCta)"
      pattern: "context\\.watch<ThemeNotifier>"
    - from: "lib/screens/schedule/widgets/chunk_card.dart"
      to: "Flutter MouseRegion"
      via: "onEnter/onExit setState toggles _hovered which AnimatedOpacity reads"
      pattern: "MouseRegion"
---

<objective>
Add all the desktop affordances: hover-revealed icons on ChunkCard / GoalCard / commitment rows, platform-gated always-on drag handle, breathing pulse on the pre-check-in HomeScreen CTA, and the mood-tap -> ThemeNotifier.setMoodSeed wiring. Also delete the duplicate `_moodColors` maps that ThemeNotifier.moodSeeds now supersedes.

Purpose: AC-2 (hover reveals checkbox + drag handle on the ReorderableListView consumers; restores on exit), the deferred AC-6 partial (mood tap reaching ThemeNotifier so the 500ms theme warming actually fires), and the breathing pulse spec from D-08 / UI-SPEC. The MouseRegion-on-pointer-only invariant (RESEARCH.md Pitfall 5) means mobile swipe gestures are preserved without explicit platform gating for hover icons; the drag handle DOES need explicit `defaultTargetPlatform` gating per UI-SPEC §Drag Handle Visibility.

Output: Seven files modified (six screens/widgets + one cross-screen ReorderableListView in quarterly_review). After this plan, the full UAT path works end-to-end on macOS: launch app, see pulsing CTA, tap mood, watch theme warm, hover chunk cards, click hover-revealed checkbox. Plan 06 then adds the test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-desktop-and-web-polish/06-CONTEXT.md
@.planning/phases/06-desktop-and-web-polish/06-RESEARCH.md
@.planning/phases/06-desktop-and-web-polish/06-PATTERNS.md
@.planning/phases/06-desktop-and-web-polish/06-UI-SPEC.md
@.planning/phases/06-desktop-and-web-polish/06-04-SUMMARY.md
@lib/providers/theme_notifier.dart
@lib/providers/schedule_notifier.dart
@lib/screens/schedule/widgets/chunk_card.dart
@lib/screens/schedule/widgets/swipeable_chunk_card.dart
@lib/screens/goals/widgets/goal_card.dart
@lib/screens/goals/goals_screen.dart
@lib/screens/commitments/commitments_screen.dart
@lib/screens/home/home_screen.dart
@lib/screens/schedule/checkin_screen.dart
@lib/screens/quarterly_review/sections/adjustments_section.dart

<interfaces>
From lib/providers/theme_notifier.dart (Plan 02 product — public surface):
- `static const Map<int, Color> ThemeNotifier.moodSeeds` (5 entries indexed 1..5)
- `static const Color ThemeNotifier.curiousSeed`
- `bool get isPreCheckin`  (true when _moodSeed == null)
- `Future<void> setMoodSeed(Color seed)`  (called from checkin_screen on mood tap)
- `ThemeData get currentTheme`  (used by main.dart Consumer<ThemeNotifier>)

From lib/providers/schedule_notifier.dart (Phase 4 product — the methods ChunkCard hover icons will invoke):
- `Future<void> markComplete(String chunkId)` — marks chunk complete, saves schedule, appends CompletionLog (idempotent: no-op if already completed)
- `Future<void> markSkipped(String chunkId)` — marks chunk skipped, saves schedule, appends CompletionLog (idempotent: no-op if already skipped)
- Both methods are the SAME path SwipeableChunkCard's Dismissible.confirmDismiss invokes (see swipeable_chunk_card.dart lines 41-51) — hover-click and swipe-gesture share identical state writes.

From lib/screens/schedule/widgets/swipeable_chunk_card.dart (Phase 4 product — DO NOT MODIFY in this plan):
- Wraps ChunkCard in a Dismissible for swipe-to-complete (right) / swipe-to-skip (left) on work chunks only
- confirmDismiss calls `context.read<ScheduleNotifier>().markComplete(chunk.id)` or `markSkipped(chunk.id)` and always returns false (Dismissible used purely as gesture affordance)
- Break chunks pass through as plain ChunkCard without Dismissible wrapping
- Resolved chunks have DismissDirection.none (cannot be re-swiped)
- The Dismissible wrapper is OUTSIDE the ChunkCard. The new MouseRegion will be INSIDE ChunkCard. Per RESEARCH.md Pitfall 5 (MouseRegion + Dismissible safety): MouseRegion fires only on pointer-enter events (mouse), and Dismissible fires only on pan/drag gestures (touch or mouse-drag) — they cannot conflict because the gesture systems are orthogonal. Hover-click on the IconButton and swipe-drag-on-card both end at the same `markComplete(chunk.id)` / `markSkipped(chunk.id)` call; the user just chooses which input modality.

From RESEARCH.md Code Examples §MouseRegion hover-reveal (lines 820-863) — the chunk_card pattern. Key details:
- `MouseRegion(onEnter: (_) => setState(() => _hovered = true), onExit: (_) => setState(() => _hovered = false), child: Stack(children: [...existing card..., Positioned(right: 0, top: 0, bottom: 0, child: AnimatedOpacity(opacity: _hovered ? 1.0 : 0.0, duration: Duration(milliseconds: 120), curve: Curves.easeOut, child: Row(children: [IconButton(check), IconButton(skip)])))]))`
- Tooltips per UI-SPEC §Copywriting: `'Mark complete'` for check icon, `'Skip'` for skip icon, icons `Icons.check_circle_outline` + `Icons.skip_next_outlined`

From RESEARCH.md Code Examples §Breathing pulse (lines 754-817) — the _BreathingPulseButton pattern:
- `AnimationController(vsync: this, duration: Duration(milliseconds: 2400))` — UI-SPEC locked 2400ms
- `MediaQuery / WidgetsBinding.instance.platformDispatcher.accessibilityFeatures.disableAnimations` check — if true, controller does NOT `.repeat()`
- `AnimatedBuilder` with `CurvedAnimation(parent: _controller, curve: Curves.easeInOut)` — UI-SPEC locked easeInOut
- Inside builder: `blur = 8.0 + 8.0 * t` (8 -> 16 -> 8 cycle)
- `BoxShadow(color: primary.withValues(alpha: 0.25), blurRadius: blur, spreadRadius: 1)` — UI-SPEC locked alpha 0.25

From RESEARCH.md Code Examples §Drag handle visibility (lines 866-897):
- `final isMobileTouch = defaultTargetPlatform == TargetPlatform.android || defaultTargetPlatform == TargetPlatform.iOS;`
- `trailing: isMobileTouch ? const SizedBox.shrink() : ReorderableDelayedDragStartListener(index: i, child: AnimatedOpacity(duration: Duration(milliseconds: 120), opacity: 0.6, child: Padding(padding: EdgeInsets.symmetric(horizontal: 8), child: Icon(Icons.drag_handle, color: theme.colorScheme.outline))))`

From PATTERNS.md §"lib/screens/schedule/checkin_screen.dart" (lines 607-650):
- Existing onTap at checkin_screen.dart:148-153 writes `setState(() => _selectedMood = mood);`
- Phase 6 ADDS (does not replace): `final seed = ThemeNotifier.moodSeeds[mood]!; context.read<ThemeNotifier>().setMoodSeed(seed);`
- Duplicate `_moodColors` at checkin_screen.dart:19-25 — DELETE (sole consumer was the AnimatedContainer color; now driven by theme)
- Keep `_moodEmojis` and `_moodPrefix` (UI strings, not colors)

From PATTERNS.md §"lib/screens/home/home_screen.dart" (lines 528-603):
- Existing `_buildEmptyState` (lines 167-210) wraps the CTA — wrap the existing `OutlinedButton(onPressed: () => context.push('/schedule/checkin'), child: const Text('Start your day'))` in a new public `BreathingPulseCta` widget (extracted to a top-level public widget in home_screen.dart per W-3 so Plan 06 Task 3 can pump it directly)
- `BreathingPulseCta` is a public StatefulWidget with `SingleTickerProviderStateMixin`
- The pulse only animates when `context.watch<ThemeNotifier>().isPreCheckin == true` — if false, the pulse stops (controller.stop() or skip the repeat call). Implementation: pass `enabled: themeNotifier.isPreCheckin` into `BreathingPulseCta` and have the controller respond in didUpdateWidget.
- Duplicate `_moodColors` at home_screen.dart:21-27 — DELETE (consumers in `_buildSummary` or similar that show mood emoji color tint should switch to `ThemeNotifier.moodSeeds[mood]` if needed; if only used for cosmetic tint on the home screen's own widgets and the theme is already mood-derived, the static map call sites may simplify to `Theme.of(context).colorScheme.primary`)
- Keep `_moodEmojis` and `_moodDescriptions` (UI strings)

From UI-SPEC §Hover Reveals:
- ChunkCard: MouseRegion (no existing InkWell) — reveal at 120ms easeOut on 80px trailing slot
- GoalCard: InkWell.onHover (existing InkWell at goal_card.dart:62) — reveal at 120ms easeOut
- Commitment row: InkWell.onHover (wrap if not already) — reveal edit + delete
- Card elevation 1->2 on hover handled automatically by Material 3 (NO override)

From UI-SPEC §Mouse / Touch Interaction Parity:
- Mobile swipe-right (complete) -> Desktop hover-revealed checkbox -> click
- Mobile swipe-left (skip) -> Desktop hover-revealed skip button -> click
- Mobile long-press drag -> Desktop click-and-drag the always-visible drag handle (opacity 0.6)
- Tap card stays the same

From PATTERNS.md cross-cutting landmines §"Hover-revealed icons on mobile = inaccessible delete on commitments":
- Mobile users would lose access to delete because pointer-driven onHover keeps icons at opacity 0
- Resolution: keep the existing always-on delete IconButton on mobile (use `defaultTargetPlatform` gate at row level OR use the same delete button always-visible and add edit beside it on hover). Decision: use `defaultTargetPlatform` to determine which trailing layout (always-on for mobile / hover-revealed for desktop) so the delete affordance never disappears on mobile.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChunkCard MouseRegion hover-reveal + checkin_screen mood-seed wiring</name>
  <files>lib/screens/schedule/widgets/chunk_card.dart, lib/screens/schedule/checkin_screen.dart</files>
  <read_first>
    - lib/screens/schedule/widgets/chunk_card.dart (current `_buildWork` method ~lines 91-185, plus the file header `hexToColor` at lines 5-7)
    - lib/screens/schedule/widgets/swipeable_chunk_card.dart (Phase 4 Dismissible wrapper at lines 14-67 — confirms ScheduleNotifier.markComplete(chunk.id) / markSkipped(chunk.id) is the public path; Plan 05 must NOT touch this file)
    - lib/providers/schedule_notifier.dart (markComplete + markSkipped signatures — both take String chunkId and are idempotent)
    - lib/screens/schedule/checkin_screen.dart (existing mood tap GestureDetector ~lines 148-153, existing `_moodColors` map at ~lines 19-25, `_generate()` at ~lines 59-67)
    - lib/providers/theme_notifier.dart (Plan 02 product — uses ThemeNotifier.moodSeeds[mood]!)
    - .planning/phases/06-desktop-and-web-polish/06-RESEARCH.md §Code Examples §MouseRegion hover-reveal (lines 820-863) and §Pitfall 5 (MouseRegion + Dismissible safety)
    - .planning/phases/06-desktop-and-web-polish/06-UI-SPEC.md §Hover Reveals (120ms easeOut) and §Copywriting Contract (tooltips)
    - .planning/phases/06-desktop-and-web-polish/06-PATTERNS.md §"lib/screens/schedule/widgets/chunk_card.dart" (lines 320-402)
  </read_first>
  <action>
    Edit `lib/screens/schedule/widgets/chunk_card.dart`:
    1. Add imports: `import 'package:provider/provider.dart';` and `import '../../../providers/schedule_notifier.dart';`. (The file currently imports only material.dart and the scheduled_chunk model; ChunkCard becomes Provider-aware to call markComplete/markSkipped directly — Option A from B-1.)
    2. Extract the work-variant body (current `_buildWork` method at lines 91-185) into a private `StatefulWidget` named `_HoverableChunkContent`. The widget receives the ChunkCard's `chunk` and `goalColor` via its constructor. The public `ChunkCard` widget remains the entry point and its build() returns the appropriate variant: short-break and long-break unchanged (call the existing methods), work returns `_HoverableChunkContent(chunk: chunk, goalColor: goalColor)`. ChunkCard's public constructor signature is UNCHANGED (still `{required this.chunk, this.goalColor}`) — no new parameters added. Per B-1 Option A, all complete/skip wiring lives inside `_HoverableChunkContent` via `context.read<ScheduleNotifier>()` directly.
    3. Inside `_HoverableChunkContent` State, declare `bool _hovered = false;`. The build method wraps the existing card-body Stack in a `MouseRegion(onEnter: (_) => setState(() => _hovered = true), onExit: (_) => setState(() => _hovered = false), child: Stack(children: [...existing card children unchanged..., Positioned(right: 0, top: 0, bottom: 0, child: AnimatedOpacity(opacity: _hovered ? 1.0 : 0.0, duration: const Duration(milliseconds: 120), curve: Curves.easeOut, child: Row(mainAxisSize: MainAxisSize.min, children: [IconButton(icon: const Icon(Icons.check_circle_outline), tooltip: 'Mark complete', onPressed: _hovered && !widget.chunk.isCompleted && !widget.chunk.isSkipped ? () => context.read<ScheduleNotifier>().markComplete(widget.chunk.id) : null), IconButton(icon: const Icon(Icons.skip_next_outlined), tooltip: 'Skip', onPressed: _hovered && !widget.chunk.isCompleted && !widget.chunk.isSkipped ? () => context.read<ScheduleNotifier>().markSkipped(widget.chunk.id) : null)])))]))`.
    4. The onPressed guards (`_hovered && !isCompleted && !isSkipped`) mirror SwipeableChunkCard's resolved-chunk behavior (lines 35-38: resolved chunks have DismissDirection.none). Hover icons on resolved chunks are still rendered when hovered (visual consistency) but their tap is a no-op.
    5. The Dismissible wrapper in SwipeableChunkCard is unaffected (MouseRegion is INSIDE the work Card body, Dismissible wraps the entire ChunkCard from outside — Pitfall 5 invariant). Hover-click and swipe-gesture both terminate at the same ScheduleNotifier method, just via different input modalities.
    6. Preserve all existing card structure: 5dp left bar (chunk_card.dart:105-119), `barColor` fallback to `theme.colorScheme.primary`, `Icons.check_circle` completion state with `Colors.green.shade600`, `withValues(alpha: ...)` over `withOpacity` (UI-SPEC State of the Art).

    Edit `lib/screens/schedule/checkin_screen.dart`:
    1. Add import `import 'package:provider/provider.dart';` (if not already present), `import '../../providers/theme_notifier.dart';`.
    2. Locate the existing `GestureDetector.onTap` at line 148-153 that writes `_selectedMood = mood`. ADD (do not replace) the mood-seed write after the existing setState: `context.read<ThemeNotifier>().setMoodSeed(ThemeNotifier.moodSeeds[mood]!);`. The `setState` and the existing `_generate()` flow at line 59-67 stay intact — PATTERNS.md confirms the `generateToday(...)` call must NOT be removed.
    3. DELETE the `static const Map<int, Color> _moodColors` at lines 19-25. Find all references (typically used to color the AnimatedContainer background per mood) and replace each with `ThemeNotifier.moodSeeds[mood]!` (the seed color directly — same hex values that previously lived in `_moodColors`). Keep `_moodEmojis` and `_moodPrefix` — those are UI strings.

    Per D-XX traceability: Implements D-01 (mood seed map centralized in ThemeNotifier), D-09 (setMoodSeed triggers the warming transition via the MaterialApp.router themeAnimationDuration wired in Plan 04), AC-2 (ChunkCard hover reveal — Option A wiring: hover icons call ScheduleNotifier directly, same path as Phase 4 Dismissible), AC-6 (mood tap reaches the ThemeNotifier).
  </action>
  <verify>
    <automated>flutter analyze 2>&amp;1 | tee /tmp/p5t1_analyze.log | tail -20 &amp;&amp; ! grep -E "error.*lib/screens/schedule/widgets/chunk_card\.dart" /tmp/p5t1_analyze.log &amp;&amp; ! grep -E "error.*lib/screens/schedule/checkin_screen\.dart" /tmp/p5t1_analyze.log &amp;&amp; grep -q "MouseRegion" lib/screens/schedule/widgets/chunk_card.dart &amp;&amp; grep -q "Icons.check_circle_outline" lib/screens/schedule/widgets/chunk_card.dart &amp;&amp; grep -q "Icons.skip_next_outlined" lib/screens/schedule/widgets/chunk_card.dart &amp;&amp; grep -q "Duration(milliseconds: 120)" lib/screens/schedule/widgets/chunk_card.dart &amp;&amp; grep -q "Curves.easeOut" lib/screens/schedule/widgets/chunk_card.dart &amp;&amp; grep -q "'Mark complete'" lib/screens/schedule/widgets/chunk_card.dart &amp;&amp; grep -q "'Skip'" lib/screens/schedule/widgets/chunk_card.dart &amp;&amp; grep -q "context.read<ScheduleNotifier>().markComplete" lib/screens/schedule/widgets/chunk_card.dart &amp;&amp; grep -q "context.read<ScheduleNotifier>().markSkipped" lib/screens/schedule/widgets/chunk_card.dart &amp;&amp; grep -q "context.read<ThemeNotifier>().setMoodSeed" lib/screens/schedule/checkin_screen.dart &amp;&amp; grep -q "ThemeNotifier.moodSeeds" lib/screens/schedule/checkin_screen.dart &amp;&amp; ! grep -q "static const Map<int, Color> _moodColors" lib/screens/schedule/checkin_screen.dart &amp;&amp; flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `lib/screens/schedule/widgets/chunk_card.dart`:
      - `grep "MouseRegion"` exits 0
      - Both `Icons.check_circle_outline` and `Icons.skip_next_outlined` present
      - Animation literals `Duration(milliseconds: 120)` and `Curves.easeOut` present (UI-SPEC locked)
      - Tooltips `'Mark complete'` and `'Skip'` present (UI-SPEC §Copywriting)
      - `context.read<ScheduleNotifier>().markComplete` and `.markSkipped` BOTH present (Option A wiring — B-1 resolution)
      - The 5dp left color bar code at lines 105-119 is UNCHANGED (grep `width: 5`)
      - The `Icons.check_circle` completion state with `Colors.green.shade600` is UNCHANGED (UI-SPEC contrast lock)
    - `lib/screens/schedule/checkin_screen.dart`:
      - `grep "context.read<ThemeNotifier>().setMoodSeed"` exits 0
      - `grep "ThemeNotifier.moodSeeds"` exits 0
      - `grep "static const Map<int, Color> _moodColors"` returns nothing (exit 1 — deleted)
      - The existing `generateToday(...)` call inside `_generate()` (lines 59-67) is preserved
      - `_moodEmojis` and `_moodPrefix` maps are preserved
    - `flutter analyze` 0 errors in the two edited files (gate enforces no errors in chunk_card.dart or checkin_screen.dart)
    - `flutter test` passes existing suite
  </acceptance_criteria>
  <done>
    ChunkCard work variant supports mouse hover reveal. Hover-revealed checkbox click calls ScheduleNotifier.markComplete(chunk.id); skip click calls markSkipped(chunk.id) — same notifier path Phase 4 Dismissible uses, no callback parameters added to public ChunkCard API. Checkin screen routes mood taps through ThemeNotifier (so Plan 04's themeAnimationDuration kicks in). The duplicate `_moodColors` map in checkin_screen is gone.
  </done>
</task>

<task type="auto">
  <name>Task 2: GoalCard InkWell.onHover + goals_screen / quarterly_review drag handle visibility</name>
  <files>lib/screens/goals/widgets/goal_card.dart, lib/screens/goals/goals_screen.dart, lib/screens/quarterly_review/sections/adjustments_section.dart</files>
  <read_first>
    - lib/screens/goals/widgets/goal_card.dart (existing InkWell at lines ~62-64; `trailing` parameter at line ~20; Stack with content at lines ~58-138)
    - lib/screens/goals/goals_screen.dart (ReorderableListView usage at lines ~178-188 where `GoalCard.trailing:` is built)
    - lib/screens/quarterly_review/sections/adjustments_section.dart (ReorderableListView usage with `GoalAdjustmentTile`)
    - .planning/phases/06-desktop-and-web-polish/06-RESEARCH.md §Code Examples §Drag handle visibility (lines 866-897)
    - .planning/phases/06-desktop-and-web-polish/06-UI-SPEC.md §Drag Handle Visibility (opacity 0.6 desktop / 0.0 mobile, `defaultTargetPlatform` gate)
    - .planning/phases/06-desktop-and-web-polish/06-PATTERNS.md §"lib/screens/goals/widgets/goal_card.dart" (lines 404-482) and Cross-Cutting Landmines table
  </read_first>
  <action>
    Edit `lib/screens/goals/widgets/goal_card.dart`:
    1. Convert `GoalCard` (currently StatelessWidget) to a `StatefulWidget` if it isn't already. Need `bool _hovered = false`.
    2. Add `onHover: (hovered) => setState(() => _hovered = hovered)` to the existing `InkWell` at line ~62-64.
    3. After the existing Stack children, add (only when `widget.trailing == null`): a `Positioned(right: 0, top: 0, bottom: 0, child: AnimatedOpacity(opacity: _hovered ? 1.0 : 0.0, duration: const Duration(milliseconds: 120), curve: Curves.easeOut, child: Row(mainAxisSize: MainAxisSize.min, children: [IconButton(icon: const Icon(Icons.edit_outlined), tooltip: 'Edit goal', onPressed: _hovered ? widget.onEdit : null), IconButton(icon: const Icon(Icons.archive_outlined), tooltip: 'Archive goal', onPressed: _hovered ? widget.onArchive : null)])))`.
    4. Add `final VoidCallback? onEdit;` and `final VoidCallback? onArchive;` to the GoalCard fields; pass null at current call sites if not wired, or wire to existing callbacks if present.
    5. Existing `trailing` parameter behavior is preserved (when caller passes a trailing — e.g. from goals_screen for the drag handle — the hover-revealed icons are NOT shown; the trailing slot is exclusively used). This is the "trailing == null" guard from PATTERNS.md.
    6. Preserve: 5dp left bar (lines 65-82), goalColor fallback, all existing constructor parameters.

    Edit `lib/screens/goals/goals_screen.dart`:
    1. Add import `import 'package:flutter/foundation.dart' show defaultTargetPlatform, TargetPlatform;` if not present.
    2. In the ReorderableListView.builder.itemBuilder (lines ~178-188), where `GoalCard.trailing:` is built, compute `final isMobileTouch = defaultTargetPlatform == TargetPlatform.android || defaultTargetPlatform == TargetPlatform.iOS;` once at the start of `build` (or memoize via local final).
    3. Replace the existing trailing with the conditional from RESEARCH.md lines 866-897:
       - `trailing: isMobileTouch ? null : ReorderableDelayedDragStartListener(index: i, child: AnimatedOpacity(duration: const Duration(milliseconds: 120), opacity: 0.6, child: Padding(padding: const EdgeInsets.symmetric(horizontal: 8), child: Icon(Icons.drag_handle, color: Theme.of(context).colorScheme.outline))))`
    4. Note: when `isMobileTouch == true`, `trailing` is `null` (fully omits the trailing slot). GoalCard's `trailing == null` guard then DOES try to show hover icons — but on mobile pointer-only onHover never fires (RESEARCH.md Pitfall 5), so hover icons stay at opacity 0 anyway. Result: mobile shows neither the drag handle NOR (visibly) the hover icons; desktop shows the drag handle (hover icons hidden because trailing is non-null).

    Edit `lib/screens/quarterly_review/sections/adjustments_section.dart`:
    1. Apply the same platform-gated drag handle pattern: `final isMobileTouch = defaultTargetPlatform == TargetPlatform.android || defaultTargetPlatform == TargetPlatform.iOS;` and use the same `isMobileTouch ? null : ReorderableDelayedDragStartListener(...)` shape on the existing `GoalAdjustmentTile` reorderable item.

    Per D-XX traceability: AC-2 (GoalCard hover reveal), AC-2 (drag handle hover affordance — UI-SPEC §Drag Handle Visibility, applies to GoalCard + adjustments_section per clarifications above; ChunkCard is NOT a ReorderableListView consumer), D-11 (platform-gated visibility for desktop vs mobile).
  </action>
  <verify>
    <automated>flutter analyze 2>&amp;1 | tee /tmp/p5t2_analyze.log | tail -20 &amp;&amp; ! grep -E "error.*lib/screens/goals/widgets/goal_card\.dart" /tmp/p5t2_analyze.log &amp;&amp; ! grep -E "error.*lib/screens/goals/goals_screen\.dart" /tmp/p5t2_analyze.log &amp;&amp; ! grep -E "error.*lib/screens/quarterly_review/sections/adjustments_section\.dart" /tmp/p5t2_analyze.log &amp;&amp; grep -q "onHover:" lib/screens/goals/widgets/goal_card.dart &amp;&amp; grep -q "Icons.edit_outlined" lib/screens/goals/widgets/goal_card.dart &amp;&amp; grep -q "Icons.archive_outlined" lib/screens/goals/widgets/goal_card.dart &amp;&amp; grep -q "'Edit goal'" lib/screens/goals/widgets/goal_card.dart &amp;&amp; grep -q "'Archive goal'" lib/screens/goals/widgets/goal_card.dart &amp;&amp; grep -q "defaultTargetPlatform" lib/screens/goals/goals_screen.dart &amp;&amp; grep -q "ReorderableDelayedDragStartListener" lib/screens/goals/goals_screen.dart &amp;&amp; grep -q "Icons.drag_handle" lib/screens/goals/goals_screen.dart &amp;&amp; grep -q "opacity: 0.6" lib/screens/goals/goals_screen.dart &amp;&amp; grep -q "defaultTargetPlatform" lib/screens/quarterly_review/sections/adjustments_section.dart &amp;&amp; flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `lib/screens/goals/widgets/goal_card.dart`:
      - `onHover:` callback present on the existing InkWell
      - Both `Icons.edit_outlined` and `Icons.archive_outlined` present
      - Tooltips `'Edit goal'` and `'Archive goal'` present (UI-SPEC §Copywriting)
      - Animation literals `Duration(milliseconds: 120)` and `Curves.easeOut` present
      - The existing 5dp left bar (lines 65-82) is UNCHANGED
    - `lib/screens/goals/goals_screen.dart`:
      - `defaultTargetPlatform` imported and used in a `isMobileTouch` local
      - `ReorderableDelayedDragStartListener` wrapping a `Icon(Icons.drag_handle, ...)` for the desktop branch
      - `opacity: 0.6` literal present (UI-SPEC dragHandle.opacity.desktop lock)
      - The mobile branch uses `null` for `trailing` (so GoalCard's `trailing == null` guard reveals nothing on touch — hover icons stay invisible on mobile too because pointer-only onHover never fires)
    - `lib/screens/quarterly_review/sections/adjustments_section.dart`: same `defaultTargetPlatform` + `ReorderableDelayedDragStartListener` pattern
    - `flutter analyze` 0 errors in the three edited files
    - `flutter test` passes existing suite
  </acceptance_criteria>
  <done>
    GoalCard supports InkWell.onHover with edit/archive reveal. Goals + quarterly-review ReorderableListView usages show the drag handle at opacity 0.6 on desktop only. Per B-1 resolution, SwipeableChunkCard is NOT modified in this plan — ChunkCard's hover-revealed icons call ScheduleNotifier directly via context.read inside _HoverableChunkContent (Task 1), so no callback forwarding through SwipeableChunkCard is needed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commitments row hover + HomeScreen breathing pulse (extracted as BreathingPulseCta) + duplicate _moodColors cleanup</name>
  <files>lib/screens/commitments/commitments_screen.dart, lib/screens/home/home_screen.dart</files>
  <read_first>
    - lib/screens/commitments/commitments_screen.dart (existing list row at lines ~144-191 with Card+InkWell+Container swatch+Expanded column+delete IconButton; _confirmDelete at lines ~44-65; _openAddSheet at lines ~24-42)
    - lib/screens/home/home_screen.dart (existing `_buildEmptyState` ~lines 167-210 with the `OutlinedButton onPressed: () => context.push('/schedule/checkin')`; existing `_moodColors` static const at lines ~21-27)
    - lib/providers/theme_notifier.dart (uses `isPreCheckin` getter)
    - .planning/phases/06-desktop-and-web-polish/06-RESEARCH.md §Code Examples §Breathing pulse (lines 754-817 — full `_BreathingPulseButton` widget code)
    - .planning/phases/06-desktop-and-web-polish/06-UI-SPEC.md §Breathing Pulse (D-08 — 2400ms, easeInOut, blur 8->16, alpha 0.25, MediaQuery.disableAnimations respected)
    - .planning/phases/06-desktop-and-web-polish/06-PATTERNS.md §"lib/screens/commitments/commitments_screen.dart" (lines 485-525) and §"lib/screens/home/home_screen.dart" (lines 528-603)
  </read_first>
  <action>
    Edit `lib/screens/commitments/commitments_screen.dart`:
    1. Convert the row builder (lines ~144-191) to a small StatefulWidget `_CommitmentRow` (or stateful private widget within the existing file) that holds `bool _hovered = false`.
    2. Add `onHover: (hovered) => setState(() => _hovered = hovered)` to the existing InkWell at line ~146.
    3. Compute `final isMobileTouch = defaultTargetPlatform == TargetPlatform.android || defaultTargetPlatform == TargetPlatform.iOS;` at the top of the row's build.
    4. The existing trailing `IconButton(icon: Icon(Icons.delete_outline), onPressed: () => _confirmDelete(...))` becomes a conditional Row:
       - On `isMobileTouch == true`: keep the existing always-visible delete icon (so mobile users retain delete access per PATTERNS.md Cross-Cutting Landmines table)
       - On `isMobileTouch == false` (desktop): show an `AnimatedOpacity(opacity: _hovered ? 1.0 : 0.0, duration: const Duration(milliseconds: 120), curve: Curves.easeOut, child: Row(mainAxisSize: MainAxisSize.min, children: [IconButton(icon: const Icon(Icons.edit_outlined), tooltip: 'Edit commitment', onPressed: _hovered ? () => _openAddSheet(context, block) : null), IconButton(icon: const Icon(Icons.delete_outline), tooltip: 'Delete commitment', onPressed: _hovered ? () => _confirmDelete(context, block) : null)]))`.
    5. Preserve: the Card+InkWell+Container color swatch + Expanded column structure, the existing `_confirmDelete` dialog copy (UI-SPEC §Copywriting locked), the existing `_openAddSheet` route.

    Edit `lib/screens/home/home_screen.dart`:
    1. Add imports: `import 'package:provider/provider.dart';` (likely already present), `import '../../providers/theme_notifier.dart';`.
    2. Create a PUBLIC `BreathingPulseCta` StatefulWidget as a top-level class in home_screen.dart (NOT private, NOT nested inside _HomeScreenState — per W-3 it must be importable by Plan 06 Task 3's home_screen_breathing_pulse_test.dart for direct pumping without HomeScreen's full provider tree). The widget structure follows RESEARCH.md Code Examples lines 754-817:
       - Constructor: `const BreathingPulseCta({super.key, required this.enabled, required this.onPressed, required this.child});`
       - Public fields: `final bool enabled;` (drives whether the pulse animates), `final VoidCallback onPressed;` (forwarded to inner widget if needed), `final Widget child;` (the OutlinedButton or other CTA being decorated).
       - State has `late final AnimationController _controller` with `duration: const Duration(milliseconds: 2400)`, `vsync: this`, `SingleTickerProviderStateMixin` on the State.
       - In `initState`, check `WidgetsBinding.instance.platformDispatcher.accessibilityFeatures.disableAnimations`. If true OR `widget.enabled == false`, do NOT call `_controller.repeat(reverse: true)` — set `_controller.value = 0.5;` (midpoint, blur 12px). UI-SPEC requires "render pulse at midpoint and do not animate" for reduced motion. If animations enabled AND `widget.enabled == true`, call `_controller.repeat(reverse: true)`.
       - Override `didUpdateWidget`: if `widget.enabled` changed from true to false (or disableAnimations is true), call `_controller.stop(); _controller.value = 0.5;` (settle to midpoint, not animating); if from false to true and animations not disabled, call `_controller.repeat(reverse: true)`.
       - Dispose: `_controller.dispose(); super.dispose();`
       - Build: wraps `widget.child` in an `AnimatedBuilder(animation: CurvedAnimation(parent: _controller, curve: Curves.easeInOut), builder: (context, child) { final t = _controller.value; final blur = 8.0 + 8.0 * t; return Container(decoration: BoxDecoration(borderRadius: BorderRadius.circular(30), boxShadow: [BoxShadow(color: Theme.of(context).colorScheme.primary.withValues(alpha: 0.25), blurRadius: blur, spreadRadius: 1)]), child: child); }, child: widget.child)`.
    3. In `_buildEmptyState`, replace the existing `OutlinedButton(onPressed: () => context.push('/schedule/checkin'), child: const Text('Start your day'))` with `BreathingPulseCta(enabled: context.watch<ThemeNotifier>().isPreCheckin, onPressed: () => context.push('/schedule/checkin'), child: OutlinedButton(onPressed: () => context.push('/schedule/checkin'), child: const Text('Start your day')))` — `BreathingPulseCta` accepts the existing OutlinedButton as its `child` and adds the pulsing shadow decoration via AnimatedBuilder + Container.
    4. DELETE the `static const Map<int, Color> _moodColors` at lines ~21-27. If any call sites reference it (typically inside `_buildSummary` or similar to show the mood emoji color), replace each with `ThemeNotifier.moodSeeds[mood]` (since the theme itself is now mood-derived and ThemeNotifier.moodSeeds is the single source of truth, the previous `_moodColors[mood]` lookup IS the same value). Keep `_moodEmojis` and `_moodDescriptions` (UI strings).

    Per D-XX traceability: D-08 (breathing pulse), AC-2 (commitment hover reveal), AC-6 (HomeScreen reflects mood theme via ThemeNotifier.isPreCheckin reading), UI-SPEC §Breathing Pulse (disableAnimations respect), PATTERNS.md cross-cutting landmine resolution for commitments delete access on mobile, W-3 resolution (BreathingPulseCta extracted as public widget so Plan 06 can test it in isolation).
  </action>
  <verify>
    <automated>flutter analyze 2>&amp;1 | tee /tmp/p5t3_analyze.log | tail -20 &amp;&amp; ! grep -E "error.*lib/screens/commitments/commitments_screen\.dart" /tmp/p5t3_analyze.log &amp;&amp; ! grep -E "error.*lib/screens/home/home_screen\.dart" /tmp/p5t3_analyze.log &amp;&amp; grep -q "onHover:" lib/screens/commitments/commitments_screen.dart &amp;&amp; grep -q "Icons.edit_outlined" lib/screens/commitments/commitments_screen.dart &amp;&amp; grep -q "Icons.delete_outline" lib/screens/commitments/commitments_screen.dart &amp;&amp; grep -q "'Edit commitment'" lib/screens/commitments/commitments_screen.dart &amp;&amp; grep -q "'Delete commitment'" lib/screens/commitments/commitments_screen.dart &amp;&amp; grep -q "defaultTargetPlatform" lib/screens/commitments/commitments_screen.dart &amp;&amp; grep -q "class BreathingPulseCta" lib/screens/home/home_screen.dart &amp;&amp; grep -q "Duration(milliseconds: 2400)" lib/screens/home/home_screen.dart &amp;&amp; grep -q "context.watch<ThemeNotifier>" lib/screens/home/home_screen.dart &amp;&amp; grep -q "isPreCheckin" lib/screens/home/home_screen.dart &amp;&amp; grep -q "disableAnimations" lib/screens/home/home_screen.dart &amp;&amp; grep -q "blurRadius" lib/screens/home/home_screen.dart &amp;&amp; grep -q "withValues(alpha: 0.25)" lib/screens/home/home_screen.dart &amp;&amp; ! grep -q "static const Map<int, Color> _moodColors" lib/screens/home/home_screen.dart &amp;&amp; flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `lib/screens/commitments/commitments_screen.dart`:
      - `onHover:` callback present on InkWell
      - `Icons.edit_outlined` and `Icons.delete_outline` present
      - Tooltips `'Edit commitment'` and `'Delete commitment'` present
      - `defaultTargetPlatform` import + `isMobileTouch` local gate
      - Mobile branch preserves the always-visible delete IconButton (per cross-cutting landmine resolution)
      - `_confirmDelete` and `_openAddSheet` existing methods unchanged
    - `lib/screens/home/home_screen.dart`:
      - `class BreathingPulseCta` present as a PUBLIC top-level widget (no leading underscore) — verified by `grep -c "^class BreathingPulseCta"` returning 1
      - `Duration(milliseconds: 2400)` literal present (UI-SPEC locked loop duration)
      - `Curves.easeInOut` curve present
      - `8.0 + 8.0 * t` blur math present (8 -> 16 cycle)
      - `withValues(alpha: 0.25)` literal present (UI-SPEC shadow color lock)
      - `context.watch<ThemeNotifier>` reading `isPreCheckin` present
      - `disableAnimations` check present (UI-SPEC accessibility lock)
      - `static const Map<int, Color> _moodColors` REMOVED (grep returns nothing)
      - `_moodEmojis` and `_moodDescriptions` preserved
    - `flutter analyze` 0 errors in the two edited files
    - `flutter test` passes existing suite
  </acceptance_criteria>
  <done>
    Commitment rows reveal edit + delete on desktop hover and keep mobile delete affordance. HomeScreen CTA is decorated by the new PUBLIC `BreathingPulseCta` widget driven by `ThemeNotifier.isPreCheckin` — Plan 06 Task 3 can pump `BreathingPulseCta` directly without HomeScreen's full provider tree (W-3 resolution). The duplicate `_moodColors` map is removed from home_screen. UI-SPEC §Breathing Pulse fully implemented (2400ms, easeInOut, blur 8->16, alpha 0.25, disableAnimations honored).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User pointer events (mouse/touch) → widget hover state | MouseRegion/InkWell.onHover fire on local pointer events only. No remote trust crossing. |
| Mood tap → ThemeNotifier.setMoodSeed → Hive write | Untrusted input is the mood index 1-5 selected by the user from a discrete picker (no free-text). ThemeNotifier reads from the locked static const map; out-of-range indices throw because the map has only 5 entries. |
| Hover IconButton tap → ScheduleNotifier.markComplete/markSkipped → Hive write | Same path as Phase 4 Dismissible swipe gesture. No new trust boundary — identical state transitions, just a different input modality. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-1 | Denial of Service | Breathing pulse AnimationController leak on HomeScreen | mitigate | `BreathingPulseCta._BreathingPulseCtaState.dispose()` calls `_controller.dispose()` (RESEARCH.md Code Examples line 783-786). Verified by source grep for `dispose()` containing `_controller.dispose()`. |
| T-06-05-2 | Tampering | A future contributor adds an `onTap` to ChunkCard's MouseRegion-wrapped Stack and intercepts the Dismissible swipe | mitigate | MouseRegion has no tap semantics by construction (PATTERNS.md cross-cutting landmines: "Don't add InkWell to ChunkCard to get onHover — would intercept Dismissible's drag gesture"). Plan 06 adds `test/screens/chunk_card_hover_test.dart` exercising the touch-drag invariant. |
| T-06-05-3 | Information Disclosure | Mood index out of range crashes the app revealing stack trace | accept | UI never offers anything but 1-5 (5 discrete tap targets); `ThemeNotifier.moodSeeds[mood]!` would throw on bad index but only via a code bug, not via untrusted input. Local-only app; stack trace privacy is N/A. |
| T-06-05-4 | DoS — Reduced Motion respect | If `disableAnimations` is true but the AnimationController still repeats, accessibility users see unwanted motion | mitigate | UI-SPEC §Breathing Pulse mandates `WidgetsBinding.instance.platformDispatcher.accessibilityFeatures.disableAnimations` check; if true, set `_controller.value = 0.5` (midpoint static render) instead of calling `.repeat()`. Verified by source grep for `disableAnimations`. Plan 06 adds widget test asserting controller is NOT animating when MediaQuery.disableAnimations is true. |
| T-06-05-5 | Tampering | Hover-click on a resolved (completed/skipped) chunk fires markComplete/markSkipped again, double-logging the completion | mitigate | The IconButton.onPressed in _HoverableChunkContent is guarded by `_hovered && !widget.chunk.isCompleted && !widget.chunk.isSkipped`; ScheduleNotifier.markComplete/markSkipped are also idempotent (early-return if already in target state — schedule_notifier.dart:73, 98). Double-defense. |

No new HIGH-severity threats. The mood-tap → setMoodSeed → Hive write path was threat-modeled in Plan 02 (T-06-02-1, accepted).
</threat_model>

<verification>
- `flutter analyze` 0 errors in all edited files (gates filter to specific paths to avoid swallowing failures elsewhere)
- `flutter test` passes the existing suite (quarterly_review_test.dart, service tests, repository test)
- All UI-SPEC-locked literals present in source per the per-task acceptance criteria
- Two duplicate `_moodColors` maps removed (home_screen + checkin_screen)
- BreathingPulseCta is a public top-level widget (no leading underscore) — testable in isolation by Plan 06
- Manual smoke (deferred to Plan 07): on `flutter run -d macos`, hovering a ChunkCard reveals checkbox + skip; hovering a GoalCard reveals edit + archive; hovering a commitment row reveals edit + delete; the HomeScreen CTA pulses subtly when no mood is set; tapping a mood at check-in warms the theme over 500ms; the drag handle is visible at 0.6 opacity on desktop and hidden on mobile.
</verification>

<success_criteria>
- All 7 files modified per the per-task instructions (Note: swipeable_chunk_card.dart is intentionally NOT modified — B-1 Option A wiring puts the ScheduleNotifier calls inside _HoverableChunkContent directly, eliminating the need for callback forwarding)
- AC-2 (hover affordances on ChunkCard, GoalCard, commitment row) functionally complete — hover-click on ChunkCard's checkbox/skip is FUNCTIONAL (calls ScheduleNotifier directly), not just visually present
- D-08 (breathing pulse on pre-check-in HomeScreen CTA) functionally complete — `BreathingPulseCta` is a public widget for Plan 06 isolation testing
- AC-6 mood-tap path (mood index → ThemeNotifier.setMoodSeed → 500ms theme warming via Plan 04's themeAnimationDuration) functionally complete
- Two duplicate `_moodColors` maps removed; ThemeNotifier.moodSeeds is the single source of truth
- Mobile delete affordance on commitments preserved via platform-gated branch (cross-cutting landmine resolved)
- Plan 06 can now write tests for: chunk_card hover, goal_card hover, goal_card drag handle visibility, BreathingPulseCta breathing pulse (in isolation, no HomeScreen pumping required)
</success_criteria>

<output>
After completion, create `.planning/phases/06-desktop-and-web-polish/06-05-SUMMARY.md` listing:
- 7 files modified (chunk_card, goal_card, goals_screen, commitments_screen, home_screen, checkin_screen, adjustments_section)
- Confirmation that swipeable_chunk_card.dart was NOT modified (B-1 Option A — ChunkCard reads ScheduleNotifier directly via context.read inside _HoverableChunkContent)
- Confirmation that BreathingPulseCta was extracted as a public top-level widget (W-3 — testable in isolation)
- Confirmation that `_moodColors` duplicates are removed (count: 2 maps deleted)
- Note that Plan 06 tests verify the affordances and the breathing pulse behavior
</output>
</content>
</invoke>