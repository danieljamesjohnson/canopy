---
phase: 06-desktop-and-web-polish
plan: 07
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05, 06]
files_modified:
  - .planning/ROADMAP.md
  - .planning/phases/06-desktop-and-web-polish/06-VALIDATION.md
autonomous: false
requirements: [AC-1, AC-2, AC-3, AC-4, AC-5, AC-6]
threat_refs: []

must_haves:
  truths:
    - "ROADMAP.md §Phase 6 deliverables list includes the mood theming deliverable text from CONTEXT.md <deferred>"
    - "ROADMAP.md §Phase 6 has 6 acceptance criteria (1-5 original + new AC-6 for mood theming)"
    - "VALIDATION.md has nyquist_compliant: true in frontmatter"
    - "Manual UAT checklist (AC-3 macOS resize, AC-4 web URL bar, AC-6 visual timing) has been walked and approved"
  artifacts:
    - path: .planning/ROADMAP.md
      provides: "Updated Phase 6 deliverables + AC-6"
      contains: "Tapping each of the 5 moods at check-in changes the app-wide ColorScheme within 600ms"
    - path: .planning/phases/06-desktop-and-web-polish/06-VALIDATION.md
      provides: "nyquist_compliant: true frontmatter + manual UAT signoff"
      contains: "nyquist_compliant: true"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/06-desktop-and-web-polish/06-CONTEXT.md"
      via: "deferred ROADMAP action text matches CONTEXT.md <deferred> suggested addition"
      pattern: "Full app mood theming"
---

<objective>
Finalize Phase 6 by applying the ROADMAP action carried over from CONTEXT.md `<deferred>` (add mood theming to the Phase 6 deliverables list + add AC-6), updating VALIDATION.md frontmatter, and walking through the manual UAT checklist for the three acceptance criteria that cannot be automated.

Purpose: CONTEXT.md `<deferred>` explicitly notes: "Update `.planning/ROADMAP.md` §Phase 6 to add mood theming to the deliverables list ... This action belongs to the planner — do not silently write it into ROADMAP here. Surface in confirm_creation." Plan 07 is that surface — it asks the user to approve the ROADMAP update and then applies it. It also runs the manual UAT for AC-3 (macOS resize), AC-4 (Web URL bar), and AC-6 (visual timing of mood warming transition).

Output: ROADMAP.md and VALIDATION.md updated. Manual UAT checkpoint completed. Phase 6 is ready for `/gsd-verify-work`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-desktop-and-web-polish/06-CONTEXT.md
@.planning/phases/06-desktop-and-web-polish/06-RESEARCH.md
@.planning/phases/06-desktop-and-web-polish/06-VALIDATION.md
@.planning/phases/06-desktop-and-web-polish/06-06-SUMMARY.md

<interfaces>
From CONTEXT.md `<deferred>` section:
- Suggested ROADMAP additions deliverable text (verbatim):
  > Full app mood theming (deferred from Phase 3): ColorScheme.fromSeed swap driven by today's mood with time-of-day brightness/saturation modulation; pre-check-in "curious" theme with breathing pulse on the check-in CTA; ~400–600ms warming transition into the chosen mood on tap. Palette: #4A6275 / #5C7A8A / #4A8C7A / #7AAF6A / #E8C547 (locked Phase 3).
- Suggested AC-6 (verbatim):
  > Tapping each of the 5 moods at check-in changes the app-wide ColorScheme within 600ms; all existing widget tests pass (with a test fixture that pins a known mood).

From VALIDATION.md frontmatter:
- Current: `nyquist_compliant: false`, `wave_0_complete: false`
- After Plan 07: `nyquist_compliant: true`, `wave_0_complete: true`

From ROADMAP.md §Phase 6 (current state, lines 264-296):
- Current deliverables list (8 bullets) needs the mood theming addition
- Current acceptance criteria (5 numbered items) needs AC-6 added
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual UAT — three macOS/Web visual checks</name>
  <files>(none — manual checkpoint, no files modified)</files>
  <read_first>
    - .planning/phases/06-desktop-and-web-polish/06-VALIDATION.md §Manual-Only Verifications (5 rows defining the exact procedure for each manual check)
    - .planning/phases/06-desktop-and-web-polish/06-UI-SPEC.md §Window Minimums (D-11 locked 480x640) + §Breathing Pulse (D-08 locked 2400ms easeInOut) + §Mood Warming Transition (D-09 locked 500ms easeOutCubic) + §Contrast Floors (mood 5 amber risk)
    - .planning/phases/06-desktop-and-web-polish/06-CONTEXT.md (D-08, D-09 specifications)
  </read_first>
  <action>
    PAUSE for human verification of three manual acceptance criteria. The executor must NOT proceed to Task 2 until the user has walked the three UAT checks below and confirmed PASS for each. The executor displays this checklist verbatim to the user and waits for the resume signal.

    Procedure displayed to the user:

    **AC-3 — macOS window resize minimum (VALIDATION.md row "Window resize refuses below 480px on macOS"):**
    1. Run `flutter run -d macos` from the repo root.
    2. Wait for the app window to open at default size.
    3. Grab any window edge (left, right, top, bottom, or corner) and drag inward to try to make the window narrower than 480 logical pixels wide.
    4. Expected: The window snaps back, refuses to resize below 480px width, OR the cursor stops at 480px. Confirm visually.
    5. Also try to make it shorter than 640 logical pixels tall — same behaviour expected.
    Report PASS / FAIL.

    **AC-4 — Web URL bar deep-link navigation (VALIDATION.md rows "Direct URL /schedule, /goals, /review loads correct screen on Web"):**
    1. Run `flutter run -d chrome` from the repo root.
    2. Wait for the app to load in Chrome.
    3. In the browser URL bar, type `<base_url>/schedule` and press Enter. Verify the Schedule screen loads (empty state acceptable).
    4. Repeat for `<base_url>/goals` — Goals screen loads.
    5. Repeat for `<base_url>/review` — Quarterly Review screen loads (likely "Not enough data yet").
    6. Verify the redirect-to-onboarding still works with `onboardingComplete = false` (use a private window or clear local browser storage), then complete onboarding and re-test the three URLs.
    Report PASS / FAIL.

    **AC-6 — Mood tap warming transition visual timing (VALIDATION.md row "Mood tap → ColorScheme change visible within 600ms"):**
    1. Run `flutter run -d macos` (or chrome). Ensure no mood is set for today (clear local storage or wait for daily rollover).
    2. Verify HomeScreen CTA "Start your day" pulses subtly (shadow expanding/contracting on ~2.4 sec cycle).
    3. Tap "Start your day" to navigate to check-in.
    4. Tap each of the 5 mood emoji in sequence (mood 1 stormy → mood 5 sunny). Between each tap, observe the AppBar tint, surfaces, and primary color shift.
    5. Expected per AC-6: The color change completes within 600ms (UI-SPEC locks 500ms easeOutCubic). It should feel like a smooth warming "settle," not a snap or a flash.
    6. Specifically verify mood 5 (#E8C547 amber) does NOT cause illegible text on AppBar titles or buttons.
    Report PASS / FAIL with notes.

    **Light-mode-only scope confirmation (W-5 — single-line developer check, not a separate AC):**
    - Phase 6 maintains the existing light-mode-only theme model. There is no `darkTheme` slot on `MaterialApp.router`; dark theme support is deferred to a future phase. Confirm with the user that this scope is acceptable for Phase 6 signoff. (If the user wants dark theme in this phase, halt and raise a CONTEXT.md amendment before continuing.)
    Report ACK / OBJECT.

    Once all three checks PASS and the light-mode-only scope is ACK'd, type "approved" to proceed.

    Report any failures with specifics. If any check FAILS, halt and create a follow-up gap-closure plan referencing the failing AC; do not proceed to Task 2.
  </action>
  <verify>
    Manual verification — proceed only after user types "approved" or equivalent confirmation that all three UAT rows PASS. If FAIL is reported on any row, stop and capture the failure in `.planning/phases/06-desktop-and-web-polish/06-07-UAT-FAILURE.md` for the gap-closure planner.
  </verify>
  <done>
    User has explicitly approved all three UAT rows (AC-3 macOS resize, AC-4 Web URL bar, AC-6 mood warming transition timing). Approval is captured in the human resume signal — no automated artifact is required for this checkpoint.
  </done>
  <what-built>
    Plans 01-06 collectively deliver Phase 6's full surface: adaptive layout (Plan 04), hover affordances (Plan 05), breathing pulse (Plan 05), mood-driven theme (Plans 02 + 04 + 05), window minimum (Plans 01 + 03 + 04), and the automated test coverage (Plan 06). Three acceptance-criterion rows are MANUAL per VALIDATION.md §Manual-Only Verifications because they involve native window-manager interaction, browser URL bar typing, and visual perception of animation timing.
  </what-built>
  <how-to-verify>
    See the `<action>` block above — it contains the verbatim procedure for AC-3, AC-4, and AC-6 manual checks.
  </how-to-verify>
  <resume-signal>Type "approved" if all three manual checks pass, or describe any failures.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply ROADMAP update and finalize VALIDATION.md</name>
  <files>.planning/ROADMAP.md, .planning/phases/06-desktop-and-web-polish/06-VALIDATION.md</files>
  <read_first>
    - .planning/ROADMAP.md (current §Phase 6 lines 264-296)
    - .planning/phases/06-desktop-and-web-polish/06-CONTEXT.md `<deferred>` section (lines 122-140 — the exact text to add)
    - .planning/phases/06-desktop-and-web-polish/06-VALIDATION.md (current frontmatter)
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md` §Phase 6 (between line 264 "### Phase 6: Desktop and Web Polish" and line 296 ending the section). Apply the CONTEXT.md `<deferred>` action verbatim:

    1. In the deliverables list (currently 8 bullets at lines 273-281), append a new bullet at the end:
       ```
       - **Full app mood theming (deferred from Phase 3):** `ColorScheme.fromSeed` swap driven by today's mood with time-of-day brightness/saturation modulation; pre-check-in "curious" theme with breathing pulse on the check-in CTA; ~400–600ms warming transition into the chosen mood on tap. Palette: `#4A6275` / `#5C7A8A` / `#4A8C7A` / `#7AAF6A` / `#E8C547` (locked Phase 3).
       ```
    2. In the acceptance criteria (currently 5 numbered items at lines 290-295), append a 6th item:
       ```
       6. Tapping each of the 5 moods at check-in changes the app-wide ColorScheme within 600ms; all existing widget tests pass (with a test fixture that pins a known mood).
       ```
    3. Update the "Plans:" count under Phase 6 (currently "0/0 plans complete") to reflect the actual count: 7 plans total. Set `Plans: 0/7 plans complete` (will be updated by execute-phase as plans land).
    4. Append the seven plan filenames to the Phase 6 plan checklist:
       ```
       Plans:
       - [ ] 06-01-PLAN.md — Wave 0 preflight: window_manager dep, AppSettings schema bump to v3, test helpers
       - [ ] 06-02-PLAN.md — ThemeNotifier with HSL modulator and lifecycle ticker
       - [ ] 06-03-PLAN.md — Conditional window_setup trio (re-export, io, stub)
       - [ ] 06-04-PLAN.md — ResponsiveShell + main.dart wiring + router migration
       - [ ] 06-05-PLAN.md — Card hover affordances + breathing pulse + checkin mood wiring
       - [ ] 06-06-PLAN.md — Widget + unit tests (8 new files + quarterly_review migration)
       - [ ] 06-07-PLAN.md — Manual UAT + ROADMAP update + finalize VALIDATION
       ```

    Edit `.planning/phases/06-desktop-and-web-polish/06-VALIDATION.md`:
    1. In the frontmatter, change `nyquist_compliant: false` to `nyquist_compliant: true`.
    2. In the frontmatter, change `wave_0_complete: false` to `wave_0_complete: true`.
    3. At the bottom of the file under "**Approval:** pending", change to "**Approval:** approved YYYY-MM-DD" with today's date.
    4. Update the "Validation Sign-Off" checklist (lines 109-115) — check all 6 boxes:
       - [x] All tasks have `<automated>` verify or Wave 0 dependencies (populated by planner)
       - [x] Sampling continuity: no 3 consecutive tasks without automated verify
       - [x] Wave 0 covers all MISSING references (10 new files listed above)
       - [x] No watch-mode flags (Flutter test uses single-pass by default)
       - [x] Feedback latency < 10s for quick command, < 10s for full suite
       - [x] `nyquist_compliant: true` set in frontmatter
  </action>
  <verify>
    <automated>grep -q "Full app mood theming (deferred from Phase 3)" .planning/ROADMAP.md &amp;&amp; grep -q "Tapping each of the 5 moods at check-in changes the app-wide ColorScheme within 600ms" .planning/ROADMAP.md &amp;&amp; grep -q "06-01-PLAN.md" .planning/ROADMAP.md &amp;&amp; grep -q "06-07-PLAN.md" .planning/ROADMAP.md &amp;&amp; grep -q "nyquist_compliant: true" .planning/phases/06-desktop-and-web-polish/06-VALIDATION.md &amp;&amp; grep -q "wave_0_complete: true" .planning/phases/06-desktop-and-web-polish/06-VALIDATION.md &amp;&amp; flutter test 2>&amp;1 | tail -5 &amp;&amp; flutter analyze 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md`:
      - Contains the deliverable text "Full app mood theming (deferred from Phase 3)" with all 5 locked hex values
      - Contains AC-6 text "Tapping each of the 5 moods at check-in changes the app-wide ColorScheme within 600ms"
      - Lists all 7 Phase 6 plan filenames in the Plans checklist
      - Plans count updated from "0/0" to "0/7" (or correctly reflects the completed count)
    - `.planning/phases/06-desktop-and-web-polish/06-VALIDATION.md`:
      - Frontmatter has `nyquist_compliant: true`
      - Frontmatter has `wave_0_complete: true`
      - All 6 Validation Sign-Off checklist boxes checked
      - Approval line dated with today's date
    - `flutter test` exits 0 (suite remains green — no regression from doc-only edits)
    - `flutter analyze` exits 0
  </acceptance_criteria>
  <done>
    ROADMAP §Phase 6 reflects mood theming + AC-6 per CONTEXT.md `<deferred>` action. VALIDATION.md frontmatter shows nyquist_compliant + wave_0_complete true. Manual UAT signoff documented. Full test suite still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. This plan only modifies documentation files (`.planning/ROADMAP.md`, `.planning/phases/06-desktop-and-web-polish/06-VALIDATION.md`). No code changes, no new attack surface.

## STRIDE Threat Register

No new threats. Plan 07 is a documentation-only finalization plus manual UAT signoff.
</threat_model>

<verification>
- `flutter test` full suite exits 0 (no regression from Plans 01-06)
- `flutter analyze` exits 0
- Manual UAT signoff captured for AC-3, AC-4, AC-6 visual paths
- ROADMAP.md properly reflects the Phase 6 final scope including mood theming
- VALIDATION.md marked nyquist_compliant: true
</verification>

<success_criteria>
- ROADMAP.md updated with mood theming deliverable + AC-6 per CONTEXT.md <deferred> verbatim text
- VALIDATION.md finalized
- Phase 6 ready for `/gsd-verify-work`
- All 6 acceptance criteria (AC-1 through AC-6) are now backed by either automated tests (Plan 06) or manual UAT (Task 1 above)
</success_criteria>

<output>
After completion, create `.planning/phases/06-desktop-and-web-polish/06-07-SUMMARY.md` listing:
- ROADMAP.md update applied (deliverable bullet + AC-6 + 7 plan filenames + plan count)
- VALIDATION.md finalized with nyquist_compliant: true and wave_0_complete: true
- Manual UAT results (PASS for each of AC-3, AC-4, AC-6) with any noted observations
- Confirmation that `flutter test` and `flutter analyze` both exit 0
- Recommended next step: `/gsd-verify-work 06-desktop-and-web-polish`
</output>
