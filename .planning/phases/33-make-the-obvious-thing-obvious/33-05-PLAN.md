---
phase: 33-make-the-obvious-thing-obvious
plan: 05
type: execute
wave: 4
depends_on: ["33-01", "33-02", "33-03", "33-04"]
files_modified:
  - .planning/spikes/001-live-row-in-a-true-grid/tools/drive.cjs
  - .planning/phases/33-make-the-obvious-thing-obvious/shots/
  - .planning/phases/33-make-the-obvious-thing-obvious/33-UAT.md
autonomous: false
requirements: [OBVIOUS-01, OBVIOUS-02, OBVIOUS-03]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The phase ends in exactly ONE human UAT gate, on port 8143, serving the debug build per CLAUDE.md. Four items would otherwise mean four builds to judge; Phase 32 alone took four rounds."
    - "Trap #4 is ruled on explicitly rather than copied: no plan in this phase touched `schedule_generator.dart`, so a previously-generated day renders through the new code on load and ⟳ Re-check-in is NOT required. The reason is stated in the UAT itself."
    - "The unlabelled circle is the 2026-06-12 item and leads the UAT as Item 1. It has historically never been reached because it always sat behind items that fail first."
    - "Structural questions are agent-verified with `drive.cjs` before the gate and reported as already-answered. Only perceptual and touch judgments go to the human. Routing a browser-answerable question to a human is how Phase 32's G-32-05 produced three non-answers in a row."
    - "Every item is judgeable by looking, in one build, without a rebuild between items."
  artifacts:
    - ".planning/phases/33-make-the-obvious-thing-obvious/33-UAT.md"
    - ".planning/phases/33-make-the-obvious-thing-obvious/shots/*.png"
    - ".planning/spikes/001-live-row-in-a-true-grid/tools/drive.cjs — `--tap=<label>` option"
  key_links:
    - "`drive.cjs` today parks on the Today timeline only; it has `clickLabel` but no CLI surface for it, so reaching Goals and Restoratives requires a repeatable `--tap=<label>` option."
    - "Port 8143 currently has a python3 server on it (PID 2191804 at plan time). The stale-server trap has fired twice on this project — check the port before trusting what is on it."
    - "A name-grep on `main.dart.js` is a valid presence probe only for symbols dart2js preserves: string literals survive, `const double`s inline to literals and icons survive only as codepoints (Phase 32 pre-flight, STATE.md)."
---

<objective>
Look at the running app, answer every browser-answerable question before asking a human anything, and
hand the owner one build and one document containing only the judgments that genuinely need a thumb.

Purpose: Phase 32's gap closure was half-wrong until a screenshot showed the live row still sitting in
a hole that code review had missed twice, and its G-32-05 spent three rounds asking a human three
questions a browser could answer. This plan is the correction to both.

Output: a debug build served on 8143, screenshots and semantics dumps in `shots/`, and `33-UAT.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/33-make-the-obvious-thing-obvious/33-UI-SPEC.md
@.planning/phases/33-make-the-obvious-thing-obvious/33-01-SUMMARY.md
@.planning/phases/33-make-the-obvious-thing-obvious/33-03-SUMMARY.md
@.planning/phases/33-make-the-obvious-thing-obvious/33-04-SUMMARY.md
</context>

<toolchain>
```
export PATH="/home/dan/.nvm/versions/node/v24.16.0/bin:/home/dan/development/flutter/bin:$PATH"
```
Playwright resolves from the global node install: prefix `drive.cjs` runs with
`NODE_PATH=$(npm root -g)`.
</toolchain>

<tasks>

<task type="auto">
  <name>Task 1: Green suite, honest build, clean port</name>
  <files>build/web/ (generated by `flutter build web`; not committed)</files>
  <read_first>
    - `CLAUDE.md` — the "Local hosting for UAT" section and all four traps
    - `tools/serve-uat.py` — the no-store static server that closes trap #3
    - `.planning/phases/33-make-the-obvious-thing-obvious/33-01-SUMMARY.md`,
      `33-02-SUMMARY.md`, `33-03-SUMMARY.md`, `33-04-SUMMARY.md`
  </read_first>
  <precondition>Port 8143 has a python3 server on it from a prior phase (PID 2191804 at plan time,
  possibly different now) — it must be identified and killed before this build is served, or the
  owner will judge the wrong bundle. This has happened twice on this project.</precondition>
  <action>
**A. Prove the tree is green before building anything.** Run `flutter analyze` (must be clean across
the repo) and `flutter test` (must be green; the count should be at or above 621 plus this phase's
new cases — 621 was the baseline at the end of Phase 32). Record the exact count in the SUMMARY. If
either fails, stop and fix before proceeding; a UAT on a red tree is not a UAT.

**B. Kill whatever is on 8143.** Do not assume the PID recorded at plan time is still current —
resolve it now:

```
ss -tlnp | grep ':8143'
```

and kill the owning process. Then confirm the port is free before serving. The stale-server trap has
fired twice on this project: in Phase 32 round one a two-day-old server was found squatting 8143 and
nearly had the wrong bundle judged, and the same server was *still* listening at the start of the
next round, serving the pre-fix bundle the owner had already failed.

**C. Build the debug bundle, exactly as CLAUDE.md specifies.**

```
flutter build web --debug --source-maps --pwa-strategy=none
```

Debug, not release: unminified Dart stack traces are the point at this stage, and load speed is
explicitly not the concern. `--pwa-strategy=none` means this build registers no service worker, so it
cannot create a trap #1 collision. Do **not** use `flutter run -d web-server` — its DDC output fans
into ~866 module files and never finishes loading over tailscale latency.

**D. Serve it.**

```
python3 tools/serve-uat.py 8143 --dir build/web
```

in the background. `serve-uat.py`, not `python3 -m http.server`: it sends `Cache-Control: no-store`
and strips `If-Modified-Since`/`If-None-Match`, which is what stops a cached `main.dart.js` from
being served against the new build (trap #3, which cost a round trip in Phase 25's UAT).

**E. Pre-flight the served bytes** — the trap #3 diagnosis, run proactively rather than after a
false failure:

```
curl -s http://danserver:8143/main.dart.js | grep -c 'Priority order'
curl -s http://danserver:8143/main.dart.js | grep -c 'To do'
curl -s http://danserver:8143/main.dart.js | grep -c 'Something that restores you'
curl -s http://danserver:8143/main.dart.js | grep -c 'Walk outside'
```

All four must be non-zero. **These are valid presence probes and the reason matters**: dart2js
preserves string literals that reach a `Text` widget, while it const-folds numeric constants to
literals and reduces icons to codepoints — which is why Phase 32's pre-flight greps for
`drag_indicator` and `kSubCompactGripSize` both returned zero on a bundle that definitely contained
them. Probe for strings, never for constant names or icon identifiers.

Also confirm no service worker is being served: `curl -s -o /dev/null -w '%{http_code}'
http://danserver:8143/flutter_service_worker.js` should not return 200.

Record the served-bundle sha (`sha256sum build/web/main.dart.js` compared against
`curl -s http://danserver:8143/main.dart.js | sha256sum`) so a later reader can tell whether the
pre-flight is still true without re-running it.
  </action>
  <verify>
    <automated>export PATH="/home/dan/.nvm/versions/node/v24.16.0/bin:/home/dan/development/flutter/bin:$PATH" && flutter analyze && flutter test 2>&1 | tail -5 && curl -s http://danserver:8143/main.dart.js | grep -c 'Priority order'</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` clean across the repo
    - `flutter test` green; the exact test count is recorded in the SUMMARY and is ≥ 621
    - `ss -tlnp | grep ':8143'` shows the new `serve-uat.py` process, not a pre-existing one
    - All four `curl | grep -c` probes return non-zero
    - `/flutter_service_worker.js` does not return 200
    - The on-disk and on-the-wire `main.dart.js` sha256 values match and are recorded
  </acceptance_criteria>
  <done>A debug build of the current green tree is serving on 8143, proven to be the bytes on disk,
  with no stale server and no service worker in the way.</done>
</task>

<task type="auto">
  <name>Task 2: Look at it — answer every browser-answerable question before asking a human</name>
  <files>.planning/spikes/001-live-row-in-a-true-grid/tools/drive.cjs, .planning/phases/33-make-the-obvious-thing-obvious/shots/</files>
  <read_first>
    - `.planning/spikes/001-live-row-in-a-true-grid/tools/drive.cjs` (all ~200 lines — `semantics`,
      `clickLabel`, `enableSemantics`, `onboardIfNeeded`, `checkInIfNeeded`, `dismissSplash`, and the
      option parsing at the top)
    - `.planning/phases/32-breaks-you-can-tap/shots/` — what a prior phase's evidence set looks like
  </read_first>
  <action>
**A. Give `drive.cjs` a `--tap=<label>` option.** It parks on the Today timeline and has a working
`clickLabel(page, label, {role})`, but no CLI surface for it, so Goals and Restoratives are currently
unreachable. Add a **repeatable** `--tap=<exact semantic label>` option, collected in order from
`process.argv`, and apply each tap in sequence after the `--at` / `--scroll` handling and before the
`--dump` / screenshot. Wait ~900ms between taps (the existing `clickLabel` already does). Log each
tap and whether it hit, to stderr, so a missed label is visible rather than silently producing a
screenshot of the wrong screen.

Keep the change additive — every existing invocation must behave identically with no `--tap`.

**B. Capture the evidence set** into
`.planning/phases/33-make-the-obvious-thing-obvious/shots/`, using one persistent profile dir under
the scratch directory so onboarding and check-in happen once and every capture sees the same day:

| File | Invocation | What it must show |
|---|---|---|
| `01-today-status-chips.png` | `--at=10:15 --dump` | unresolved rows carrying `To do`, a `Done` row, a `Skipped` row |
| `02-today-free-time.png` | `--at=07:30` | the leading free block rendered as a filled bordered card |
| `03-goals-priority-order.png` | `--tap=Goals --dump` | the `Priority order` heading, one list, rank numbers |
| `04-goals-progress-lines.png` | `--tap=Goals --scroll=200` | the left progress lines at their band colours |
| `05-restoratives-quick-pick.png` | `--tap=Goals --tap=What restores you --dump` | nine labelled chips |
| `06-add-fork.png` | `--tap=Goals --tap=Add goal --dump` | both fork doors and both consequence lines |

If a semantic label does not match (for example the overflow menu needs opening before
`What restores you` is reachable), add the intermediate `--tap` rather than giving up and routing the
question to the human — that is the exact failure this task exists to prevent.

**C. Answer these six structural questions from the dumps, and write each answer with its evidence
into the SUMMARY.** Each is a question about the *kind* of thing, not a matter of taste, and each is
therefore the agent's to answer:

- **S1** — Does an unresolved work row expose the text `To do` in the semantics tree, and does no
  node on any chunk row carry an unlabelled circular status mark?
- **S2** — Does an unresolved work row still expose exactly one `Complete` and one `Skip`? (No second
  completion path was introduced — UI-SPEC item 4.)
- **S3** — Does the Goals screen expose `Priority order`, and expose neither `Your goals` nor
  `Drag to prioritize. Tap to edit.`?
- **S4** — Do rank numbers `1`..`N` appear, exactly once each, with no gaps?
- **S5** — Do all nine restorative names appear as chip labels?
- **S6** — Does tapping `Add goal` expose both door titles and both consequence lines, and expose no
  goal-form field (`Weekly hours`, the type picker) at that point?

**D. Then actually look at the screenshots.** Read each PNG. Phase 32's gap closure was half-wrong
until a screenshot showed the live row sitting in a hole that two code reviews had missed; a
rebuild-and-look found it on the first pass. Specifically check for the failure modes this phase's
own plans flagged:

- the free-time card **collapsed to label height** instead of filling its slot (33-01's trap);
- a progress line rendering **shorter at a higher percentage** than at a lower one (33-03's item 18
  backstop) — compare two cards of different heights on `04-goals-progress-lines.png`;
- the status chip pushing a goal title into ellipsis on a narrow row (item 6 backstop);
- rank numbers shuffling between `03-` and `04-` captures (the `sortOrder` tie-break).

If any of those is visible, **stop and fix it before writing the UAT** rather than letting the owner
find it. Record what you looked for and what you saw, including the negatives.

**E. Note what this task cannot answer.** Whether `To do` reads as a status rather than a button,
whether a filled free-time card reads as "yours" rather than as clutter, whether the red/amber/green
scale is legible at a glance with no key, and whether the extra fork tap is worth it — none of these
are structural. They go to the human, and only they do.
  </action>
  <verify>
    <automated>ls .planning/phases/33-make-the-obvious-thing-obvious/shots/*.png | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `drive.cjs` accepts a repeatable `--tap=<label>` and behaves identically when none is passed
    - Six PNGs exist in `.planning/phases/33-make-the-obvious-thing-obvious/shots/`
    - Each of S1–S6 has a recorded answer in the SUMMARY with the semantics-tree evidence quoted
    - The SUMMARY records that each screenshot was read, and names the four failure modes checked for
      and what was seen for each
    - Any defect found by looking is fixed and the suite re-run green before Task 3 begins
  </acceptance_criteria>
  <done>Every question a browser can answer is answered, with evidence, and the screenshots have been
  looked at rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 3: Write the UAT document</name>
  <files>.planning/phases/33-make-the-obvious-thing-obvious/33-UAT.md</files>
  <read_first>
    - `.planning/phases/32-breaks-you-can-tap/32-UAT-R2.md` — the shape that finally worked: the
      never-reached items lead, and agent-verified items are recorded as such rather than flattened
      into the count
    - `CLAUDE.md` — trap #4, to be ruled on rather than copied
  </read_first>
  <action>
Write `.planning/phases/33-make-the-obvious-thing-obvious/33-UAT.md`, for a human with a phone.

**Header, before any item:**

- **URL:** `http://danserver:8143/` — debug build, served with `serve-uat.py`. State the served-bundle
  sha and that the pre-flight was run, so it does not need re-running.
- **Trap #4 ruling, stated with its reason rather than copied.** Write, in the UAT's own words: *no
  plan in this phase modified `lib/services/schedule_generator.dart` or any part of the scheduling
  engine — every change is rendering, a list screen, or an entry form — so a previously-generated day
  renders through the new code on load and **⟳ Re-check-in is NOT required**. If a future round of
  this phase comes to touch the generator, the rule returns and Step 0 becomes mandatory.* Confirm
  against `git diff --name-only` across the phase's commits before writing this; if the generator is
  in that list, invert the ruling and make Re-check-in mandatory Step 0 instead.
- **Already answered, so you don't have to** — the six structural findings S1–S6 from Task 2, each
  one line with its screenshot filename. Say plainly that these were driven in a browser rather than
  asked, because Phase 32 spent three rounds asking a human three questions a browser could answer.

**Items, in this order.** Item 1 is the unlabelled circle, because it is the 2026-06-12 complaint,
because it has never been reached in any prior round, and because ordering is the only thing that
fixed the same problem in Phase 32 round two.

1. **The unlabelled circle is gone (OBVIOUS-01).** Open Today and look at an unfinished work row.
   Where a bare circle used to sit there is now the word `To do`. *Structural half already
   confirmed:* the circle is gone and `To do` is present in the semantics tree (S1). **Your call:**
   does the row now read as *unfinished* without inviting a tap? Is `To do` worth saying, or is it
   three more characters competing with the goal name — the question sketch 003 asked and left to the
   real build.
2. **Free time is a filled card (Today).** Look at the free stretch at the top of the day and any
   mid-day gap. It is now a filled card like a break, not a dashed outline. **Your call:** does the
   day read better, and does a free stretch now read as *yours* rather than as absence?
3. **The Goals screen says what it is (OBVIOUS-02).** Open Goals. One list, headed `Priority order`,
   numbered 1..N across every type, with type as a chip on each card. *Already confirmed:* the
   heading, the single list, and the unbroken rank sequence (S3, S4). **Your call:** does this answer
   *"I don't know about this goals page"*? And is losing the type grouping — all your Regular time
   goals together — a real loss in daily use?
4. **The colour means something now (OBVIOUS-02).** The line on the left of each card is this week's
   progress, filling bottom-up: red under 20%, amber 20–69%, green 70% and up. There is no key, on
   purpose. **Your call:** can you read it at a glance without one? And does a green low-ranked goal
   next to an amber top-ranked one actually prompt you to re-rank, which is the whole argument for
   the line existing?
5. **Adding a restorative is a tap (OBVIOUS-03).** Goals → ⋮ → *What restores you*. Nine chips.
   **Tap five in a row**, then check the list underneath reflects them and that tapping one again
   removes it. **Your call:** are any of the nine wrong or missing? They are a hard-coded list, so
   changing them is free.
6. **Energizing is no longer forced to be a goal (OBVIOUS-03).** Tap **Add goal** on the Goals
   screen. You are asked which kind first, before any form. *Already confirmed:* both doors and both
   consequence lines are present, and the restorative door never shows a goal form (S6, plus a test
   asserting no `Goal` is created). **Your call:** is the extra tap on every add worth it — sketch
   005 recorded that cost as accepted, and this is where it gets tested for real? And do you *believe*
   the promise that a restorative is never scheduled and never counted?

**Optional, skip if short on time:** mood 5's yellow seed was flagged in sketch 004 as the stress
case for the progress line. The band colours are deliberately hard-coded rather than theme-derived
precisely so they cannot collapse into a yellow seed — so this is a check on that reasoning, not on
the implementation. It costs a ⟳ Re-check-in at mood 5, which is safe here for the same reason
Re-check-in is not required above.

**Format for answers.** Per item: PASS / FAIL / PARTIAL plus one sentence. A count that seems to work
is not a count — if an item asks for a number (item 5's five taps), the number is the answer.
  </action>
  <verify>
    <automated>grep -c 'Re-check-in is NOT required' .planning/phases/33-make-the-obvious-thing-obvious/33-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `33-UAT.md` exists and leads with the unlabelled circle as Item 1
    - It carries the trap #4 ruling with its reason rather than a copied rule, and that ruling was
      confirmed against `git diff --name-only` across the phase's commits
    - All six items are present, in the specified order, and each is judgeable in one build with no
      rebuild between items
    - Every structural finding S1–S6 is reported as already answered, with its screenshot named
    - No item asks the human a question the semantics tree already answered
  </acceptance_criteria>
  <done>`33-UAT.md` is written, ordered so the 2026-06-12 item is first, and contains only judgments
  that need a human.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: The one human gate</name>
  <what-built>
Phase 33's six legibility changes, all live in one debug build already serving on
`http://danserver:8143/` (built with `flutter build web --debug --source-maps --pwa-strategy=none`,
served by `tools/serve-uat.py`, port cleared of its prior squatter, served bytes proven equal to the
built bytes by sha):

1. A chunk row now says `To do` / `Done` / `Skipped` instead of showing an unlabelled circle.
2. Free time renders as a filled card matching the break vocabulary.
3. Goals is one list headed `Priority order`, ranked 1..N across every type.
4. Each goal card's left line is this week's real progress, red / amber / green, with no key.
5. The restoratives screen offers nine one-tap chips.
6. Tapping *Add goal* asks which kind first, and the restorative door never shows a goal form.

Six screenshots and the semantics dumps behind findings S1–S6 are in
`.planning/phases/33-make-the-obvious-thing-obvious/shots/`.
  </what-built>
  <how-to-verify>
Open `http://danserver:8143/` on a phone and work through
`.planning/phases/33-make-the-obvious-thing-obvious/33-UAT.md`, which lists the six items in order
and states which parts are already confirmed.

**⟳ Re-check-in is NOT required.** No plan in this phase modified `lib/services/schedule_generator.dart`
or any part of the scheduling engine — every change is rendering, a list screen, or an entry form —
so a previously-generated day renders through the new code on load. (Trap #4 binds only a UAT that
judges scheduling-engine output.)

1. **Today, an unfinished work row** — the circle is gone and the word `To do` is there (already
   confirmed structurally). Does the row read as *unfinished* without inviting a tap?
2. **Today, free time** — filled card, not a dashed outline. Does the day read better?
3. **Goals** — one list headed `Priority order`, numbered, type as a chip. Does this answer "I don't
   know about this goals page"? Is losing the type grouping a real loss?
4. **Goals, the left line** — red under 20%, amber 20–69%, green 70%+, no key on purpose. Can you
   read it at a glance? Does a green low-ranked goal beside an amber top-ranked one prompt a re-rank?
5. **Goals → ⋮ → What restores you** — **tap five chips in a row**, then check the list below
   reflects them and that tapping one again removes it. Are any of the nine wrong or missing?
6. **Goals → Add goal** — you are asked which kind first. Is the extra tap on every add worth it? Do
   you believe the promise that a restorative is never scheduled and never counted?
  </how-to-verify>
  <resume-signal>
Reply with PASS / FAIL / PARTIAL per item plus one sentence each. Item 5 asks for a number — give the
count, not an impression ("it seems to work" is what cost Phase 32 three rounds). Type "approved" only
if all six pass.
  </resume-signal>
</task>

</tasks>

<checkpoint>
**Verify:** open `http://danserver:8143/` on a phone and work through `33-UAT.md`'s six items.

**Expected:** an unfinished work row says `To do` where a bare circle used to be; free time renders
as a filled card; Goals is one list headed `Priority order` with rank numbers and a coloured progress
line on the left of each card; the restoratives screen offers nine one-tap chips; tapping *Add goal*
asks which kind first.

**Report:** PASS / FAIL / PARTIAL per item, with one sentence each. Item 5 asks for a count — give
the number, not an impression.

This gate is blocking. Phases 27, 29, 31 and 32 each had a green suite contradicted by this owner's
thumb, twice in 31. `flutter test` cannot tell whether a human understands what a control is.
</checkpoint>

<deferred_and_recorded>
**The Phase 32 LOW review finding is deliberately NOT taken in this phase.**
`live_row_card.dart:395-401`'s `BreakSkippedIndicator` branch is unreachable because the only call
site passes `showActions: isBreak ? !chunk.isSkipped : true`. It was cheap to take *if a plan already
touched that file* — and no plan in this set does. `live_row_card.dart` appears in no plan's
`files_modified`, so taking it would grow the diff on its own, which the phase's own scoping
forbids. It stays open, and the SUMMARY must say so rather than leaving a reader to wonder whether it
was forgotten.
</deferred_and_recorded>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tailnet → served build | `serve-uat.py` binds `0.0.0.0` and is reachable by anything on the tailnet and LAN |
| Browser profile → app data | `drive.cjs` writes `localStorage['flutter.dev_clock_offset_micros']` to simulate a clock |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-15 | Information Disclosure | `serve-uat.py` on 0.0.0.0:8143 | low | accept | The host is reachable only over Tailscale and the local LAN with no public exposure (global operating guide); the served artifact is a debug build of a public repo containing no secrets. Binding to all interfaces is required for the owner's phone to reach it |
| T-33-16 | Spoofing | stale server on 8143 | high | mitigate | A prior phase's server serving a pre-fix bundle would have the owner judge the wrong code — this has happened twice on this project. Task 1 resolves and kills the port owner, then proves the served bytes match the on-disk build by sha |
| T-33-17 | Tampering | `DevClock` offset written from `drive.cjs` | low | accept | The write targets a dedicated agent-owned Chromium profile directory, not the owner's browser; the offset is a debug-only affordance that already ships |
| T-33-18 | Tampering | package supply chain | n/a | accept | No packages installed. `drive.cjs` uses the already-present global `playwright`; `NODE_PATH=$(npm root -g)` resolves it rather than installing |
</threat_model>

<verification>
- `flutter analyze` clean and `flutter test` green, both recorded with counts.
- Six screenshots exist and have been read.
- S1–S6 answered with quoted semantics-tree evidence.
- Served bytes proven equal to built bytes by sha.
- A human verdict recorded on all six UAT items.
</verification>

<success_criteria>
The owner opens one URL on his phone, reads a document that tells him what has already been proven and
asks him only what a browser could not, judges six items without a rebuild between them, and the
2026-06-12 circle is the first thing he is asked about rather than the last thing never reached.
</success_criteria>

<output>
Create `.planning/phases/33-make-the-obvious-thing-obvious/33-05-SUMMARY.md` when done, and
`.planning/phases/33-make-the-obvious-thing-obvious/33-UAT.md` as the gate document.
</output>
