---
phase: 33-make-the-obvious-thing-obvious
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/screens/schedule/widgets/chunk_card.dart
  - lib/screens/today/widgets/free_time_row.dart
  - test/screens/today_row_widgets_test.dart
  - test/screens/chunk_card_status_chip_test.dart
autonomous: true
requirements: [OBVIOUS-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`_buildTrailingStatus()` in `chunk_card.dart` renders a labelled chip in all three states: `To do` (unresolved), `Done` (completed), `Skipped` (skipped). One vocabulary, three words. (UI-SPEC item 1)"
    - "The unchecked-radio icon no longer appears anywhere in `chunk_card.dart`. This is the 2026-06-12 complaint and the phase's headline requirement. (UI-SPEC item 2)"
    - "The chip is display-only — no `onTap`, no `InkWell`, no `IconButton`, nothing that reads as tappable. (UI-SPEC item 3)"
    - "No second way to complete a chunk is introduced. `_buildActionRow()`'s `Complete` and `Skip` remain the only completion affordances. (UI-SPEC item 4)"
    - "Chip styling follows the existing container-role convention and never uses the error slot — `Done` on `primaryContainer`, `To do` on `surfaceContainerHighest` with an `outlineVariant` border, `Skipped` transparent with a dashed `outlineVariant` border. (UI-SPEC item 5)"
    - "`FreeTimeRow` renders as a filled card matching the break vocabulary: a `surfaceContainer` fill with a 1dp `outlineVariant` border and the same 12dp radius, not a dashed outline. (UI-SPEC item 7)"
    - "Free-time copy is unchanged and not reworded — `Free until 8:00 AM` / `Free · 1h 40m`. (UI-SPEC item 8)"
    - "The file-private dashed-region painter becomes unused and is deleted rather than left dead. (UI-SPEC item 9)"
    - statement: "The chip must not push the title into ellipsis at the compact tier. The title keeps `Expanded` + `TextOverflow.ellipsis`; the chip is flex 0 0 auto. (UI-SPEC item 6)"
      verification: backstop
  artifacts:
    - "lib/screens/schedule/widgets/chunk_card.dart — `_StatusChip`, `_DashedChipBorderPainter`"
    - "lib/screens/today/widgets/free_time_row.dart — `Card`-based `FreeTimeRow`"
    - "test/screens/chunk_card_status_chip_test.dart"
  key_links:
    - "`_buildTrailingStatus` is called from two sites — the detailed/full title row (chunk_card.dart:694) and the compact tier (chunk_card.dart:750). The compact site's `if (isResolved)` guard is the line that must change for item 1 to hold at every tier."
    - "Three live tests in `today_row_widgets_test.dart`'s `ChunkCard row vocabulary` group assert the retired trailing status directly — line 455 asserts `Icons.radio_button_unchecked` itself. Task 1 and those tests cannot both survive; Task 3 B2 carries the disposition for each."
    - "`FreeTimeRow` is laid out inside `TimelineRowTile` under a `Positioned` with an explicit `height:` (today_screen.dart:697-732). A `Card` sizes to its child, so the row must still fill that allocated height rather than collapsing to label height."
---

<objective>
Make the Today timeline say its own state. Replace the unlabelled circle on every unresolved work
row with a labelled `To do` / `Done` / `Skipped` chip, and restore the Phase 22 match by making
free time a filled card like a break.

Purpose: OBVIOUS-01 — no unlabelled affordance on a chunk row. The circle has been on screen since
the owner complained about it on 2026-06-12; it survived 2.5 months because no phase ever aimed at
it. This plan aims at it first, deliberately.

Output: a file-private `_StatusChip` (+ its dashed-border painter) in `chunk_card.dart`, a
`Card`-based `FreeTimeRow`, and tests that pin both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-make-the-obvious-thing-obvious/33-UI-SPEC.md
@.planning/phases/33-make-the-obvious-thing-obvious/33-PATTERNS.md
@.planning/sketches/003-the-unlabelled-circle/README.md
@CLAUDE.md
</context>

<design_authority>
The design is settled. `.planning/sketches/003-the-unlabelled-circle/README.md` records the owner's
2026-09-01 verdict against a served mockup; `33-UI-SPEC.md` transcribes it. **If the UI-SPEC and the
sketch README disagree, the sketch README wins.** Do not reconsider variant A (delete the glyph) or
variant C (a real checkbox) — both were built, shown, and rejected, and the reasons are recorded.

**Charter you must not "clean up":** `chunk_card.dart:810-815` and `free_time_row.dart:78-81` both
state in code that these chips and painters are *deliberately* file-private duplicates of their
`goal_card.dart` counterparts. Do NOT extract a shared chip widget or a shared painter. The new
`_StatusChip` and `_DashedChipBorderPainter` are file-private to `chunk_card.dart` by design.
</design_authority>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-level list, recorded once here in the first plan. Every new symbol, file and widget Phase 33
creates across all five plans.

**New files (source)**

| File | Plan |
|---|---|
| `lib/services/weekly_progress_service.dart` | 33-02 |
| `lib/screens/goals/widgets/add_kind_fork.dart` | 33-04 |

**New files (test)**

| File | Plan |
|---|---|
| `test/screens/chunk_card_status_chip_test.dart` | 33-01 |
| `test/services/weekly_progress_test.dart` | 33-02 |
| `test/screens/goal_card_progress_line_test.dart` | 33-03 |
| `test/screens/goals_screen_ranked_list_test.dart` | 33-03 |
| `test/screens/restoratives_quick_pick_test.dart` | 33-04 |
| `test/screens/goals_add_fork_test.dart` | 33-04 |

**New files (planning)**

| File | Plan |
|---|---|
| `.planning/seeds/SEED-006-week-start-carries-time-of-day.md` | 33-02 |
| `.planning/phases/33-make-the-obvious-thing-obvious/33-UAT.md` | 33-05 |
| `.planning/phases/33-make-the-obvious-thing-obvious/shots/01-today-status-chips.png` … `06-add-fork.png` | 33-05 |

**New symbols and widgets**

| Symbol | Kind | Where | Plan |
|---|---|---|---|
| `_StatusChip` | file-private `StatelessWidget` | `chunk_card.dart` | 33-01 |
| `_DashedChipBorderPainter` | file-private `CustomPainter` | `chunk_card.dart` | 33-01 |
| `WeeklyProgressService` | public pure-Dart class | `weekly_progress_service.dart` | 33-02 |
| `WeeklyProgressService.weekStart` | static method | " | 33-02 |
| `completedChunksThisWeek` / `completedDaysThisWeek` / `completedHoursThisWeek` / `weekFractionFor` | instance methods | " | 33-02 |
| `kGoalProgressTrackHeight` | public `const double` (40.0) | `goal_card.dart` | 33-03 |
| `_kProgressRed` / `_kProgressAmber` / `_kProgressGreen` | file-private `const Color` | `goal_card.dart` | 33-03 |
| `_ProgressLine` | file-private `StatelessWidget` | `goal_card.dart` | 33-03 |
| `_TypeChip` | file-private `StatelessWidget` | `goal_card.dart` | 33-03 |
| `GoalCard.rank` / `GoalCard.weekProgress` | optional constructor params | `goal_card.dart` | 33-03 |
| `GoalsScreen.completionLogRepository` | optional constructor param | `goals_screen.dart` | 33-03 |
| `_buildReorderableList` | private method (replaces `_buildReorderableSection`) | `goals_screen.dart` | 33-03 |
| `kCommonRestoratives` | public `const List<(String, String)>` | `restoratives_screen.dart` | 33-04 |
| `_QuickPickSection` | file-private `StatelessWidget` | `restoratives_screen.dart` | 33-04 |
| `AddKind` | public `enum { goal, restorative }` | `add_kind_fork.dart` | 33-04 |
| `showAddKindFork` | public function | `add_kind_fork.dart` | 33-04 |
| `showRestorativeQuickAdd` | public function | `add_kind_fork.dart` | 33-04 |
| `--tap=<label>` | CLI option | `drive.cjs` | 33-05 |

**Symbols this phase deletes**

| Symbol | Where | Plan |
|---|---|---|
| `_DashedRegionPainter` | `free_time_row.dart` | 33-01 |
| `_PriorityChip` | `goal_card.dart` only — **not** `chunk_card.dart`'s | 33-03 |
| `_buildSectionHeader` / `_buildFullOrderedIds` | `goals_screen.dart` | 33-03 |
| `test/screens/goal_card_priority_chip_test.dart` | retired, reason recorded in place | 33-03 |
| `test/screens/goal_card_priority_chip_rebuild_test.dart` | retired, reason recorded in place | 33-03 |

**No new Hive type, no migration, no `pubspec.yaml` change, and `lib/services/schedule_generator.dart`
is not modified by any plan.**
</artifacts_this_phase_produces>

<toolchain>
Every shell command below assumes:

```
export PATH="/home/dan/.nvm/versions/node/v24.16.0/bin:/home/dan/development/flutter/bin:$PATH"
```

Baseline entering this phase: `flutter test` 621/621 green, `flutter analyze` clean. A plan that
leaves the suite red is not done.
</toolchain>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The chunk row names its own state</name>
  <files>lib/screens/schedule/widgets/chunk_card.dart</files>
  <read_first>
    - `lib/screens/schedule/widgets/chunk_card.dart` lines 620-830 (the `_WorkChunkContent` content
      builders, `_buildTrailingStatus`, `_buildActionRow`, and the `_ValenceChip` precedent)
    - `.planning/phases/33-make-the-obvious-thing-obvious/33-PATTERNS.md` §1 — the verbatim
      `_ValenceBadge` shape and the measured padding/radius/icon-size/text-style table
    - `lib/screens/goals/widgets/goal_card.dart` lines 246-298 (`_ValenceBadge`, the canonical
      labelled display-only container-role chip)
  </read_first>
  <behavior>
    - An unresolved work chunk renders the text `To do` in its trailing slot, at every density tier
      that calls the trailing status (compact, detailed, full).
    - A completed chunk renders `Done`.
    - A skipped chunk renders `Skipped` (capitalised — the retired lowercase `skipped` string is
      replaced, not kept alongside).
    - No `InkWell`, `GestureDetector`, `IconButton` or `onTap` exists anywhere inside the chip.
    - The `Complete` and `Skip` buttons still render on an unresolved chunk at the tiers that
      already showed them, and their count is unchanged (exactly one of each).
    - At the compact tier a long goal title still ellipsises rather than overflowing, with the chip
      at its natural width beside it.
  </behavior>
  <action>
Add a file-private `_StatusChip` to `chunk_card.dart`, placed immediately before the existing
`_ValenceChip` class so the file's chip family stays together:

```
class _StatusChip extends StatelessWidget {
  const _StatusChip({required this.isCompleted, required this.isSkipped});
  final bool isCompleted;
  final bool isSkipped;
  ...
}
```

Three branches, resolved in this order — completed, then skipped, then the unresolved default —
mirroring the ternary being replaced so the state precedence cannot drift:

- **completed** → label `Done`, fill `colorScheme.primaryContainer`, text/icon
  `colorScheme.onPrimaryContainer`, icon `Icons.check`, no border.
- **skipped** → label `Skipped`, transparent fill, text/icon `colorScheme.onSurfaceVariant`,
  icon `Icons.remove`, dashed `colorScheme.outlineVariant` border.
- **unresolved** → label `To do`, fill `colorScheme.surfaceContainerHighest`, text/icon
  `colorScheme.onSurfaceVariant`, icon `Icons.schedule`, solid 1dp `colorScheme.outlineVariant`
  border via `Border.all`.

**Never the error slot** (UI-SPEC item 5): `colorScheme.error` is reserved for the destructive Skip
*button* at `chunk_card.dart:800-801` and must not appear in this chip.

Copy the geometry verbatim from `_ValenceChip` in the same file so the two chips sit at the same
visual weight: `padding: EdgeInsets.symmetric(horizontal: 8, vertical: 4)`,
`BorderRadius.circular(8)`, `Icon(..., size: 12)`, `SizedBox(width: 4)`,
`labelSmall.copyWith(fontWeight: FontWeight.w600)`, and a `Row(mainAxisSize: MainAxisSize.min)`.
Each chip is glyph **plus word** — never a bare glyph (UI-SPEC item 30 is the same rule as item 2).

For the skipped branch's dashed border, add a second file-private
`_DashedChipBorderPainter` to `chunk_card.dart` and wrap the chip's `Container` in a `CustomPaint`.
Take the dash rhythm from the painter Task 2 retires — `strokeWidth = 1`, dash `2.0`, gap `2.0`,
walked with `path.computeMetrics()` so the rhythm stays even around the corners — with a
`Radius.circular(8)` to match the chip. Document in its doc comment that it is a deliberate
file-private duplicate carrying the retired free-time painter's rhythm forward, per the
duplication charter this file already states at line 810.

Then rewrite `_buildTrailingStatus(ThemeData theme)` to a single expression returning
`_StatusChip(isCompleted: chunk.isCompleted, isSkipped: chunk.isSkipped)`, deleting the three-way
ternary — the `Icons.check_circle` branch, the lowercase `skipped` `Text`, and the unresolved
branch's unchecked-radio `Icon`. Update its doc comment (currently lines 755-757) to describe the
chip rather than the icons; leaving the old comment in place would have it contradict the code.

At the compact call site (`chunk_card.dart:750`) delete the `if (isResolved)` guard so all three
states render a chip, per UI-SPEC item 1:

```
        const SizedBox(width: 8),
        _buildTrailingStatus(theme),
```

Update the `_buildCompactContent` doc comment (lines 726-731) — its stated rationale ("dropping it
for an unresolved chunk removes an empty icon slot") is exactly what this phase overturns, so
rewrite it rather than leaving it asserting a retired rule.

**Do not touch `_buildActionRow`.** No new completion affordance is added; variant C was rejected
for exactly that (sketch 003 README).

**Do not touch the break tiers** (`chunk_card.dart:143-370`) or `kPixelsPerMinute`. The break
vocabulary and the 64×30dp Skip rail are settled (D-32-03) and out of scope.
  </action>
  <verify>
    <automated>export PATH="/home/dan/.nvm/versions/node/v24.16.0/bin:/home/dan/development/flutter/bin:$PATH" && flutter analyze lib/screens/schedule/widgets/chunk_card.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' lib/screens/schedule/widgets/chunk_card.dart | grep -c 'radio_button_unchecked'` returns `0`
    - `grep -c "_StatusChip" lib/screens/schedule/widgets/chunk_card.dart` returns at least `3`
      (class declaration, constructor, call site)
    - `grep -v '^\s*//' lib/screens/schedule/widgets/chunk_card.dart | grep -c "'To do'"` returns `1`
    - `grep -v '^\s*//' lib/screens/schedule/widgets/chunk_card.dart | grep -c "'Done'"` returns `1`
    - `grep -v '^\s*//' lib/screens/schedule/widgets/chunk_card.dart | grep -c "'Skipped'"` returns `1`
    - `awk '/^class _StatusChip/,/^}/' lib/screens/schedule/widgets/chunk_card.dart | grep -cE 'InkWell|GestureDetector|IconButton|onTap'` returns `0`
    - `grep -v '^\s*//' lib/screens/schedule/widgets/chunk_card.dart | grep -c "label: const Text('Complete')"` returns `1` — still exactly one Complete affordance
    - `flutter analyze lib/screens/schedule/widgets/chunk_card.dart` is clean
  </acceptance_criteria>
  <done>Every density tier renders a labelled status chip in all three states; the unchecked-radio
  icon is gone from the file; no new tap target exists.</done>
</task>

<task type="auto">
  <name>Task 2: Free time is a filled card</name>
  <files>lib/screens/today/widgets/free_time_row.dart</files>
  <read_first>
    - `lib/screens/today/widgets/free_time_row.dart` (all 123 lines — class doc, both constructors,
      `_resolveLabel`, the `CustomPaint` build, and `_DashedRegionPainter`)
    - `lib/screens/schedule/widgets/chunk_card.dart` lines 165-235 — the compact-tier break `Card`
      whose `color` / `shape` / `clipBehavior` this row must match, and the infinite-height
      deviation note at lines 166-185
    - `lib/screens/today/today_screen.dart` lines 690-735 — the two `Positioned(height: ...)` call
      sites that allocate this row's height
    - `lib/screens/today/widgets/timeline_row_tile.dart` lines 97-120 — the `Row(crossAxisAlignment:
      start)` wrapper the row is laid out inside
  </read_first>
  <action>
Replace `FreeTimeRow`'s `CustomPaint` treatment with the break row's `Card`, so free time and a
break share one visual vocabulary again (UI-SPEC item 7; sketch 003's "Free time: FILLED").

Keep the outer `Container(margin: const EdgeInsets.symmetric(vertical: 4), width: double.infinity)`
— its existing comment says the margin exists so a free region and a break line up on the timeline,
and that is still true. Inside it, swap `CustomPaint(painter: ...)` for a `Card` copied from the
break's compact tier:

```
      child: Card(
        margin: EdgeInsets.zero,
        color: theme.colorScheme.surfaceContainer,
        clipBehavior: Clip.antiAlias,
        shape: RoundedRectangleBorder(
          borderRadius: BorderRadius.circular(12),
          side: BorderSide(color: theme.colorScheme.outlineVariant),
        ),
        child: Center(child: Text(...)),
      ),
```

Note the radius moves 8 → 12: the retired painter used `_radius = 8.0`, the break card uses 12, and
matching the break vocabulary means matching 12.

`color: theme.colorScheme.surfaceContainer` is deliberate. UI-SPEC item 7 says "a `surface` fill",
but the break card's actual role token is `surfaceContainer` (`chunk_card.dart:196`), and the whole
point of the item is that the two match. `33-PATTERNS.md` §2 records this reconciliation; follow the
break card.

**Keep the `Center` child.** It is what makes the card fill the height the parent allocated: both
call sites wrap this row in `Positioned(height: geometry.heightFor(...))`, and
`TimelineRowTile`'s `Row(crossAxisAlignment: start)` hands the child *loose* height constraints, so
an `Align`/`Center` expands to the incoming maximum while a bare `Text` would not. This is the
inverse of the trap `chunk_card.dart:166-185` documents — a `Card` sizes to its child, so the child
must be the thing that fills. Task 3 pins this with a measured height assertion; do not replace
`Center` with a `Padding` or a bare `Text`.

**Do not touch `_resolveLabel()` or either constructor.** The strings `Free until ${...}` and
`Free · ${...}` are locked copy per the file's own comment and `sketches/MANIFEST.md` ("Free time is
named"). Only the treatment is open (UI-SPEC item 8).

Delete `_DashedRegionPainter` outright, and remove the now-dead `CustomPaint` import path if
`package:flutter/material.dart` no longer covers anything else the file used. This codebase deletes
retired mechanism rather than leaving it unreferenced (`33-PATTERNS.md` "Retiring a mechanism").

Rewrite the class doc (lines 5-27). Two of its claims become false: "Renders no [Card] — just a
quiet label behind a dotted left rule" and "It now draws the same dashed outline a break row uses".
Keep the D-05 LOCKED-copy statement and the two-named-constructors rationale; replace the dashed
paragraphs with a short note that Phase 32 made breaks filled cards and Phase 33 restored the match
(sketch 003, 2026-09-01), and that an outline reads as absence where a fill reads as "this is
yours".
  </action>
  <verify>
    <automated>export PATH="/home/dan/.nvm/versions/node/v24.16.0/bin:/home/dan/development/flutter/bin:$PATH" && flutter analyze lib/screens/today/widgets/free_time_row.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' lib/screens/today/widgets/free_time_row.dart | grep -c '_DashedRegionPainter'` returns `0`
    - `grep -v '^\s*//' lib/screens/today/widgets/free_time_row.dart | grep -c 'CustomPaint'` returns `0`
    - `grep -c 'Card(' lib/screens/today/widgets/free_time_row.dart` returns `1`
    - `grep -c "surfaceContainer" lib/screens/today/widgets/free_time_row.dart` returns at least `1`
    - `grep -c "BorderRadius.circular(12)" lib/screens/today/widgets/free_time_row.dart` returns `1`
    - `grep -c "Free until \${formatMinutes(_untilMinutes)}" lib/screens/today/widgets/free_time_row.dart` returns `1` — locked copy intact
    - `grep -c "Free · \${formatDurationShort(_durationMinutes!)}" lib/screens/today/widgets/free_time_row.dart` returns `1` — locked copy intact
    - `flutter analyze lib/screens/today/widgets/free_time_row.dart` is clean
  </acceptance_criteria>
  <done>Free time renders a bordered filled card at the break's fill/radius/border tokens, with the
  locked copy unchanged and the dashed painter deleted.</done>
</task>

<task type="auto">
  <name>Task 3: Pin both changes, and repoint the four tests whose premise changed</name>
  <files>test/screens/chunk_card_status_chip_test.dart, test/screens/today_row_widgets_test.dart</files>
  <read_first>
    - `test/screens/today_row_widgets_test.dart` lines 205-235 (the `FreeTimeRow (D-05, LOCKED copy)`
      group) and lines 735-765 (the codebase's precedent for retiring a test with a recorded reason
      left in place)
    - `test/test_helpers/mood_pump.dart` — `pumpWithMood`, which every widget test in this area uses
    - `test/screens/chunk_card_priority_badge_test.dart` lines 1-15 — the `_stubWorkChunk` factory
      shape to copy
  </read_first>
  <action>
**A. New file `test/screens/chunk_card_status_chip_test.dart`.** Open it with a comment naming the
phase and requirement (`Phase 33 (OBVIOUS-01, UI-SPEC items 1-6, sketch 003 variant B)`), following
this codebase's convention. Declare a file-local `_stubWorkChunk({bool completed, bool skipped})`
top-level factory modelled on `chunk_card_priority_badge_test.dart:7-12`. Assert, via
`pumpWithMood`:

1. An unresolved chunk at `ChunkCardDensity.compact` renders `find.text('To do')` once.
2. An unresolved chunk at `ChunkCardDensity.full` renders `find.text('To do')` once.
3. A completed chunk renders `find.text('Done')` and no `To do`.
4. A skipped chunk renders `find.text('Skipped')` and no `To do`.
5. **The chip is not tappable**: `find.descendant(of: find.byType(<the trailing chip's Container>),
   matching: find.byType(InkWell))` finds nothing. Prefer asserting on the widget subtree reachable
   from `find.text('To do')` — walk up with `find.ancestor(of: find.text('To do'), matching:
   find.byType(InkWell))` and expect `findsNothing`.
6. **No second completion affordance**: an unresolved chunk at `full` renders exactly one
   `find.text('Complete')` and one `find.text('Skip')`.
7. **UI-SPEC item 6 backstop**: pump a compact-tier chunk with a deliberately long title inside a
   narrow `SizedBox(width: 220)` and assert `find.text('To do')` is still found and the title `Text`
   still carries `overflow: TextOverflow.ellipsis` — i.e. the chip took its natural width and the
   title yielded, not the reverse. Assert the rendered chip width is under 80 logical pixels via
   `tester.getSize`, so a chip that grew to eat the row would fail.

Remember the harness caveat in STATE.md: `flutter test`'s placeholder font inflates glyph widths, so
any *text-driven* width bound is a harness bound, not a device requirement. Keep the 80px bound as a
generous ceiling and note that in a comment rather than tightening it.

**B. Repoint the inverted free-time test in `test/screens/today_row_widgets_test.dart`.** The test
named `'neither form renders a Card'` (line 223) asserts the exact mechanism UI-SPEC item 7 retires.
Its premise inverted rather than its expected value, so replace it with `'both forms render a Card'`
asserting `find.byType(Card)` is `findsOneWidget` for each constructor, and leave a short comment
above it recording that it was inverted by Phase 33 / sketch 003 and why — matching the in-place
recorded-reason precedent at lines 740-763 of the same file.

**Keep the two locked-copy tests at lines 211-221 untouched.** They still pass and they are the
guard on item 8.

**B2. Repoint the three live tests that currently demand the retired trailing status.** This is the
same file, group `ChunkCard row vocabulary (D-06, D-07, P10)` (opens at line 426). **Task 1 cannot
delete the unchecked-radio icon and leave this group green — one of the two has to move, and it is
the tests.** Each of the four entries below has a stated disposition; do not discover them one at a
time from a red run.

| Line | Current name | Disposition |
|---|---|---|
| 427 | `'completed work chunk: struck through + primary check icon'` | **Repoint** → rename to `'completed work chunk: struck through + Done chip'`. Keep the `TextDecoration.lineThrough` assertion on the title (unchanged — strikethrough lives in the content builder, not the trailing status). Replace `tester.widget<Icon>(find.byIcon(Icons.check_circle))` and its `colorScheme.primary` check with `expect(find.text('Done'), findsOneWidget)`. Reason: the completed branch is now a labelled chip whose glyph is `Icons.check`, so the old finder cannot match and the colour assertion is on the wrong element. |
| 439 | `'skipped work chunk: struck through + literal "skipped" text'` | **Repoint** → rename to `'skipped work chunk: struck through + Skipped chip'`. Keep the strikethrough assertion and keep `expect(find.byIcon(Icons.arrow_forward), findsNothing)` (an unrelated legacy guard). Change `find.text('skipped')` to `find.text('Skipped')`. Reason: UI-SPEC item 1 replaces the lowercase word with the capitalised chip label — expected value AND casing both change, and the test name quotes the retired string. |
| 449 | `'unresolved work chunk: no strikethrough, unchecked icon, Complete/Skip present'` | **Repoint** → rename to `'unresolved work chunk: no strikethrough, To do chip, Complete/Skip present'`. Keep the `isNot(TextDecoration.lineThrough)` assertion and **keep both** `find.widgetWithText(FilledButton, 'Complete')` and `find.widgetWithText(OutlinedButton, 'Skip')` — they are the standing guard on UI-SPEC item 4. Replace `expect(find.byIcon(Icons.radio_button_unchecked), findsOneWidget)` with `expect(find.text('To do'), findsOneWidget)`. Reason: this line asserts the exact icon the phase exists to delete; it is the single most important repoint in the plan. |
| 615 | `'no hardcoded Colors literal reaches the widget tree'` | **Untouched.** It pumps `_workChunk(completed: true)` but asserts only `find.byType(ChunkCard)` renders, so the trailing-status change cannot affect it. Named here so its presence in the group is not mistaken for an oversight. |

**Explicitly out of scope in this same group — do not touch:** lines 461 / 474 / 481 (`showStartTime`
and swipe forwarding), 507 / 525 / 544 / 566 (break tiers and `BreakSkippedIndicator`), and 599
(commitment chunk colours). None reads the trailing status. `test/screens/chunk_card_priority_badge_test.dart`
is likewise out of scope, per Task 1.

**C. Add the non-collapse height test** to the same `FreeTimeRow` group — **through the production
constraint shape, which is the whole point of the test.**

```
      await pumpWithMood(
        tester,
        const SizedBox(
          height: 240,
          child: TimelineRowTile(child: FreeTimeRow.gap(durationMinutes: 40)),
        ),
      );
      expect(tester.getSize(find.byType(Card)).height, 232.0);
```

232 = 240 minus the row's 4dp top and 4dp bottom margin.

**A bare `SizedBox(height: 240, child: FreeTimeRow.gap(...))` harness is forbidden here, and the
reason must be written into a comment above the test so nobody re-simplifies it later.** A `SizedBox`
imposes a **tight** height; `Container` deflates it by its own margins and passes tight 232 straight
down, so the `Card` would measure 232 whether its child is a `Center` or a bare `Text` — the test
would pass over the exact defect it exists to catch, and the RED observation demanded below would be
unobtainable. That is this project's fifth-time failure mode verbatim (STATE.md: "two defects behind
green tests, both assertions that could not fail").

The production path hands this row **loose** height constraints, and only a loose harness can
distinguish a filling child from a collapsing one. The chain, verified:
`today_screen.dart:697` `Positioned(height: geometry.heightFor(...))` → `TimelineRowTile` →
`timeline_row_tile.dart:106-115` `Row(crossAxisAlignment: CrossAxisAlignment.start)` →
`Expanded(child: child)`. `CrossAxisAlignment.start` is what makes the child's height loose. Using
`TimelineRowTile` itself as the harness means the test cannot drift from the production shape; a
hand-rolled `Row(crossAxisAlignment: start)` + `Expanded` is acceptable only if `TimelineRowTile`
cannot be used, and carries the same comment.

**Observe RED before accepting the test:** temporarily replace the card's `Center` with a bare `Text`
and watch the measured height collapse from 232 to roughly the label's line height. Record the
observed number in the SUMMARY, then restore `Center`. STATE.md's carry-forward invariants require a
proven-RED regression test; a green run on an un-failable assertion is not evidence.

**Do not delete `test/screens/chunk_card_priority_badge_test.dart`.** It covers a different priority
badge on the chunk row and is out of scope for this phase.
  </action>
  <verify>
    <automated>export PATH="/home/dan/.nvm/versions/node/v24.16.0/bin:/home/dan/development/flutter/bin:$PATH" && flutter test test/screens/chunk_card_status_chip_test.dart test/screens/today_row_widgets_test.dart test/screens/chunk_card_priority_badge_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `test/screens/chunk_card_status_chip_test.dart` exists and its 7 assertions pass
    - `grep -c "neither form renders a Card" test/screens/today_row_widgets_test.dart` returns `0`
    - `grep -c "both forms render a Card" test/screens/today_row_widgets_test.dart` returns `1`
    - `grep -v '^\s*//' test/screens/today_row_widgets_test.dart | grep -c 'radio_button_unchecked'`
      returns `0` — the repoint at line 449 landed
    - `grep -v '^\s*//' test/screens/today_row_widgets_test.dart | grep -c "find.text('skipped')"`
      returns `0` — the lowercase string is gone from the work-chunk test
    - `grep -c "'To do'" test/screens/today_row_widgets_test.dart` returns at least `1`
    - `grep -c 'TimelineRowTile' test/screens/today_row_widgets_test.dart` returns at least `2` — the
      non-collapse test uses the loose-constraint harness, not a bare `SizedBox`
    - `test/screens/chunk_card_priority_badge_test.dart` still exists and passes
    - Lines 461/474/481/507/525/544/566/599 of `today_row_widgets_test.dart` are unchanged in
      `git diff` — the out-of-scope tests were not swept up
    - The non-collapse test was observed RED against a bare-`Text` card child before being accepted;
      the SUMMARY records the observed collapsed height
    - `flutter test` — full suite green, count at or above 621
    - `flutter analyze` clean across the repo
  </acceptance_criteria>
  <done>The suite is green at 621 or more; the status chip and the filled free-time card are both
  pinned by tests that were observed able to fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hive → UI | Persisted `ScheduledChunk` state (`isCompleted` / `isSkipped`) crosses into the render path |
| User → UI | Taps on the chunk row's action affordances |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-01 | Spoofing | `_StatusChip` | low | mitigate | The chip is display-only with no gesture recognizer; Task 1's acceptance criteria negative-grep `InkWell`/`GestureDetector`/`IconButton`/`onTap` inside the class body so it cannot masquerade as a control (UI-SPEC item 3) |
| T-33-02 | Elevation of Privilege | chunk completion path | medium | mitigate | No new mutation path is introduced; `markComplete`/`markSkipped` remain reachable only from `_buildActionRow`, pinned by the "exactly one Complete" criterion |
| T-33-03 | Tampering | package supply chain | n/a | accept | This plan installs no packages — no `flutter pub add`, no new dependency in `pubspec.yaml`. The package-legitimacy gate does not apply |
</threat_model>

<verification>
- `flutter analyze` clean across the repo.
- `flutter test` green, count ≥ 621.
- The three greps that prove the headline requirement: no unchecked-radio icon in `chunk_card.dart`,
  exactly one `'To do'`, exactly one `'Complete'` label.
- The free-time card's measured height equals its allocated slot minus margin.
</verification>

<success_criteria>
An unresolved work row on the Today timeline shows the words `To do` where a bare circle used to be;
a completed row shows `Done`; a skipped row shows `Skipped`; free time renders as a filled bordered
card at the same fill and radius as a break. Nothing on the row is tappable that was not tappable
before.
</success_criteria>

<output>
Create `.planning/phases/33-make-the-obvious-thing-obvious/33-01-SUMMARY.md` when done.
</output>
