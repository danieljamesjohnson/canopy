---
phase: 05-quarterly-review
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - lib/screens/quarterly_review/quarterly_review_screen.dart
  - lib/screens/quarterly_review/sections/data_section.dart
  - lib/screens/quarterly_review/sections/reflection_section.dart
  - lib/screens/quarterly_review/sections/adjustments_section.dart
  - lib/screens/quarterly_review/widgets/donut_chart.dart
  - lib/screens/quarterly_review/widgets/bar_chart_weekly.dart
  - lib/screens/quarterly_review/widgets/reflection_question_card.dart
  - lib/screens/quarterly_review/widgets/goal_adjustment_tile.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "Section 1 shows a hero stat with total completed chunks as a big number"
    - "Section 1 shows a donut chart with per-goal proportions and a 'Time not spent' slice"
    - "Section 1 shows a bar chart with completed chunks per week over the quarter"
    - "Section 2 presents 5 reflection questions one per screen with tap-to-pick answers"
    - "Section 2 suggestion chips are data-driven from the user's actual goal data"
    - "Section 3 shows all goals in a drag-to-reorder list for priority adjustment"
    - "Section 3 shows inline archive suggestions for goals with <=20% completion rate"
    - "Completing Section 3 persists a QuarterlySnapshot and updates goal priorities"
  artifacts:
    - path: "lib/screens/quarterly_review/quarterly_review_screen.dart"
      provides: "Three-section PageView review flow replacing the stub"
      min_lines: 80
    - path: "lib/screens/quarterly_review/sections/data_section.dart"
      provides: "Hero stat, donut chart, bar chart, top-3 goals"
      contains: "DataSection"
    - path: "lib/screens/quarterly_review/sections/reflection_section.dart"
      provides: "PageView of 5 reflection question cards"
      contains: "ReflectionSection"
    - path: "lib/screens/quarterly_review/sections/adjustments_section.dart"
      provides: "ReorderableListView with archive prompts and finish button"
      contains: "AdjustmentsSection"
    - path: "lib/screens/quarterly_review/widgets/donut_chart.dart"
      provides: "PieChart wrapper with centerSpaceRadius"
      contains: "DonutChart"
    - path: "lib/screens/quarterly_review/widgets/bar_chart_weekly.dart"
      provides: "BarChart wrapper for weekly completed counts"
      contains: "WeeklyBarChart"
    - path: "lib/screens/quarterly_review/widgets/reflection_question_card.dart"
      provides: "Question card with suggestion chips and Other option"
      contains: "ReflectionQuestionCard"
    - path: "lib/screens/quarterly_review/widgets/goal_adjustment_tile.dart"
      provides: "ListTile with color bar, drag handle, archive prompt"
      contains: "GoalAdjustmentTile"
  key_links:
    - from: "lib/screens/quarterly_review/quarterly_review_screen.dart"
      to: "lib/services/quarterly_aggregation_service.dart"
      via: "Aggregation in initState/didChangeDependencies"
      pattern: "QuarterlyAggregationService"
    - from: "lib/screens/quarterly_review/sections/adjustments_section.dart"
      to: "lib/providers/goals_notifier.dart"
      via: "reorderAll and archiveGoal on finish"
      pattern: "reorderAll|archiveGoal"
    - from: "lib/screens/quarterly_review/sections/adjustments_section.dart"
      to: "lib/data/repositories/quarterly_snapshot_repository.dart"
      via: "Snapshot append on finish"
      pattern: "append"
---

<objective>
Build the complete quarterly review screen with all three sections: data visualization, guided reflection, and goal adjustments.

Purpose: This is the core user-facing feature of Phase 5 — the quarterly review flow that transforms raw CompletionLog data into actionable insights and lets users adjust their priorities for the next quarter.

Output: Fully functional QuarterlyReviewScreen with DataSection (hero stat, donut chart, bar chart), ReflectionSection (5 questions with tap-to-pick), and AdjustmentsSection (drag reorder, archive suggestions, snapshot persistence).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-quarterly-review/05-CONTEXT.md
@.planning/phases/05-quarterly-review/05-RESEARCH.md
@.planning/phases/05-quarterly-review/05-UI-SPEC.md
@.planning/phases/05-quarterly-review/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From lib/services/quarterly_aggregation_service.dart (created in Plan 01):
```dart
class QuarterlyAggregationService {
  Map<String, int> completedByGoal(List<CompletionLog> logs, String startYmd, String endYmd);
  Map<String, int> completedByWeek(List<CompletionLog> logs, String startYmd, String endYmd);
  int notSpentCount(List<CompletionLog> logs, String startYmd, String endYmd);
  int totalCompleted(List<CompletionLog> logs, String startYmd, String endYmd);
  bool isInReviewWindow({QuarterlySnapshot? latestSnapshot, List<CompletionLog> allLogs, DateTime? now});
  Map<String, double> completionRateByGoal(List<CompletionLog> logs, String startYmd, String endYmd);
}
```

From lib/data/models/quarterly_snapshot.dart (expanded in Plan 01):
```dart
@HiveType(typeId: 5)
class QuarterlySnapshot extends HiveObject {
  @HiveField(0) final String id;
  @HiveField(1) String periodStartYmd;
  @HiveField(2) String periodEndYmd;
  @HiveField(3) DateTime completedAt;
  @HiveField(4) Map<String, int> goalChunkTotals;
  @HiveField(5) List<String> reflectionAnswers;
  @HiveField(6) Map<String, int> goalPrioritySnapshot;
  @HiveField(7) List<String> archivedGoalIds;
}
```

From lib/providers/goals_notifier.dart (extended in Plan 01):
```dart
class GoalsNotifier extends ChangeNotifier {
  List<Goal> get goals;
  static const List<String> _colorPalette = ['#4CAF50', '#2196F3', ...];
  String autoColor();
  Future<void> archiveGoal(String id) async;
  Future<void> reorderAll(List<String> orderedGoalIds) async;
}
```

From lib/data/models/goal.dart:
```dart
enum GoalType { timeTarget, outcome, habit }
class Goal extends HiveObject {
  final String id;
  String name;
  int goalTypeIndex;
  String? color;
  double? priorityWeight;
  int sortOrder;
  GoalType get goalType => GoalType.values[goalTypeIndex];
}
```

From lib/screens/schedule/widgets/chunk_card.dart:
```dart
Color hexToColor(String hex) => Color(int.parse(hex.replaceFirst('#', '0xFF')));
```

From lib/screens/end_of_day/end_of_day_summary_screen.dart (hero stat pattern):
- Hero number: `theme.textTheme.titleLarge?.copyWith(fontWeight: FontWeight.bold, fontSize: 48)`
- Label: `theme.textTheme.bodyMedium?.copyWith(color: theme.colorScheme.onSurfaceVariant)`
- Per-goal row: `CircleAvatar(radius: 6, backgroundColor: hexToColor(colorHex))` + name + count

From lib/screens/onboarding/onboarding_screen.dart (PageView pattern):
- `PageController` + `PageView` with `NeverScrollableScrollPhysics()`
- `_nextPage()`: `_controller.nextPage(duration: Duration(milliseconds: 350), curve: Curves.easeInOut)`
- `_isSaving` bool to prevent double-tap on final button
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chart widgets and DataSection</name>
  <files>
    lib/screens/quarterly_review/widgets/donut_chart.dart,
    lib/screens/quarterly_review/widgets/bar_chart_weekly.dart,
    lib/screens/quarterly_review/sections/data_section.dart
  </files>
  <read_first>
    lib/screens/quarterly_review/quarterly_review_screen.dart,
    lib/screens/end_of_day/end_of_day_summary_screen.dart,
    lib/screens/schedule/widgets/chunk_card.dart,
    lib/data/models/goal.dart,
    lib/providers/goals_notifier.dart,
    .planning/phases/05-quarterly-review/05-UI-SPEC.md,
    .planning/phases/05-quarterly-review/05-RESEARCH.md
  </read_first>
  <action>
    Create the `lib/screens/quarterly_review/widgets/` and `lib/screens/quarterly_review/sections/` directories.

    **DonutChart widget** (`lib/screens/quarterly_review/widgets/donut_chart.dart`):
    - Stateless widget accepting `Map<String, int> goalChunkTotals`, `int notSpentCount`, `List<Goal> goals`
    - Wrap `PieChart` in a `SizedBox(height: 200)` (per UI-SPEC)
    - `centerSpaceRadius: 60`, `centerSpaceColor: Theme.of(context).colorScheme.surface` (per RESEARCH.md Pattern 2)
    - `sectionsSpace: 2`
    - One `PieChartSectionData` per goal: `value` = completed count, `color` = `hexToColor(goal.color!)` with fallback to `GoalsNotifier._colorPalette` index color (per RESEARCH.md Pitfall 3), `radius: 50`, `showTitle: false`
    - One "Time not spent" section: `value` = notSpentCount, `color` = `Theme.of(context).colorScheme.outlineVariant` (per D-02, UI-SPEC Color section), `showTitle: false`
    - No touch interaction — leave `pieTouchData` at default (per D-01)
    - Below the chart, render a Column of legend rows: each row has a `CircleAvatar(radius: 6, backgroundColor: goalColor)` + `Text(goalName)` + `Text('${pct.toStringAsFixed(0)}%')`. Include "Time not spent" row with `outlineVariant` color. Use `bodySmall` for legend text.
    - Import `hexToColor` from `'../../schedule/widgets/chunk_card.dart'` (per UI-SPEC: import, do not duplicate)

    **WeeklyBarChart widget** (`lib/screens/quarterly_review/widgets/bar_chart_weekly.dart`):
    - Stateless widget accepting `Map<String, int> weeklyData` (Monday ISO date key -> count)
    - Wrap `BarChart` in a `SizedBox(height: 180)` (per UI-SPEC)
    - Sort entries by date key ascending. Each entry maps to one `BarChartGroupData` with `x` = index, one `BarChartRodData` with `toY` = count, `color` = `Theme.of(context).colorScheme.primary` (per UI-SPEC accent role), `width: 14`, `borderRadius: BorderRadius.circular(4)`
    - Bottom titles: `SideTitles(showTitles: true)` with `getTitlesWidget` returning `Text(weekLabel, style: TextStyle(fontSize: 9))` — format week key as "Jan 6" using `intl` `DateFormat('MMM d')` (per RESEARCH.md Pattern 3)
    - Left titles: `SideTitles(showTitles: true, reservedSize: 28)`
    - Top/right titles: hidden. `gridData: const FlGridData(show: false)`, `borderData: FlBorderData(show: false)`

    **DataSection widget** (`lib/screens/quarterly_review/sections/data_section.dart`):
    - Stateless widget accepting all aggregated data + goals list + `VoidCallback onNext`
    - Layout (per UI-SPEC Screen Layout Contract):
      - `SingleChildScrollView` child of the section
      - `SizedBox(height: 32)` top padding
      - Hero stat: Center-aligned `Text('$totalCompleted', style: titleLarge.copyWith(fontWeight: FontWeight.bold, fontSize: 48))` (per D-03, UI-SPEC Typography)
      - Label: Center-aligned `Text('chunks completed this quarter', style: bodyMedium.copyWith(color: onSurfaceVariant))` (per UI-SPEC Copywriting)
      - `SizedBox(height: 24)` spacing
      - `DonutChart` widget
      - `SizedBox(height: 24)` spacing
      - `WeeklyBarChart` widget
      - `SizedBox(height: 24)` spacing
      - "Top 3 goals by time" section: `Text('By goal', style: titleMedium.copyWith(fontWeight: FontWeight.bold))` heading, then top 3 goals from goalChunkTotals sorted descending by count, each rendered as a `ListTile` with `CircleAvatar(radius: 6)` leading, goal name title, "N chunks" trailing (same pattern as end_of_day_summary_screen.dart)
      - Bottom: `Padding(padding: EdgeInsets.fromLTRB(16, 24, 16, 48))` with `ElevatedButton(onPressed: onNext, child: Text('Next: Reflect'))` (per UI-SPEC Copywriting)
    - All horizontal padding: 16px (per UI-SPEC spacing md token)

    Run `flutter analyze` after creating all three files.
  </action>
  <verify>
    <automated>flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - lib/screens/quarterly_review/widgets/donut_chart.dart exists and contains `class DonutChart`
    - lib/screens/quarterly_review/widgets/donut_chart.dart contains `PieChart` and `centerSpaceRadius: 60`
    - lib/screens/quarterly_review/widgets/donut_chart.dart contains import of `hexToColor` from chunk_card.dart
    - lib/screens/quarterly_review/widgets/donut_chart.dart contains `outlineVariant` for "Time not spent" slice
    - lib/screens/quarterly_review/widgets/bar_chart_weekly.dart exists and contains `class WeeklyBarChart`
    - lib/screens/quarterly_review/widgets/bar_chart_weekly.dart contains `BarChart` and `fontSize: 9`
    - lib/screens/quarterly_review/sections/data_section.dart exists and contains `class DataSection`
    - lib/screens/quarterly_review/sections/data_section.dart contains `fontSize: 48` for hero stat
    - lib/screens/quarterly_review/sections/data_section.dart contains text `chunks completed this quarter`
    - lib/screens/quarterly_review/sections/data_section.dart contains text `Next: Reflect`
    - `flutter analyze` exits 0
  </acceptance_criteria>
  <done>DataSection renders hero stat, donut chart with goal colors and "Time not spent" slice, bar chart with weekly data, top 3 goals, and "Next: Reflect" button. All per UI-SPEC and CONTEXT.md decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Create ReflectionSection, AdjustmentsSection, and wire QuarterlyReviewScreen</name>
  <files>
    lib/screens/quarterly_review/widgets/reflection_question_card.dart,
    lib/screens/quarterly_review/widgets/goal_adjustment_tile.dart,
    lib/screens/quarterly_review/sections/reflection_section.dart,
    lib/screens/quarterly_review/sections/adjustments_section.dart,
    lib/screens/quarterly_review/quarterly_review_screen.dart
  </files>
  <read_first>
    lib/screens/quarterly_review/sections/data_section.dart,
    lib/screens/onboarding/onboarding_screen.dart,
    lib/screens/goals/goals_screen.dart,
    lib/providers/goals_notifier.dart,
    lib/data/repositories/hive_quarterly_snapshot_repository.dart,
    lib/data/repositories/hive_completion_log_repository.dart,
    lib/data/models/quarterly_snapshot.dart,
    lib/services/quarterly_aggregation_service.dart,
    .planning/phases/05-quarterly-review/05-UI-SPEC.md,
    .planning/phases/05-quarterly-review/05-CONTEXT.md
  </read_first>
  <action>
    **ReflectionQuestionCard widget** (`lib/screens/quarterly_review/widgets/reflection_question_card.dart`):
    - Stateful widget accepting: `String question`, `List<String> suggestedAnswers`, `ValueChanged<String> onAnswered`
    - Layout (per UI-SPEC Component Inventory):
      - Card background: `colorScheme.surfaceContainerHighest` (per UI-SPEC Color secondary role)
      - Question text: `headlineSmall` (per UI-SPEC Typography)
      - Below question: Row/Wrap of tappable `ActionChip` widgets for each suggested answer. On tap, call `onAnswered(answer)`.
      - Below chips: `TextButton(child: Text('Other...'))` that toggles an inline `TextField`. On submit (keyboard or "Done" button), call `onAnswered(textFieldValue)`. (Per D-06)
      - Padding: 16px horizontal, 24px vertical

    **GoalAdjustmentTile widget** (`lib/screens/quarterly_review/widgets/goal_adjustment_tile.dart`):
    - Stateless widget accepting: `Goal goal`, `Color goalColor`, `double? completionRate`, `bool showArchivePrompt`, `VoidCallback? onArchive`, `VoidCallback? onKeep`, `int index`
    - Layout (per UI-SPEC Component Inventory):
      - Row with 4-6dp left color bar (Container with width: 4, color: goalColor), goal name, drag handle icon (`Icons.drag_handle`, 48dp touch target)
      - If `showArchivePrompt` is true: below the main row, render archive prompt with `AnimatedSwitcher(duration: Duration(milliseconds: 200), switchInCurve: Curves.easeIn)` (per UI-SPEC Animation Contract)
      - Archive prompt text: "This one rarely made it in — archive it?" (per UI-SPEC Copywriting)
      - Two inline `TextButton`s: "Archive" (`colorScheme.error` text color) and "Keep" (per UI-SPEC Interaction Contracts)
      - Wrap in `ReorderableDelayedDragStartListener(index: index, child: ...)` for drag (per GoalsScreen pattern)

    **ReflectionSection** (`lib/screens/quarterly_review/sections/reflection_section.dart`):
    - Stateful widget accepting: `List<Goal> goals`, `Map<String, int> goalChunkTotals`, `Map<String, double> completionRates`, `ValueChanged<List<String>> onComplete`
    - Internal state: `PageController`, `List<String> _answers = []`, `int _currentQuestion = 0`
    - 5 fixed questions (per D-08, UI-SPEC Interaction Contracts):
      1. "Which goal gave you the most energy this quarter?" — suggestions: top 3 goals by completion rate + "Other"
      2. "What are you most proud of from the last 90 days?" — suggestions: top 3 goals by absolute chunks completed + "Other"
      3. "Which goal felt harder to show up for?" — suggestions: bottom 3 goals by completion rate (only those with >=1 scheduled chunk) + "Other"
      4. "Is there a goal you want to give more focus next quarter?" — suggestions: all active goal names + "Other"
      5. "Anything you want to let go of for now?" — suggestions: goals with <=20% completion rate + "Nothing -- keep them all" + "Other"
    - Section heading: "A few questions" (per UI-SPEC Copywriting) with step dots below (per UI-SPEC: reuse `_StepDots` pattern from onboarding — `AnimatedContainer` with `width` change, 200ms, active dot uses `colorScheme.primary`)
    - `PageView` with `NeverScrollableScrollPhysics` containing 5 `ReflectionQuestionCard` widgets
    - On answer: record to `_answers`, call `_controller.nextPage(duration: Duration(milliseconds: 300), curve: Curves.easeInOut)`. After last answer, call `onComplete(_answers)` (per UI-SPEC: auto-advance to Section 3)

    **AdjustmentsSection** (`lib/screens/quarterly_review/sections/adjustments_section.dart`):
    - Stateful widget accepting: `List<Goal> goals`, `Map<String, double> completionRates`, `List<String> reflectionAnswers`, `String periodStartYmd`, `String periodEndYmd`, `Map<String, int> goalChunkTotals`
    - Internal state: `List<Goal> _orderedGoals` (copy of goals for reorder), `Set<String> _archivedIds`, `bool _isSaving`
    - Heading: "Set your priorities for next quarter" (per UI-SPEC Copywriting)
    - Body text: "Drag to reorder. Your schedule will reflect this tomorrow." (per UI-SPEC Copywriting)
    - `ReorderableListView.builder` with `GoalAdjustmentTile` per goal. Show archive prompt for goals with `completionRates[goalId] <= 0.20` (per D-10, 20% threshold). Remove goal from list on "Archive" tap (calls `GoalsNotifier.archiveGoal`). "Keep" dismisses the prompt.
    - "Finish review" `ElevatedButton` at bottom (per UI-SPEC Copywriting):
      - On tap: set `_isSaving = true`, button shows "Saving..." text
      - Call `context.read<GoalsNotifier>().reorderAll(_orderedGoals.map((g) => g.id).toList())` (per D-11)
      - Create `QuarterlySnapshot` with all fields populated: `periodStartYmd`, `periodEndYmd`, `goalChunkTotals` from aggregation, `reflectionAnswers` from Section 2, `goalPrioritySnapshot` = Map of goalId -> index from reordered list, `archivedGoalIds` = list of archived goal IDs
      - Call `HiveQuarterlySnapshotRepository().append(snapshot)` (append-only, never overwrite per RESEARCH.md anti-patterns)
      - Pop route via `context.pop()`
      - On error: show inline error message per UI-SPEC Copywriting: "Something went wrong saving your review. Your answers are not lost -- try again." with "Retry" button

    **QuarterlyReviewScreen** (`lib/screens/quarterly_review/quarterly_review_screen.dart`):
    Replace the stub entirely. StatefulWidget with:
    - `initState`: load data via `HiveCompletionLogRepository().getAll()`, `HiveQuarterlySnapshotRepository().getLatest()`, `context.read<GoalsNotifier>().goals`. Run `QuarterlyAggregationService` methods to produce all aggregated data. Determine period: if latestSnapshot exists, start = latestSnapshot.periodEndYmd, end = today; else start = earliest log date, end = today. Handle empty data with empty state: "Not enough data yet" heading + "Keep using Canopy daily and your quarterly review will appear here." body (per UI-SPEC Copywriting).
    - AppBar: title "Your quarter", leading `IconButton(icon: Icon(Icons.close), onPressed: () => context.pop())`, elevation 0 (per UI-SPEC Screen Layout)
    - Outer `PageController` for 3 sections with `NeverScrollableScrollPhysics` (per UI-SPEC: sections navigated by explicit buttons, not swipe)
    - Section 1: `DataSection` with `onNext` callback advancing to section 2
    - Section 2: `ReflectionSection` with `onComplete` callback advancing to section 3 and passing answers
    - Section 3: `AdjustmentsSection` with all required data
    - Section indicator: 3 dots above the PageView (per UI-SPEC: section label or dots)

    Run `flutter analyze` after all files created.
  </action>
  <verify>
    <automated>flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - lib/screens/quarterly_review/quarterly_review_screen.dart is NOT the stub (does not contain "coming in Phase 5")
    - lib/screens/quarterly_review/quarterly_review_screen.dart contains `PageController` and `PageView`
    - lib/screens/quarterly_review/quarterly_review_screen.dart contains `QuarterlyAggregationService`
    - lib/screens/quarterly_review/quarterly_review_screen.dart contains `HiveCompletionLogRepository`
    - lib/screens/quarterly_review/quarterly_review_screen.dart contains text "Your quarter"
    - lib/screens/quarterly_review/quarterly_review_screen.dart contains text "Not enough data yet"
    - lib/screens/quarterly_review/sections/reflection_section.dart contains all 5 question strings: "most energy", "most proud", "harder to show up", "more focus", "let go"
    - lib/screens/quarterly_review/sections/reflection_section.dart contains `NeverScrollableScrollPhysics`
    - lib/screens/quarterly_review/sections/adjustments_section.dart contains `ReorderableListView`
    - lib/screens/quarterly_review/sections/adjustments_section.dart contains `reorderAll`
    - lib/screens/quarterly_review/sections/adjustments_section.dart contains text "Finish review"
    - lib/screens/quarterly_review/sections/adjustments_section.dart contains text "Set your priorities for next quarter"
    - lib/screens/quarterly_review/sections/adjustments_section.dart contains `HiveQuarterlySnapshotRepository`
    - lib/screens/quarterly_review/widgets/reflection_question_card.dart contains `ActionChip` or `ChoiceChip`
    - lib/screens/quarterly_review/widgets/reflection_question_card.dart contains `TextField`
    - lib/screens/quarterly_review/widgets/goal_adjustment_tile.dart contains text "rarely made it in"
    - lib/screens/quarterly_review/widgets/goal_adjustment_tile.dart contains `colorScheme.error`
    - `flutter analyze` exits 0
  </acceptance_criteria>
  <done>Full quarterly review screen with all three sections is functional: data visualization with charts, 5-question reflection flow with tap-to-pick answers, and goal adjustments with drag reorder and archive suggestions. Completing the review persists a QuarterlySnapshot and updates goal priorities.</done>
</task>

</tasks>

<verification>
- `flutter analyze` — zero issues
- `grep -r 'QuarterlyAggregationService' lib/screens/quarterly_review/` — at least 1 match
- `grep -r 'reorderAll' lib/screens/quarterly_review/` — at least 1 match
- `grep -r 'HiveQuarterlySnapshotRepository' lib/screens/quarterly_review/` — at least 1 match
- Review screen stub is fully replaced
</verification>

<success_criteria>
The quarterly review flow is complete: users see their data (hero stat, donut chart, bar chart), answer 5 celebratory reflection questions with tap-to-pick answers, reorder goal priorities via drag, get archive suggestions for underused goals, and finishing the review persists a QuarterlySnapshot and updates GoalsNotifier priorities.
</success_criteria>

<output>
After completion, create `.planning/phases/05-quarterly-review/05-02-SUMMARY.md`
</output>
