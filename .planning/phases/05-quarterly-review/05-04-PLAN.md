---
phase: 05-quarterly-review
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Full review flow works end-to-end: banner -> data section -> reflection -> adjustments -> finish"
    - "Charts render with real or test data without errors"
    - "Goal priorities update after review and affect next schedule"
    - "Past reviews appear in Settings after completing a review"
    - "flutter test and flutter analyze pass"
---

<objective>
Run final automated checks and present the complete quarterly review for human verification.

Purpose: Ensure all plans integrated correctly and the user can visually confirm the review flow works as intended.

Output: Verified passing test suite and human sign-off on the quarterly review experience.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-quarterly-review/05-CONTEXT.md
@.planning/phases/05-quarterly-review/05-02-SUMMARY.md
@.planning/phases/05-quarterly-review/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and fix any issues</name>
  <files></files>
  <read_first>
    test/services/quarterly_aggregation_test.dart
  </read_first>
  <action>
    Run the full test suite and analyzer:
    ```bash
    flutter test
    flutter analyze
    ```
    If any tests fail or analyzer reports issues, fix them. Common issues to check:
    - Import paths for new files
    - Missing const constructors where required
    - Unused imports in new files
    - Type mismatches between aggregation service output and widget inputs

    Run `dart format lib/screens/quarterly_review/ lib/screens/home/widgets/ lib/screens/settings/past_reviews_screen.dart lib/services/quarterly_aggregation_service.dart` to ensure formatting is clean.
  </action>
  <verify>
    <automated>flutter test && flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test` exits 0 with all tests passing
    - `flutter analyze` exits 0 with no issues
  </acceptance_criteria>
  <done>Full test suite green, zero analyzer issues, all new code formatted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of quarterly review flow</name>
  <files></files>
  <action>
    Present the complete quarterly review feature for human verification. The user will run the app and test the full flow.

    What was built:
    1. Data section with hero stat ("N chunks completed this quarter"), donut chart (per-goal time distribution with "Time not spent" slice), bar chart (weekly completed chunks), top 3 goals
    2. Reflection section with 5 celebratory questions, tap-to-pick answers with data-driven suggestions
    3. Adjustments section with drag-to-reorder goal priorities and archive suggestions for underused goals
    4. Home screen review banner when within 90-day review window
    5. Past reviews accessible from Settings

    How to verify:
    1. Run `flutter run` on your preferred device/simulator
    2. Review banner: If you have completion data spanning 90+ days, check the home screen for the review banner "Your quarterly review is ready". If not, navigate directly to /review.
    3. Data section: Verify the hero stat shows a number, donut chart renders with goal colors and a "Time not spent" grey slice, bar chart shows weekly bars, and top 3 goals are listed. Tap "Next: Reflect".
    4. Reflection section: Verify 5 questions appear one at a time with suggestion chips populated from your goals. Tap answers or use "Other" to type a custom answer. Each answer advances to the next question.
    5. Adjustments section: Verify goals appear in a draggable list. Try dragging to reorder. Check if any goals with low completion show the "This one rarely made it in" archive prompt. Tap "Finish review".
    6. Post-review: Go to Settings > Past reviews and verify the review you just completed appears with the correct date and chunk count.
    7. Charts visual check: Donut chart has a hollow center (not a full pie). Bar chart bars are primary color. No red-colored default slices.
    8. Tone check: All copy is celebratory -- no evaluative language like "missed", "failed", or "behind".

    Resume signal: Type "approved" to complete Phase 5, or describe any issues to fix.
  </action>
  <verify>
    <automated>flutter test && flutter analyze</automated>
  </verify>
  <done>Human confirms quarterly review flow works end-to-end: charts render correctly, reflection questions have data-driven suggestions, goal priorities update after review, past reviews appear in Settings.</done>
</task>

</tasks>

<verification>
- `flutter test` -- all tests pass
- `flutter analyze` -- zero issues
- Human visual verification of charts, flow, and copy tone
</verification>

<success_criteria>
All automated tests pass, analyzer is clean, and human confirms the quarterly review flow works end-to-end with correct charts, celebratory tone, and functional goal priority updates.
</success_criteria>

<output>
After completion, create `.planning/phases/05-quarterly-review/05-04-SUMMARY.md`
</output>
