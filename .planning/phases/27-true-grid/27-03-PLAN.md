---
phase: 27-true-grid
plan: 03
type: execute
wave: 3
depends_on: ["27-02"]
files_modified:
  - test/screens/today_screen_now_state_test.dart
  - test/screens/today_screen_test.dart
autonomous: true
requirements: [GRID-02]
must_haves:
  truths:
    - "The whole suite is green again, with no assertion weakened into vacuity to get there"
    - "A running break still announces itself as rest, on both tiers, by whichever means that tier actually uses"
    - "The live row's rendered slot is asserted to be duration-exact — the positive claim that replaces the deleted swell test"
    - "Complete is still reachable through the now-line, now found by tooltip rather than by button label"
  artifacts:
    - path: "test/screens/today_screen_now_state_test.dart"
      provides: "Tier-aware LIVE-01/LIVE-02 coverage"
    - path: "test/screens/today_screen_test.dart"
      provides: "GRID-02 duration-exact live-slot regression test"
      contains: "GRID-02"
  key_links:
    - from: "test/screens/today_screen_test.dart"
      to: "the live row's ClipRect slot"
      via: "find.ancestor(of: LiveRowCard, matching: ClipRect) measured against heightFor"
      pattern: "kPixelsPerMinute"
---

<objective>
Repair every screen-level assertion that encoded the deleted swell, progress bar, "Next ·" line and
labelled buttons — and add the positive claims that replace them.

Purpose: plan 27-02 deliberately left seven tests red. Six of them assert behaviour this phase
removed on purpose; one asserts behaviour that survives but is now reached differently. Getting the
suite green must not mean deleting coverage — three of these tests protect real contracts
(LIVE-01's "a running break reads as rest", `_chunkTitle`'s break-awareness, and the
IgnorePointer hit-test proof) that must land somewhere they can still fail.

Output: a green suite, with LIVE-01 covered on BOTH tiers, `_chunkTitle`'s break-awareness
repointed to its surviving render surface, and the live row's duration-exact slot asserted for the
first time.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

`flutter`/`dart` are NOT on a non-login shell's PATH. Prefix commands with:
`export PATH="$PATH:/home/dan/development/flutter/bin"`
</execution_context>

<context>
@.planning/phases/27-true-grid/27-UI-SPEC.md
@.planning/phases/27-true-grid/27-VALIDATION.md
@.planning/phases/27-true-grid/27-02-SUMMARY.md
@lib/screens/today/widgets/live_row_card.dart
@lib/screens/today/today_screen.dart
</context>

<locked_planner_decisions>
- **PD-27-09 — LIVE-01 gets tier-split coverage, not a deleted test.** "A running break announces
  itself as rest" is currently proved by the `RIGHT NOW — RESTING` kicker. The single-line tier
  drops the kicker by design, so that assertion can only survive on the compact tier. Cover BOTH:
  keep a kicker assertion on a live break long enough for the compact tier, and assert the
  single-line tier's locked `'Right now: Taking a break, …'` semantics label plus its visible
  title for the short-break case. Deleting either half would leave a tier uncovered for the exact
  contract Phase 23 was built to establish.
- **PD-27-10 — the "next-is-a-break" test is repointed, not deleted.** Its subject looks like the
  live row's "Next ·" line, but its actual claim is that `_chunkTitle` names a break correctly
  instead of falling through to `'Work block'`. `_chunkTitle` survives with two callers
  (`today_screen.dart` ~460 PreStart, ~507 GapBeforeNext), and `today_screen.dart`'s own comment
  at ~494-498 explicitly names break-awareness at the GapBeforeNext site as load-bearing ("do NOT
  add a `chunkType` check in this case; the shared helper already handles it"). Repoint the test
  at the GapBeforeNext "Up next" banner — its only remaining rendered surface. Deleting it would
  silently retire the guard that comment relies on.
- **PD-27-07 (carried from 27-01) — the deleted swell test's positive replacement lands HERE.**
  `'the live row renders taller than its duration-implied slot, capped at kLiveRowReservedHeight'`
  was deleted in wave 1 because its replacement could not be true until wave 2 shipped. Add it now.
- **No assertion may be weakened into vacuity to go green.** `'live break shows no Complete/Skip
  (D-02)'` currently searches for `FilledButton`/`OutlinedButton`, widget types the live row no
  longer uses in any tier — it will now pass trivially whether or not the gate works. Strengthen
  it, do not leave it.
</locked_planner_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Repair and re-aim the six now-state live-row tests</name>

  <read_first>
    - `.planning/phases/27-true-grid/27-02-SUMMARY.md` — the OBSERVED failing-test list. Work from
      it, not from the forecast; if it names a test this plan does not mention, that test is a real
      regression and must be understood before it is touched.
    - `test/screens/today_screen_now_state_test.dart` — the whole file, but especially: the
      overdue test at ~632-706, the live-break group at ~955-1126, `'live work chunk still shows
      Complete/Skip'` at ~1148-1162, and `'the progress bar tracks the same value as the label'`
      at ~1396-1424
    - `lib/screens/today/today_screen.dart` lines ~455-530 — the PreStart and GapBeforeNext edge
      state bodies, including the ~480-504 comment block explaining why `_chunkTitle`'s
      break-awareness at the GapBeforeNext site must not be replaced by a `chunkType` check
    - `lib/screens/today/widgets/live_row_card.dart` — the shipped tiers, so the new finders match
      what actually renders
    - `.planning/phases/27-true-grid/27-UI-SPEC.md` — "Single-line tier", for the exact locked
      semantics label string
  </read_first>

  <files>test/screens/today_screen_now_state_test.dart</files>

  <action>
Work through the observed failures one at a time. Six edits:

**1. `'between-chunks (overdue): 10am, c1 8:30–9:30, c2 10:30–11:30 → LiveRowCard (c1) + Next
(c2)'`.** c1 is 60 minutes, so at 240dp it renders compact and its kicker survives — keep the
`'RIGHT NOW · 8:30 AM'` identity assertion and the `findsWidgets` `'8:30 AM'` assertion untouched.
DELETE only the trailing `find.textContaining('Next ·')` expectation and its `reason`. Then fix the
comment at ~682-684, which currently justifies using the kicker as the identity check by saying a
blanket "10:30 AM must not appear" would be wrong "because the 'Next · … at 10:30 AM' line
legitimately names c2". With the Next line gone, that caveat is obsolete — and the stronger
assertion it was avoiding is now available. Replace it with a positive assertion that
`find.descendant(of: find.byType(LiveRowCard), matching: find.textContaining('10:30 AM'))` finds
nothing, which is exactly the identity check the old comment said it could not make. Also update
the test's own name to drop `+ Next (c2)`.

**2. `'live break announces itself as rest'`** (the `RIGHT NOW — RESTING` test, fixture: b1 is a
5-minute short break at 8:25, now 8:27). At 20dp this is the single-line tier, which has no kicker.
Per PD-27-09, split into two tests:
  - Keep this test, with its fixture unchanged, but change its assertions to the single-line
    tier's actual surface: call `tester.ensureSemantics()`, assert
    `find.bySemanticsLabel(RegExp(r'^Right now: Taking a break,'))` finds one widget, keep
    `expect(find.text('Taking a break'), findsOneWidget)`, and dispose the handle. Rename it to
    say "single-line tier" and add a comment stating that the visible kicker is deliberately
    dropped below `kCompactLiveMinHeight` (`27-UI-SPEC.md`) so the semantics label is where the
    "this is current, and it is rest" framing lives for this tier.
  - Add a sibling test asserting the compact tier still shows `'RIGHT NOW — RESTING'`, using a
    live LONG break long enough to clear `kCompactLiveMinHeight` — the fixture in the adjacent
    `'live long break: title says long'` test (a 25-minute long break at 8:25, now 8:27) is
    already exactly that, so copy its schedule rather than inventing one.

**3. `'live break still shows a progress bar (D-04)'`.** DELETE outright. The bar is gone from both
tiers by design (`27-UI-SPEC.md`: the now-line's position within a duration-exact card IS the
fraction elapsed). Leave a comment in its place naming this phase and the reason, so the next
reader does not restore it.

**4. `'next-is-a-break renders the reference name, not "Work block"'`.** Repoint per PD-27-10.
Change the fixture so the screen is in `GapBeforeNext`: mark w1 resolved, keep b1 a short break,
and pick a `now` that sits after w1's window and before b1's start with a gap wide enough to
trigger the banner (read `resolveNowState` and `kMinGapMinutes` rather than guessing). Assert the
"Up next" banner shows `'Short break'` and that `'Work block'` appears nowhere. Update the test
name to say the banner, not the live row, and add a comment cross-referencing
`today_screen.dart`'s ~494-498 block so the link between the guard and the comment it protects is
explicit.

**5. `'live work chunk still shows Complete/Skip'`.** The 60-minute chunk renders compact, so both
actions exist — as `IconButton`s with tooltips. Change the finder to
`find.descendant(of: find.byType(LiveRowCard), matching: find.byTooltip('Complete'))` and add the
matching `'Skip'` assertion, which the test was missing. Scope BOTH to `LiveRowCard`: a lone
unresolved work chunk also renders its own non-live card elsewhere in other fixtures, and an
unscoped finder invites a future false pass.

**6. `'the progress bar tracks the same value as the label'`.** DELETE outright. Its stated purpose
was that the bar and the label can never disagree (23-02's D-04 decision); with the bar gone there
is only the label, and the surrounding LIVE-02 countdown tests already assert it. Leave a comment
recording that the invariant is now satisfied by construction — there is only one renderer of
`secondsRemaining` left.

**One more, not on the failure list but required by "no vacuous assertions":** strengthen
`'live break shows no Complete/Skip (D-02)'`. Its `find.widgetWithText(FilledButton, 'Complete')`
and `find.widgetWithText(OutlinedButton, 'Skip')` now match widget types the live row cannot
produce in either tier, so it passes for the wrong reason. Add `find.byTooltip('Complete')` and
`find.byTooltip('Skip')` `findsNothing` assertions and an `find.descendant(of: find.byType(
LiveRowCard), matching: find.byType(IconButton))` `findsNothing`. Keep the existing comment
explaining why w1 is marked completed in this fixture — that reasoning is still correct.

Finally, re-grep the file for `'Next ·'`, `nextLine`, `LinearProgressIndicator`, `FilledButton` and
`OutlinedButton` and confirm every surviving hit is intentional. `27-RESEARCH.md`'s assumption A3
explicitly flags that its own grep may have missed a site; this is the cheap insurance it asks for.
  </action>

  <verify>
    <automated>export PATH="$PATH:/home/dan/development/flutter/bin" &amp;&amp; flutter test test/screens/today_screen_now_state_test.dart</automated>
  </verify>

  <acceptance_criteria>
    - `flutter test test/screens/today_screen_now_state_test.dart` is green.
    - `grep -c "LinearProgressIndicator" test/screens/today_screen_now_state_test.dart` prints `0`.
    - `grep -c "Next ·" test/screens/today_screen_now_state_test.dart` prints `0`.
    - `grep -n "RIGHT NOW — RESTING" test/screens/today_screen_now_state_test.dart` returns exactly
      one hit, and it is inside the new compact-tier long-break test.
    - `grep -c "bySemanticsLabel" test/screens/today_screen_now_state_test.dart` is at least `1`,
      matching the single-line tier's `'Right now: Taking a break,'` label.
    - `grep -c "byTooltip" test/screens/today_screen_now_state_test.dart` is at least `4` (Complete
      and Skip, asserted present for a live work chunk and absent for a live break).
    - The test count in this file is at most one lower than before (two deleted, one added) —
      record the before/after counts in the summary so coverage loss is visible rather than
      inferred.
    - No surviving test in this file asserts the presence of a progress bar or a "Next ·" line.
  </acceptance_criteria>

  <done>All six now-state failures are resolved, LIVE-01 is covered on both tiers, `_chunkTitle`'s break-awareness is guarded at its surviving render site, and the D-02 no-actions test can actually fail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix the hit-test finder and assert the duration-exact live slot</name>

  <read_first>
    - `test/screens/today_screen_test.dart` — the `'Phase 26 — CAL-01 the day has a shape'` group
      (~588-800), including the two existing slot-measurement tests that use
      `find.ancestor(of: <text>, matching: find.byType(ClipRect))` and the comment left in place of
      the deleted swell test; `buildDayFixture`/`pumpDay` (~317-368); `twoChunkFixture` (~760-778);
      and the hit-test test at ~1087-1132
    - `lib/screens/today/today_screen.dart` — the live arm of `_buildPositionedRow` as shipped by
      plan 27-02, so the `ClipRect` ancestor the new test measures is the right one
    - `lib/screens/today/timeline_geometry.dart` — `kPixelsPerMinute`, `kCompactLiveMinHeight`
    - `.planning/phases/27-true-grid/27-VALIDATION.md` — the arithmetic-vs-pixels split: slot
      heights computed from `int` minutes are harness-safe; anything about whether TEXT fits is not
  </read_first>

  <files>test/screens/today_screen_test.dart</files>

  <behavior>
    - With `pumpDay` (c3 is a live 5-minute chunk at 10:45), the live row's slot measures exactly
      `5 * kPixelsPerMinute` — 20.0 — and the row renders the single-line tier (no kicker text).
    - With `twoChunkFixture` at 9:12 (w1 is a live 25-minute chunk), the live row's slot measures
      exactly `25 * kPixelsPerMinute` — 100.0 — and the row renders the compact tier (kicker text
      present).
    - Tapping Complete inside `LiveRowCard` while the now-line crosses it still reaches
      `markComplete('w1')`.
  </behavior>

  <action>
**Fix the hit-test finder.** In `'hit-testing — a Complete tap still lands through the now-line
(IgnorePointer proof)'`, replace `find.widgetWithText(FilledButton, 'Complete')` with
`find.byTooltip('Complete')`, keeping the `find.descendant(of: find.byType(LiveRowCard), ...)`
scoping and its comment explaining why the scoping is necessary (w2 renders its own Complete
button). `twoChunkFixture`'s w1 is 25 minutes, so at 100dp the compact tier renders and the icon
button exists — state that in a comment, because the finder now silently depends on the fixture
clearing `kCompactLiveMinHeight`. Keep the two preceding assertions that prove the now-line
genuinely overlaps the card; without them the test proves nothing, which is exactly the failure
mode `.planning/STATE.md`'s "Regression tests must be proven RED" invariant was written about.

**Add the GRID-02 duration-exact slot test.** Add to the `'Phase 26 — CAL-01 the day has a shape'`
group a test named `'GRID-02: the live row's rendered slot is duration-exact, not swelled'`. It
replaces the test plan 27-01 deleted. Use `pumpDay` (c3, 10:45–10:50, live at 10:47), find the
live row's slot with `find.ancestor(of: find.byType(LiveRowCard), matching: find.byType(ClipRect))
.first`, and assert `tester.getSize(...).height` equals `5 * kPixelsPerMinute`.

Measure the `ClipRect`, NOT the `LiveRowCard` itself: `OverflowBox` deliberately lets the card lay
out at its natural height, so `getSize(find.byType(LiveRowCard))` reports the card's content
height, not its slot — asserting on that would be measuring the placeholder font and would be
worthless per `27-VALIDATION.md`. The `ClipRect` is the slot. Follow the two existing
slot-measurement tests in this same group, which already use exactly this `find.ancestor` shape.
Assert `tester.takeException()` is null, matching the group's convention.

**Add a compact-slot sibling.** Add a second test using `twoChunkFixture` at 9:12 asserting the
live row's `ClipRect` measures `25 * kPixelsPerMinute`, and that
`find.descendant(of: find.byType(LiveRowCard), matching: find.textContaining('RIGHT NOW'))` finds
one widget. Together with the `pumpDay` case (20dp, no kicker) this is the screen-level proof that
tier selection is driven by real geometry rather than by a widget-test-only `slotHeight` argument —
the unit tests in plan 27-02 pass `slotHeight` directly and therefore cannot prove the wiring.

**Fix the stale comment at ~502-505.** It justifies the exact-match `find.text('Reading')` finder
by saying `'Reading'` is also a substring of the live row's "Next · Reading at 10:50 AM" line. That
line no longer exists. Keep the exact-match finder (it is still correct) and rewrite the comment to
give the reason that still holds — an exact `find.text` is used because the fixture's other rows
carry longer strings containing the same word — or, if no such ambiguity remains, say plainly that
the finder is exact-match for stability and note that the old Next-line ambiguity is gone.
  </action>

  <verify>
    <automated>export PATH="$PATH:/home/dan/development/flutter/bin" &amp;&amp; flutter analyze &amp;&amp; flutter test</automated>
  </verify>

  <acceptance_criteria>
    - `flutter test` (FULL suite) is green — zero failures, zero skips added.
    - `flutter analyze` reports no issues.
    - `flutter test test/screens/today_screen_test.dart --plain-name 'GRID-02'` matches at least
      one test and passes.
    - `grep -c "Next ·" test/screens/today_screen_test.dart` prints `0`.
    - `grep -rn "liveExtraPx\|kLiveRowReservedHeight\|nextLine" lib/ test/ | wc -l` prints `0` —
      repo-wide, the last check that the deleted concepts left no residue.
    - The new duration-exact test measures a `ClipRect`, not a `LiveRowCard` — confirm by reading
      the finder.
    - The hit-test test still contains both `lineTop`-vs-`cardRect` overlap assertions before the
      tap.
    - `git diff --exit-code pubspec.yaml pubspec.lock` — no dependency change.
  </acceptance_criteria>

  <done>The full suite is green, Complete is reachable by tooltip through the now-line, and the live row's slot is asserted duration-exact on both tiers at the screen level.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols created, renamed or deleted by THIS plan (▶), against the phase-wide list:

**Deleted**
- `kLiveRowReservedHeight`, `TimelineGeometry.liveExtraPx` (27-01)
- `LiveRowCard.progress`, `LiveRowCard.nextLine` (27-02)
- ▶ test `'live break still shows a progress bar (D-04)'`
- ▶ test `'the progress bar tracks the same value as the label'`

**Created**
- `kCompactLiveMinHeight` (27-01, re-measured 27-04)
- `LiveRowCard.slotHeight`, `LiveRowCard.onTap`, `_buildCompact`, `_buildSingleLine` (27-02)
- ▶ test `'GRID-02: the live row's rendered slot is duration-exact, not swelled'`
- ▶ a compact-tier sibling asserting a 25-minute live chunk's slot is `25 * kPixelsPerMinute` and
  its kicker renders
- ▶ a compact-tier `'RIGHT NOW — RESTING'` test for a live long break (PD-27-09)

**Renamed / re-aimed**
- ▶ `'live break announces itself as rest'` → single-line-tier semantics-label assertion
- ▶ `'next-is-a-break renders the reference name, not "Work block"'` → repointed from the live
  row's deleted Next line to the `GapBeforeNext` "Up next" banner (PD-27-10)
- ▶ `'between-chunks (overdue): … + Next (c2)'` → name drops `+ Next (c2)`; gains a positive
  `'10:30 AM'`-absent identity assertion
- ▶ `'live work chunk still shows Complete/Skip'` → tooltip finders, both actions, scoped to
  `LiveRowCard`
- ▶ `'live break shows no Complete/Skip (D-02)'` → strengthened with tooltip and `IconButton`
  absence assertions
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none introduced | Test-only plan. No `lib/` change, no dependency, no runtime surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04 | Repudiation | the regression suite itself | mitigate | The specific risk here is a test that passes without being able to fail — this project shipped two such defects in v1.5. Mitigated by explicitly strengthening `'live break shows no Complete/Skip (D-02)'` (its finders now match widget types the code cannot produce) and by requiring before/after test counts in the summary so deletions are visible. |
| T-27-SC | Tampering | package installs | accept | Zero packages installed; `git diff --exit-code pubspec.yaml pubspec.lock` must be empty. |
</threat_model>

<verification>
1. `flutter analyze` — no issues.
2. `flutter test` — FULL suite green. This is the wave gate `27-VALIDATION.md` requires.
3. `grep -rn "liveExtraPx\|kLiveRowReservedHeight\|nextLine" lib/ test/ | wc -l` — `0`.
4. Before/after test counts for both edited files recorded in the summary.
</verification>

<success_criteria>
- Full `flutter test` green; `flutter analyze` clean.
- LIVE-01's "a running break reads as rest" is asserted on both tiers.
- `_chunkTitle`'s break-awareness is guarded at the `GapBeforeNext` banner.
- The live row's slot is asserted duration-exact at 20dp and 100dp, measured on the `ClipRect`.
- No surviving assertion references a progress bar, a "Next ·" line, or a `FilledButton`/
  `OutlinedButton` inside `LiveRowCard`.
- No test was deleted without either a stated replacement or a stated reason its subject no longer
  exists.
</success_criteria>

<output>
Create `.planning/phases/27-true-grid/27-03-SUMMARY.md` when done, including the before/after test
counts per file and the list of tests deleted with the reason for each.
</output>
