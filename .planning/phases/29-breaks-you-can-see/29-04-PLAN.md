---
phase: 29-breaks-you-can-see
plan: 04
type: execute
wave: 4
depends_on: ["29-03"]
files_modified:
  - .planning/phases/29-breaks-you-can-see/shots/
  - .planning/phases/29-breaks-you-can-see/29-UAT.md
  - lib/screens/schedule/widgets/chunk_card.dart
autonomous: false
requirements: [SEEBREAK-01, SEEBREAK-02]
user_setup: []

must_haves:
  truths:
    - "`measure_hours.py` prints UNIFORM against a real-browser screenshot in which a short break is rendering at sub-compact density — SEEBREAK-02 proven in painted pixels, not only in arithmetic"
    - "The sub-compact break's rendered ink fits inside its 20dp slot, observed in a real browser, and its label is legible in the screenshot"
    - "The work chunk's own 26dp overflow question is answered with a real-browser number and a disposition — fixed or dismissed on evidence, never left unexamined (D-06)"
    - "A human has opened the served build and recorded a verdict on whether a 5-minute break reads as a break — the phase does not close on automated verification alone (D-07)"
    - "The build the human judged is hash-identical to the build that was measured"
  artifacts:
    - path: ".planning/phases/29-breaks-you-can-see/shots/"
      provides: "uniform-subcompact-break.png and work-chunk-fit.png — the SEEBREAK-02 and D-06 evidence"
    - path: ".planning/phases/29-breaks-you-can-see/29-UAT.md"
      provides: "The human verdict, item by item, recorded verbatim"
      contains: "Verdict"
  key_links:
    - from: ".planning/phases/29-breaks-you-can-see/shots/uniform-subcompact-break.png"
      to: "SEEBREAK-02"
      via: "measure_hours.py band-centre spacing verdict"
      pattern: "UNIFORM"
    - from: ".planning/phases/29-breaks-you-can-see/29-UAT.md"
      to: "SEEBREAK-01"
      via: "human perceptual verdict on the served debug build"
      pattern: "Verdict"
---

<objective>
Prove in painted pixels that the grid is still true with the new tier rendering, answer the work
card's 26dp overflow question with a real-browser number, and then stop and ask a person the one
thing no assertion settles.

Purpose: `29-VALIDATION.md`'s phase gate is deliberately stricter than a green suite, and the
ROADMAP writes the reason in bold. **Phase 27 scored 16/17 on automated verification and then failed
2 of 3 items on the human check** — both failures had passed everything automated. Two independent
mechanisms lie here: `flutter test`'s placeholder font can call a break legible when it is not, and
headless Chromium's `CONTEXT_LOST_WEBGL` can return a blank screenshot readable as either a pass or
a false failure. The phase's actual success criterion is "can a person see that this is a break",
and that is a perceptual judgement.

Output: `UNIFORM` from `measure_hours.py` with a sub-compact break on screen, a recorded disposition
for ROADMAP item 4, and `29-UAT.md` holding a human verdict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

- `export PATH="$PATH:/home/dan/development/flutter/bin"` — flutter/dart are not on a non-login
  shell's PATH.
- `export NVM_DIR=$HOME/.nvm && . $NVM_DIR/nvm.sh` then `NODE_PATH=$(npm root -g)` — node is
  nvm-only and Playwright is a global install.
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/29-breaks-you-can-see/29-UI-SPEC.md
@.planning/phases/29-breaks-you-can-see/29-VALIDATION.md
@.planning/phases/29-breaks-you-can-see/29-03-SUMMARY.md
@lib/screens/today/timeline_geometry.dart
</context>

<source_decisions>
## Decision IDs — planner-assigned, for traceability

Repeated from 29-01. Source: `29-CONTEXT.md` § "Implementation Decisions".

| ID | Locked decision | Honoured in this plan |
|----|-----------------|------------------------|
| **D-03** | Raising `kPixelsPerMinute` was rejected | If anything does not fit, the TIER is tightened — the scale and the slot are off-limits, including in Task 2's fix branch |
| **D-05** | Do not buy legibility with grid accuracy — a regression test must assert no chunk's rendered height deviates from `durationMinutes × kPixelsPerMinute` | Task 1 — the `measure_hours.py` UNIFORM run is the painted half of that proof; 29-01's Tests 7 and 8 are the arithmetic half |
| **D-06** | The work card's own 26dp overflow must be resolved, not left unexamined — fix it or explicitly dismiss it with the evidence | Task 2 |
| **D-07** | This phase MUST end in a `checkpoint:human-verify` task; it cannot close on automated verification alone | Task 3 |
</source_decisions>

<locked_planner_decisions>
- **PD-29-05 — staleness is proven by sha256 equality** between the served `main.dart.js` and the
  freshly built one, before any screenshot and again before the human is asked to look. The human
  must never be handed a stale bundle to judge.
- **PD-29-06 — fit and legibility are real-browser claims only.** No `flutter test` number may be
  offered as evidence for either in this plan.
- **Port 8143, debug builds only** — claimed in 29-03. Do not switch build types on it (CLAUDE.md
  trap #1).
- **If ROADMAP item 4 turns out to be a real on-device defect, the fix is the `full` tier's
  vertical content padding (currently `8`), not the slot.** `29-UI-SPEC.md` says "padding/typography,
  not a slot-height change", and D-03 forbids the scale. Tighten
  `_WorkChunkContent`'s `contentPadding` `full` arm and re-measure. Do not touch
  `kPixelsPerMinute`, `kFullTierMinHeight`, or the `Positioned(height: slot)` wrapper.
- **The human checkpoint is never auto-approved.** `workflow.auto_advance` and the repo's `yolo`
  mode do not apply to it; this is the one gate the ROADMAP writes in bold. Do not substitute your
  own screenshot inspection for the human's eye — you already inspected screenshots in Task 1, and
  Phase 27 proved that is not the same evidence.
</locked_planner_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild with the measured constant and prove SEEBREAK-02 in painted pixels</name>

  <read_first>
    - `.planning/spikes/001-live-row-in-a-true-grid/tools/measure_hours.py` — its usage line, its
      `--gutter` / `--top` / `--bottomtrim` options, its `spread <= 2.0` tolerance and its
      `UNIFORM` / `NOT UNIFORM` verdict format. Reuse it **unmodified**.
    - `.planning/phases/29-breaks-you-can-see/tools/measure_card_extent.py` — the script 29-03
      wrote, and the tuned `--bridge` / `--minband` values 29-03's summary recorded.
    - `.planning/phases/29-breaks-you-can-see/29-03-SUMMARY.md` — the measured constant, the
      instant/scroll that worked on this profile, and both sha256 hashes.
    - `.planning/spikes/001-live-row-in-a-true-grid/README.md` — the baseline's known-bad numbers.
      A `132.0` spread anywhere is Phase 27's defect signature and would mean the served build is
      not the built one.
    - `CLAUDE.md` — the three traps, again. This task re-serves and re-drives, so all three are live.
  </read_first>

  <files>.planning/phases/29-breaks-you-can-see/shots/</files>

  <action>
**Rebuild so the build that ships is the build that gets proven.** 29-03 measured against a build
containing the *placeholder* constant and a throwaway forcing edit. Run
`flutter build web --debug --source-maps --pwa-strategy=none` again on the current tree, re-serve
with `python3 tools/serve-uat.py 8143 --dir build/web`, and re-run the sha256 equality check between
`curl -s http://localhost:8143/main.dart.js | sha256sum` and `sha256sum build/web/main.dart.js`
before believing any pixel (PD-29-05). Quote both hashes.

**Drive to a short break and screenshot it.** Reuse `/tmp/canopy-p29-profile` so the day is the same
one 29-03 measured against — that continuity is worth more than a fresh profile here, and it is why
`drive.cjs` uses a persistent profile at all. Run with `--dump` first, locate a `Short break` node
in the semantics tree, note its `y` and the `--scroll` needed, then screenshot to
`.planning/phases/29-breaks-you-can-see/shots/uniform-subcompact-break.png`. The screenshot must
contain **at least three hour-axis labels** — `measure_hours.py` needs at least two bands to compare
anything and three to make "uniform" mean something; a shot with two is vacuous.

**Prove the grid (D-05).** Run
`python3 .planning/spikes/001-live-row-in-a-true-grid/tools/measure_hours.py` against that PNG. It
must print `VERDICT: UNIFORM`. Paste its **full** output — every band centre, every consecutive
spacing, the min/max/spread — into the summary. The verdict word alone is not the evidence; the
numbers are. Every consecutive spacing should be `60 * kPixelsPerMinute` = `240.0`.

If it prints `NOT UNIFORM`, do **not** loosen the script's tolerance and do not edit the script at
all. Read the band numbers, work out which hour is wrong and by how much, and check that figure
against the sub-compact card's own extent — the arithmetic will name the cause. Report rather than
paper over.

**Prove the sub-compact tier fits its 20dp slot (SEEBREAK-01, real-browser half).** Using the
`Short break` node's `y` from the dump, bound `measure_card_extent.py` to that row's 20dp slot
(centre ± ~30 is enough) and run it. The overall ink extent must be **≤ 20**. Paste the full band
list. Then **open the screenshot with the Read tool** and record, as observations and not
inferences: the label reads `Short break` and is not cut off at the top or bottom; the two hairline
segments are visible either side of it; and it reads as a distinct row rather than as a border
belonging to the work card above or below.

If the extent exceeds 20, tighten the **tier** — never the slot and never the scale (D-03,
`29-UI-SPEC.md` step 9). The candidates, in order: a stray `Semantics` layout box, any padding that
crept back in, or the `bodySmall` line height. Re-measure after any change, and re-run the full
suite.

**Leave the server running** — Task 3 hands its URL to a human, and restarting it there would risk
serving a different build than the one proven here.
  </action>

  <verify>
    <automated>python3 .planning/spikes/001-live-row-in-a-true-grid/tools/measure_hours.py .planning/phases/29-breaks-you-can-see/shots/uniform-subcompact-break.png</automated>
  </verify>

  <acceptance_criteria>
    - The served and built `main.dart.js` sha256 hashes matched, checked AFTER the rebuild and
      BEFORE any screenshot. Both quoted in the summary.
    - `measure_hours.py` printed `VERDICT: UNIFORM` against
      `shots/uniform-subcompact-break.png`, and the full output — at least three label bands, every
      consecutive spacing, min/max/spread — is pasted verbatim into the summary.
    - Every consecutive spacing in that output is the same number, and the output contains no
      `132.0` spread (Phase 27's defect signature — its presence would mean the served bundle is not
      the built one).
    - `measure_card_extent.py`, bounded to the short break's slot, printed an overall ink extent
      **≤ 20**. The full band list is in the summary. If it exceeded 20, the summary records what
      was tightened in the tier, the re-measured extent, and that neither `kPixelsPerMinute` nor the
      slot was touched.
    - The screenshot was opened with the Read tool, and the three named observations (label not cut
      off; both hairlines visible; reads as a distinct row) are recorded as observations.
    - `git diff --exit-code lib/screens/today/timeline_geometry.dart` passes — no constant moved in
      this task.
  </acceptance_criteria>

  <done>With a sub-compact break rendering, `measure_hours.py` prints UNIFORM against a build hash-identical to the one served, and the tier's painted ink fits inside its 20dp slot.</done>
</task>

<task type="auto">
  <name>Task 2: Answer the work card's 26dp overflow with a real-browser number, and dispose of it (D-06)</name>

  <read_first>
    - `lib/screens/today/timeline_geometry.dart` `kPixelsPerMinute`'s doc comment, lines 12-56 — it
      already records a real-browser measurement of the `full`-tier work card: **70dp measured card
      + 8dp `Card` margin = 78dp against a 100dp slot, ~22dp slack, 2026-08-18**. This is the prior,
      and it is why this task is a confirmation and not a re-derivation.
    - `.planning/phases/29-breaks-you-can-see/29-UI-SPEC.md` § "Verification obligation — the work
      chunk's own 26dp overflow (ROADMAP item 4)" — the default expectation (harness artifact), and
      the explicit instruction not to accept a contradicting measurement just because the document
      predicted otherwise.
    - `.planning/seeds/SEED-005-five-minute-breaks-clip-to-a-sliver.md` — the `126dp natural vs
      100dp slot` row and its own caveat that 52dp and 126dp are harness bounds.
    - `lib/screens/schedule/widgets/chunk_card.dart` `_WorkChunkContent`'s `contentPadding` switch
      (~line 346) — only relevant if the fix branch fires.
  </read_first>

  <files>.planning/phases/29-breaks-you-can-see/shots/, lib/screens/schedule/widgets/chunk_card.dart</files>

  <action>
SEED-005's table says a 25-minute work chunk needs 126dp in a 100dp slot and is clipped by 26dp.
That figure came from `flutter test`, whose placeholder font inflates glyph metrics — and
`kPixelsPerMinute`'s doc comment already carries a **real-browser** measurement of the same card at
70dp + 8dp margin = 78dp in the same 100dp slot. The two cannot both be right, and the ROADMAP
forbids leaving the question open. This task settles it with one screenshot rather than a
re-derivation.

**Capture.** From the same running server, profile and day, use `drive.cjs --dump` to locate a
standard **25-minute work chunk** (a `ChunkCard`, not the live row — pick an instant where the
target chunk is not live, or scroll to one that is not) and screenshot to
`.planning/phases/29-breaks-you-can-see/shots/work-chunk-fit.png`. Record which chunk and which
instant.

**Measure.** Bound `measure_card_extent.py` to that chunk's 100dp slot — top = the slot's first row,
bottom = the slot's last row, derived from the semantics `y` and the known `25 * kPixelsPerMinute`
= 100 slot height. The `full`-tier work card is a `Card` with an `outlineVariant` border, so its top
and bottom edges are both ink. The disposition test is precise and falsifiable:

- **DISMISSED (expected):** the card's **bottom** border band appears strictly inside the window, at
  least 1px above the slot's last row, and the ink extent is comfortably under 100. That is a card
  that fits, and it means SEED-005's 126dp was the harness bound its own caveat says it was. Record
  the measured extent and the slack.
- **REAL DEFECT:** no bottom border band exists and ink runs to the slot's last row — the card is
  being clipped by the `ClipRect`.

**Look, do not only measure.** Open the screenshot with the Read tool and record whether the work
card's bottom edge and its full content (title, time range, Complete/Skip row) are visible and
uncut. A band list can be misread; a glyph sliced in half cannot.

**Disposition.**
- If DISMISSED: record the number, the method and the verdict in the summary and in the phase's
  verification record. Add one sentence to `kPixelsPerMinute`'s doc comment noting the 2026-08-20
  re-confirmation at the 100dp slot with the measured extent, so the next reader finds the
  confirmation next to the original claim rather than buried in a plan. That is a comment-only
  change to `timeline_geometry.dart`.
- If REAL DEFECT: fix it in the **`full` tier's vertical content padding**, tightening
  `_WorkChunkContent`'s `contentPadding` `full` arm from `vertical: 8` toward `vertical: 4`, then
  rebuild, re-measure and re-run the full suite. Do **not** touch `kPixelsPerMinute`,
  `kFullTierMinHeight`, or the `Positioned(height: slot)` wrapper — D-03 rejected buying fit with
  scale, and changing the slot would reopen GRID-01, which Phase 27 existed to close. Add the
  contradicting measurement to `kPixelsPerMinute`'s doc comment either way: a number that refutes a
  documented claim is worth more than one that confirms it.

Either way, the phase does not close with this question open.
  </action>

  <verify>
    <automated>python3 .planning/phases/29-breaks-you-can-see/tools/measure_card_extent.py .planning/phases/29-breaks-you-can-see/shots/work-chunk-fit.png</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/29-breaks-you-can-see/shots/work-chunk-fit.png` exists, is non-empty, and
      shows a non-live 25-minute work chunk. The chunk and the instant are named in the summary.
    - `measure_card_extent.py`, bounded to that chunk's 100dp slot, printed a band list and an ink
      extent, both pasted verbatim into the summary.
    - The summary states one of exactly two verdicts — **DISMISSED (harness artifact)** or **REAL
      DEFECT** — with the measured extent, the slack or overflow in dp, and whether the card's
      bottom border band was found inside the window.
    - The screenshot was opened with the Read tool and the summary records, as an observation,
      whether the card's bottom edge and full content are uncut.
    - `kPixelsPerMinute`'s doc comment gained a dated 2026-08-20 sentence recording this
      re-confirmation (or the contradiction), and
      `grep -c "kPixelsPerMinute = 4.0" lib/screens/today/timeline_geometry.dart` still returns `1`
      — the value itself did not move (D-03).
    - If the fix branch fired: `git diff lib/screens/schedule/widgets/chunk_card.dart` shows a change
      only inside the `contentPadding` `full` arm, the full suite is green, and the re-measured
      extent is recorded.
    - `git diff --exit-code pubspec.yaml pubspec.lock` passes.
  </acceptance_criteria>

  <done>ROADMAP item 4 has a real-browser number and an explicit written disposition; the question is closed on evidence rather than on the document's prediction.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT — can a person see that this is a break?</name>

  <read_first>
    - `CLAUDE.md` § "Local hosting for UAT" and the three traps — the human must not be handed a
      stale bundle or a wrong-build port.
    - `.planning/phases/29-breaks-you-can-see/29-VALIDATION.md` § "Manual-Only Verifications" — the
      two rows that route to a human and the reasons each cannot be automated.
    - `.planning/phases/27-true-grid/27-04-PLAN.md` Task 3 — the format of this project's last human
      checkpoint, and `27-04-SUMMARY.md` for what happened when 2 of its 3 items failed after a
      16/17 automated score.
    - `.planning/phases/29-breaks-you-can-see/29-03-SUMMARY.md` and this plan's Task 1/Task 2
      summaries — the numbers to state in `<what-built>` so the human knows what has and has not
      already been checked.
  </read_first>

  <files>.planning/phases/29-breaks-you-can-see/29-UAT.md</files>

  <action>
Automate everything up to the human's eye, then stop and ask.

**Pre-flight, before presenting anything.** Confirm the debug build on port 8143 is still running
and still current: if anything changed in Task 2, rebuild with
`flutter build web --debug --source-maps --pwa-strategy=none` and re-serve with
`python3 tools/serve-uat.py 8143 --dir build/web`. Then re-run the sha256 equality check between
the served and built `main.dart.js` and record both hashes in `29-UAT.md`. The human must never be
asked to judge a bundle that is not the one this phase built — that failure has cost this project a
round trip before.

Create `.planning/phases/29-breaks-you-can-see/29-UAT.md` with the three items below as headed
sections, each with an empty `**Verdict:**` line, and the pre-flight hashes at the top. Making the
*recording* mechanical is the point: the judgement is not automatable, but the artifact is.

Then present the `<what-built>` and `<how-to-verify>` blocks verbatim and **WAIT**. Do not
self-approve any item. Do not substitute your own screenshot inspection — you already did that in
Tasks 1 and 2, and Phase 27 demonstrated it is not the same evidence: it scored 16/17 automated and
then failed 2 of 3 human items, both of which had passed everything automated.

Record the human's verdict on each of the three items **verbatim** in `29-UAT.md` and in the
summary, including a plain pass. If item 1 fails — a 5-minute break still does not read as a break —
the agreed remedies are, in order: raise the label's contrast or weight within the existing
`onSurfaceVariant`/`bodySmall` contract, or revisit the sub-compact layout. The remedies that are
**off the table** are raising `kPixelsPerMinute` (D-03, rejected on evidence) and letting the card
exceed its slot (D-05, the trade Phase 27 existed to eliminate). Route any failure to a gap-closure
plan rather than fixing it inside this checkpoint.
  </action>

  <what-built>
    Every 5-minute break in the day now renders as a **hairline with a centred label** — a thin rule
    running left and right of the words "Short break" — instead of a card whose top edge was all
    that survived a 20dp slot. Long breaks are unchanged: they still get the full dashed-outline
    card with their duration.

    The rule that picks between them is slot height, not chunk type, so it stays correct if the
    break durations ever change. Its threshold, `kSubCompactBreakMinHeight`, was measured in a real
    browser on this port rather than estimated — the constant's doc comment records the number, the
    method, and what would invalidate it.

    Already verified automatically, so you do not need to re-check it: the timeline's hour spacing
    is still uniform with the new tier rendering (`measure_hours.py`: `UNIFORM`), the sub-compact
    row's painted ink fits inside its 20dp slot, and the 25-minute work card's own fit inside its
    100dp slot has been re-measured in the browser.

    What automation **cannot** tell us, and why you are being asked: `flutter test`'s font has no
    real glyph metrics, so it can call a break legible when it is not; and headless Chromium can
    blank the canvas outright and return a screenshot that reads as either a pass or a false
    failure. Phase 27 scored 16 of 17 automated checks and then failed 2 of 3 on exactly this kind
    of look.
  </what-built>

  <how-to-verify>
    Open `http://danserver:8143/` — on a phone or tablet if you can, since that is where a 20dp row
    is hardest to read. Generate a day from the morning check-in at any mood; every mood's lattice
    contains 5-minute breaks.

    1. **Does a 5-minute break read as a break?** This is the whole phase. Scroll through the
       morning. Between two work cards there should be a thin line with "Short break" sitting on it.
       The question is not "can I find it" — it is: glancing at the day, does that row say *break*,
       or does it read as a divider, a separator, or the edge of the card above it? If it reads as a
       divider, say so plainly; that is the exact complaint that opened this phase ("there's no 5
       minute breaks in between") and a near-miss is a fail here.
    2. **Is the label actually legible at this size?** Look at the words themselves, not the layout.
       Is "Short break" cut off at the top or bottom, crowded by the cards above and below, or too
       faint against the background? Try it once at a larger system text scale — that is the case
       automation is least able to judge. If the text is fine but feels weak, say which: too small,
       too pale, or too tight.
    3. **Does the day still look right around it?** Scroll a full day. Hour brackets should all look
       the same height. Nothing should overlap, no card should be clipped mid-glyph, and a
       30-minute long break should still look clearly heavier than a 5-minute one. If a long break
       and a short break ever sit next to each other, they should not read as the same thing.

    Worth a glance while you are there: what a 5-minute break looks like while it is the *current*
    activity (the live row has its own separate treatment from Phase 27 and is deliberately not
    changed by this phase) — if the two now disagree in a way that looks wrong, that is worth
    recording even though it is out of scope.
  </how-to-verify>

  <resume-signal>Reply with a verdict for each of the three items — "1 pass, 2 pass, 3 pass", or describe what is wrong. Type "approved" only if all three pass.</resume-signal>

  <verify>
    <automated>curl -s http://localhost:8143/main.dart.js | sha256sum; sha256sum build/web/main.dart.js</automated>
  </verify>

  <acceptance_criteria>
    - The two sha256 hashes printed by the verify command are **identical**, and both are recorded
      at the top of `.planning/phases/29-breaks-you-can-see/29-UAT.md`.
    - `.planning/phases/29-breaks-you-can-see/29-UAT.md` exists and contains three headed items,
      each with a `**Verdict:**` line that is non-empty after the human responds.
    - `grep -c "Verdict" .planning/phases/29-breaks-you-can-see/29-UAT.md` returns `3`.
    - The human's response is recorded verbatim, per item, in both `29-UAT.md` and the summary —
      including a plain pass. No item is marked passed on the basis of a screenshot the agent
      looked at.
    - If any item failed, the summary names the follow-up and states explicitly that neither
      `kPixelsPerMinute` (D-03) nor the slot height (D-05) is an available remedy.
    - The phase is NOT recorded as complete until this task's verdict exists. A green suite,
      `UNIFORM`, and a fitting extent together do not close it (`29-VALIDATION.md` phase gate; D-07).
  </acceptance_criteria>

  <done>A human has opened the served build, judged all three items, and their verdict is recorded verbatim in `29-UAT.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local HTTP (`serve-uat.py` on 8143) → headless Chromium, and → the human's phone over Tailscale | A debug web bundle is reachable on the tailnet for the duration of UAT. No secrets, no auth, a synthetic onboarded profile only. |
| screenshot / served build → recorded verdict | The integrity question is provenance: is the artifact being judged the artifact this phase produced? |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05 | Spoofing (of build state) | served `main.dart.js` vs `build/web/main.dart.js` | mitigate | sha256 equality asserted after the rebuild in Task 1 and again as Task 3's pre-flight, with both hashes written into `29-UAT.md`. A human verdict on a stale bundle is worse than no verdict. |
| T-29-06 | Information Disclosure | debug UAT server on 8143 left running | mitigate | Loopback/Tailscale only; this box has no public path. Layout-only build, no secrets. Stop the server once the verdict is recorded and note it in the summary. |
| T-29-08 | Repudiation | the human verdict | mitigate | Recorded verbatim per item in a committed `29-UAT.md`, including plain passes, so "the human approved it" is an artifact rather than a recollection. The checkpoint is never auto-approved regardless of `mode: yolo`. |
| T-29-01 | Tampering | `pubspec.yaml` / `pubspec.lock` | mitigate | No package installed this phase; `git diff --exit-code` asserted in Task 2. |

ASVS L1 V2/V3/V4/V5/V6 do not apply — no accounts, no sessions, no server-side state, no new input
surface. This phase is pure client-side layout.
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Created by **this plan** (29-04):

| Symbol / path | Kind | Location |
|---|---|---|
| `uniform-subcompact-break.png` | evidence screenshot (SEEBREAK-02 pixel proof) | `.planning/phases/29-breaks-you-can-see/shots/` |
| `work-chunk-fit.png` | evidence screenshot (ROADMAP item 4 / D-06) | `.planning/phases/29-breaks-you-can-see/shots/` |
| `29-UAT.md` | human verdict record, three items | `.planning/phases/29-breaks-you-can-see/` |
| dated re-confirmation sentence in `kPixelsPerMinute`'s doc comment | comment-only change | `lib/screens/today/timeline_geometry.dart` |
| (conditional) tightened `full`-tier `contentPadding` | modified switch arm — only if D-06 finds a real defect | `lib/screens/schedule/widgets/chunk_card.dart` |

Created by 29-01: `ChunkCardDensity.subCompact`, `_SubCompactRow`, both `_WorkChunkContent` switch
arms, `kSubCompactBreakMinHeight` (placeholder), `_pumpBreakCardUnbounded`, 8 tests,
`29-RED-subcompact.txt`.
Created by 29-02: `_buildBreak`'s `subCompact` branch, the three-band break ternary,
`29-GREEN-final.txt`.
Created by 29-03: `tools/measure_card_extent.py`, `shots/compact-long-break-forced.png`, the
measured value and rewritten doc comment for `kSubCompactBreakMinHeight`, port 8143 claimed as
debug-only.
</artifacts_this_phase_produces>

<verification>
- Served bytes hash-identical to the built bundle, checked after the rebuild and again before the
  human is asked.
- `measure_hours.py` prints `UNIFORM` with a sub-compact break on screen; full band/spacing output
  recorded; no `132.0` spread anywhere.
- Sub-compact ink extent ≤ 20 in a real browser, with the screenshot looked at.
- ROADMAP item 4 carries an explicit DISMISSED or REAL DEFECT verdict with its number.
- `29-UAT.md` holds three non-empty human verdicts.
- Full suite green and `flutter analyze` clean if any `lib/` change was made in Task 2.
</verification>

<success_criteria>
- SEEBREAK-02 is proven in painted pixels, not only in arithmetic.
- SEEBREAK-01's fit claim rests on a real browser, and its legibility claim rests on a person.
- ROADMAP item 4 is closed on evidence.
- The phase closes only after a recorded human verdict (D-07).
</success_criteria>

<output>
Create `.planning/phases/29-breaks-you-can-see/29-04-SUMMARY.md` when done.
Record: both sha256 hashes at each check; `measure_hours.py`'s full output; the sub-compact ink
extent and the Read-tool observations; the ROADMAP item 4 verdict with its number; the human's
verbatim verdict per item; and confirmation that the background server was stopped.
</output>
