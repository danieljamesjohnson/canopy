---
phase: 07-unbreak-the-morning
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - lib/screens/home/home_screen.dart
  - lib/screens/schedule/schedule_screen.dart
  - test/screens/cold_launch_morning_loop_test.dart
autonomous: false
requirements: [LOOP-01, LOOP-03]
must_haves:
  truths:
    - "When a schedule already exists, Home shows a persistent, always-visible 'Re-check-in / Regenerate' action that routes to /schedule/checkin."
    - "When a schedule already exists, the Schedule screen shows the same persistent 'Re-check-in / Regenerate' action (not hidden in the overflow menu)."
    - "An automated widget test reproduces the cold-launch path (check-in mood tap WITHOUT visiting Goals/Commitments tabs) and asserts the generated schedule contains the user's real seeded goal."
    - "The full existing test suite plus the new regression test pass under Flutter 3.44.1."
  artifacts:
    - path: "test/screens/cold_launch_morning_loop_test.dart"
      provides: "Cold-launch regression test locking LOOP-01 closed"
      contains: "pumpWidget"
    - path: "lib/screens/home/home_screen.dart"
      provides: "Persistent re-check-in/regenerate entry point on the populated Home state"
      contains: "/schedule/checkin"
    - path: "lib/screens/schedule/schedule_screen.dart"
      provides: "Persistent re-check-in/regenerate entry point on the populated Schedule state"
      contains: "/schedule/checkin"
  key_links:
    - from: "test/screens/cold_launch_morning_loop_test.dart"
      to: "ScheduleNotifier.generateToday via CheckinScreen mood tap"
      via: "in-memory GoalsNotifier/CommitmentsNotifier seeded via the Plan 01 injectable seam, NO Goals/Commitments tab visited"
      pattern: "generateToday|goalId"
    - from: "lib/screens/home/home_screen.dart"
      to: "context.push('/schedule/checkin')"
      via: "always-visible action when hasScheduleToday"
      pattern: "/schedule/checkin"
    - from: "lib/screens/schedule/schedule_screen.dart"
      to: "context.push('/schedule/checkin')"
      via: "always-visible action when hasScheduleToday"
      pattern: "/schedule/checkin"
---

<objective>
Add the persistent re-check-in / regenerate entry point (LOOP-03) to both Home and Schedule when a schedule exists, and add the cold-launch regression test that locks LOOP-01 closed.

Purpose: Once a schedule exists, the user currently has no path back into check-in (the empty-state buttons no longer render). This plan adds a prominent, always-visible entry point on both surfaces. It also adds the automated regression test the phase explicitly requires — reproducing the cold-launch morning path and asserting the generated schedule contains the user's real goals, so the B1 bug cannot silently regress.

Output: A visible re-check-in/regenerate action on the populated Home and Schedule screens, plus `test/screens/cold_launch_morning_loop_test.dart` that fails if generation ever reads empty data again.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-unbreak-the-morning/07-CONTEXT.md
@.planning/phases/07-unbreak-the-morning/07-01-PLAN.md
@lib/screens/home/home_screen.dart
@lib/screens/schedule/schedule_screen.dart
@test/test_helpers/mood_pump.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add persistent re-check-in / regenerate entry point on Home and Schedule</name>
  <files>lib/screens/home/home_screen.dart, lib/screens/schedule/schedule_screen.dart</files>
  <read_first>
    - lib/screens/home/home_screen.dart (populated state build() lines 64-163 — has an AppBar 'Canopy' at line 87 and renders content when hasScheduleToday; empty state lines 165-212 already pushes '/schedule/checkin' at lines 198/200)
    - lib/screens/schedule/schedule_screen.dart (populated state lines 25-79 — AppBar 'Today' line 42-60 currently exposes the summary only via a PopupMenuButton gated at >=50% resolved lines 47-58; empty state pushes '/schedule/checkin' lines 147/179)
    - lib/router.dart (route '/schedule/checkin' exists at lines 71-74; `context.push('/schedule/checkin')` is the established navigation call used in both screens' empty states)
  </read_first>
  <action>
    On the populated Home state (the `hasScheduleToday` branch, build() around lines 86-162), add a prominent, always-visible re-check-in/regenerate affordance — e.g. an AppBar action `IconButton` (Icons.refresh) or a labelled button — that calls `context.push('/schedule/checkin')`. It must render whenever a schedule exists, not be hidden in an overflow menu (per CONTEXT.md "prominent, always-visible button"). Use a clear tooltip/label such as "Re-check-in".

    On the populated Schedule state, add the same affordance to the 'Today' AppBar `actions` (alongside or replacing reliance on the existing >=50% PopupMenuButton) so it is visible regardless of completion ratio; it calls `context.push('/schedule/checkin')`. Keep the existing 'View your day' summary menu item available. Exact widget placement and label are at implementation discretion guided by existing conventions; the requirement is that the entry point is always reachable when a schedule exists.

    Do not change the empty-state buttons (they already route correctly). `generateToday` already supports silent replace, so re-running check-in regenerates today's schedule — no notifier changes needed.
  </action>
  <verify>
    <automated>flutter analyze 2>&1 | grep -v '^#' | grep -iE "error" ; test ${PIPESTATUS[1]} -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `lib/screens/home/home_screen.dart` populated (`hasScheduleToday`) branch contains a `context.push('/schedule/checkin')` action that renders unconditionally when a schedule exists (grep `/schedule/checkin` now appears in the populated build path, not only the empty state).
    - `lib/screens/schedule/schedule_screen.dart` populated branch's AppBar exposes a `context.push('/schedule/checkin')` action not gated behind the >=50% resolved condition.
    - `flutter analyze` reports zero errors.
  </acceptance_criteria>
  <done>Both Home and Schedule, when a schedule already exists, show an always-visible action that re-enters check-in and regenerates today's schedule. flutter analyze clean.</done>
</task>

<task type="auto">
  <name>Task 2: Cold-launch morning-loop regression test (locks LOOP-01)</name>
  <files>test/screens/cold_launch_morning_loop_test.dart</files>
  <read_first>
    - test/test_helpers/mood_pump.dart (pumpWithMood(tester, child, extraProviders:) constructs a MaterialApp with mood-seeded theme and an optional MultiProvider — note it disables ThemeNotifier ticker by construction; use/adapt this pattern)
    - test/repositories/goal_repository_test.dart (lines 1-24: `InMemoryGoalRepository implements GoalRepository` — copy this in-memory repo pattern; build an equivalent in-memory CommitmentBlockRepository implementing its interface)
    - lib/providers/goals_notifier.dart (after Plan 01: accepts an injectable GoalRepository; loadGoals() populates `.goals`)
    - lib/providers/commitments_notifier.dart (after Plan 01: accepts an injectable CommitmentBlockRepository; loadBlocks())
    - lib/providers/schedule_notifier.dart (generateToday reads goals/blocks args; the generator returns chunks with goalId set for goal-derived chunks)
    - lib/screens/schedule/checkin_screen.dart (lines 55-59: _generate() reads context.read<GoalsNotifier>().goals / CommitmentsNotifier().blocks and calls generateToday; mood emoji tap at lines 145-156; "Let's go" confirm button lines 198-225)
    - lib/data/models/goal.dart and lib/data/models/scheduled_chunk.dart (Goal has id/name/color/goalTypeIndex; ScheduledChunk exposes goalId and chunkType)
  </read_first>
  <action>
    Create a widget test that reproduces the cold-launch path WITHOUT ever pumping the Goals or Commitments tab screens. Steps: (1) construct a GoalsNotifier with an in-memory GoalRepository pre-seeded with a known goal (e.g. a habit named "Meditate" with a fixed color) and call `loadGoals()`; construct a CommitmentsNotifier with an in-memory CommitmentBlockRepository (empty is fine) and call `loadBlocks()`. Note: ScheduleNotifier uses hardcoded Hive repos, so initialize a temp Hive for the test — call the project's Hive init (register adapters + open boxes against a `Directory.systemTemp` path via `Hive.init`) in setUp, and `Hive.deleteFromDisk`/close in tearDown. (2) Pump `CheckinScreen` inside a provider tree supplying the seeded GoalsNotifier, CommitmentsNotifier, a real ScheduleNotifier (init'd), and a ThemeNotifier with `timeModulationEnabled: false` — adapt pumpWithMood's MultiProvider approach. Do NOT pump GoalsScreen/CommitmentsScreen at any point. (3) Tap a mood emoji, then tap the "Let's go" confirm button, and pump/settle. (4) Assert the resulting `ScheduleNotifier.todaySchedule` is non-null and that its chunks include at least one chunk whose `goalId` equals the seeded goal's id — proving the generator received the real saved goal, not an empty list.

    Add a descriptive test name like "cold launch check-in generates schedule from real saved goals without visiting Goals tab". The assertion on `goalId` is the lock: if startup loading regresses to empty lists, the generated schedule has no goal-derived chunk and the test fails. Keep the test deterministic — no Timer.periodic, no real notifications (NotificationService.requestIOSPermissions is a no-op off-Darwin; if it interferes, stub or guard around it).
  </action>
  <verify>
    <automated>flutter test test/screens/cold_launch_morning_loop_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `test/screens/cold_launch_morning_loop_test.dart` exists and never instantiates GoalsScreen or CommitmentsScreen (grep: those class names do not appear in the test).
    - The test seeds a goal via an in-memory repo + loadGoals(), pumps CheckinScreen, taps a mood + confirm, and asserts a chunk's `goalId` matches the seeded goal id.
    - `flutter test test/screens/cold_launch_morning_loop_test.dart` passes under Flutter 3.44.1.
    - `flutter test` (full suite) passes — the existing 90 tests plus this new one stay green.
  </acceptance_criteria>
  <done>A failing-if-regressed widget test reproduces the exact cold-launch morning path and asserts real goals flow into generation; full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Wave 1 + Wave 2 changes: startup data loading, day rollover, go_router-correct + auto-scheduled morning notification (Linux/web-guarded), goal-form cursor fix, persistent re-check-in entry points, and the cold-launch regression test.
  </what-built>
  <how-to-verify>
    On-device iOS is the daily driver and cannot be automated on this Linux box, so verify the notification firing/tap path manually on a physical iPhone (or trusted simulator):
    1. Fresh install / first launch → complete onboarding with at least one goal. Confirm no Settings toggle is touched.
    2. In Settings, confirm the morning notification time; wait for (or set near-term) the morning notification to fire.
    3. Tap the notification → app opens to the schedule (or check-in if no schedule) via go_router with NO crash.
    4. On a day with an existing schedule, confirm both Home and the Schedule screen show a visible "Re-check-in / Regenerate" action; tap it → check-in reopens and regenerates.
    5. Background the app overnight (or change device date to next day) and resume → app shows the un-generated "Start your day" state, not yesterday's schedule.
    6. In the goal form, type into "Weekly hours target" and the outcome "Description" fields → cursor stays put, no jump to position 0.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issue (e.g. notification did not fire, tap crashed, rollover showed stale schedule, cursor still jumped).</resume-signal>
</task>

</tasks>

<verification>
- `flutter analyze` reports zero errors.
- `flutter test` passes (existing 90 + new cold-launch regression test).
- The cold-launch regression test fails if startup data loading (Plan 01) is reverted — confirming it genuinely locks LOOP-01.
- Manual UAT checkpoint covers on-device iOS notification firing/tap and the rollover/entry-point/cursor behaviors that cannot be exercised on Linux CI.
</verification>

<success_criteria>
- LOOP-03: persistent, always-visible re-check-in/regenerate entry point on both Home and Schedule when a schedule exists.
- LOOP-01 (locked): automated regression test reproduces the cold-launch path and asserts real goals are present in the generated schedule.
- flutter analyze clean; full flutter test suite green; manual iOS UAT approved.
</success_criteria>

<output>
Create `.planning/phases/07-unbreak-the-morning/07-02-SUMMARY.md` when done.
</output>
