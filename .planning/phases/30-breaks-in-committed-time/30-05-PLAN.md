---
phase: 30-breaks-in-committed-time
plan: 05
type: execute
wave: 4
depends_on: ["30-04"]
files_modified:
  - CLAUDE.md
  - .planning/phases/30-breaks-in-committed-time/30-UAT.md
autonomous: false
requirements: [COMMITBREAK-01, COMMITBREAK-02]

estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The owner, looking at a re-checked-in day in a real browser, can see breaks between the work chunks inside his committed Work block."
    - "The committed block still starts and ends at the times he entered."
    - "Any future agent reading CLAUDE.md learns trap #4 before running a UAT on an engine change, so the 2026-08-21 false failure cannot repeat."
  artifacts:
    - "CLAUDE.md — trap #4 (data layer) promoted into the UAT trap list, with its cost recorded"
    - ".planning/phases/30-breaks-in-committed-time/30-UAT.md — the human verdict that closes the phase"
  key_links:
    - "The UAT instructions MUST contain the ⟳ Re-check-in step. ScheduleNotifier._loadToday() reads today's schedule from Hive and generate() runs only at check-in, so an already-generated day is never regenerated on load — omitting this step produced a false failure on 2026-08-21 that cost a round trip and let the real defect survive three more days."
    - "Port 8143 is debug-only (CLAUDE.md trap #1: a service worker left by a release build on the same origin serves a cached shell forever). Never serve a release build on it."
    - "This same screenshot also unblocks Phase 29's deferred UAT item — a day built on a committed Work block finally has breaks in it to look at — but Phase 29's verdict is recorded separately via /gsd-verify-work 29, not here."
---

<objective>
Close the phase the way it has to be closed: a generated-day screenshot from the owner, and the
durable documentation fix that stops the next agent repeating the miss.

Purpose: the arithmetic is trustworthy — this is integer minute math, not glyph metrics, the same
category as Phase 28, which needed no human gate. The human checkpoint exists for one reason only:
**the owner has reported this symptom twice** (2026-08-21 and 2026-08-24) and a screenshot of a real
generated day is what closes it credibly. The first report was misdiagnosed as stale Hive data
because the UAT omitted the re-check-in step; the second was the real defect, three days later. Trap
#4 is the durable fix for that miss, and it is cheap.

Output: CLAUDE.md gains trap #4; `30-UAT.md` carries the verdict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-breaks-in-committed-time/30-VALIDATION.md
@.planning/phases/30-breaks-in-committed-time/30-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote trap #4 (data layer) into CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md lines 50-125 (the "Local hosting for UAT" section, the build/serve commands, and the "Three traps that fake a broken build" list — match its numbering, bolding and prose voice exactly)
    - lib/providers/schedule_notifier.dart — `_loadToday` and the check-in generation path, to state the mechanism correctly rather than from memory
    - .planning/ROADMAP.md § "Phase 30" (the paragraph beginning "A human UAT checkpoint is required" — it flags trap #4 as "to be promoted into CLAUDE.md")
  </read_first>
  <action>
Add a fourth trap to CLAUDE.md's UAT trap list.

Retitle the section heading and its lead-in sentence so the count and the framing are both right:
the list currently promises three traps and says traps #1 and #2 fake a blank page while trap #3
fakes a missing feature. Trap #4 also fakes a missing feature, but at the data layer rather than the
transport layer — the served bytes are correct and even trap #3's `curl | grep` diagnosis will
confirm they are, which is precisely why it is so convincing. Say that explicitly, because an agent
who has just run the trap #3 check will otherwise treat a clean result as proof the feature is
missing.

State the mechanism, not just the remedy: `ScheduleNotifier._loadToday()` reads today's schedule
from Hive, and `ScheduleGeneratorService.generate()` runs only at check-in, so a day that has
already been generated is never regenerated on load. Any engine change is therefore invisible in the
running app until the user taps ⟳ Re-check-in.

Record what it cost, the way the other traps do: on 2026-08-21 a UAT of an engine change omitted this
step, produced a false failure, cost the owner a round trip, and let the real defect survive three
more days until he reported the same symptom a second time on 2026-08-24.

Give the remedy as a rule, not a suggestion: any UAT that judges generator output must ⟳ Re-check-in
first, and any plan that writes such a UAT must put that step in the UAT's own instructions.

Do not restructure or reword traps #1-#3, and do not touch any other section of CLAUDE.md.
  </action>
  <acceptance_criteria>
    - CLAUDE.md's trap-list heading names four traps, and its lead-in sentence attributes the blank-page symptom to traps #1-#2 and the missing-feature symptom to traps #3-#4.
    - A fourth numbered item exists containing all four of: the string `_loadToday`, the string `Re-check-in`, the date `2026-08-21`, and an explicit statement that the served bytes are correct in this case so trap #3's diagnosis will not detect it.
    - `grep -c "Re-check-in" CLAUDE.md` is at least 1 and `grep -c "_loadToday" CLAUDE.md` is at least 1.
    - `git diff CLAUDE.md` shows changes confined to the trap-list heading, its lead-in sentence, and the appended fourth item — no edits inside items 1, 2 or 3.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "_loadToday" CLAUDE.md && grep -c "Re-check-in" CLAUDE.md && git diff --stat CLAUDE.md</automated>
  </verify>
  <done>Trap #4 is in CLAUDE.md with its mechanism, its cost and its rule, and traps #1-#3 are byte-identical.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Human UAT — see the breaks inside a committed block, in a real browser</name>
  <files>.planning/phases/30-breaks-in-committed-time/30-UAT.md</files>
  <what-built>
Breaks now land between the work chunks *inside* a committed block, on the same 25-minutes-of-work
plus 5-minutes-of-break rhythm the rest of the day already used — and, on a long enough block, a
full 30-minute break at the mood's cadence. The block's own start and end times were not touched.

Before this phase, a committed `Work` block was a solid wall of back-to-back 25-minute chunks with
no breaks at all: the scheduling engine only ever applied the lattice to your *discretionary* time,
and the commitment path had never been tested. That is the thing you reported twice.

The same fix now also applies when you add an event mid-day from the Today screen, not just when a
day is generated at check-in — that was a second copy of the same bug in a different file.
  </what-built>
  <how-to-verify>
**Open `http://danserver:8143/` on your phone, tablet or laptop.**

**STEP 0 — MANDATORY, DO THIS FIRST: tap ⟳ Re-check-in.**
Nothing below is meaningful until you do. The app reads today's schedule from Hive and only runs the
scheduling engine at check-in, so a day that was already generated is never rebuilt on load — you
would be judging a pre-fix day. This is exactly what happened on 2026-08-21: a false failure, a
wasted round trip, and the real bug survived another three days. Re-check-in first. Every time.

**STEP 1 — make sure today has a committed block.** If your day has no `Work` block (or similar)
spanning at least a couple of hours, add one on the Commitments screen and re-check-in again. This
whole phase is about what happens *inside* a committed block, so a day without one proves nothing.

**Then judge these four things and say yes or no to each:**

1. **Breaks inside the block.** Between the work chunks inside your committed block, is there a
   break? There should be a short break after every 25 minutes of work — the same rhythm the rest of
   your day already has.
2. **Your appointment did not move.** Does the block still start and end at exactly the times you
   entered? Not a minute earlier, not a minute later, not rounded to the half hour. This is the one
   thing that must not have changed.
3. **A long break, if the block is long enough.** On a block of roughly four hours or more, is there
   also one full 30-minute break partway through (two of them on a six-hour block, at a normal mood)?
   If your block is short, skip this one and say so.
4. **The 5-minute breaks read as breaks.** Can you tell at a glance that a 5-minute break is a break
   — not a divider, not a sliver, not a mystery line? (This one is Phase 29's outstanding question,
   which has been waiting for a day that actually has breaks in it to look at. Answer it here; it
   gets recorded against Phase 29 separately.)

**A screenshot of the generated day is the thing that closes this.** You have reported this symptom
twice; the arithmetic is proven in tests, but the screenshot is what makes it credible.

If anything reads wrong, say what you saw rather than what you think caused it — and note whether
you did Step 0.
  </how-to-verify>
  <resume-signal>
Reply with a yes/no for each of the four items (item 3 may be "skipped — block too short"), plus
confirmation that you tapped ⟳ Re-check-in first. Say **"approved"** to close Phase 30, or describe
what you saw for anything that reads wrong — that gets routed to a gap-closure plan, not patched
inline.
  </resume-signal>
  <precondition>Port 8143 has only ever served DEBUG builds (CLAUDE.md trap #1 — a service worker left by a release build on the same origin serves a cached shell forever). If any release build has ever been served on 8143, pick a fresh unused port and record the substitution.</precondition>
  <read_first>
    - CLAUDE.md § "Local hosting for UAT" (the exact build and serve commands, and why `python3 -m http.server` is forbidden here)
    - CLAUDE.md § the trap list, including trap #4 as landed by Task 1
    - .planning/phases/30-breaks-in-committed-time/30-04-SUMMARY.md (what actually shipped, so the verdict is judged against it)
    - .planning/STATE.md § "Deferred Verification" (Phase 29's UAT is judged after this phase lands, separately)
  </read_first>
  <action>
Build and serve the app, then hand the owner the URL and the checklist below. Do not proceed past
this task without a recorded verdict.

Build a single-bundle DEBUG build and serve it statically. Do not use `flutter run -d web-server`
(its DDC incremental compiler fans the app into ~866 module files, which the 6-connections-per-origin
limit turns into minutes of serial round-trips over tailscale). Do not use `python3 -m http.server`
(it sends no `Cache-Control`, so the browser may keep serving a two-day-old `main.dart.js` —
CLAUDE.md trap #3). Use `tools/serve-uat.py`, which sends `no-store` and strips the conditional-request
headers.

Run, with `flutter` on PATH via the project's toolchain note:
`flutter build web --debug --source-maps --pwa-strategy=none`, then
`python3 tools/serve-uat.py 8143 --dir build/web` in the background.

Before handing over, confirm the served bytes actually contain this phase's change, so a negative
verdict can be attributed to behaviour rather than to a stale bundle:
`curl -s http://danserver:8143/main.dart.js | grep -c buildCommitmentChunks` must be non-zero.

Then present the owner with the checklist verbatim, with the re-check-in step FIRST and marked
mandatory. Record his verdict in `.planning/phases/30-breaks-in-committed-time/30-UAT.md`.
  </action>
  <acceptance_criteria>
    - `flutter build web --debug --source-maps --pwa-strategy=none` completed and `build/web/main.dart.js` exists.
    - The server was started with `tools/serve-uat.py` on port 8143 (or the recorded substitute) — not `python3 -m http.server`, and not `flutter run -d web-server`.
    - `curl -s http://danserver:8143/main.dart.js | grep -c buildCommitmentChunks` returned a non-zero count before the owner was asked to judge.
    - `.planning/phases/30-breaks-in-committed-time/30-UAT.md` exists and records: the date, the port, an explicit confirmation that Step 0 (⟳ Re-check-in) was performed, a yes/no for each of the four checklist items, and the owner's verbatim words for anything he flagged.
    - If any item is a NO, `30-UAT.md` records the observed behaviour rather than a diagnosis, and the gap is routed to a gap-closure plan rather than fixed inline.
    - Item 4's answer is recorded with a note that it also answers Phase 29's deferred UAT question and that Phase 29's verdict is filed via `/gsd-verify-work 29`.
  </acceptance_criteria>
  <verify>
    <human-check>The owner has opened the served debug build, re-checked-in, and given a yes/no on all four checklist items, recorded in `30-UAT.md`.</human-check>
  </verify>
  <done>A human verdict exists in `30-UAT.md` covering all four items, with Step 0 confirmed performed.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and files Phase 30 creates. This plan produces the rows marked **[30-05]**.

| Symbol / file | Kind | Plan |
|---|---|---|
| `ScheduleGeneratorService.buildCommitmentChunks(CommitmentBlock, {required int longBreakEvery})` | public static method, `lib/services/schedule_generator.dart` | 30-03 |
| `ScheduleGeneratorService.breakCadenceForMood(int moodIndex)` | public static method, same file | 30-03 |
| Narrowed STEP E trim (`commitmentId == null`) | behavior change, same file | 30-03 |
| `.planning/phases/30-breaks-in-committed-time/30-GREEN-wave2.txt` | evidence artifact | 30-03 |
| Narrowed `ScheduleNotifier._trimTrailingNonWork` (`commitmentId == null`) | behavior change, `lib/providers/schedule_notifier.dart` | 30-04 |
| `addEventToday` delegating to `buildCommitmentChunks` | behavior change, same file — the duplicate loop is deleted | 30-04 |
| `.planning/phases/30-breaks-in-committed-time/30-GREEN-final.txt` | evidence artifact | 30-04 |
| `group('COMMITBREAK — breaks inside a committed block')` + its 6 tests | test group, `test/services/schedule_generator_test.dart` | 30-01 |
| `.planning/phases/30-breaks-in-committed-time/30-RED-generator.txt` | evidence artifact | 30-01 |
| `COMMITBREAK-01/ADD-EVENT`, `COMMITBREAK-02/ADD-EVENT`, `COMMITBREAK-01/ADD-EVENT-TRIM` | 3 named tests, `test/providers/schedule_notifier_add_event_test.dart` | 30-02 |
| `_commitmentDay()` + `COMMITBREAK-01/RENDER` | fixture helper + widget test, `test/screens/lattice_break_pair_test.dart` | 30-02 |
| `.planning/phases/30-breaks-in-committed-time/30-RED-secondpath.txt` | evidence artifact | 30-02 |
| **`CLAUDE.md` § trap #4 (data layer)** | **documentation** | **[30-05]** |
| **`.planning/phases/30-breaks-in-committed-time/30-UAT.md`** | **UAT verdict record** | **[30-05]** |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tailnet → local static server | `tools/serve-uat.py` binds `0.0.0.0` on port 8143 to serve a debug build. Reachable only over Tailscale and the local LAN; no public exposure. |
| browser → service worker cache | A service worker left by a prior release build on the same origin can intercept requests and serve a stale shell (CLAUDE.md trap #1). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-08 | Information disclosure | debug web build served with source maps on `0.0.0.0` | low | accept | The host is reachable only over Tailscale and the LAN, single-user, and the app holds no secrets — its data is the owner's own local schedule. Debug-with-source-maps is a deliberate project decision (CLAUDE.md: diagnostics over speed during early UAT). The server is torn down after the checkpoint. |
| T-30-09 | Spoofing | a stale service worker or browser cache serving a pre-fix bundle and producing a false verdict | medium | mitigate | `--pwa-strategy=none` registers no service worker; `tools/serve-uat.py` sends `Cache-Control: no-store` and strips conditional-request headers; port 8143 is debug-only; and the `curl \| grep buildCommitmentChunks` gate proves the served bytes contain the change before the owner is asked to judge. |
| T-30-10 | Spoofing | stale Hive data producing a false verdict (trap #4) | high | mitigate | The dominant failure mode for this phase — it already caused one false failure on 2026-08-21. Mitigated by making ⟳ Re-check-in Step 0 of the checklist, marked mandatory, and by promoting the trap into CLAUDE.md in Task 1 so it outlives this phase. |

**No package-manager installs occur in this phase** — no `pubspec.yaml` change, no new dependency.
The `T-{phase}-SC` supply-chain row and its blocking human checkpoint are not applicable.
</threat_model>

<verification>
- `grep -c "_loadToday" CLAUDE.md` and `grep -c "Re-check-in" CLAUDE.md` are both non-zero.
- `git diff CLAUDE.md` shows no edits inside traps #1-#3.
- The debug build was served via `tools/serve-uat.py` on a debug-only port.
- `curl -s http://danserver:8143/main.dart.js | grep -c buildCommitmentChunks` was non-zero before the owner judged.
- `30-UAT.md` records the verdict, confirms Step 0 was performed, and answers all four checklist items.
</verification>

<success_criteria>
- Trap #4 lives in CLAUDE.md with its mechanism, its 2026-08-21 cost, and its rule.
- The owner has seen a re-checked-in generated day in a real browser and given a verdict.
- Any NO is routed to gap closure, not patched inline.
</success_criteria>

<output>
Create `.planning/phases/30-breaks-in-committed-time/30-05-SUMMARY.md` when done, and
`.planning/phases/30-breaks-in-committed-time/30-UAT.md` with the verdict.
</output>
</content>
