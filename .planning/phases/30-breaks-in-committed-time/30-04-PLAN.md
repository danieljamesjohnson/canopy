---
phase: 30-breaks-in-committed-time
plan: 04
type: execute
wave: 3
depends_on: ["30-03"]
files_modified:
  - lib/providers/schedule_notifier.dart
  - .planning/phases/30-breaks-in-committed-time/30-GREEN-final.txt
  - .planning/phases/30-breaks-in-committed-time/30-VALIDATION.md
autonomous: true
requirements: [COMMITBREAK-01, COMMITBREAK-02]

estimate:
  tokens: 48000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "An event added mid-day from the Today screen gets breaks between its work chunks on the same 25+5 lattice as a day generated at check-in — the owner's symptom cannot reproduce through the second entry point."
    - "The added event's own start and end minutes are unchanged (COMMITBREAK-02 on the second path)."
    - "ScheduleNotifier's trailing-non-work trim can no longer delete a commitment break, so a mid-day edit never erases persisted committed minutes."
    - "There is exactly ONE implementation of commitment-window chunking in the codebase, with two call sites."
    - "The whole suite is green, flutter analyze is clean, and no test was deleted."
  artifacts:
    - "lib/providers/schedule_notifier.dart — addEventToday delegates to ScheduleGeneratorService.buildCommitmentChunks; _trimTrailingNonWork narrowed to commitmentId == null"
    - ".planning/phases/30-breaks-in-committed-time/30-GREEN-final.txt — the phase's closing RED→GREEN cross-check and wave-boundary git proof"
  key_links:
    - "addEventToday's chunks flow into _todaySchedule.chunks and are rendered by the same timeline as generate()'s — the identical defect there produced the identical user-visible symptom, which is the whole reason D-30-03 pulled it into scope."
    - "_reflowDiscretionaryWork partitions on `anchoredStartMinutes != null`, so the new anchored break chunks land in `kept` and their spans become fixed occupancy windows — discretionary work cannot be reflowed into a commitment's internal break gap. This is the notifier's analogue of the generator's STEP B merge and needs no change."
    - "_trimTrailingNonWork runs BEFORE new chunks are inserted, so it can only eat a break left by a PREVIOUS addEventToday call — which is exactly the scenario COMMITBREAK-01/ADD-EVENT-TRIM builds."
---

<objective>
Close the second path: an event added mid-day from the Today screen gets the same breaks as one
generated at check-in, and the notifier's trailing trim stops being able to delete them.

Purpose: D-30-03 (recorded in full in `30-01-PLAN.md`) is a **deliberate scope extension beyond the
ROADMAP**, and this plan is where it lands. `addEventToday` carries its own copy of the identical
bare `cursor += 25` walk under a doc comment saying it "mirrors the commitment-anchoring step in
ScheduleGeneratorService.generate()". That mirroring comment is itself the drift vector — this plan
deletes the duplicate rather than repairing it, so the loop has one implementation and two call
sites. The steer for including it: this defect has been missed twice by scoping the investigation
too narrowly, and the owner has reported the symptom twice; leaving a known second source of the
same symptom in place invites a third report.

This plan touches only `lib/`. The tests it turns green were written and proven RED in wave 1
(`30-RED-secondpath.txt`).

Output: a shared-helper `addEventToday`, a narrowed `_trimTrailingNonWork`, and
`30-GREEN-final.txt`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-breaks-in-committed-time/30-01-PLAN.md
@.planning/phases/30-breaks-in-committed-time/30-03-SUMMARY.md
@.planning/phases/30-breaks-in-committed-time/30-RED-secondpath.txt
@lib/providers/schedule_notifier.dart
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: addEventToday calls the shared helper; the trailing trim stops eating commitment breaks</name>
  <files>lib/providers/schedule_notifier.dart</files>
  <precondition>Plan 30-03 is committed and `ScheduleGeneratorService.buildCommitmentChunks` / `breakCadenceForMood` exist as public static members of `lib/services/schedule_generator.dart`.</precondition>
  <reversibility rating="costly">Break chunks written here carry `commitmentId` and are persisted into Hive `DailySchedule` records; a code revert does not rewrite days already saved. Harmless (no reader treats `commitmentId` on a break specially — proven by `COMMITBREAK-01/RENDER`), but not undone by `git revert` alone.</reversibility>
  <read_first>
    - lib/providers/schedule_notifier.dart lines 214-343 (`addEventToday` in full: the idempotent stale-chunk removal, the `_trimTrailingNonWork` call at 265, the `!anchorsToday` early return, the bare `cursor += 25` walk at 282-303 with its "mirrors the commitment-anchoring step" doc comment, and both persist branches including the minimal-schedule branch's `moodIndex: 3` default at 334)
    - lib/providers/schedule_notifier.dart lines 345-369 (`_trimTrailingNonWork` — the loop that removes ALL trailing non-work chunks)
    - lib/providers/schedule_notifier.dart lines 371-464 (`_reflowDiscretionaryWork` — read to confirm the anchored-break spans land in `kept` and become fixed occupancy windows; it needs no change)
    - lib/services/schedule_generator.dart — `buildCommitmentChunks` and `breakCadenceForMood` as landed by plan 30-03, including their doc comments
    - .planning/phases/30-breaks-in-committed-time/30-01-PLAN.md § `<phase_decisions>` (D-30-02, D-30-03, D-30-04)
  </read_first>
  <action>
Replace `addEventToday`'s hand-rolled commitment walk with a call to
`ScheduleGeneratorService.buildCommitmentChunks(block, longBreakEvery: ...)`. Delete the local
`cursor` loop and the local tail-stretch block outright — do not leave a second copy behind for any
reason. Add the `schedule_generator.dart` import if the file does not already have it.

Source the cadence from today's own mood, not a constant: when a schedule for today already exists,
use its `moodIndex`; otherwise use 3, matching the default the minimal-schedule branch already
writes at line 334 for exactly this situation. Pass it through
`ScheduleGeneratorService.breakCadenceForMood`, which already carries the out-of-range fallback.

Rewrite the doc comment above the call. The current one says this code "mirrors the
commitment-anchoring step in ScheduleGeneratorService.generate()" — that mirroring is what let the
same defect live in two files, so the new comment must state that the chunking is now *delegated*,
name D-30-03 as the reason the duplicate was removed, and record that both entry points therefore
produce identical windows by construction. Keep the existing `newChunks.isEmpty` guard: the helper
returns an empty list for a window shorter than one 25-minute cell, which is the same degenerate
case the guard already handles.

Then narrow `_trimTrailingNonWork` (the notifier's analogue of the generator's STEP E, and the same
defect shape — D-30-02). Its loop currently removes every trailing chunk whose type is not `work`,
with no regard for origin, and it writes through to Hive, so once anchored breaks exist it can
durably delete minutes from inside a committed window. Add the `commitmentId == null` condition so
only discretionary breaks are trimmed. Do not otherwise change its behaviour — in particular do not
align it with STEP E's short-break-only rule; that asymmetry is pre-existing and out of scope.
Rewrite its doc comment to state the new condition and why, citing D-30-02.

Do not quote the removed loop's advance expression verbatim in any comment. An acceptance criterion
greps this file for its absence, so reciting the old code in prose would fail the gate while the
code itself is correct.

Do not touch `_reflowDiscretionaryWork`. Its `const longBreakEvery = 4` at line 389 governs
discretionary reflow, not commitment windows, and its `kept` partition already treats the new
anchored break chunks as fixed occupancy — verify that by reading it, and record the finding in the
SUMMARY rather than changing anything.
  </action>
  <acceptance_criteria>
    - `grep -c "cursor += 25" lib/providers/schedule_notifier.dart` returns 0, and `grep -c "buildCommitmentChunks" lib/providers/schedule_notifier.dart` returns at least 1.
    - `grep -c "breakCadenceForMood" lib/providers/schedule_notifier.dart` returns at least 1 — the cadence comes from today's mood, not a literal.
    - `_trimTrailingNonWork`'s loop condition includes `commitmentId == null`, and `grep -c "D-30-02" lib/providers/schedule_notifier.dart` is at least 1.
    - `grep -c "D-30-03" lib/providers/schedule_notifier.dart` is at least 1 — the delegation and its reasoning are documented at the call site.
    - Adding a `2:00pm–3:00pm` event to a mood-3 day produces exactly four chunks for that block — work@840/25, shortBreak@865/5, work@870/25, shortBreak@895/5 — each with `commitmentId == block.id`, and the block's own `startMinutes`/`endMinutes` still read 840/900.
    - A day whose chronologically-last chunk is an anchored shortBreak at 1195 with a non-null `commitmentId` still holds that chunk after an `addEventToday` call for a block dated to another day.
    - `export PATH="$PATH:/home/dan/development/flutter/bin" && flutter test test/providers/schedule_notifier_add_event_test.dart --concurrency=1` — all tests pass, including the three `ADD-EVENT` tests and the unchanged `CR-01`.
    - `export PATH="$PATH:/home/dan/development/flutter/bin" && flutter analyze` reports no issues.
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$PATH:/home/dan/development/flutter/bin" && flutter test test/providers/ --concurrency=1</automated>
  </verify>
  <done>There is one implementation of commitment-window chunking with two call sites; the mid-day path emits the same lattice as check-in; the trailing trim can no longer delete a commitment break.</done>
</task>

<task type="auto">
  <name>Task 2: Closing full-suite gate and the phase's RED→GREEN cross-check</name>
  <files>.planning/phases/30-breaks-in-committed-time/30-GREEN-final.txt, .planning/phases/30-breaks-in-committed-time/30-VALIDATION.md</files>
  <read_first>
    - .planning/phases/30-breaks-in-committed-time/30-RED-generator.txt and 30-RED-secondpath.txt (the RED side)
    - .planning/phases/30-breaks-in-committed-time/30-GREEN-wave2.txt (wave 2's cross-check — this task closes the remaining three rows)
    - .planning/phases/30-breaks-in-committed-time/30-VALIDATION.md § "Sampling Rate" and § "Validation Sign-Off"
  </read_first>
  <action>
Run the full suite at `--concurrency=1` and write the output to
`.planning/phases/30-breaks-in-committed-time/30-GREEN-final.txt`. Default concurrency drops and
duplicates per-test-name lines at this suite's scale, which makes the by-name cross-check
unverifiable.

Prepend the phase's closing cross-check table: every test named RED in `30-RED-generator.txt` and
`30-RED-secondpath.txt`, paired with its status now and the wave that cleared it. Every row must
read GREEN. Every test named GUARD must also read GREEN.

Prove the wave boundary again: `git diff --stat` across this plan's commits must show
`lib/providers/schedule_notifier.dart` and `.planning/` paths only — zero paths under `test/`. If
any test file was modified in this wave, the RED→GREEN claim is void; stop and report rather than
reconciling it.

Confirm the total is at least 587 tests (the pre-phase baseline) plus this phase's additions, and
that no test was deleted to reach green. Update `30-VALIDATION.md`'s per-requirement status rows from
pending to green and tick the sign-off boxes that are now true — leave the UAT box unticked, it
belongs to plan `30-05`.
  </action>
  <acceptance_criteria>
    - `.planning/phases/30-breaks-in-committed-time/30-GREEN-final.txt` exists, records the exact `--concurrency=1` command line, and contains a cross-check table in which every wave-1 RED test reads GREEN with its clearing wave named.
    - `grep -c "COMMITBREAK-01/ADD-EVENT-TRIM" .planning/phases/30-breaks-in-committed-time/30-GREEN-final.txt` is greater than 0 and that row reads GREEN.
    - The evidence file contains a `git diff --stat` block whose only non-`.planning/` path is `lib/providers/schedule_notifier.dart`, with zero paths under `test/`.
    - The full-suite run reports zero failures and a total of at least 587 tests.
    - `export PATH="$PATH:/home/dan/development/flutter/bin" && flutter analyze` reports no issues.
    - `.planning/phases/30-breaks-in-committed-time/30-VALIDATION.md` has both requirement rows at ✅ green and the "Primary regression fixture is a commitment block" box ticked.
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$PATH:/home/dan/development/flutter/bin" && flutter analyze && flutter test --concurrency=1</automated>
  </verify>
  <done>The whole suite is green, every wave-1 RED test is accounted for by name, and git proves neither `lib/` wave touched a test file.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and files Phase 30 creates. This plan produces the rows marked **[30-04]**.

| Symbol / file | Kind | Plan |
|---|---|---|
| `ScheduleGeneratorService.buildCommitmentChunks(CommitmentBlock, {required int longBreakEvery})` | public static method, `lib/services/schedule_generator.dart` | 30-03 |
| `ScheduleGeneratorService.breakCadenceForMood(int moodIndex)` | public static method, same file | 30-03 |
| Narrowed STEP E trim (`commitmentId == null`) | behavior change, same file | 30-03 |
| `.planning/phases/30-breaks-in-committed-time/30-GREEN-wave2.txt` | evidence artifact | 30-03 |
| **Narrowed `ScheduleNotifier._trimTrailingNonWork` (`commitmentId == null`)** | **behavior change, `lib/providers/schedule_notifier.dart`** | **[30-04]** |
| **`addEventToday` delegating to `buildCommitmentChunks`** | **behavior change, same file — the duplicate loop is deleted** | **[30-04]** |
| **`.planning/phases/30-breaks-in-committed-time/30-GREEN-final.txt`** | **evidence artifact** | **[30-04]** |
| `group('COMMITBREAK — breaks inside a committed block')` + its 6 tests | test group, `test/services/schedule_generator_test.dart` | 30-01 |
| `.planning/phases/30-breaks-in-committed-time/30-RED-generator.txt` | evidence artifact | 30-01 |
| `COMMITBREAK-01/ADD-EVENT`, `COMMITBREAK-02/ADD-EVENT`, `COMMITBREAK-01/ADD-EVENT-TRIM` | 3 named tests, `test/providers/schedule_notifier_add_event_test.dart` | 30-02 |
| `_commitmentDay()` + `COMMITBREAK-01/RENDER` | fixture helper + widget test, `test/screens/lattice_break_pair_test.dart` | 30-02 |
| `.planning/phases/30-breaks-in-committed-time/30-RED-secondpath.txt` | evidence artifact | 30-02 |
| `CLAUDE.md` § trap #4 (data layer) | documentation | 30-05 |
| `.planning/phases/30-breaks-in-committed-time/30-UAT.md` | UAT verdict record | 30-05 |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hive → `ScheduleNotifier` | `addEventToday` reads a user-authored `CommitmentBlock` and writes chunks back into the persisted `DailySchedule` via `_repo.save`. Single-user, local-only, no network origin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-03 | Tampering | `_trimTrailingNonWork` deleting persisted committed minutes | medium | mitigate | The trim writes through to Hive, so an over-broad trim is durable data loss. Narrowed to `commitmentId == null`; pinned by `COMMITBREAK-01/ADD-EVENT-TRIM`, with the discretionary half still pinned by the unchanged `CR-01`. |
| T-30-07 | Tampering | divergence between the two commitment-chunking implementations | medium | mitigate | Two copies of the same rule is how this defect survived into a second file. Eliminated structurally: the duplicate loop is deleted and both entry points call one public static helper. Pinned by `grep -c "cursor += 25"` returning 0 in both files. |
| T-30-02 | Tampering | a corrupt or legacy `CommitmentBlock` with `endMinutes <= startMinutes` | low | accept | The entry form enforces a >= 25-minute window; the helper returns an empty list and `addEventToday`'s existing `newChunks.isEmpty` guard returns false rather than claiming a placement. |

**No package-manager installs occur in this phase** — no `pubspec.yaml` change, no new dependency.
The `T-{phase}-SC` supply-chain row and its blocking human checkpoint are not applicable.
</threat_model>

<verification>
- `export PATH="$PATH:/home/dan/development/flutter/bin" && flutter analyze` — clean.
- `export PATH="$PATH:/home/dan/development/flutter/bin" && flutter test --concurrency=1` — zero failures, at least 587 tests.
- `grep -c "cursor += 25" lib/providers/schedule_notifier.dart lib/services/schedule_generator.dart` returns 0 for both files.
- `git diff --stat` for this plan's commits shows zero paths under `test/`.
- `30-GREEN-final.txt` accounts for every wave-1 RED and GUARD test by name.
</verification>

<success_criteria>
- COMMITBREAK-01 and COMMITBREAK-02 hold on both entry points — check-in generation and mid-day add.
- One implementation of commitment-window chunking, two call sites.
- The notifier's trailing trim cannot delete a commitment break.
- The full suite is green with no test deleted, proven by name against the wave-1 RED capture.
</success_criteria>

<output>
Create `.planning/phases/30-breaks-in-committed-time/30-04-SUMMARY.md` when done. Its
`key-decisions` must carry D-30-03 (the deliberate scope extension beyond the ROADMAP, with the
"missed twice, reported twice" reasoning) and D-30-02's notifier half.
</output>
</content>
