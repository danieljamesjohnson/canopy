---
phase: 30-breaks-in-committed-time
plan: 03
type: execute
wave: 2
depends_on: ["30-01", "30-02"]
files_modified:
  - lib/services/schedule_generator.dart
  - .planning/phases/30-breaks-in-committed-time/30-GREEN-wave2.txt
autonomous: true
requirements: [COMMITBREAK-01, COMMITBREAK-02]

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "generate() emits a shortBreak at 09:25-09:30 for a commitment block running 09:00-11:40 — a break lands between consecutive work chunks inside a committed window (COMMITBREAK-01)."
    - "block.startMinutes and block.endMinutes are never written to; a 09:10 block still starts at 09:10 after generate() returns (COMMITBREAK-02 / Phase 28 D-01)."
    - "A 6-hour block at mood 3 gets exactly 2 long breaks, from its own per-block counter (D-30-01)."
    - "A tail-stretched commitment break at the end of the day survives STEP E (D-30-02) — no committed minute is silently erased."
    - "The whole 587-test baseline is still green and flutter analyze is still clean; no test was deleted to get there."
  artifacts:
    - "ScheduleGeneratorService.buildCommitmentChunks — the single implementation of commitment-window chunking, shared with ScheduleNotifier in plan 30-04"
    - "ScheduleGeneratorService.breakCadenceForMood — the mood→N lookup, exposed for the same reason"
    - "lib/services/schedule_generator.dart — Step 1 rewritten to call the helper; STEP E's trim narrowed to commitmentId == null"
    - ".planning/phases/30-breaks-in-committed-time/30-GREEN-wave2.txt — by-name RED→GREEN cross-check plus the wave-boundary git proof"
  key_links:
    - "Every chunk the helper emits — work AND break — must carry anchoredStartMinutes, or STEP A's `anchoredStartMinutes != null` filter drops the breaks, STEP B's window merge sees a gap where each break sits, and discretionary work gets packed into the user's committed time (30-RESEARCH.md Pitfall 2)."
    - "Every break must carry commitmentId, or STEP E's narrowed trim cannot tell it from a discretionary break and deletes it (D-30-02, D-30-04)."
    - "lastForBlock must track the last chunk emitted — work OR break — or the tail stretch grows a work chunk over a break that was already placed (30-RESEARCH.md Pitfall 3)."
    - "buildCommitmentChunks is public and static specifically so plan 30-04 can call it from ScheduleNotifier instead of keeping a third copy of this loop."
---

<objective>
Land the engine fix: commitment windows get breaks on the same 25+5 lattice as discretionary time,
their own start and end never move, and nothing downstream deletes what the loop emitted.

Purpose: this is the wave the RED-proof exists for. Every test that turns green here was written and
proven failing in wave 1 against this exact unfixed file, and this plan touches **only** `lib/` —
that is what makes RED→GREEN attributable to the production change rather than to an adjusted
assertion. v1.5 shipped two defects behind green tests; this discipline is the standing answer to it.

Task 1 is the phase's **tracer**: one thin path — a single short break between two work chunks
inside one committed window — wired end to end through every layer the phase modifies (Step 1
emission → STEP A's anchored split → STEP B's free-slot window merge → STEP C's pass-through →
STEP D's sort → STEP E's trim) and verified by the real `generate()`, not by a per-layer unit test.
It is production-quality, not a spike: the helper it creates is the one plan `30-04` calls and the
one that ships. Tasks 2 expands horizontally from that proven slice (the cadence boundary, the long
break, the tail arithmetic).

Output: `buildCommitmentChunks`, `breakCadenceForMood`, a narrowed STEP E, and `30-GREEN-wave2.txt`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-breaks-in-committed-time/30-RESEARCH.md
@.planning/phases/30-breaks-in-committed-time/30-01-PLAN.md
@.planning/phases/30-breaks-in-committed-time/30-RED-generator.txt
@lib/services/schedule_generator.dart
@CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — one break inside one committed window, all the way to the returned list</name>
  <files>lib/services/schedule_generator.dart</files>
  <precondition>Plans 30-01 and 30-02 are committed, and `.planning/phases/30-breaks-in-committed-time/30-RED-generator.txt` exists on disk — a RED→GREEN claim with no captured RED is not evidence.</precondition>
  <reversibility rating="costly">Break chunks emitted here carry `commitmentId` and are persisted into Hive `DailySchedule` records; a later code revert does not rewrite days already generated and saved. Harmless (no reader treats `commitmentId` on a break specially — proven by `COMMITBREAK-01/RENDER`), but not undone by `git revert` alone.</reversibility>
  <read_first>
    - lib/services/schedule_generator.dart lines 32-77 (class doc comment, `_moodBreakCadence`, `_shortBreakMinutes`, `_longBreakMinutes`, `_latticeMinutes`) and lines 78-106 (the existing "Public static helpers — exposed for ScheduleNotifier" section, the precedent this task extends)
    - lib/services/schedule_generator.dart lines 256-309 (`generate()`'s preamble and Step 1's commitment walk — the code being replaced)
    - lib/services/schedule_generator.dart lines 617-699 (STEP A through STEP E — every layer this tracer crosses)
    - lib/services/schedule_generator.dart lines 700-717 (`_shortBreakChunk` — the existing break-construction field pattern and its WR-02 no-drift rationale)
    - lib/services/schedule_generator.dart lines 796-852 (`_assignSyntheticStartTimes`'s packing loop — the footprint-reservation shape this mirrors structurally, and the WR-01 comment warning against a second independent cadence counter)
    - .planning/phases/30-breaks-in-committed-time/30-RESEARCH.md § "Common Pitfalls" 1-3 and § "Code Examples"
    - .planning/phases/30-breaks-in-committed-time/30-01-PLAN.md § `<phase_decisions>` (D-30-01 through D-30-04)
  </read_first>
  <action>
Add two public static members to `ScheduleGeneratorService`, in the existing "Public static helpers"
section:

`breakCadenceForMood(int moodIndex)` returns the `_moodBreakCadence` entry for the mood, falling back
to 4 — exactly the expression currently inlined at line 264. Replace that inline lookup with a call
to it, so the table has one reader inside the class and one reachable reader outside it.

`buildCommitmentChunks(CommitmentBlock block, {required int longBreakEvery})` returns the ordered
`List<ScheduledChunk>` for one block's window. It owns the whole window walk and nothing else — it
does NOT decide whether the block anchors today; `generate()`'s existing `anchoredToday` check stays
where it is, and `addEventToday` keeps its own equivalent check in plan `30-04`.

In this tracer, the helper reserves the short break only. Walk from `block.startMinutes` while a
25-minute work chunk still fits before `block.endMinutes`; emit the work chunk; then, if the
5-minute short break also fits before `block.endMinutes`, emit it too. Track the last chunk emitted
— work or break — in a single mutable reference and, after the loop, extend THAT chunk's duration by
whatever remains to `block.endMinutes`. Never re-derive which chunk should have been last from
position: that is the defect path where the stretch grows a work chunk over a break already placed
(30-RESEARCH.md Pitfall 3). When the window is shorter than one 25-minute cell the loop body never
runs and the helper returns an empty list — the pre-phase behaviour, unchanged.

Every chunk the helper emits — work and break alike — carries `anchoredStartMinutes` set to its own
start and `commitmentId` set to `block.id`. Both are load-bearing, not cosmetic: without
`anchoredStartMinutes`, STEP A drops the breaks out of `commitmentChunks` and STEP B's interval merge
sees a gap exactly where each break sits, letting discretionary work be packed into the user's
committed time (Pitfall 2); without `commitmentId`, STEP E cannot tell a commitment break from a
discretionary one (D-30-02, D-30-04). Break chunks otherwise take the same field shape
`_shortBreakChunk` already uses — `goalId: null`, `rationale: ''` — and work chunks keep
`rationale: block.name` as today. Do not introduce a `reservedBreakMinutes` footprint on commitment
chunks: the loop knows its own cursor, so its breaks are fully formed at emission time and there is
no reason to add the decode indirection the discretionary path needs.

Rewrite Step 1's body to call the helper and `addAll` its result.

Then narrow STEP E (D-30-02). Its trim currently removes any trailing `shortBreak` from the whole
day's flat list. Add the `commitmentId == null` condition so a commitment break can never be
removed. Rewrite the comment above it to say why: a trailing discretionary short break is 5 idle
minutes and reads as noise, but a trailing commitment break may have been tail-stretched, so
removing it deletes however many minutes the stretch absorbed from inside the user's own committed
window. Cite `D-30-02` and `COMMITBREAK-01` by name in that comment.

Also rewrite STEP C's comment, which currently states the pre-phase behaviour outright and is quoted
in the ROADMAP as part of the root cause, and the class doc comment's break-pass paragraph, which
describes the interleave as discretionary-only. Both must now describe commitment windows getting
their own lattice; cite `COMMITBREAK-01` in the STEP C comment.

Do not touch `_assignSyntheticStartTimes`, `_moodCap`, `_moodBreakCadence`'s values, or
`_roundUpToLattice`. Never write to `block.startMinutes` or `block.endMinutes` (Phase 28 D-01).

Do not quote the removed loop's advance expression verbatim in any comment. An acceptance criterion
greps this file for its absence, so reciting the old code in prose would fail the gate while the
code itself is correct.

**Intermediate state notice:** with the short break only, three wave-1 tests are still expected RED
after this task — `COMMITBREAK-01/PRIMARY`, `COMMITBREAK-01/CADENCE` and `COMMITBREAK-02/D-01` in
`schedule_generator_test.dart`, plus `COMMITBREAK-01/RENDER` in `lattice_break_pair_test.dart`. All
four need the cadence boundary, which Task 2 adds. Do not "fix" them by weakening an assertion.
  </action>
  <acceptance_criteria>
    - `ScheduleGeneratorService.buildCommitmentChunks` and `ScheduleGeneratorService.breakCadenceForMood` exist as public static members: `grep -c "static List<ScheduledChunk> buildCommitmentChunks\|static int breakCadenceForMood" lib/services/schedule_generator.dart` returns 2.
    - `generate()` no longer contains a bare commitment-window walk: `grep -c "cursor += 25" lib/services/schedule_generator.dart` returns 0.
    - Every chunk returned by the helper has non-null `anchoredStartMinutes` and `commitmentId == block.id` — proven by `COMMITBREAK-01/PRIMARY`'s per-chunk `commitmentId` assertions once Task 2 lands, and by `Test 2`'s two short-break assertions now.
    - These wave-1 tests are GREEN after this task: `Test 2`, `Test 10`, `Test 13`, `WR-03`, `GUARD 7 / LATTICE-01/D-01`, `LATTICE-02/D-05`, `COMMITBREAK-01/TAIL`, `COMMITBREAK-01/STEP-E`, `COMMITBREAK-01/NO-DOUBLE-BOOK`.
    - Specifically: `generate()` for a `Work` block 540-600 at mood 3 with no goals returns exactly 4 anchored chunks — work@540/25, shortBreak@565/5, work@570/25, shortBreak@595/5 — and `result.last` is the shortBreak at 595, not trimmed.
    - Specifically: `generate()` for a block 540-610 at mood 3 returns a last chunk that is a shortBreak anchored at 595 with `durationMinutes == 15`, reaching exactly 610.
    - `grep -c "COMMITBREAK-01" lib/services/schedule_generator.dart` is at least 2, and `grep -c "D-30-02" lib/services/schedule_generator.dart` is at least 1.
    - `export PATH="$PATH:/home/dan/development/flutter/bin" && flutter analyze` reports no issues.
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$PATH:/home/dan/development/flutter/bin" && flutter test test/services/schedule_generator_test.dart --concurrency=1 --plain-name "COMMITBREAK-01/TAIL"; flutter test test/services/schedule_generator_test.dart --concurrency=1 --plain-name "COMMITBREAK-01/STEP-E"</automated>
  </verify>
  <done>One short break lands between consecutive work chunks inside a committed window and survives all the way to `generate()`'s returned list; the block's own window is untouched; the nine named wave-1 tests are green.</done>
</task>

<task type="auto">
  <name>Task 2: Expand the slice — the per-block cadence counter, the long break, and the tail</name>
  <files>lib/services/schedule_generator.dart</files>
  <read_first>
    - lib/services/schedule_generator.dart — `buildCommitmentChunks` as written by Task 1
    - lib/services/schedule_generator.dart lines 818-848 (the discretionary loop's `isBoundary` / `breakFootprint` / short-only-fallback shape, including the IN-01 and D-05 comments that explain why the fallback is capacity-driven rather than a suppression rule)
    - .planning/phases/30-breaks-in-committed-time/30-RESEARCH.md § "Cadence Decision" (the 6-hour simulation) and § "Pitfall 3" (the four tail cases)
    - .planning/phases/30-breaks-in-committed-time/30-01-PLAN.md § `<phase_decisions>` (D-30-01)
  </read_first>
  <action>
Extend `buildCommitmentChunks` with the cadence boundary, mirroring the footprint-reservation shape
`_assignSyntheticStartTimes` already uses rather than inventing a second algorithm for the same rule.

Give the helper its own break counter, local to the call and therefore fresh for each block instance
(D-30-01). Increment it once per work chunk emitted. A chunk is a cadence boundary when the counter
is an exact multiple of `longBreakEvery`. A boundary reserves a footprint of the short break plus a
separate 30-minute long-break cell; an ordinary chunk reserves the short break alone. `isBoundary`
selects a footprint, never a duration — the long break is an additional cell after the chunk's own
short break, it never replaces it (Phase 28 D-06).

Keep the three-way outcome the discretionary loop already proves correct: emit the full footprint
when it fits before `block.endMinutes`; otherwise fall back to the short break alone when that fits;
otherwise emit no break at all. The third case is a genuine capacity-driven omission, the same shape
Phase 28's D-05 established — not a suppression rule, and it must not be defended by a comment
alone. `COMMITBREAK-01/TAIL`'s `540-627` row is its fixture.

Keep tracking the last chunk emitted across all three outcomes so the tail stretch still extends
whichever unit is actually last. After Task 1 that reference can be a work chunk or a short break;
it can now also be a long break — the ROADMAP repro's own tail is a long break stretched from 30 to
40 minutes.

Document D-30-01 in the helper's doc comment, with the reasoning, not just the choice: a shared
counter would count in code-execution order rather than wall-clock order, because Step 1 runs before
discretionary packing while a discretionary chunk can sit chronologically earlier in the day. Record
the simulation result that settled it — a 6-hour block at mood 3 yields exactly 2 long breaks, the
same density six hours of discretionary work already gets at that mood.

Do not make the cadence adaptive, and do not change `_moodBreakCadence`'s values. N is set once from
the morning check-in mood; the owner declined adaptivity explicitly on 2026-08-19.
  </action>
  <acceptance_criteria>
    - `generate()` for a `Work` block 540-700 at mood 3 plus one habit returns an anchored sub-list of exactly 9 chunks: work@540/25, shortBreak@565/5, work@570/25, shortBreak@595/5, work@600/25, shortBreak@625/5, work@630/25, shortBreak@655/5, longBreak@660/40 — every one carrying `commitmentId == block.id`.
    - `generate()` for a block 540-900 at mood 3 with no goals returns exactly 10 work chunks, 10 short breaks and 2 long breaks; the long breaks are anchored at 660 and 810 and are 30 minutes each; the anchored durations sum to 360 and run contiguously from 540 to 900.
    - `generate()` for a block 550-700 at mood 3 returns anchored work-chunk starts of exactly `[550, 580, 610, 640]`, and `block.startMinutes` reads back as 550 after the call.
    - `generate()` for a block 540-627 at mood 3 emits no break after the third work chunk (the 5-minute short break does not fit before 627) and stretches that work chunk to `durationMinutes == 27`.
    - The helper's doc comment states D-30-01 and its reasoning: `grep -c "D-30-01" lib/services/schedule_generator.dart` is at least 1.
    - `export PATH="$PATH:/home/dan/development/flutter/bin" && flutter test test/services/schedule_generator_test.dart --concurrency=1` — all tests in the file pass.
    - `export PATH="$PATH:/home/dan/development/flutter/bin" && flutter test test/screens/lattice_break_pair_test.dart --concurrency=1` — all tests pass, including `COMMITBREAK-01/RENDER`.
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$PATH:/home/dan/development/flutter/bin" && flutter test test/services/schedule_generator_test.dart test/screens/lattice_break_pair_test.dart --concurrency=1</automated>
  </verify>
  <done>Every generator-side and render-side wave-1 test is green; the commitment block's cadence is its own and is documented with the simulation that settled it.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite gate, and prove the wave boundary from git</name>
  <files>.planning/phases/30-breaks-in-committed-time/30-GREEN-wave2.txt</files>
  <read_first>
    - .planning/phases/30-breaks-in-committed-time/30-RED-generator.txt and 30-RED-secondpath.txt (the RED side of the cross-check)
    - .planning/phases/30-breaks-in-committed-time/30-VALIDATION.md § "Sampling Rate" (why the FULL suite runs at every wave boundary here, even though the verified blast radius is one file)
  </read_first>
  <action>
Run the full suite at `--concurrency=1` and write the output to
`.planning/phases/30-breaks-in-committed-time/30-GREEN-wave2.txt`. The full suite at this boundary is
deliberate and not redundant: commitment-block breaks flow through `buildTimeline` and
`chunk_card.dart` for the first time, so the Phase 28 D-06 render tests need re-confirming even
though nothing in `lib/screens/` changed.

Prepend a cross-check table pairing every test named RED in `30-RED-generator.txt` and
`30-RED-secondpath.txt` with its status now. The three `ADD-EVENT` tests are expected to remain RED —
they clear in wave 3 (plan `30-04`) — and the table must say so rather than leaving them looking
like regressions.

Then prove the wave boundary: run `git diff --stat` across this plan's commits and confirm the only
paths touched are `lib/services/schedule_generator.dart` and files under `.planning/`. Zero paths
under `test/`. Record that output in the evidence file. If any test file was modified in this wave,
the RED→GREEN claim is void — stop and report rather than reconciling it.

Baseline check: the suite was 587 green before this phase. Confirm the count did not go down and
that no test was deleted.
  </action>
  <acceptance_criteria>
    - `.planning/phases/30-breaks-in-committed-time/30-GREEN-wave2.txt` exists, its header records the exact `--concurrency=1` command line, and it contains a RED→GREEN cross-check table naming every wave-1 RED test with its current status.
    - The table marks `COMMITBREAK-01/ADD-EVENT`, `COMMITBREAK-02/ADD-EVENT` and `COMMITBREAK-01/ADD-EVENT-TRIM` as still-RED-by-design, clearing in wave 3.
    - The evidence file contains a `git diff --stat` block whose only non-`.planning/` path is `lib/services/schedule_generator.dart`, and zero paths under `test/`.
    - `export PATH="$PATH:/home/dan/development/flutter/bin" && flutter analyze` reports no issues.
    - The full-suite total is at least 587 tests and every failure in the run belongs to `test/providers/schedule_notifier_add_event_test.dart`.
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$PATH:/home/dan/development/flutter/bin" && flutter analyze && git diff --stat HEAD~2 --name-only | grep -c '^test/' || true</automated>
  </verify>
  <done>The generator fix is committed, the full suite's only failures are the wave-3 add-event tests, and git proves no test file was touched in this wave.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and files Phase 30 creates. This plan produces the rows marked **[30-03]**.

| Symbol / file | Kind | Plan |
|---|---|---|
| **`ScheduleGeneratorService.buildCommitmentChunks(CommitmentBlock block, {required int longBreakEvery}) → List<ScheduledChunk>`** | **public static method, `lib/services/schedule_generator.dart`** | **[30-03]** |
| **`ScheduleGeneratorService.breakCadenceForMood(int moodIndex) → int`** | **public static method, same file** | **[30-03]** |
| **Narrowed STEP E trim (`commitmentId == null`)** | **behavior change, same file** | **[30-03]** |
| **`.planning/phases/30-breaks-in-committed-time/30-GREEN-wave2.txt`** | **evidence artifact** | **[30-03]** |
| Narrowed `ScheduleNotifier._trimTrailingNonWork` (`commitmentId == null`) | behavior change, `lib/providers/schedule_notifier.dart` | 30-04 |
| `group('COMMITBREAK — breaks inside a committed block')` + its 6 tests | test group, `test/services/schedule_generator_test.dart` | 30-01 |
| `.planning/phases/30-breaks-in-committed-time/30-RED-generator.txt` | evidence artifact | 30-01 |
| `COMMITBREAK-01/ADD-EVENT`, `COMMITBREAK-02/ADD-EVENT`, `COMMITBREAK-01/ADD-EVENT-TRIM` | 3 named tests, `test/providers/schedule_notifier_add_event_test.dart` | 30-02 |
| `_commitmentDay()` + `COMMITBREAK-01/RENDER` | fixture helper + widget test, `test/screens/lattice_break_pair_test.dart` | 30-02 |
| `.planning/phases/30-breaks-in-committed-time/30-RED-secondpath.txt` | evidence artifact | 30-02 |
| `.planning/phases/30-breaks-in-committed-time/30-GREEN-final.txt` | evidence artifact | 30-04 |
| `CLAUDE.md` § trap #4 (data layer) | documentation | 30-05 |
| `.planning/phases/30-breaks-in-committed-time/30-UAT.md` | UAT verdict record | 30-05 |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hive → generator | `CommitmentBlock.startMinutes`/`endMinutes` are read from the local Hive store into `generate()` and `buildCommitmentChunks`. Single-user, local-only, no network origin. |
| generator → Hive | The chunks this loop emits are persisted into `DailySchedule` records by `ScheduleNotifier`; a chunk lost here is durable data loss, not a render glitch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Denial of Service | `buildCommitmentChunks`'s `while` loop | low | mitigate | The cursor advances by at least 25 per iteration and the loop is bounded by `block.endMinutes`, so it cannot spin; a window under 25 minutes exits before the first body execution. Pinned by `COMMITBREAK-01/TAIL`'s `540-560` row asserting an empty result. |
| T-30-02 | Tampering | a corrupt or legacy `CommitmentBlock` with `endMinutes <= startMinutes` | low | accept | Local single-user store; the entry form enforces a >= 25-minute window and the generator only reads these fields. Degrades to emitting nothing rather than throwing. |
| T-30-05 | Tampering | STEP E deleting a tail-stretched commitment break | medium | mitigate | Directly erases persisted committed minutes (observed as `Expected: <45> Actual: <25>` on the D-05 fixture during research). Mitigated by D-30-02's `commitmentId == null` narrowing and pinned by `COMMITBREAK-01/STEP-E`. |
| T-30-06 | Tampering | discretionary work packed into a commitment's internal break gap | medium | mitigate | Would double-book the user's committed time. Mitigated by setting `anchoredStartMinutes` on every emitted break so STEP B's interval merge covers the whole span; pinned by `COMMITBREAK-01/NO-DOUBLE-BOOK`. |

**No package-manager installs occur in this phase** — no `pubspec.yaml` change, no new dependency
(30-RESEARCH.md § "Package Legitimacy Audit"). The `T-{phase}-SC` supply-chain row and its blocking
human checkpoint are not applicable.
</threat_model>

<verification>
- `export PATH="$PATH:/home/dan/development/flutter/bin" && flutter analyze` — clean.
- `export PATH="$PATH:/home/dan/development/flutter/bin" && flutter test --concurrency=1` — at least 587 tests, failures confined to `test/providers/schedule_notifier_add_event_test.dart` (cleared in wave 3).
- `30-GREEN-wave2.txt` cross-checks every wave-1 RED test by name.
- `git diff --stat` for this plan's commits shows zero paths under `test/`.
- `block.startMinutes` / `block.endMinutes` appear only on the right-hand side of expressions in `buildCommitmentChunks` — never assigned (Phase 28 D-01, COMMITBREAK-02).
</verification>

<success_criteria>
- COMMITBREAK-01 holds: a break lands between consecutive work chunks inside a committed block, on the 25+5 lattice, seeded from the block's own unrounded start.
- COMMITBREAK-02 holds: the commitment's own start and end are unchanged.
- D-30-01, D-30-02 and D-30-04 are implemented and documented in the source with their reasoning.
- RED→GREEN is attributable to `lib/` alone, proven from git rather than asserted.
</success_criteria>

<output>
Create `.planning/phases/30-breaks-in-committed-time/30-03-SUMMARY.md` when done. Its
`key-decisions` must carry D-30-01, D-30-02 and D-30-04 with reasoning, and must record the
correction to 30-RESEARCH.md Pitfall 6.
</output>
</content>
