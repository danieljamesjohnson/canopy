---
phase: 30-breaks-in-committed-time
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/providers/schedule_notifier_add_event_test.dart
  - test/screens/lattice_break_pair_test.dart
  - .planning/phases/30-breaks-in-committed-time/30-RED-secondpath.txt
autonomous: true
requirements: [COMMITBREAK-01, COMMITBREAK-02]

estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An event added mid-day from the Today screen gets the same 25+5 lattice inside its window as a day generated at check-in — the owner's symptom cannot reproduce through the second entry point."
    - "The added event's own start and end minutes never move (COMMITBREAK-02 holds on the second path too)."
    - "ScheduleNotifier's trailing-non-work trim cannot delete a commitment break, so mid-day edits never erase committed minutes."
    - "A break chunk carrying commitmentId renders as a break, not as a commitment-tinted work card — D-30-04 is proven, not assumed."
  artifacts:
    - "test/providers/schedule_notifier_add_event_test.dart — 3 new named tests plus 3 re-pointed existing tests"
    - "test/screens/lattice_break_pair_test.dart — COMMITBREAK-01/RENDER, generated from the real ScheduleGeneratorService"
    - ".planning/phases/30-breaks-in-committed-time/30-RED-secondpath.txt — by-name RED evidence at --concurrency=1"
  key_links:
    - "addEventToday's output flows straight into _todaySchedule.chunks and is rendered by the same timeline as generate()'s — which is why the identical defect there produces the identical user-visible symptom."
    - "_trimTrailingNonWork (schedule_notifier.dart:357-369) runs BEFORE new chunks are inserted, so it can only eat a break left by a PREVIOUS addEventToday call — that is the exact scenario the new trim test builds."
    - "The render guard closes D-30-04 by exercising ChunkCard.build()'s chunkType switch, the mechanism that makes commitmentId visually inert on a break."
---

<objective>
Write the second-path and render-layer regression tests for Phase 30 and prove them RED against the
unfixed code.

Purpose: `lib/providers/schedule_notifier.dart:addEventToday` carries its own copy of the identical
bare `cursor += 25` commitment walk (lines 282-303) under a doc comment that says it "mirrors the
commitment-anchoring step in ScheduleGeneratorService.generate()". Fixing only the generator would
leave a second live source of the owner's exact reported symptom — see D-30-03 in `30-01-PLAN.md`
for the full reasoning and the deliberate scope-extension argument. This plan writes that path's
tests, plus the one widget test that settles D-30-04 empirically instead of by assertion.

Runs in parallel with plan `30-01` — disjoint files, no shared surface.

Output: 3 new add-event tests, 3 re-pointed add-event tests, 1 render guard, and
`30-RED-secondpath.txt`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-breaks-in-committed-time/30-RESEARCH.md
@.planning/phases/30-breaks-in-committed-time/30-01-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The add-event lattice tests — new and re-pointed</name>
  <files>test/providers/schedule_notifier_add_event_test.dart</files>
  <precondition>`flutter` resolves after `export PATH="$PATH:/home/dan/development/flutter/bin"`, and `flutter --version` reports Flutter 3.44.1 / Dart 3.12.1.</precondition>
  <read_first>
    - test/providers/schedule_notifier_add_event_test.dart lines 71-135 (the `makeNotifier` helper — it seeds `moodIndex: 3`, which is the cadence source every expectation below assumes) and lines 355-495 (the three tests being re-pointed plus the CR-01 guard)
    - lib/providers/schedule_notifier.dart lines 238-343 (`addEventToday`: the stale-chunk removal, the `_trimTrailingNonWork` call at 265, the bare `cursor += 25` walk at 282-303, and both persist branches)
    - lib/providers/schedule_notifier.dart lines 345-369 (`_trimTrailingNonWork` — trims ALL trailing non-work chunks, unaware that an anchored break can now exist)
    - .planning/phases/30-breaks-in-committed-time/30-01-PLAN.md § `<phase_decisions>` (D-30-02, D-30-03, D-30-04)
  </read_first>
  <action>
Add three new tests to `test/providers/schedule_notifier_add_event_test.dart` and re-point three
existing ones, all keyed on the same cadence the generator uses: the seeded schedule's
`moodIndex: 3` gives N = 4, so a one-hour window reaches no cadence boundary and emits short breaks
only.

The three new tests prove COMMITBREAK-01 and COMMITBREAK-02 on the mid-day path, and prove D-30-02's
sibling defect in the notifier: `_trimTrailingNonWork` trims every trailing non-work chunk with no
regard for origin, so once anchored breaks exist it can delete a commitment break — the same silent
erasure of committed minutes STEP E can cause in the generator. Build that test around the real
call sequence: a day already holding a previous event's anchored break as its chronologically last
chunk, then an `addEventToday` for a block that does not anchor today (which runs the trim and
returns early).

Do not add a test for the discretionary half of the trim — the existing `CR-01` test at line 396
already covers it and must stay green unchanged. Say so in the new test's comment so a future reader
does not "fix" the apparent gap.

Do not touch `_reflowDiscretionaryWork`'s hardcoded `const longBreakEvery = 4` (line 389). It
governs discretionary reflow, not commitment windows, and is out of this phase's scope.

For the re-pointed tests, rewrite the leading comments too: two of them currently describe the
pre-phase back-to-back shape in prose ("2 chunks", "2pm/2:25pm") that will be false after the fix.
Do not delete any test.
  </action>
  <acceptance_criteria>
    - New test `COMMITBREAK-01/ADD-EVENT: an event added mid-day gets the same 25+5 lattice as a generated day` — `makeNotifier` day, block `startMinutes: 840, endMinutes: 900` dated `2026-03-23`. Asserts the block's chunks (filtered by `commitmentId == block.id`, sorted by `anchoredStartMinutes`) are exactly: work@840/25, shortBreak@865/5, work@870/25, shortBreak@895/5 — four chunks, and every one has `commitmentId == block.id`.
    - New test `COMMITBREAK-02/ADD-EVENT: the added event's own window never moves` — same fixture. Asserts the first block chunk's `anchoredStartMinutes == 840`, the last block chunk's `anchoredStartMinutes + durationMinutes == 900`, and `block.startMinutes == 840 && block.endMinutes == 900` read back off the block object after `addEventToday` returns.
    - New test `COMMITBREAK-01/ADD-EVENT-TRIM: the trailing trim must not delete a commitment break (D-30-02)` — seeds today with work@480 discretionary (completed), work anchored@1170/25 `commitmentId: 'other-block'`, and shortBreak anchored@1195/5 `commitmentId: 'other-block'`; then calls `addEventToday` with a block dated `2026-03-24`. Asserts the call returns `false` AND that a chunk with `anchoredStartMinutes == 1195`, `chunkType == ChunkType.shortBreak`, `commitmentId == 'other-block'` is still present in `notifier.todaySchedule!.chunks`.
    - Re-pointed `inserts a same-day one-off, preserving existing progress` (line 107): additionally asserts the second `rationale == 'Dentist'` work chunk has `anchoredStartMinutes == 870` and that a `ChunkType.shortBreak` chunk anchored at `865` with `durationMinutes == 5` and `commitmentId == block.id` exists.
    - Re-pointed `editing an event re-anchors today (idempotent by commitmentId)` (line 358): asserts `callChunks.length == 4` and still asserts every one has `anchoredStartMinutes! >= 16 * 60`.
    - Re-pointed `creates a minimal today schedule when none exists (empty-state add)` (line 463): asserts `chunks.length == 4`, and the `rationale == 'Dentist'` assertion is scoped to `ChunkType.work` chunks only (break chunks carry `rationale: ''` per D-30-04).
    - `CR-01: moving a commitment off today trims the now-trailing break` (line 396) is unchanged and still asserts `chunks.map((c) => c.id) == ['w1']`.
    - `export PATH="$PATH:/home/dan/development/flutter/bin" && flutter analyze test/providers/schedule_notifier_add_event_test.dart` reports no issues.
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$PATH:/home/dan/development/flutter/bin" && flutter test test/providers/schedule_notifier_add_event_test.dart --concurrency=1 2>&1 | grep -c "ADD-EVENT"</automated>
  </verify>
  <done>Three new tests and three re-pointed tests exist and compile; the ADD-EVENT tests run and fail against the unfixed notifier.</done>
</task>

<task type="auto">
  <name>Task 2: COMMITBREAK-01/RENDER — prove a commitmentId-carrying break still renders as a break</name>
  <files>test/screens/lattice_break_pair_test.dart</files>
  <read_first>
    - test/screens/lattice_break_pair_test.dart lines 1-70 (the file's stated convention: every fixture comes from the REAL `ScheduleGeneratorService`, plus `_FakeScheduleNotifier`) and lines 234-300 (the existing `D-06: the pair renders` widget test — the exact `pumpWithMood` + `TimelineGeometry.forDay` + `TimelineRowTile(child: ChunkCard(...))` pattern to mirror)
    - lib/screens/schedule/widgets/chunk_card.dart lines 110-160 (`ChunkCard.build`'s `chunkType` switch and `_buildBreak`'s `title` derivation — the mechanism this test exercises)
    - lib/screens/schedule/widgets/chunk_card.dart lines 412-460 (`_WorkChunkContent` and its `isCommitment` styling at line 449 — the branch a break must never reach)
  </read_first>
  <action>
Add a `_commitmentDay()` fixture helper and one `testWidgets` case to
`test/screens/lattice_break_pair_test.dart`, in a new group sibling to the existing `D-06` groups.

`_commitmentDay()` generates a day from the REAL `ScheduleGeneratorService` using a single
`CommitmentBlock` spanning 540 to 700 with `daysOfWeek: const [1]`, no goals, `moodIndex: 3`, on the
same Monday the rest of the file uses — the ROADMAP's own reproduction window. Import
`CommitmentBlock` if the file does not already.

The test settles D-30-04 empirically. 30-RESEARCH.md Pitfall 6 asserts that setting `commitmentId`
on a break chunk silently gives it `tertiaryContainer` commitment styling. Reading
`chunk_card.dart` shows that cannot happen — `ChunkCard.build()` switches on `chunk.chunkType`
first and only constructs `_WorkChunkContent` under `case ChunkType.work`. Prove that by pumping the
generated day's own long break (which carries `commitmentId`) and asserting the break title text
renders: if `_WorkChunkContent` had been reached instead, the card would show the chunk's rationale
(empty for a break), not a break title.

Mirror the existing D-06 test's harness exactly — same `TimelineGeometry.forDay` call shape, same
`SizedBox(height: geometry.heightFor(...))` wrapper, same `extraProviders` with
`_FakeScheduleNotifier`. Do not assert on text width, glyph metrics, or label layout: the test
harness's placeholder font inflates those (STATE.md carry-forward invariant). A title-presence
assertion is a tree assertion, not a metric one, and is safe.

Guard the fixture the way the existing D-06 test guards its pair index: if the generated day
contains no break chunk at all, fail with a `reason:` that says so plainly rather than throwing an
index error. That guard is what makes this test legible as RED in this wave.
  </action>
  <acceptance_criteria>
    - `test/screens/lattice_break_pair_test.dart` contains a helper `_commitmentDay()` that calls `ScheduleGeneratorService().generate(...)` with `blocks:` holding one `CommitmentBlock(startMinutes: 540, endMinutes: 700, daysOfWeek: const [1])`, `goals: const []`, `moodIndex: 3`.
    - A `testWidgets` case named `COMMITBREAK-01/RENDER: a break carrying commitmentId renders as a break, not a commitment-tinted work card (D-30-04)` exists.
    - It asserts the generated day contains at least one `ChunkType.shortBreak` and exactly one `ChunkType.longBreak`, each with a non-null `commitmentId`, with an explicit `reason:` covering the "no breaks in the generated day" case.
    - It pumps the day's shortBreak (anchored 565) and longBreak (anchored 660) through `pumpWithMood` using `TimelineRowTile(child: ChunkCard(chunk: ...))` inside height-constrained `SizedBox`es derived from `TimelineGeometry.forDay`.
    - It asserts `find.text('Long break')` finds one widget, `find.byType(ChunkCard)` finds 2 widgets, and `tester.takeException()` is null.
    - `export PATH="$PATH:/home/dan/development/flutter/bin" && flutter analyze test/screens/lattice_break_pair_test.dart` reports no issues.
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$PATH:/home/dan/development/flutter/bin" && flutter test test/screens/lattice_break_pair_test.dart --concurrency=1 2>&1 | grep -c "COMMITBREAK-01/RENDER"</automated>
  </verify>
  <done>The render guard exists, compiles, and fails against the unfixed engine with its fixture guard's reason string (the generated day has no breaks yet).</done>
</task>

<task type="auto">
  <name>Task 3: Capture second-path RED evidence and commit the test-only wave</name>
  <files>.planning/phases/30-breaks-in-committed-time/30-RED-secondpath.txt</files>
  <read_first>
    - .planning/phases/30-breaks-in-committed-time/30-VALIDATION.md § "Test Infrastructure" (why `--concurrency=1` is not optional)
    - .planning/STATE.md § "Carry-Forward Invariants" ("Regression tests must be proven RED")
  </read_first>
  <action>
Run both touched test files at `--concurrency=1` against the unfixed code and write the combined
output to `.planning/phases/30-breaks-in-committed-time/30-RED-secondpath.txt`. Default concurrency
drops and duplicates per-test-name lines at this suite's scale, which makes a by-name RED→GREEN
cross-check unverifiable — do not capture evidence any other way.

Prepend a header classifying each touched test as RED (must fail now, must pass after `30-04` for
the notifier tests and after `30-03` for the render guard) or GUARD (green now and after). Note the
split explicitly: `COMMITBREAK-01/RENDER` turns green when the generator lands in wave 2, while the
three ADD-EVENT tests stay red until the notifier lands in wave 3. An evidence file that does not
say which wave clears which test cannot be used to check the wave boundary later.

If a test you predicted RED comes back green, do not distort the test to force failure — investigate
and record the real reason in the header, the way Phase 28 did with WR-03.

Then run `git diff --stat` and confirm this plan's commit touches only paths under `test/` and
`.planning/`. Record that output in the evidence file. Commit.
  </action>
  <acceptance_criteria>
    - `.planning/phases/30-breaks-in-committed-time/30-RED-secondpath.txt` exists and its header table classifies all 4 new tests and all 3 re-pointed tests as RED or GUARD, and names the clearing wave (2 or 3) for each RED entry.
    - The exact command lines used appear in the header and both contain `--concurrency=1`.
    - `grep -c "COMMITBREAK-01/ADD-EVENT-TRIM" .planning/phases/30-breaks-in-committed-time/30-RED-secondpath.txt` is greater than 0.
    - The evidence file contains a `git diff --stat` block showing zero paths under `lib/`.
    - `git log -1 --name-only` for this plan's commit lists no path beginning `lib/`.
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$PATH:/home/dan/development/flutter/bin" && flutter test test/providers/schedule_notifier_add_event_test.dart test/screens/lattice_break_pair_test.dart --concurrency=1 2>&1 | tail -5; git diff --stat HEAD~1 --name-only | grep -c '^lib/' || true</automated>
  </verify>
  <done>RED evidence is committed with per-test wave attribution, and the commit provably contains no `lib/` change.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and files Phase 30 creates. This plan produces the rows marked **[30-02]**.

| Symbol / file | Kind | Plan |
|---|---|---|
| `ScheduleGeneratorService.buildCommitmentChunks(CommitmentBlock, {required int longBreakEvery})` | public static method, `lib/services/schedule_generator.dart` | 30-03 |
| `ScheduleGeneratorService.breakCadenceForMood(int moodIndex)` | public static method, same file | 30-03 |
| Narrowed STEP E trim (`commitmentId == null`) | behavior change, same file | 30-03 |
| Narrowed `ScheduleNotifier._trimTrailingNonWork` (`commitmentId == null`) | behavior change, `lib/providers/schedule_notifier.dart` | 30-04 |
| `group('COMMITBREAK — breaks inside a committed block')` + its 6 tests | test group, `test/services/schedule_generator_test.dart` | 30-01 |
| `.planning/phases/30-breaks-in-committed-time/30-RED-generator.txt` | evidence artifact | 30-01 |
| **`COMMITBREAK-01/ADD-EVENT`, `COMMITBREAK-02/ADD-EVENT`, `COMMITBREAK-01/ADD-EVENT-TRIM`** | **3 named tests, `test/providers/schedule_notifier_add_event_test.dart`** | **[30-02]** |
| **`_commitmentDay()`** | **test fixture helper, `test/screens/lattice_break_pair_test.dart`** | **[30-02]** |
| **`COMMITBREAK-01/RENDER`** | **named widget test, same file** | **[30-02]** |
| **`.planning/phases/30-breaks-in-committed-time/30-RED-secondpath.txt`** | **evidence artifact** | **[30-02]** |
| `.planning/phases/30-breaks-in-committed-time/30-GREEN-wave2.txt` | evidence artifact | 30-03 |
| `.planning/phases/30-breaks-in-committed-time/30-GREEN-final.txt` | evidence artifact | 30-04 |
| `CLAUDE.md` § trap #4 (data layer) | documentation | 30-05 |
| `.planning/phases/30-breaks-in-committed-time/30-UAT.md` | UAT verdict record | 30-05 |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hive → `ScheduleNotifier` | `addEventToday` reads a user-authored `CommitmentBlock` and writes chunks back into the persisted `DailySchedule`. Single-user, local-only, no network origin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-03 | Tampering | `_trimTrailingNonWork` deleting persisted committed minutes | medium | mitigate | The trim writes through to Hive via `_repo.save`, so an over-broad trim is durable data loss, not a render glitch. `COMMITBREAK-01/ADD-EVENT-TRIM` pins the exact scenario, and plan `30-04` narrows the predicate to `commitmentId == null`. |
| T-30-04 | Repudiation | break chunks with no `commitmentId` attribution | low | accept | Break chunks are not logged to `CompletionLog` (only work chunks are, via the `commitmentId ?? ''` guard at `scheduled_chunk.dart:57`), so attribution of a break carries no audit consequence. D-30-04 sets `commitmentId` anyway, for the trim predicate. |

**No package-manager installs occur in this phase** — no `pubspec.yaml` change, no new dependency.
The `T-{phase}-SC` supply-chain row and its blocking human checkpoint are not applicable.
</threat_model>

<verification>
- `export PATH="$PATH:/home/dan/development/flutter/bin" && flutter analyze` — clean.
- Both touched files run at `--concurrency=1` and their failures are enumerated by name in `30-RED-secondpath.txt`.
- `CR-01` is green and unchanged (the discretionary half of the trim still works).
- `git diff --stat` for this plan's commit shows zero `lib/` paths.
</verification>

<success_criteria>
- 3 new add-event tests, 3 re-pointed add-event tests, 1 render guard — none deleted.
- `30-RED-secondpath.txt` classifies each test RED or GUARD and names the wave that clears it.
- D-30-04 has an executable proof rather than an assertion.
</success_criteria>

<output>
Create `.planning/phases/30-breaks-in-committed-time/30-02-SUMMARY.md` when done. Record in
`key-decisions` that 30-RESEARCH.md Pitfall 6's premise was corrected (D-30-04) and how it was
proven.
</output>
</content>
