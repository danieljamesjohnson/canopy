---
phase: 08-a-schedule-you-can-read
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/data/models/scheduled_chunk.dart
  - lib/data/models/scheduled_chunk.g.dart
  - lib/data/database/migrations.dart
  - lib/services/schedule_generator.dart
  - test/services/schedule_generator_test.dart
  - test/screens/chunk_card_goal_name_test.dart
  - test/screens/chunk_detail_sheet_test.dart
  - test/providers/schedule_notifier_defer_test.dart
  - test/screens/focus_screen_test.dart
autonomous: true
requirements: [READ-02]
must_haves:
  truths:
    - "Discretionary chunks are assigned synthetic clock times that fill the gaps around anchored commitment windows"
    - "The final chunk list is sorted top-to-bottom in day order by effective start time"
    - "No break is inserted between consecutive commitment-block chunks"
    - "No break appears as the final element of the schedule (trailing break trimmed)"
    - "All four Wave 0 test stub files exist and run (red), unblocking downstream plans"
  artifacts:
    - path: "lib/data/models/scheduled_chunk.dart"
      provides: "isDeferred HiveField(8) + transient syntheticStartMinutes field"
      contains: "isDeferred"
    - path: "lib/data/database/migrations.dart"
      provides: "currentSchemaVersion bumped to 4; _migration3to4 no-op"
      contains: "_migration3to4"
    - path: "lib/services/schedule_generator.dart"
      provides: "READ-02 ordering: synthetic start-time assignment, commitment/discretionary stream split, trailing-break trim"
    - path: "test/screens/chunk_card_goal_name_test.dart"
      provides: "Wave 0 stub for READ-01"
    - path: "test/screens/chunk_detail_sheet_test.dart"
      provides: "Wave 0 stub for READ-03 sheet"
    - path: "test/providers/schedule_notifier_defer_test.dart"
      provides: "Wave 0 stub for READ-03 markDeferred"
    - path: "test/screens/focus_screen_test.dart"
      provides: "Wave 0 stub for READ-04"
  key_links:
    - from: "lib/services/schedule_generator.dart"
      to: "ScheduledChunk.syntheticStartMinutes"
      via: "sort key in generate()"
      pattern: "syntheticStartMinutes"
    - from: "lib/data/database/migrations.dart"
      to: "currentSchemaVersion"
      via: "version bump 3 -> 4"
      pattern: "currentSchemaVersion = 4"
---

<objective>
Establish the data-layer and service-layer foundation for Phase 8, and scaffold all Wave 0 test stubs so downstream plans execute against existing (red) tests.

This plan delivers READ-02 in full: the schedule generator stops inserting breaks between commitment-block chunks, trims any dangling trailing break, assigns synthetic clock times to discretionary chunks so they fill the gaps around anchored commitment windows, and sorts the flat list into day order. It also adds the `isDeferred` HiveField(8) and a transient `syntheticStartMinutes` field on `ScheduledChunk` (needed by READ-03's defer and by the ordering sort), bumps the Hive schema version with a no-op migration, and regenerates the TypeAdapter.

Finally it creates the four Wave 0 test-stub files (per 08-VALIDATION.md) so Nyquist Dimension 8 passes and plans 02/03 have failing tests to turn green.

Purpose: ordering + breaks + schema are the lowest layer everything else depends on; getting them right and unit-tested first de-risks the UI plans.
Output: modified `scheduled_chunk.dart`/`.g.dart`/`migrations.dart`/`schedule_generator.dart`, extended generator tests, and four new Wave 0 test stubs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/08-a-schedule-you-can-read/08-CONTEXT.md
@.planning/phases/08-a-schedule-you-can-read/08-RESEARCH.md
@.planning/phases/08-a-schedule-you-can-read/08-PATTERNS.md
@.planning/phases/08-a-schedule-you-can-read/08-UI-SPEC.md
@.planning/phases/08-a-schedule-you-can-read/08-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add isDeferred HiveField(8) + transient syntheticStartMinutes, bump schema, regenerate adapter</name>
  <files>lib/data/models/scheduled_chunk.dart, lib/data/models/scheduled_chunk.g.dart, lib/data/database/migrations.dart</files>
  <read_first>
    - lib/data/models/scheduled_chunk.dart (HiveField(6)/(7) bool pattern at lines 44-48; constructor at line 12)
    - lib/data/database/migrations.dart (currentSchemaVersion=3 at line 3; _migrations list lines 9-13; no-op _migration2to3 at lines 27-33)
    - 08-PATTERNS.md "scheduled_chunk.dart" and "migrations.dart" sections (exact code to copy)
    - 08-RESEARCH.md Pattern 3 (Hive Schema Addition) and Pitfall 6 (schemaVersion downgrade)
  </read_first>
  <behavior>
    - After regeneration, `ScheduledChunk().isDeferred` defaults to `false`.
    - `ScheduledChunk` exposes a non-persisted mutable `int? syntheticStartMinutes` (NOT a HiveField; assignable post-construction; defaults null).
    - Existing persisted ScheduledChunk records with no HiveField(8) deserialize with `isDeferred == false` (additive-safe).
    - `currentSchemaVersion == 4` and `_migrations` has 4 entries.
  </behavior>
  <action>
    Add `@HiveField(8) bool isDeferred = false;` immediately after the HiveField(7) `isSkipped` declaration in `scheduled_chunk.dart`, mirroring the existing bool field pattern (per D: Hive Schema Change, READ-03 defer support). Add a non-persisted transient field `int? syntheticStartMinutes;` (with the documenting comment from 08-PATTERNS.md) AFTER the HiveField declarations — it must NOT carry a `@HiveField` annotation and must NOT be added to the constructor; it is assigned post-construction by the generator. Do not change the `effectiveStartMinutes` decision: there is no new getter — the generator computes the sort key inline using `anchoredStartMinutes ?? syntheticStartMinutes ?? 9999`.

    In `migrations.dart`, bump `currentSchemaVersion` from `3` to `4`, append `_migration3to4` to the `_migrations` list, and define `_migration3to4` as an async no-op copying the comment style of `_migration2to3` (additive bool, Hive returns false for missing HiveField(8); no data transformation). Never decrement the version (Pitfall 6).

    Regenerate the TypeAdapter with `dart run build_runner build --delete-conflicting-outputs` (per CLAUDE.md / 08-UI-SPEC Hive Schema Change). Do NOT hand-edit `scheduled_chunk.g.dart`.
  </action>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs && flutter analyze lib/data/models/scheduled_chunk.dart lib/data/database/migrations.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' lib/data/models/scheduled_chunk.dart | grep -c 'isDeferred'` >= 1
    - `grep -v '^[[:space:]]*//' lib/data/models/scheduled_chunk.dart | grep -c 'syntheticStartMinutes'` >= 1
    - `grep -c 'currentSchemaVersion = 4' lib/data/database/migrations.dart` == 1
    - `grep -c '_migration3to4' lib/data/database/migrations.dart` >= 2 (list entry + definition)
    - `flutter analyze` reports no new errors in the touched files
  </acceptance_criteria>
  <done>isDeferred HiveField(8) and transient syntheticStartMinutes exist, schema version is 4 with a no-op _migration3to4, and the regenerated adapter analyzes clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite generator ordering/break pass for READ-02 + extend generator tests (Tests 10-13)</name>
  <files>lib/services/schedule_generator.dart, test/services/schedule_generator_test.dart</files>
  <read_first>
    - lib/services/schedule_generator.dart (existing break-insertion loop at lines ~154-188 — the block to replace; imports lines 1-5)
    - 08-PATTERNS.md "schedule_generator.dart" section (Steps A-E + `_assignSyntheticStartTimes` helper — exact algorithm to implement)
    - 08-RESEARCH.md Pattern 2 (ordering algorithm), edge-case table, Pitfalls 2 & 3
    - test/services/schedule_generator_test.dart (existing tests 1-9; helpers makeHabit/makeOutcome/makeBlock/workCount at lines 23-56; Test 6 template at lines 149-169)
  </read_first>
  <behavior>
    - Test 10: commitment block (e.g. 540-600 → 2 anchored 25-min pieces) + discretionary habits → the two commitment chunks are adjacent in the result (no break between them).
    - Test 11: 2 habits only → last element of result is a work chunk (trailing break trimmed).
    - Test 12: commitment block + 1 habit → the discretionary chunk receives a syntheticStartMinutes value placed in a free slot; result is sorted by effective start time (anchored ?? synthetic).
    - Test 13: all-commitment day (no discretionary chunks) → result contains only the commitment work chunks, no break chunks anywhere.
    - Empty schedule (no goals, no blocks) → returns [] (unchanged).
  </behavior>
  <action>
    Replace the existing uniform break-insertion loop (the `for (final chunk in workChunks)` block that appends a break after every chunk including the last) with the Step A-E algorithm from 08-PATTERNS.md, keeping the generator pure Dart with no Flutter imports (per D: all ordering/break logic lives in ScheduleGeneratorService). Implement exactly:

    Step A — split `workChunks` into `commitmentChunks` (anchoredStartMinutes != null, sorted ascending by anchoredStartMinutes) and `discretionaryChunks` (anchoredStartMinutes == null).
    Step B — add a private pure helper `_assignSyntheticStartTimes({required discretionaryChunks, required commitmentChunks, required longBreakEvery})` that builds merged commitment windows, derives free slots from dayStart=480 to cap=1320, and greedily packs each discretionary chunk's `syntheticStartMinutes` (25-min step, accounting for 5-min short / 25-min long break spacing using longBreakEvery). Use the exact window-merge and slot-packing logic in 08-PATTERNS.md. Discretionary chunks that do not fit any slot keep `syntheticStartMinutes == null` and are dropped during the sort (Open Question 1 recommendation: drop overflow).
    Step C — build `result = [...commitmentChunks]`, then append each discretionary chunk followed by its interleaved short/long break (longBreakEvery cadence). Do NOT insert breaks between commitment chunks (Pitfall 2).
    Step D — sort the full flat list by `anchoredStartMinutes ?? syntheticStartMinutes ?? 9999`.
    Step E — trim trailing non-work chunks: `while (result.isNotEmpty && result.last.chunkType != ChunkType.work) result.removeLast();` (Pitfall 3).
    Preserve the existing `if (workChunks.isEmpty) return [];` guard.

    Then extend `test/services/schedule_generator_test.dart` with Tests 10-13 per the templates in 08-PATTERNS.md and 08-RESEARCH.md "Wave 0 Gaps", reusing the existing makeHabit/makeBlock helpers and adding `workChunksOf` / `hasTrailingBreak` helpers. Keep all existing tests 1-9 green.
  </action>
  <verify>
    <automated>flutter test test/services/schedule_generator_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '_assignSyntheticStartTimes' lib/services/schedule_generator.dart` >= 2 (call + definition)
    - `grep -c 'removeLast' lib/services/schedule_generator.dart` >= 1 (trailing-break trim)
    - `grep -v '^[[:space:]]*//' lib/services/schedule_generator.dart | grep -c "import 'package:flutter"` == 0 (still pure Dart)
    - `grep -c 'Test 1[0-3]' test/services/schedule_generator_test.dart` == 4
    - `flutter test test/services/schedule_generator_test.dart` passes (tests 1-13 green)
  </acceptance_criteria>
  <done>Generator splits commitment/discretionary streams, assigns synthetic times, sorts by day order, trims trailing break, inserts no break inside commitment windows; tests 1-13 pass.</done>
</task>

<task type="auto">
  <name>Task 3: Create four Wave 0 test stub files (READ-01/03/04) so downstream plans run red-then-green</name>
  <files>test/screens/chunk_card_goal_name_test.dart, test/screens/chunk_detail_sheet_test.dart, test/providers/schedule_notifier_defer_test.dart, test/screens/focus_screen_test.dart</files>
  <read_first>
    - test/screens/chunk_card_hover_test.dart (widget-test template: imports, _workChunk factory, pumpWithMood usage)
    - test/test_helpers/mood_pump.dart (pumpWithMood signature lines 24-48)
    - test/providers/theme_notifier_test.dart (notifier unit-test template; _CountingListener; ensureInitialized)
    - 08-VALIDATION.md "Wave 0 Requirements" + "Per-Task Verification Map"
    - 08-PATTERNS.md test sections (chunk_card_goal_name_test, chunk_detail_sheet_test, schedule_notifier_defer_test, focus_screen_test analogs)
  </read_first>
  <action>
    Create the four Wave 0 stub test files listed in 08-VALIDATION.md. Each file must compile and run with `flutter test`, asserting the target behavior so it fails RED until its implementation plan lands (downstream plans turn each green). Do NOT implement production code here — these are failing scaffolds.

    - `test/screens/chunk_card_goal_name_test.dart` (READ-01): pump a `ChunkCard` for a work chunk with `goalName: 'Morning Run'` and `displayRationale: 'Daily habit'`; expect `find.text('Morning Run')` (primary title) and `find.text('Daily habit')` (secondary). Because `ChunkCard` does not yet accept `goalName`/`displayRationale`, leave the call written against the target constructor — it will fail to compile/find until Plan 02. Use `pumpWithMood`.
    - `test/screens/chunk_detail_sheet_test.dart` (READ-03): pump `ChunkDetailSheet` (target widget) for an unresolved work chunk with a fake/stub `ScheduleNotifier`; expect the goal name, rationale, and three action labels 'Mark complete' / 'Skip chunk' / 'Defer to later' to be present. Reference the not-yet-created widget path so it is RED.
    - `test/providers/schedule_notifier_defer_test.dart` (READ-03): construct a `ScheduleNotifier` over an in-memory/fake daily-schedule + log repo (mirror InMemoryAppSettingsRepository / a minimal hand-rolled fake per 08-PATTERNS.md "No Analog Found"), seed a schedule with one work chunk, call `markDeferred(chunkId)`, assert the chunk has `isDeferred == true` and `isSkipped == true` and that listeners were notified. RED until Plan 02 adds markDeferred.
    - `test/screens/focus_screen_test.dart` (READ-04): pump `FocusScreen(chunkId: 'c1')` (target widget) inside a router/provider harness sufficient to render; assert the timer label '25:00' (or 'Start 25 min timer' button) is present, and include the dispose-no-leak test pattern from 08-PATTERNS.md (pump then remove → no setState-after-dispose exception). RED until Plan 03.

    Each stub MUST reference the real target symbol/path so it stays red rather than trivially passing.
  </action>
  <verify>
    <automated>flutter test test/screens/chunk_card_goal_name_test.dart test/screens/chunk_detail_sheet_test.dart test/providers/schedule_notifier_defer_test.dart test/screens/focus_screen_test.dart 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist: `ls test/screens/chunk_card_goal_name_test.dart test/screens/chunk_detail_sheet_test.dart test/providers/schedule_notifier_defer_test.dart test/screens/focus_screen_test.dart` succeeds
    - Each file contains at least one `testWidgets(` or `test(` block referencing its target symbol (`ChunkCard` goalName / `ChunkDetailSheet` / `markDeferred` / `FocusScreen`)
    - The four files run (compile or fail as expected RED — not silently passing); no syntax errors that abort collection of the rest of the suite
  </acceptance_criteria>
  <done>Four Wave 0 stub test files exist and run, each asserting its READ-01/03/04 target behavior so downstream plans have failing tests to turn green; Nyquist Dimension 8 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted Hive box → app on launch | Existing ScheduledChunk records (schema v3) read by new v4 code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering / Data integrity | Hive isDeferred HiveField(8) additive migration | mitigate | Additive-only bool field at a new index; Hive CE returns `false` for missing field; no-op `_migration3to4`; never decrement currentSchemaVersion (Pitfall 6). No existing record is rewritten or lost. |
| T-08-02 | Information Disclosure | local Hive store | accept | Local-only single-user app, no PII transmission, no network — no realistic disclosure surface. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new pub packages introduced this phase (08-RESEARCH Package Legitimacy Audit: N/A). No package-manager install tasks. |
</threat_model>

<verification>
- `flutter test test/services/schedule_generator_test.dart` — generator tests 1-13 green.
- `flutter analyze` — no new errors from schema/regeneration.
- The four Wave 0 stub files run (RED expected) and do not abort suite collection.
</verification>

<success_criteria>
- isDeferred HiveField(8) + transient syntheticStartMinutes added; schema at v4 with no-op migration; adapter regenerated and analyzes clean.
- Generator produces day-ordered output with no break inside commitment windows and no trailing break; tests 10-13 pass.
- Four Wave 0 test stubs exist and run, unblocking plans 02 and 03.
</success_criteria>

<output>
Create `.planning/phases/08-a-schedule-you-can-read/08-01-SUMMARY.md` when done.
</output>
