---
phase: 03-schedule-generation-and-morning-check-in
plan: 05
type: execute
wave: 4
depends_on:
  - "03-03"
  - "03-04"
files_modified: []
autonomous: false
requirements:
  - SCHED-01
  - SCHED-02
  - SCHED-03
  - SCHED-04

must_haves:
  truths:
    - "Tapping a mood emoji generates a schedule and shows acknowledgment within 1 second on device"
    - "Commitment block chunks appear at correct anchored times and only on the correct weekdays"
    - "Mood 1 or 2 produces habits-only discretionary schedule (no outcome or time-target goal chunks)"
    - "Break chunks appear in correct positions — 5-min short break after every work chunk; 25-min long break after every 3 chunks (mood 1-2) or 4 chunks (mood 3-5)"
    - "Schedule persists across app restart — relaunching on the same day shows the morning's schedule"
  artifacts:
    - path: lib/services/schedule_generator.dart
      provides: "Algorithm — verified end-to-end"
    - path: lib/screens/schedule/checkin_screen.dart
      provides: "Check-in flow — verified visually"
    - path: lib/screens/schedule/schedule_screen.dart
      provides: "Schedule display — verified visually"
    - path: lib/screens/home/home_screen.dart
      provides: "Home summary — verified visually"
  key_links:
    - from: "Morning mood tap"
      to: "Schedule screen populated"
      via: "CheckinScreen → ScheduleNotifier.generateToday → HiveDailyScheduleRepository.save → ScheduleScreen rebuild"
      pattern: "end-to-end user flow"
---

<objective>
Human end-to-end verification of the full Phase 3 product loop — mood tap through schedule display and persistence.

Purpose: Phase 3 acceptance criteria require human confirmation of visual states, timing, and cross-restart persistence.
Output: Verified working morning loop; any issues logged for gap closure.
</objective>

<execution_context>
@C:/Users/Dan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-schedule-generation-and-morning-check-in/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final analyze and unit test pass</name>
  <files></files>
  <action>
    Run the full test suite and analyzer before handing off to human verification:
    ```bash
    flutter analyze
    flutter test
    ```
    Fix any remaining issues found. Ensure all passing tests from Phase 1 and 2 remain green.
  </action>
  <verify>
    <automated>flutter analyze && flutter test</automated>
  </verify>
  <done>flutter analyze reports zero issues. flutter test passes with no failures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify end-to-end morning loop</name>
  <action>Run the app and walk through the full morning ritual flow as described in how-to-verify steps.</action>
  <verify>Human confirms all 7 verification steps pass.</verify>
  <done>Human types "approved" — all Phase 3 acceptance criteria confirmed visually on device.</done>
  <what-built>
    Complete Phase 3 product loop:
    - Schedule tab shows "Start your day" empty state when no schedule exists
    - Check-in screen with 5 weather emoji tap targets and background tint on tap
    - Mood 1-2 follow-up toggle "Want a lighter day?"
    - Acknowledgment screen with weather-themed copy, chunk count, first chunk name
    - Swipe-up gesture returns to schedule
    - Schedule screen: progress bar, mood-tinted AppBar, chunk card list
    - Work cards: colored left bar, goal name, duration, rationale, optional time label for commitment chunks
    - Short break cards: compact ~48dp strip, pause icon
    - Long break cards: full height, coffee icon
    - Home tab shows schedule summary (progress, mood emoji, next chunk)
  </what-built>
  <how-to-verify>
    Run the app on a device or simulator:
    ```
    flutter run
    ```

    Verify the following flow:

    1. **Empty state:** Navigate to the Schedule tab — confirm "Start your day" button is visible, no schedule shown.

    2. **Check-in flow (mood 3-5):**
       - Tap "Start your day" to open check-in screen
       - Tap the ⛅ emoji (mood 3) — confirm background tints to muted teal (#4A8C7A) immediately
       - Confirm NO follow-up toggle is shown (mood 3 is not 1 or 2)
       - Tap "Let's go" (or confirm button) — confirm acknowledgment text appears ("Partly cloudy — steady. X chunks...")
       - Swipe up — confirm return to schedule screen
       - Confirm schedule screen shows: mood-tinted AppBar, progress bar "0 of X Chunks", card list

    3. **Check-in flow (mood 1-2):**
       - Navigate to Schedule tab, tap "Start your day" (or regenerate by tapping check-in again — silent replace)
       - Tap the 🌧️ emoji (mood 1) — confirm "Want a lighter day?" toggle appears defaulting to Yes
       - Confirm acknowledgment uses "Stormy day — keeping it light." prefix
       - Verify generated schedule has fewer work chunks than mood 5 and shows only habits (if any) in discretionary slots

    4. **Break structure:**
       - In the generated schedule, verify a short break card (compact ~48dp, pause icon) follows each work chunk
       - For mood 3-5: verify the long break card (full height, coffee icon) appears after every 4th work chunk
       - For mood 1-2: verify the long break card appears after every 3rd work chunk

    5. **Commitment block chunks (if any commitment blocks were created in Phase 2 onboarding):**
       - Verify commitment chunks show a time label (e.g. "9:00 AM")
       - Verify discretionary chunks show NO time label

    6. **Home tab:**
       - Navigate to Home tab — confirm progress bar + mood emoji + "Up next: [chunk name]" is visible
       - If no goals/blocks were created: confirm "No schedule yet" or empty state is clean

    7. **Persistence:**
       - Hot restart the app (Shift+R in terminal or restart button)
       - Navigate to Schedule tab — confirm today's schedule is still shown (not blank)
       - Navigate to Home tab — confirm summary is still present
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checks pass, or describe any issues found (e.g. "break cards missing", "persistence failed", "mood color wrong").
  </resume-signal>
</task>

</tasks>

<verification>
Human UAT covers all 5 Phase 3 acceptance criteria from ROADMAP.md:
1. Mood tap → schedule generated and displayed within 1 second
2. Commitment block chunks at correct anchored times
3. Mood 1-2 produces habits-only discretionary schedule
4. Break chunks in correct positions
5. Schedule persists across app restart
</verification>

<success_criteria>
Human approves all 7 verification steps. flutter analyze clean. flutter test all passing. Phase 3 acceptance criteria confirmed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-schedule-generation-and-morning-check-in/03-05-SUMMARY.md`
</output>
