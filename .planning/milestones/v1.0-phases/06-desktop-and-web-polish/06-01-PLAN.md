---
phase: 06-desktop-and-web-polish
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/data/models/app_settings.dart
  - lib/data/database/migrations.dart
  - test/test_helpers/mood_pump.dart
  - test/test_helpers/viewport.dart
autonomous: true
requirements: [AC-3, AC-5, AC-6]
threat_refs: [T-platform-1]

must_haves:
  truths:
    - "window_manager package is available at compile time on desktop targets"
    - "AppSettings can persist a nullable moodSeedArgb int (null = pre-check-in)"
    - "Existing widget tests can be migrated to pumpWithMood without color-assertion breakage"
    - "tester.view.physicalSize is set with automatic teardown so test order is stable"
  artifacts:
    - path: pubspec.yaml
      provides: "window_manager: ^0.5.1 dependency line"
      contains: "window_manager: ^0.5.1"
    - path: lib/data/models/app_settings.dart
      provides: "moodSeedArgb int? field at HiveField(5)"
      contains: "@HiveField(5)"
    - path: lib/data/database/migrations.dart
      provides: "_migration2to3 no-op + currentSchemaVersion=3"
      contains: "_migration2to3"
    - path: test/test_helpers/mood_pump.dart
      provides: "pumpWithMood helper with moodIndex=3 default"
      exports: ["pumpWithMood"]
    - path: test/test_helpers/viewport.dart
      provides: "setViewport helper with addTearDown teardown"
      exports: ["setViewport"]
  key_links:
    - from: "lib/data/database/migrations.dart"
      to: "lib/data/models/app_settings.dart"
      via: "schemaVersion bump aligns with new @HiveField(5) addition"
      pattern: "currentSchemaVersion = 3"
    - from: "test/test_helpers/mood_pump.dart"
      to: "lib/providers/theme_notifier.dart"
      via: "pumpWithMood reads ThemeNotifier.moodSeeds static const (resolved at Plan 02)"
      pattern: "ThemeNotifier.moodSeeds\\[moodIndex\\]"
---

<objective>
Lay the Wave 0 foundations: add the `window_manager` dependency, bump the Hive schema additively to persist `moodSeedArgb` on `AppSettings`, and create two test helpers (`pumpWithMood`, `setViewport`) that every Phase 6 widget test will use.

Purpose: Unblock parallel Wave 1 work. Plans 02 (ThemeNotifier) and 03 (window setup) depend on the schema bump and the dependency line being present. The test helpers belong to Wave 0 because Wave 4 tests reference them.

Output: pubspec.yaml updated; AppSettings + migrations.dart bumped to schemaVersion 3; two test-helper files created; build_runner regenerates `app_settings.g.dart`. No runtime behavior change yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-desktop-and-web-polish/06-CONTEXT.md
@.planning/phases/06-desktop-and-web-polish/06-RESEARCH.md
@.planning/phases/06-desktop-and-web-polish/06-PATTERNS.md
@.planning/phases/06-desktop-and-web-polish/06-UI-SPEC.md
@.planning/phases/06-desktop-and-web-polish/06-VALIDATION.md
@CLAUDE.md
@pubspec.yaml
@lib/data/models/app_settings.dart
@lib/data/database/migrations.dart
@test/screens/quarterly_review_test.dart

<interfaces>
From lib/data/models/app_settings.dart (current state):
- @HiveType(typeId: 6) class AppSettings extends HiveObject
- @HiveField(0) int morningNotificationMinutes
- @HiveField(1) bool onboardingComplete
- @HiveField(2) bool midDayNudgeEnabled
- @HiveField(3) int midDayNudgeMinutes
- @HiveField(4) bool morningNotificationEnabled
- Next free HiveField index: 5

From lib/data/database/migrations.dart (current state):
- const int currentSchemaVersion = 2
- typedef MigrationFn = Future<void> Function()
- final List<MigrationFn> _migrations = [_migration0to1, _migration1to2]
- Future<void> runMigrations(SharedPreferences prefs)
- Pattern: append new migration to list, bump currentSchemaVersion by 1

From RESEARCH.md Pattern 5 + Pattern 6:
- pumpWithMood(WidgetTester tester, Widget child, {int moodIndex = 3, Iterable<ChangeNotifierProvider> extraProviders = const []})
  - default moodIndex 3 = #4A8C7A (UI-SPEC locked)
  - wraps in MultiProvider + MaterialApp with ColorScheme.fromSeed
- setViewport(WidgetTester tester, Size size)
  - sets tester.view.devicePixelRatio = 1.0 + tester.view.physicalSize = size
  - calls addTearDown(tester.view.reset) — Pitfall 4 lock

Existing pubspec dependency line shape: `package_name: ^version`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add window_manager dependency + bump Hive schema to v3 for moodSeedArgb</name>
  <files>pubspec.yaml, lib/data/models/app_settings.dart, lib/data/database/migrations.dart</files>
  <read_first>
    - pubspec.yaml (current dependency block, after `fl_chart: ^1.2.0` line)
    - lib/data/models/app_settings.dart (current 5 @HiveField slots 0-4)
    - lib/data/database/migrations.dart (no-op migration pattern from _migration1to2)
    - .planning/phases/06-desktop-and-web-polish/06-PATTERNS.md (Pattern G: AppSettings Hive Schema Bump section, lines 1193-1207)
    - .planning/phases/06-desktop-and-web-polish/06-RESEARCH.md (Runtime State Inventory and Standard Stack window_manager row)
  </read_first>
  <action>
    Edit pubspec.yaml: after the `fl_chart: ^1.2.0` line in the `dependencies:` block, add `window_manager: ^0.5.1` on its own line preserving two-space indentation. The caret syntax matches every other dependency per UI-SPEC Registry Safety. Do not add to dev_dependencies.

    Edit lib/data/models/app_settings.dart: add a new field after `morningNotificationEnabled` (HiveField 4). Use index 5 (next free slot). Field declaration: `@HiveField(5) int? moodSeedArgb;` with doc comment "Mood seed ARGB int value. Null = pre-check-in 'curious' state (UI-SPEC §Color §Pre-Check-in Curious Seed). Set when user taps a mood at check-in; cleared on daily rollover (Phase 6 D-10)." Per D-10 — no carry-forward, always nullable. Note: do NOT initialize to a default value because nullable Hive fields default to null on decode of pre-Phase-6 records (per `_migration1to2` no-op pattern).

    Edit lib/data/database/migrations.dart: bump `currentSchemaVersion` from 2 to 3. Append `_migration2to3` to the `_migrations` list after `_migration1to2`. Add the function body as a no-op with comment "Phase 6: AppSettings expanded with nullable moodSeedArgb (HiveField 5). No data transformation needed — Hive binary reader returns null for missing nullable fields in existing records (per Phase 2 _migration1to2 pattern)."

    After edits, run `flutter pub get` (regenerates pubspec.lock + Podfile.lock + Windows/Linux CMake plugin registrations). Then run `dart run build_runner build --delete-conflicting-outputs` to regenerate `lib/data/models/app_settings.g.dart` with the new HiveField 5 slot. Both commands must complete with exit code 0.
  </action>
  <verify>
    <automated>flutter pub get &amp;&amp; dart run build_runner build --delete-conflicting-outputs &amp;&amp; flutter analyze 2>&amp;1 | tail -20 &amp;&amp; grep -q "window_manager: \\^0.5.1" pubspec.yaml &amp;&amp; grep -q "@HiveField(5)" lib/data/models/app_settings.dart &amp;&amp; grep -q "currentSchemaVersion = 3" lib/data/database/migrations.dart &amp;&amp; grep -q "_migration2to3" lib/data/database/migrations.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep "window_manager: \\^0.5.1" pubspec.yaml` exits 0
    - `grep "@HiveField(5)" lib/data/models/app_settings.dart` exits 0
    - `grep "int? moodSeedArgb" lib/data/models/app_settings.dart` exits 0
    - `grep "currentSchemaVersion = 3" lib/data/database/migrations.dart` exits 0
    - `grep "_migration2to3" lib/data/database/migrations.dart` exits 0
    - `flutter analyze` reports 0 issues (matches AC-5 baseline)
    - `lib/data/models/app_settings.g.dart` contains a reference to the new field (verified after build_runner regen via `grep "moodSeedArgb" lib/data/models/app_settings.g.dart`)
    - `flutter pub get` exits 0 — pubspec.lock now contains a window_manager entry
  </acceptance_criteria>
  <done>
    pubspec.yaml has `window_manager: ^0.5.1` in dependencies. AppSettings has `@HiveField(5) int? moodSeedArgb;` with doc comment. migrations.dart has `_migration2to3` no-op + `currentSchemaVersion = 3`. Build runner regenerates the .g.dart adapter cleanly. `flutter analyze` is clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify macOS MainFlutterWindow.swift compatibility and create test helpers</name>
  <files>test/test_helpers/mood_pump.dart, test/test_helpers/viewport.dart</files>
  <read_first>
    - macos/Runner/MainFlutterWindow.swift (currently extends NSWindow per `class MainFlutterWindow: NSWindow` — must verify this is preserved)
    - .planning/phases/06-desktop-and-web-polish/06-RESEARCH.md Pitfall 1 + Open Question 2 + Pattern 5 (mood_pump example lines 522-553) + Pattern 6 (viewport example lines 572-579)
    - .planning/phases/06-desktop-and-web-polish/06-PATTERNS.md "test/test_helpers/mood_pump.dart (NEW)" + "test/test_helpers/viewport.dart (NEW)" sections (lines ~813-870)
    - .planning/phases/06-desktop-and-web-polish/06-UI-SPEC.md "Widget Test Mood-Pinning Strategy" section
    - test/screens/quarterly_review_test.dart (the `_wrap` helper at line 31 — the analog being replaced)
  </read_first>
  <action>
    Verify macOS host: cat `macos/Runner/MainFlutterWindow.swift` and confirm the class declaration is `class MainFlutterWindow: NSWindow`. The current file already satisfies window_manager's README requirement — no edit needed. If the file diverges from this contract (which it does not at planning time), apply the minimal NSWindow extension snippet from window_manager's README. Print "macOS host verified compatible" to stdout in the verify command so the auditor sees the check ran.

    Create test/test_helpers/mood_pump.dart per RESEARCH.md Pattern 5. Imports: `package:canopy/providers/theme_notifier.dart`, `package:flutter/material.dart`, `package:flutter_test/flutter_test.dart`, `package:provider/provider.dart`. Top-level function `Future<void> pumpWithMood(WidgetTester tester, Widget child, {int moodIndex = 3, Iterable<ChangeNotifierProvider> extraProviders = const []})`. Body: reads seed from `ThemeNotifier.moodSeeds[moodIndex]!`, builds `ThemeData(useMaterial3: true, colorScheme: ColorScheme.fromSeed(seedColor: seed))`, calls `tester.pumpWidget(MultiProvider(providers: [...extraProviders], child: MaterialApp(theme: theme, home: Scaffold(body: child))))`. Default moodIndex 3 corresponds to `#4A8C7A` (UI-SPEC locked).

    Per D-XX traceability: this helper is the "test fixture that pins a known mood" required by AC-6's deferred wording. It MUST NOT instantiate a real ThemeNotifier (no time-of-day modulator runs in test paths — UI-SPEC §Color §Time-of-Day Modulation last row "Test fixture: Time-of-day modulation disabled under flutter test").

    Note: `ThemeNotifier.moodSeeds` is created in Plan 02. Plan 01 references it but Plan 01 is in Wave 0 and Plan 02 is in Wave 1; the import resolves at Plan 02 completion. To avoid blocking Wave 0 verification, also create a forward-declaration comment "// ThemeNotifier.moodSeeds will be defined in lib/providers/theme_notifier.dart (Plan 02)" above the import. The file will not compile until Plan 02 lands — this is expected; Wave 1 finalizes the contract. Plan 02's `<verify>` re-runs `flutter analyze` to confirm the helper compiles end-to-end.

    Create test/test_helpers/viewport.dart per RESEARCH.md Pattern 6 + PATTERNS.md. Imports: `package:flutter/material.dart`, `package:flutter_test/flutter_test.dart`. Top-level function `void setViewport(WidgetTester tester, Size size)`. Body: `tester.view.devicePixelRatio = 1.0; tester.view.physicalSize = size; addTearDown(tester.view.reset);`. Add doc comment citing RESEARCH.md Pitfall 4 (test-order flakiness without addTearDown).
  </action>
  <verify>
    <automated>test -f test/test_helpers/mood_pump.dart &amp;&amp; test -f test/test_helpers/viewport.dart &amp;&amp; grep -q "class MainFlutterWindow: NSWindow" macos/Runner/MainFlutterWindow.swift &amp;&amp; grep -q "pumpWithMood" test/test_helpers/mood_pump.dart &amp;&amp; grep -q "setViewport" test/test_helpers/viewport.dart &amp;&amp; grep -q "addTearDown" test/test_helpers/viewport.dart &amp;&amp; grep -q "moodIndex = 3" test/test_helpers/mood_pump.dart &amp;&amp; echo "macOS host verified compatible"</automated>
  </verify>
  <acceptance_criteria>
    - File `test/test_helpers/mood_pump.dart` exists with `pumpWithMood` function signature matching RESEARCH.md Pattern 5
    - File `test/test_helpers/viewport.dart` exists with `setViewport` function calling `addTearDown(tester.view.reset)`
    - `mood_pump.dart` default `moodIndex` is 3 (verified by `grep "moodIndex = 3"`)
    - `mood_pump.dart` imports `ThemeNotifier` from `package:canopy/providers/theme_notifier.dart` (the file landing in Plan 02 — analyzer will flag until Plan 02 lands; that is intentional and resolved end of Wave 1)
    - macOS MainFlutterWindow.swift still declares `class MainFlutterWindow: NSWindow` (no breakage)
    - The two helper files are syntactically valid Dart (verified by `dart format --output=none test/test_helpers/mood_pump.dart test/test_helpers/viewport.dart` exiting 0)
  </acceptance_criteria>
  <done>
    Two test-helper files exist with the documented signatures. macOS host verified compatible with window_manager. `flutter analyze` may report an unresolved import on `mood_pump.dart` (resolves when Plan 02 lands ThemeNotifier) — track in plan summary and verify in Plan 02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host OS → Flutter desktop process | window_manager FFI call sets a min-window-size constraint at the OS window-manager level. Untrusted code in the Flutter app cannot inflate or remove the minimum after `setMinimumSize` runs in main() (single call, no re-set path). |
| Hive disk file → process | New `moodSeedArgb` int field decodes from existing on-disk records as null. No untrusted input expands the schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-platform-1 | Tampering | Desktop window min-size bypass | accept | window_manager runs at startup before runApp; no user-facing API to disable. Defense-in-depth not warranted for a UX guarantee (480x640 prevents layout collapse, not security). |
| T-06-01-1 | Information Disclosure | moodSeedArgb persisted in Hive AppSettings record | accept | Hive box is local-only (no sync per PROJECT.md), no PII in a color int, no remote attacker model exists in v1. |
| T-06-01-2 | Denial of Service | Malformed pubspec.lock after window_manager addition | mitigate | `flutter pub get` is the only path that mutates pubspec.lock; verified in task verify step. Lock-file regeneration tested via the `flutter analyze` clean exit gate. |

No new high-severity threats introduced by Plan 01. ASVS L1 review: no auth, no untrusted input, no cryptographic state change — N/A.
</threat_model>

<verification>
After both tasks land:
- `flutter pub get` exits 0
- `dart run build_runner build --delete-conflicting-outputs` regenerates `app_settings.g.dart` with the new field
- `flutter analyze` exits 0 (note: the mood_pump.dart import of ThemeNotifier will resolve only once Plan 02 lands; track as a known transient)
- `flutter test` exits 0 against the existing test suite (no test references the new helpers or schema field yet)
</verification>

<success_criteria>
- pubspec.yaml has `window_manager: ^0.5.1` exactly as specified
- AppSettings has `@HiveField(5) int? moodSeedArgb` with doc comment referencing D-10
- migrations.dart has `_migration2to3` registered and `currentSchemaVersion = 3`
- `test/test_helpers/mood_pump.dart` and `test/test_helpers/viewport.dart` exist with the documented signatures
- macOS host verified compatible with window_manager (no Swift edit needed)
- All four ROADMAP-impacted ACs (AC-3 sets up window_manager dependency, AC-5 sets up the helper for migration, AC-6 sets up persistence, AC-1 unblocked for Wave 4 viewport tests) have their Wave 0 prerequisites satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/06-desktop-and-web-polish/06-01-SUMMARY.md` listing files added/modified, exact `window_manager` version pulled, the new schemaVersion integer, and a one-line note that `mood_pump.dart` references a forward-declared `ThemeNotifier.moodSeeds` that Plan 02 supplies.
</output>
