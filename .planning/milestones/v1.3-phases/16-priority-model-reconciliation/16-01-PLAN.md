---
phase: 16-priority-model-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/screens/goal_card_priority_chip_rebuild_test.dart
  - test/screens/goal_form_priority_test.dart
  - lib/screens/goals/goal_form_sheet.dart
autonomous: true
requirements: [PRIORITY-03, GOALFORM-02]

must_haves:
  truths:
    - "After reorderAllWithPriority moves a High goal to a mid-list (Normal) position, the goal's chip no longer shows High — the chip reflects the goal's CURRENT priorityWeight, not a stale value."
    - "After reorderAllWithPriority promotes a goal to position 0, that goal's chip shows High in the same rebuild."
    - "At the goal sheet's true opened modal height (initialChildSize 0.6 on a 390x844 viewport), the Priority selector (SegmentedButton<double>) is reachable via scrollUntilVisible for time-target, outcome, AND habit goals."
    - "At the same true modal height, the Save/Add goal button (ElevatedButton) is reachable via scrollUntilVisible for all three goal types."
    - "Selecting High and saving still persists priorityWeight == 0.75 (assertion preserved from the replaced setSurfaceSize test)."
    - "A regular-time goal still defaults the weekly budget to 3.0 hrs and persists it on save (assertion preserved from the replaced setSurfaceSize test)."
  artifacts:
    - path: "test/screens/goal_card_priority_chip_rebuild_test.dart"
      provides: "PRIORITY-03 widget test — chip reflects fresh priorityWeight after reorder rebuild"
      contains: "reorderAllWithPriority"
      min_lines: 60
    - path: "test/screens/goal_form_priority_test.dart"
      provides: "GOALFORM-02 true-modal-height group replacing the two setSurfaceSize tests"
      contains: "scrollUntilVisible"
  key_links:
    - from: "test/screens/goal_card_priority_chip_rebuild_test.dart"
      to: "GoalsNotifier.reorderAllWithPriority -> loadGoals -> notifyListeners -> Consumer<GoalsNotifier> -> GoalCard -> _PriorityChip"
      via: "Consumer rebuild"
      pattern: "reorderAllWithPriority"
    - from: "test/screens/goal_form_priority_test.dart"
      to: "showModalBottomSheet -> DraggableScrollableSheet(initialChildSize 0.6) -> GoalFormSheet"
      via: "scrollUntilVisible(scrollable: find.byType(SingleChildScrollView))"
      pattern: "scrollUntilVisible"
---

<objective>
Prove the priority model is coherent across both write paths, with automated tests — without changing production behavior (LOCKED decision D-01).

This phase has two requirements:
- **PRIORITY-03**: A widget test proves the priority chip reflects a goal's CURRENT `priorityWeight` after a drag/reorder rebuild — correct Low/Normal/High label, no stale value. Per D-01, position IS the priority model: dragging a goal to a mid-list position CORRECTLY makes it Normal (no chip). This is verified, not "fixed".
- **GOALFORM-02**: Replace the two `setSurfaceSize(800, 1200)` tests in `goal_form_priority_test.dart` with true-modal-height tests (390x844 viewport + `showModalBottomSheet` + `DraggableScrollableSheet` initialChildSize 0.6 + `scrollUntilVisible`) covering time-target, outcome, AND habit goals — folding in the preserved High-saves-0.75 and 3-hr-default assertions so no coverage is lost.

Purpose: Lock the priority chip's correctness-after-interaction and the goal sheet's reachability contract behind automated regressions, so future engine/UI changes cannot silently reintroduce stale chips or clipped Save buttons.
Output: One new test file (PRIORITY-03), one modified test file (GOALFORM-02). Production `goal_form_sheet.dart` is touched ONLY if the true-height test genuinely fails at maxChildSize 1.0 (research indicates this will not happen — `SingleChildScrollView` is already in place).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-priority-model-reconciliation/16-CONTEXT.md
@.planning/phases/16-priority-model-reconciliation/16-RESEARCH.md
@.planning/phases/16-priority-model-reconciliation/16-PATTERNS.md
@.planning/phases/16-priority-model-reconciliation/16-UI-SPEC.md
@.planning/phases/16-priority-model-reconciliation/16-VALIDATION.md

# Source files this plan exercises (read before writing tests):
@lib/providers/goals_notifier.dart
@lib/screens/goals/widgets/goal_card.dart
@lib/screens/goals/goal_form_sheet.dart

# Test analogs (copy patterns from these):
@test/screens/goal_card_priority_chip_test.dart
@test/screens/goal_form_priority_test.dart
@test/test_helpers/mood_pump.dart
@test/test_helpers/viewport.dart
</context>

<tooling_note>
Flutter/Dart are NOT on the default PATH. Use the full path for every command:
`/home/dan/development/flutter/bin/flutter test ...`
`/home/dan/development/flutter/bin/flutter analyze`

Quick run during execution:
`/home/dan/development/flutter/bin/flutter test test/screens/goal_form_priority_test.dart test/screens/goal_card_priority_chip_rebuild_test.dart`
Full suite before phase gate:
`/home/dan/development/flutter/bin/flutter test`
</tooling_note>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PRIORITY-03 — widget test proving chip reflects fresh priorityWeight after reorder</name>
  <files>test/screens/goal_card_priority_chip_rebuild_test.dart</files>
  <read_first>
    - test/screens/goal_card_priority_chip_test.dart (imports, _stubGoal pattern, chip assertions — note `color: '#4CAF50'` is passed; GoalType.timeTarget.index for this phase)
    - test/screens/goal_form_priority_test.dart lines 23-45 (_InMemoryGoalRepository shape; this NEW file uses the leaner variant without lastSaved per 16-PATTERNS.md)
    - test/test_helpers/mood_pump.dart (pumpWithMood signature: moodIndex defaults to 3, extraProviders for ChangeNotifierProvider)
    - lib/providers/goals_notifier.dart (reorderAllWithPriority linear-spread formula: high=0.75, low=0.25; loadGoals -> notifyListeners; timeTargetGoals getter)
    - lib/screens/goals/widgets/goal_card.dart (_PriorityChip band thresholds: >=0.75 High + Icons.arrow_upward; <=0.25 Low + Icons.arrow_downward; else SizedBox.shrink)
    - 16-PATTERNS.md "Core Consumer rebuild test pattern" and "Chip render assertion pattern"
  </read_first>
  <behavior>
    Per the linear-spread formula reorderAllWithPriority(n=3) writes weights {pos0: 0.75, pos1: 0.5, pos2: 0.25}:
    - Setup: three GoalType.timeTarget goals in an _InMemoryGoalRepository — g0 (priorityWeight 0.75), g1 (0.5), g2 (0.25) — loaded into a GoalsNotifier. Pump a slim `Consumer<GoalsNotifier>` wrapping `Column(children: n.timeTargetGoals.map((g) => GoalCard(goal: g)))` via pumpWithMood with the notifier as an extraProvider.
    - Test 1 (initial state): before any reorder, exactly one 'High' chip is shown (g0) and exactly one 'Low' chip is shown (g2). g1 (Normal) shows neither.
    - Test 2 (stale-chip regression — the core PRIORITY-03 proof): call `await notifier.reorderAllWithPriority([g1.id, g0.id, g2.id])` then `await tester.pumpAndSettle()`. New order weights: g1->0.75 (High), g0->0.5 (Normal/no chip), g2->0.25 (Low). Assert `find.text('High')` is findsOneWidget (it is g1 now, NOT g0 — proves the High chip did not stay stale on g0). Assert `find.text('Low')` is findsOneWidget. Total High chip count is exactly 1, proving the demoted g0 dropped its High chip atomically with the rebuild (no stale duplicate).
    - Assertions must read state THROUGH the rebuilt widget tree (find.text after pumpAndSettle), NOT from a Goal reference captured before the async call (16-RESEARCH.md Pitfall 4).
  </behavior>
  <action>
    Create test/screens/goal_card_priority_chip_rebuild_test.dart. Copy the import block and the leaner _InMemoryGoalRepository (no lastSaved field) per 16-PATTERNS.md. Build goals with `GoalType.timeTarget.index` and explicit priorityWeight values; include `color: '#4CAF50'` to match the existing _stubGoal pattern. Load them into a GoalsNotifier via loadGoals(), then pump a Consumer<GoalsNotifier> wrapping Column(GoalCard) under pumpWithMood with the notifier supplied through extraProviders. Write the two behaviors above as separate testWidgets cases (or one case with the before/after assertions clearly separated). After the reorder call, ALWAYS `await tester.pumpAndSettle()` before asserting. Use `find.text('High')`/`find.text('Low')` and the chip icons (Icons.arrow_upward / Icons.arrow_downward) for assertions. Add a header comment citing PRIORITY-03 and D-01 (position IS the priority model; this test verifies the rebuild path, it does NOT change production code). Run `/home/dan/development/flutter/bin/flutter analyze` on the new file — it must be lint-clean.
    Do NOT add or modify any production code for this task. If the test fails because the chip is genuinely stale, that is a real PRIORITY-03 bug in the Consumer rebuild path — report it; do not paper over it by reading from notifier internals.
  </action>
  <verify>
    <automated>/home/dan/development/flutter/bin/flutter test test/screens/goal_card_priority_chip_rebuild_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - File test/screens/goal_card_priority_chip_rebuild_test.dart exists and passes.
    - Before reorder: exactly one High chip and one Low chip.
    - After reorderAllWithPriority([g1,g0,g2]) + pumpAndSettle: exactly one High chip (on g1, not g0), exactly one Low chip — proving no stale chip survives the rebuild.
    - `flutter analyze` reports no issues for the new file.
    - No production code changed.
  </acceptance_criteria>
  <done>PRIORITY-03 is proven by an automated widget test: the priority chip reflects the goal's current priorityWeight after a reorder rebuild, with no stale value, and no production change was needed.</done>
</task>

<task type="auto">
  <name>Task 2: GOALFORM-02 — replace setSurfaceSize tests with true-modal-height scroll tests for all three goal types</name>
  <files>test/screens/goal_form_priority_test.dart, lib/screens/goals/goal_form_sheet.dart</files>
  <read_first>
    - test/screens/goal_form_priority_test.dart (FULL file — note the 5 surviving tests at lines 90-117, 166-239 stay UNCHANGED; the two to replace are lines 118-164 (High saves 0.75) and 241-273 (3-hr default); the file already defines _InMemoryGoalRepository with lastSaved and uses _pumpForm for the surviving tests)
    - test/test_helpers/viewport.dart (setViewport sets dpr 1.0 + physicalSize and auto-registers addTearDown(tester.view.reset) — caller adds NO teardown)
    - 16-RESEARCH.md "GOALFORM-02 Modal Test Pattern" and "Code Examples" (context-capture via Builder, showModalBottomSheet NOT awaited, DraggableScrollableSheet params, scrollUntilVisible scrollable arg)
    - 16-PATTERNS.md "Modal pump pattern", "Goal type selection pattern", "scrollUntilVisible pattern", "Save-persistence assertion pattern", "Weekly-budget assertion pattern", "Deprecated Patterns Being Replaced"
    - lib/screens/goals/goal_form_sheet.dart (ctor: GoalFormSheet({required scrollController, goal}); SingleChildScrollView(controller: widget.scrollController) wraps the form; SegmentedButton<double>; goal-type picker copy strings)
  </read_first>
  <action>
    In test/screens/goal_form_priority_test.dart:
    1. Add `import '../test_helpers/viewport.dart';` to the import block.
    2. DELETE the two tests that call `tester.binding.setSurfaceSize(const Size(800, 1200))`: 'tapping High selects it and save persists priorityWeight == 0.75' and 'selecting Regular time defaults the weekly budget to 3 hrs and saves it'. Also delete their `addTearDown(() => tester.binding.setSurfaceSize(null))` lines. Leave the other 5 tests and the existing _pumpForm helper untouched (the surviving tests still use _pumpForm).
    3. Add a new `group('GOALFORM-02 — true modal height contract', () { ... })`. Inside it, write a local helper that: calls `setViewport(tester, const Size(390, 844))`; builds an _InMemoryGoalRepository + GoalsNotifier; pumps a `Builder` that captures BuildContext via pumpWithMood (with the notifier in extraProviders); then opens the form via `showModalBottomSheet(context: capturedCtx, isScrollControlled: true, useSafeArea: true, builder: (_) => DraggableScrollableSheet(initialChildSize: 0.6, minChildSize: 0.4, maxChildSize: 1.0, expand: false, snap: true, snapSizes: const [0.6, 1.0], builder: (_, sc) => GoalFormSheet(scrollController: sc)))` WITHOUT awaiting it, followed by `await tester.pumpAndSettle()`. The helper returns the repo for save-assertion tests.
    4. Three reachability tests (one per goal type — time-target, outcome, habit): open the modal, tap the goal-type picker for that type using the EXACT copy strings ('I want to spend regular time on something', "I'm working toward a specific outcome", 'I want to build a daily habit'), pumpAndSettle, then `await tester.scrollUntilVisible(find.byType(SegmentedButton<double>), 100, scrollable: find.byType(SingleChildScrollView))` and assert findsOneWidget; then `await tester.scrollUntilVisible(find.byType(ElevatedButton), 100, scrollable: find.byType(SingleChildScrollView))` and assert findsOneWidget. Use `find.byType(SegmentedButton<double>)` WITH the type parameter (16-RESEARCH.md Pitfall 6). The outcome test MUST require real scrolling (content ~692px > 506px modal) — do not weaken it by enlarging the viewport.
    5. Preserve the two folded-in assertions inside this group at true modal height (NOT setSurfaceSize): (a) a test that selects time-target, taps High via scrollUntilVisible+tap, enters a goal name, scrolls to and taps 'Add goal'.last, and asserts `repo.lastSaved?.priorityWeight` closeTo(0.75, 0.001); (b) a test that selects time-target, asserts the weekly budget field shows '3.0' (scroll to it if needed), taps Save, and asserts `repo.lastSaved?.weeklyHourBudget` closeTo(3.0, 0.001). Enter a goal name before saving (Save is disabled until name is non-empty) and use 'Add goal'.last (the title text also reads 'Add goal').
    6. RESTRUCTURE CONDITION (D-01 / 16-UI-SPEC): Only if the outcome reachability test FAILS at maxChildSize 1.0 (Save genuinely unreachable even after full scroll) may you minimally restructure lib/screens/goals/goal_form_sheet.dart so Priority + Save are reachable — research expects this NOT to be needed because SingleChildScrollView already wraps the form. If you do not touch the file, leave it unmodified (it is listed in files_modified only to reserve the conditional edit). Do NOT add any code that makes the Low/Normal/High selector override drag (forbidden by D-01).
    Run `/home/dan/development/flutter/bin/flutter analyze` — must be lint-clean.
  </action>
  <verify>
    <automated>/home/dan/development/flutter/bin/flutter test test/screens/goal_form_priority_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - The two setSurfaceSize(800,1200) tests are gone; the file no longer references tester.binding.setSurfaceSize.
    - The 5 previously-surviving tests still pass unchanged.
    - New group 'GOALFORM-02 — true modal height contract' contains reachability tests for time-target, outcome, AND habit that find SegmentedButton<double> and ElevatedButton via scrollUntilVisible at 390x844 / initialChildSize 0.6.
    - The outcome test does real scrolling (does not pass by finding the widget already on-screen).
    - High-saves-0.75 and 3-hr-default assertions are preserved at true modal height (repo.lastSaved checks pass).
    - `flutter analyze` clean. goal_form_sheet.dart is modified ONLY if the outcome test failed at maxChildSize 1.0; otherwise unchanged.
  </acceptance_criteria>
  <done>GOALFORM-02 is proven: at the goal sheet's true opened modal height, Priority and Save are reachable via scroll for every goal type, the deprecated setSurfaceSize tests are replaced, and no save-assertion coverage was lost.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
**New test files:**
- `test/screens/goal_card_priority_chip_rebuild_test.dart` — PRIORITY-03 chip-rebuild-after-reorder widget test.

**Modified test files:**
- `test/screens/goal_form_priority_test.dart` — adds `group('GOALFORM-02 — true modal height contract')` (reachability for time-target/outcome/habit + folded-in High-saves-0.75 and 3-hr-default assertions); removes the two `setSurfaceSize(800, 1200)` tests; adds `import '../test_helpers/viewport.dart'`.

**Conditionally modified production file:**
- `lib/screens/goals/goal_form_sheet.dart` — only if the outcome reachability test fails at maxChildSize 1.0 (not expected per research). No new symbols.

**New symbols:** none in production. New test group name: `GOALFORM-02 — true modal height contract`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This phase adds/changes only test code (and at most a layout-only restructure of an existing local sheet). No new network, auth, session, storage, IPC, or external-input surface is introduced. The priorityWeight field already existed and is unchanged in shape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | Test correctness (a test that passes without exercising behavior) | mitigate | GOALFORM-02 outcome test MUST require real scrolling (content 692px > 506px modal); acceptance criteria forbid weakening via oversized viewport. PRIORITY-03 asserts through the rebuilt tree, not a pre-call reference. |
| T-16-02 | Tampering | npm/pip/cargo installs | accept | No packages added (16-RESEARCH.md Package Legitimacy Audit: none). `flutter_test`, `provider`, and project helpers already vetted. No T-16-SC checkpoint needed. |
| T-16-S/I/R/D/E | Spoofing / Info-disclosure / Repudiation / DoS / Elevation | local UI + test code | accept | N/A — local-only personal app; this phase exposes no auth/session/crypto/input-validation surface (16-RESEARCH.md Security Domain: ASVS Step 2.6 skipped, no categories apply). |
</threat_model>

<verification>
After Task 1 and Task 2:
- `/home/dan/development/flutter/bin/flutter test test/screens/goal_form_priority_test.dart test/screens/goal_card_priority_chip_rebuild_test.dart` — both files green.
- `/home/dan/development/flutter/bin/flutter analyze` — no issues.

Phase gate (before /gsd-verify-work):
- `/home/dan/development/flutter/bin/flutter test` — full suite green (no regression in the 5 surviving goal_form_priority tests or anywhere else).
</verification>

<success_criteria>
1. PRIORITY-03: An automated widget test proves the priority chip reflects a goal's CURRENT priorityWeight after a reorder rebuild — correct label, no stale value — with zero production change (honoring D-01).
2. GOALFORM-02: The two `setSurfaceSize(800, 1200)` tests are replaced by true-modal-height (390x844 / initialChildSize 0.6) reachability tests covering time-target, outcome, AND habit goals, with the High-saves-0.75 and 3-hr-default assertions preserved.
3. The outcome-goal reachability test does genuine scroll work (Save off-screen on first open).
4. Full `flutter test` suite is green; `flutter analyze` is clean.
</success_criteria>

<output>
Create `.planning/phases/16-priority-model-reconciliation/16-01-SUMMARY.md` when done.
</output>
