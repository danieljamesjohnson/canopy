---
phase: 12-home-as-landing-schedule-as-plan
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - lib/router.dart
  - lib/screens/schedule/widgets/chunk_card.dart
  - lib/screens/schedule/widgets/now_marker.dart
  - lib/screens/schedule/schedule_screen.dart
  - test/screens/router_redirect_test.dart
  - test/screens/chunk_card_hover_test.dart
  - test/screens/chunk_card_goal_name_test.dart
autonomous: true
requirements: [NAV-01, SCHED-01, SCHED-02, SCHED-03]
must_haves:
  truths:
    - "Onboarding-done launch (cold and post-onboarding) lands on /home, not /goals"
    - "Every work chunk card shows a clock time range (e.g. '9:25 AM – 9:50 AM') for both commitment and discretionary chunks"
    - "Unresolved work chunks show always-visible labeled Complete and Skip buttons with no hover required"
    - "Resolved (completed/skipped) chunks show no action buttons, only the status icon"
    - "A NowMarker divider appears before the first unresolved work chunk at/after the current wall time"
  artifacts:
    - path: "lib/router.dart"
      provides: "post-onboarding redirect to /home (NAV-01)"
      contains: "return '/home'"
    - path: "lib/screens/schedule/widgets/chunk_card.dart"
      provides: "_formatTimeRange + clock-time secondary text + always-visible action buttons"
      contains: "_formatTimeRange"
    - path: "lib/screens/schedule/widgets/now_marker.dart"
      provides: "NowMarker StatelessWidget"
      contains: "class NowMarker"
    - path: "lib/screens/schedule/schedule_screen.dart"
      provides: "NowMarker insertion logic in ListView"
      contains: "NowMarker"
  key_links:
    - from: "lib/screens/schedule/widgets/chunk_card.dart"
      to: "ScheduleNotifier.markComplete / markSkipped"
      via: "context.read in button onPressed"
      pattern: "markComplete|markSkipped"
    - from: "lib/screens/schedule/widgets/chunk_card.dart"
      to: "ScheduledChunk.displayStartMinutes"
      via: "_formatTimeRange(displayStartMinutes, displayStartMinutes + durationMinutes)"
      pattern: "displayStartMinutes"
    - from: "lib/screens/schedule/schedule_screen.dart"
      to: "NowMarker"
      via: "ListView child insertion at nowMarkerIndex"
      pattern: "NowMarker\\(\\)"
---

<objective>
Make the schedule screen read as a real timed plan: land on Home after onboarding (NAV-01), show a clock time range on every work chunk (SCHED-01 UI), replace the hover-gated action overlay with always-visible labeled Complete/Skip buttons (SCHED-03), and insert a "Now" marker anchored to the current time (SCHED-02). This plan owns every schedule/ and router file; the Home active-day view is plan 12-03 (file-disjoint, same wave).

Purpose: The dogfood found discretionary chunks displaying frequency metadata ("5x/week") instead of clock times, an unlabeled hover-only action affordance that is invisible on touch, and an app that lands on Goals instead of Home. This plan fixes all four schedule/navigation legibility gaps.

Output: one-word router redirect fix, `_formatTimeRange` helper + clock-time display in `ChunkCard`, always-visible `FilledButton.icon`/`OutlinedButton.icon` action row, new `NowMarker` widget inserted in `ScheduleScreen`, and updated tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-home-as-landing-schedule-as-plan/12-RESEARCH.md
@.planning/phases/12-home-as-landing-schedule-as-plan/12-PATTERNS.md
@.planning/phases/12-home-as-landing-schedule-as-plan/12-UI-SPEC.md
@.planning/phases/12-home-as-landing-schedule-as-plan/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Router redirect to /home (NAV-01) + update router_redirect_test</name>
  <read_first>
    - lib/router.dart (the file being modified — redirect guard at line 33: `if (onboardingDone && onOnboarding) return '/goals';`)
    - test/screens/router_redirect_test.dart (the test being updated — assertion at ~line 170 expects '/goals')
    - .planning/phases/12-home-as-landing-schedule-as-plan/12-RESEARCH.md (Pitfall 3 — exact test changes)
  </read_first>
  <behavior>
    - When onboarding is complete and the location is /onboarding, the redirect resolves to /home (not /goals)
    - Cold launch with onboarding complete resolves the initial location to /home
    - The not-onboarded guard (`return '/onboarding'`) is unchanged
  </behavior>
  <action>
    In lib/router.dart change the single redirect line `if (onboardingDone && onOnboarding) return '/goals';` to `return '/home';` per NAV-01. No other router changes — imports and route table stay identical.

    In test/screens/router_redirect_test.dart update the existing assertion (~line 170) that expects `/goals` for the onboarding-done → onOnboarding case to expect `/home`. Add a new test case "onboardingComplete=true → /onboarding redirects to /home" if not already covered, and a cold-launch case asserting the initial resolved location is /home when onboarding is complete. Keep all other existing assertions intact.
  </action>
  <verify>
    <automated>/home/dan/development/flutter/bin/flutter test test/screens/router_redirect_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "return '/home'" lib/router.dart` returns at least 1
    - `grep -c "return '/goals'" lib/router.dart` returns 0
    - `flutter test test/screens/router_redirect_test.dart` passes, including the new /home assertions
  </acceptance_criteria>
  <done>Router lands onboarding-done users on /home; the redirect test asserts /home for both the redirect and cold-launch cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clock-time display + always-visible Complete/Skip buttons in ChunkCard (SCHED-01, SCHED-03) + rewrite hover test</name>
  <read_first>
    - lib/screens/schedule/widgets/chunk_card.dart (the file being modified — _formatMinutes lines 12-18; _HoverableChunkContent _hovered state + MouseRegion onEnter/onExit; secondary-text block ~lines 249-268; AnimatedOpacity overlay ~lines 300-326)
    - lib/data/models/scheduled_chunk.dart (displayStartMinutes getter added in 12-01)
    - lib/providers/schedule_notifier.dart (markComplete / markSkipped signatures, ~lines 157, 229 — idempotent, take chunkId)
    - test/screens/chunk_card_hover_test.dart (the test being rewritten — currently asserts AnimatedOpacity opacity 0→1 on hover)
    - test/screens/chunk_card_goal_name_test.dart (the test being extended — pump pattern for ChunkCard with a goal name)
    - .planning/phases/12-home-as-landing-schedule-as-plan/12-PATTERNS.md (§ chunk_card.dart — exact _formatTimeRange, secondary-text, and action-row code)
    - .planning/phases/12-home-as-landing-schedule-as-plan/12-UI-SPEC.md (§ Clock Time Contract, § Chunk Action Contract)
  </read_first>
  <behavior>
    - A work chunk with displayStartMinutes set shows a "START – END" range string (e.g. "9:25 AM – 9:50 AM") as secondary text
    - A work chunk with displayStartMinutes == null shows the duration fallback ("25 min")
    - An unresolved work chunk renders a FilledButton.icon "Complete" (Icons.check_circle_outline) and an OutlinedButton.icon "Skip" (Icons.skip_next_outlined) with no hover gesture required
    - A resolved (completed or skipped) chunk renders neither action button, only its status icon
    - Tapping Complete calls ScheduleNotifier.markComplete; tapping Skip calls markSkipped
    - The rationale secondary text still renders below the clock time when present
  </behavior>
  <action>
    In lib/screens/schedule/widgets/chunk_card.dart: keep `_formatMinutes` and add `_formatTimeRange(int startMin, int endMin)` returning `'${_formatMinutes(startMin)} – ${_formatMinutes(endMin)}'` per SCHED-01 (use the en-dash "–" as in 12-PATTERNS.md). Replace the existing secondary-text block so that when `chunk.displayStartMinutes != null` it shows `_formatTimeRange(chunk.displayStartMinutes!, chunk.displayStartMinutes! + chunk.durationMinutes)`, else the `'${chunk.durationMinutes} min'` fallback; keep the rationale text rendering below it when goalName and displayRationale are present. Style: bodySmall, onSurfaceVariant (per UI-SPEC Typography).

    Remove the hover overlay entirely (SCHED-03): delete the `_hovered` bool, the MouseRegion `onEnter`/`onExit` callbacks, and the `Positioned` + `AnimatedOpacity` icon overlay block. Retain `MouseRegion` only with `cursor: SystemMouseCursors.click` (cursor change, no content reveal). Add an always-visible action row, rendered only when `!isResolved`, below the main content: a `FilledButton.icon` (Icons.check_circle_outline, label "Complete", tooltip "Complete", onPressed → `context.read<ScheduleNotifier>().markComplete(chunk.id)`, `visualDensity: VisualDensity.compact`) and an `OutlinedButton.icon` (Icons.skip_next_outlined, label "Skip", tooltip "Skip", onPressed → `markSkipped(chunk.id)`, compact, foreground/border = `colorScheme.error`). Resolved chunks keep the existing opacity-0.5 + status-icon treatment and render no buttons. Do not touch SwipeableChunkCard/Dismissible — swipe gestures coexist with the buttons per UI-SPEC.

    Rewrite test/screens/chunk_card_hover_test.dart to the always-visible pattern (the old AnimatedOpacity assertions will no longer find their target): assert `find.widgetWithText(FilledButton, 'Complete')` is findsOneWidget on an unresolved chunk with no hover, the same for `OutlinedButton` "Skip", and that a resolved chunk finds no FilledButton/OutlinedButton. Extend test/screens/chunk_card_goal_name_test.dart with two cases: a chunk with displayStartMinutes set renders the expected "START – END" range text, and a chunk with displayStartMinutes null renders the duration fallback.
  </action>
  <verify>
    <automated>/home/dan/development/flutter/bin/flutter test test/screens/chunk_card_hover_test.dart test/screens/chunk_card_goal_name_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '_formatTimeRange' lib/screens/schedule/widgets/chunk_card.dart` returns at least 2 (def + call site)
    - `grep -c 'AnimatedOpacity' lib/screens/schedule/widgets/chunk_card.dart` returns 0
    - `grep -c 'FilledButton.icon' lib/screens/schedule/widgets/chunk_card.dart` returns at least 1
    - `grep -c 'OutlinedButton.icon' lib/screens/schedule/widgets/chunk_card.dart` returns at least 1
    - `flutter test test/screens/chunk_card_hover_test.dart test/screens/chunk_card_goal_name_test.dart` passes
  </acceptance_criteria>
  <done>ChunkCard shows clock-time ranges with a duration fallback, exposes always-visible labeled Complete/Skip buttons on unresolved chunks (none on resolved), the hover overlay is gone, and both tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: NowMarker widget + insertion in ScheduleScreen (SCHED-02)</name>
  <read_first>
    - lib/screens/schedule/schedule_screen.dart (the file being modified — ListView children for-loop ~line 93; _buildSwipeableCard; _buildSkippedSection; activeChunks computation)
    - lib/screens/home/widgets/review_banner.dart (analog for a thin StatelessWidget that reads theme with no provider state)
    - .planning/phases/12-home-as-landing-schedule-as-plan/12-PATTERNS.md (§ NowMarker widget — full widget source; § schedule_screen.dart — insertion logic)
    - .planning/phases/12-home-as-landing-schedule-as-plan/12-UI-SPEC.md (§ Component Inventory #5 NowMarker; § States and Transitions)
    - .planning/phases/12-home-as-landing-schedule-as-plan/12-RESEARCH.md (Open Question 1 — omit marker when all work chunks resolved)
  </read_first>
  <behavior>
    - NowMarker is a StatelessWidget rendering a thin divider with a "Now" label and a primary-colored dot
    - In ScheduleScreen the marker is inserted before the first unresolved work chunk whose displayStartMinutes >= current wall minutes, or before the first unresolved work chunk overall if none qualify
    - When all work chunks are resolved (no unresolved work chunk), no NowMarker is rendered
    - The marker is decorative (ExcludeSemantics) and does not break existing skipped-section rendering
  </behavior>
  <action>
    Create lib/screens/schedule/widgets/now_marker.dart as the StatelessWidget from 12-PATTERNS.md § NowMarker — a Row with a 40dp lead line, an 8dp primary-colored circle, the "Now" label (labelMedium, primary, w600), and an Expanded trailing line; wrapped in ExcludeSemantics; margins `EdgeInsets.symmetric(horizontal: 16, vertical: 4)`; colors from `Theme.of(context).colorScheme.primary` (no hardcoded colors).

    In lib/screens/schedule/schedule_screen.dart import the new widget and replace the simple ListView children for-loop with the insertion logic from 12-PATTERNS.md: compute `nowMinutes = DateTime.now().hour * 60 + DateTime.now().minute` and a nullable `nowMarkerIndex` once per build by scanning activeChunks for the first unresolved work chunk (fallback = first unresolved; final = first whose `displayStartMinutes ?? 9999 >= nowMinutes`). Build the children list inserting `const NowMarker()` before the chunk at `nowMarkerIndex`. Per RESEARCH Open Question 1, when there is no unresolved work chunk leave `nowMarkerIndex` null so no marker renders. Preserve the existing skipped-section append at the end of the list.
  </action>
  <verify>
    <automated>/home/dan/development/flutter/bin/flutter analyze lib/screens/schedule/widgets/now_marker.dart lib/screens/schedule/schedule_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'class NowMarker' lib/screens/schedule/widgets/now_marker.dart` returns 1
    - `grep -c 'NowMarker' lib/screens/schedule/schedule_screen.dart` returns at least 2 (import + insertion)
    - `grep -c 'nowMarkerIndex' lib/screens/schedule/schedule_screen.dart` returns at least 2
    - `flutter analyze` reports zero issues on both files
    - Full suite `/home/dan/development/flutter/bin/flutter test` is green
  </acceptance_criteria>
  <done>NowMarker widget exists and is inserted before the first unresolved work chunk in ScheduleScreen, omitted when all chunks are resolved, with no analyze issues.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user tap (button/swipe) → ScheduleNotifier mutation | Complete/Skip actions mutate persisted schedule state |
| router redirect guard → navigation target | Onboarding state determines landing route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering | markComplete/markSkipped on chunk id | accept | Existing notifier methods are idempotent and guard `if (chunk == null \|\| chunk.isCompleted) return` (RESEARCH Security Domain); buttons reuse the same validated call site as swipe — no new mutation path |
| T-12-04 | Elevation of Privilege | router redirect change | accept | Single-user local app with no privileged routes; redirect only swaps /goals→/home for already-onboarded users; not-onboarded guard unchanged |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No packages added this phase; all widgets built from Material 3 primitives already in pubspec |

Spoofing, Repudiation, Information Disclosure, and DoS are N/A — local-first single-user UI rework with no network, auth, or session surface.
</threat_model>

<verification>
- `flutter analyze` clean on all four modified/new source files
- `flutter test test/screens/router_redirect_test.dart test/screens/chunk_card_hover_test.dart test/screens/chunk_card_goal_name_test.dart` green
- Full suite green at wave merge
- Manual (per 12-VALIDATION.md): confirm Now marker anchors to current time across clock states; confirm Complete/Skip render without hover on both mouse and touch surfaces
</verification>

<success_criteria>
- Onboarding-done launches land on /home (NAV-01)
- Every work chunk shows a clock-time range with a duration fallback (SCHED-01)
- Unresolved chunks show always-visible labeled Complete/Skip buttons; resolved chunks show none; hover overlay removed (SCHED-03)
- NowMarker renders before the first unresolved work chunk, omitted when all resolved (SCHED-02)
- All affected tests pass
</success_criteria>

<output>
Create `.planning/phases/12-home-as-landing-schedule-as-plan/12-02-SUMMARY.md` when done. Record: confirmation the hover overlay is fully removed (not just opacity-0), the NowMarker omission behavior when all chunks resolved, and that swipe gestures still coexist with the new buttons.
</output>

## Artifacts this phase produces (Plan 02 contribution)

- `lib/router.dart` redirect now returns `/home` for onboarding-done users (NAV-01)
- `_formatTimeRange(int startMin, int endMin)` helper in `chunk_card.dart`
- Clock-time secondary-text display reading `chunk.displayStartMinutes` in `ChunkCard`, with `'${durationMinutes} min'` fallback
- Always-visible `FilledButton.icon` "Complete" + `OutlinedButton.icon` "Skip" action row in `ChunkCard` (replaces removed `_hovered`/`AnimatedOpacity` hover overlay)
- New `NowMarker` StatelessWidget (`lib/screens/schedule/widgets/now_marker.dart`)
- `nowMarkerIndex` insertion logic in `ScheduleScreen.build`
- Rewritten `test/screens/chunk_card_hover_test.dart` (always-visible assertions) + extended `chunk_card_goal_name_test.dart` (clock-time) + updated `router_redirect_test.dart` (/home)
