---
phase: 10-close-the-day
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - lib/services/notification_service.dart
  - lib/providers/settings_notifier.dart
  - lib/screens/home/widgets/end_of_day_card.dart
  - lib/screens/home/home_screen.dart
  - lib/screens/settings/settings_screen.dart
  - lib/main.dart
  - test/end_of_day_card_test.dart
autonomous: true
requirements: [CLOSE-01]
user_setup: []
must_haves:
  truths:
    - "After ~6pm OR once >=50% of today's work chunks are resolved, the Home active-schedule view shows a dismissible end-of-day card that routes to the existing /summary screen"
    - "The card is not shown on the empty-state (no schedule today) branch, and it can be dismissed for the session (in-memory)"
    - "Settings has an opt-in evening-reminder toggle (default OFF) that schedules/cancels a notification on id 2 with the desktop/web zonedSchedule guard"
    - "When the evening reminder is enabled, it is (re)scheduled idempotently on app start"
  artifacts:
    - path: "lib/screens/home/widgets/end_of_day_card.dart"
      provides: "EndOfDayCard widget (Dismissible + primaryContainer card + Close the day CTA)"
      contains: "class EndOfDayCard"
    - path: "lib/services/notification_service.dart"
      provides: "scheduleEveningReminder + cancelEveningReminder on id 2"
      contains: "scheduleEveningReminder"
    - path: "lib/providers/settings_notifier.dart"
      provides: "eveningReminderEnabled/eveningReminderMinutes getters + setEveningReminderEnabled setter"
      contains: "setEveningReminderEnabled"
  key_links:
    - from: "lib/screens/home/home_screen.dart"
      to: "lib/screens/home/widgets/end_of_day_card.dart"
      via: "conditional insertion after ReviewBanner; trigger = hour>=18 OR resolved/total>=0.5"
      pattern: "EndOfDayCard"
    - from: "lib/screens/home/widgets/end_of_day_card.dart"
      to: "/summary"
      via: "context.push('/summary') on the Close the day button"
      pattern: "/summary"
    - from: "lib/main.dart"
      to: "lib/services/notification_service.dart scheduleEveningReminder"
      via: "app-start scheduling when SettingsNotifier.eveningReminderEnabled is true"
      pattern: "scheduleEveningReminder"
---

<objective>
Deliver the discoverable end-of-day moment and the opt-in evening reminder (CLOSE-01).

Add `scheduleEveningReminder`/`cancelEveningReminder` on notification id 2 (mirroring the mid-day nudge,
including the desktop/web guard), expose evening-reminder getter/setter on SettingsNotifier (hydrating
the HiveFields added in Plan 01), build the `EndOfDayCard` widget and insert it into Home's
active-schedule branch with the 6pm-OR-≥50%-resolved trigger routing to the existing `/summary` screen,
add the Settings opt-in toggle row, and (re)schedule the reminder on app start when enabled.

Purpose: The end-of-day summary screen already exists but is undiscoverable. CLOSE-01 is the missing
discoverability layer — a time-aware Home card that invites the user to close the day, plus an opt-in
(default OFF) evening notification so the prompt reaches the user when the app is closed.
Output: evening notification methods, settings plumbing, EndOfDayCard + Home insertion, Settings toggle,
app-start scheduling, and a widget test for the trigger logic.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-close-the-day/10-CONTEXT.md
@.planning/phases/10-close-the-day/10-PATTERNS.md
@.planning/phases/10-close-the-day/10-UI-SPEC.md
@.planning/phases/10-close-the-day/10-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan produces:
- `NotificationService.scheduleEveningReminder(int minutesFromMidnight)` + `cancelEveningReminder()` — notification id 2
- `SettingsNotifier.eveningReminderEnabled` / `eveningReminderMinutes` getters, `setEveningReminderEnabled(bool)` setter, plus init() hydration
- `EndOfDayCard` widget — `lib/screens/home/widgets/end_of_day_card.dart`
- Home `_eodCardDismissed` state + `_shouldShowEodCard(...)` trigger + conditional insertion after ReviewBanner
- Settings "Evening reminder" ListTile row (opt-in, fixed 8:00pm, no time picker)
- main.dart app-start scheduling of the evening reminder when enabled
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Evening notification methods + SettingsNotifier plumbing + Settings toggle + app-start scheduling</name>
  <files>lib/services/notification_service.dart, lib/providers/settings_notifier.dart, lib/screens/settings/settings_screen.dart, lib/main.dart</files>
  <read_first>
    - lib/services/notification_service.dart (file being modified; `scheduleMidDayNudge` lines 132-173 + `cancelMidDayNudge` lines 183-185 on id 1 are the exact template; kIsWeb / Platform.isLinux|isWindows guard at lines 87-91)
    - lib/providers/settings_notifier.dart (file being modified; `_midDayNudgeEnabled`/`_midDayNudgeMinutes` field+getter lines 18-22, init() hydration lines 31-32, setter pattern lines 61-75)
    - lib/screens/settings/settings_screen.dart (file being modified; mid-day nudge ListTile lines 122-163; settings read in build() lines 59-62; `_formatMinutes`)
    - lib/main.dart (file being modified; where scheduleMorningNotification / mid-day scheduling is wired on app start)
  </read_first>
  <action>
    In notification_service.dart add `scheduleEveningReminder(int minutesFromMidnight)` by copying
    `scheduleMidDayNudge` verbatim and changing: `id: 1` → `id: 2`; android channel id `'midday_nudge'` →
    `'evening_reminder'`; channel name `'Mid-day nudge'` → `'Evening reminder'`; channel description →
    `'Opt-in evening schedule reminder'`; title → `'Canopy'`; body →
    `'Time to close the day. See how your chunks went.'`. Keep the `kIsWeb` and
    `Platform.isLinux || Platform.isWindows` guards and the `cancel(id: 2)`-then-`zonedSchedule`
    ordering. Add `cancelEveningReminder()` mirroring `cancelMidDayNudge` with `id: 2`.

    In settings_notifier.dart add `_eveningReminderEnabled = false` / `_eveningReminderMinutes = 1200`
    fields with public getters, hydrate them in `init()` as
    `settings?.eveningReminderEnabled ?? false` and `settings?.eveningReminderMinutes ?? 1200`, and add a
    `setEveningReminderEnabled(bool)` setter mirroring `setMidDayNudgeEnabled` (load-or-create AppSettings,
    set field, save, notifyListeners). No `setEveningReminderMinutes` this phase (time is fixed at 1200).

    In settings_screen.dart add an "Evening reminder" ListTile immediately after the mid-day nudge row
    and before the Android battery note: leading `Icon(Icons.nights_stay_outlined)`, title
    `'Evening reminder'`, subtitle `eveningEnabled ? _formatMinutes(1200) : 'Opt-in reminder to close your day'`,
    trailing `Switch` whose onChanged calls `setEveningReminderEnabled(val)` then
    `NotificationService.scheduleEveningReminder(1200)` when true / `cancelEveningReminder()` when false,
    `onTap: null` (no time picker). Add `final eveningEnabled = settings.eveningReminderEnabled;` alongside
    the existing mid-day locals in build().

    In main.dart, where the morning/mid-day notifications are (re)scheduled on app start, add an
    idempotent call: when `settingsNotifier.eveningReminderEnabled` is true, call
    `NotificationService.scheduleEveningReminder(settingsNotifier.eveningReminderMinutes)` (the
    cancel-then-schedule inside the method makes it idempotent). Follow the existing app-start scheduling
    placement and await ordering.
  </action>
  <verify>
    <automated>cd /home/dan/CodeProjects/canopy && PATH="/home/dan/development/flutter/bin:$PATH" flutter analyze && grep -q "scheduleEveningReminder" lib/services/notification_service.dart && grep -q "id: 2" lib/services/notification_service.dart && grep -q "setEveningReminderEnabled" lib/providers/settings_notifier.dart && grep -q "Evening reminder" lib/screens/settings/settings_screen.dart && grep -q "scheduleEveningReminder" lib/main.dart</automated>
  </verify>
  <acceptance_criteria>
    - notification_service.dart contains `scheduleEveningReminder` and `cancelEveningReminder` on `id: 2` with the kIsWeb + Linux/Windows guards
    - settings_notifier.dart contains `setEveningReminderEnabled` and init() hydration for the evening fields
    - settings_screen.dart contains an "Evening reminder" ListTile with a Switch wired to schedule/cancel, default OFF, onTap null
    - main.dart calls `scheduleEveningReminder` on app start when the toggle is enabled
    - `flutter analyze` reports zero issues
  </acceptance_criteria>
  <done>Evening reminder fully plumbed: id-2 schedule/cancel methods, settings persistence + opt-in toggle (default OFF, fixed 8pm), and idempotent app-start scheduling; analyze clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: EndOfDayCard widget + Home insertion with 6pm/50% trigger + widget test</name>
  <files>lib/screens/home/widgets/end_of_day_card.dart, lib/screens/home/home_screen.dart, test/end_of_day_card_test.dart</files>
  <read_first>
    - lib/screens/home/widgets/review_banner.dart (full file, 79 lines — exact structural analog: Dismissible + Card(primaryContainer) + title Row with close IconButton + subtitle + ElevatedButton)
    - lib/screens/home/home_screen.dart (file being modified; `_bannerDismissed` state line 43; ReviewBanner insertion lines 103-107; active-schedule branch ~line 74; `hasScheduleToday` guard)
    - lib/data/models/scheduled_chunk.dart (chunkType / isCompleted / isSkipped / isDeferred used by the trigger + subtitle count)
    - .planning/phases/10-close-the-day/10-UI-SPEC.md (Component Inventory + Copywriting + Interaction contract for the card — authoritative)
  </read_first>
  <action>
    Create lib/screens/home/widgets/end_of_day_card.dart with a `StatelessWidget EndOfDayCard` copying
    review_banner.dart's structure verbatim and substituting per the UI-SPEC: constructor params
    `required List<ScheduledChunk> chunks`, `required VoidCallback onDismiss`,
    `required VoidCallback onGoToSummary`; Dismissible key `const Key('end_of_day_card')`,
    `DismissDirection.horizontal`, `onDismissed: (_) => onDismiss()`; Card color
    `colorScheme.primaryContainer`, margin horizontal 16 / vertical 8; title Text `'How did today go?'`
    using `titleMedium?.copyWith(fontWeight: FontWeight.bold)`; close `IconButton(Icons.close)` with
    `tooltip: 'Dismiss'` calling `onDismiss`; subtitle Text computed `'$resolved of $total chunks done'`
    (resolved = work chunks where isCompleted||isSkipped||isDeferred; total = work chunks) using
    `bodySmall?.copyWith(color: colorScheme.onSurfaceVariant)`; trailing `ElevatedButton` label
    `'Close the day'` calling `onGoToSummary`.

    In home_screen.dart add `bool _eodCardDismissed = false;` alongside `_bannerDismissed`, add a
    `_shouldShowEodCard(List<ScheduledChunk> chunks)` helper returning true when
    `DateTime.now().hour >= 18` OR (work chunks non-empty AND resolved/total >= 0.5), and insert
    `if (!_eodCardDismissed && _shouldShowEodCard(schedule.chunks)) EndOfDayCard(chunks: schedule.chunks,
    onDismiss: () => setState(() => _eodCardDismissed = true), onGoToSummary: () => context.push('/summary'))`
    immediately after the ReviewBanner conditional block — inside the active-schedule branch only, so it
    never renders on the empty/no-schedule-today branch.

    Create test/end_of_day_card_test.dart as a widget test: pump a minimal MaterialApp hosting an
    EndOfDayCard with a known chunk list; assert the title 'How did today go?', the computed
    'X of Y chunks done' subtitle, and the 'Close the day' button render; tapping the close IconButton
    fires onDismiss; tapping 'Close the day' fires onGoToSummary. Also unit-test the 6pm/50% trigger logic
    (extract `_shouldShowEodCard` to a testable top-level/static function if the widget test cannot reach
    it directly) for: hour>=18 true regardless of resolution; <18 with <50% resolved false; <18 with
    >=50% resolved true; empty work-chunk list with hour<18 false.
  </action>
  <verify>
    <automated>cd /home/dan/CodeProjects/canopy && PATH="/home/dan/development/flutter/bin:$PATH" flutter test test/end_of_day_card_test.dart && PATH="/home/dan/development/flutter/bin:$PATH" flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - end_of_day_card.dart contains `class EndOfDayCard`, the title 'How did today go?', the 'Close the day' button, and a dismiss IconButton with `tooltip: 'Dismiss'`
    - home_screen.dart contains `_eodCardDismissed`, a 6pm-OR-50%-resolved trigger, and an EndOfDayCard insertion routing to '/summary' inside the active-schedule branch only
    - test/end_of_day_card_test.dart asserts the card renders, the X/Y subtitle, and both callbacks fire; trigger logic test covers hour>=18, <50%, >=50%, and empty cases
    - `flutter test test/end_of_day_card_test.dart` exits 0; `flutter analyze` clean
  </acceptance_criteria>
  <done>The end-of-day card renders on Home when it is after 6pm or >=50% of work chunks are resolved, routes to the existing /summary, is dismissible in-session, never shows on the empty state; widget + trigger tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS notification scheduler → app | OS-scheduled local notification on id 2; no remote payload, no network. |
| user-tapped card → go_router /summary | Local navigation only; /summary reads synchronously from Provider. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Denial of Service (crash on unsupported platform) | scheduleEveningReminder zonedSchedule | mitigate | kIsWeb and `Platform.isLinux || Platform.isWindows` early-return guards copied from the mid-day nudge prevent zonedSchedule crashes on desktop/web; iOS/Android are the real targets. |
| T-10-06 | Information disclosure | evening notification body text | accept | Static, non-sensitive copy ("Time to close the day...") — no PII, no schedule contents in the notification. Low risk. |

Note: Local-only personal app, no auth/network/PII. The only genuine consideration is the desktop/web schedule guard (T-10-05), which is mitigated by reusing the established guard. No high-severity threats to block on. On-device iOS evening-notification firing is a manual UAT item (no iOS simulator on Linux).
</threat_model>

<verification>
- `flutter analyze` reports zero issues.
- `flutter test test/end_of_day_card_test.dart` passes (render, callbacks, trigger logic).
- Manual UAT (deferred to phase-final human verify): on iOS, enabling the toggle fires the evening notification at ~8pm; tapping the Home card opens /summary.
</verification>

<success_criteria>
- After ~6pm OR ≥50% of work chunks resolved, Home shows the dismissible end-of-day card routing to /summary; never on the empty state (CLOSE-01 card).
- Settings has an opt-in (default OFF) evening-reminder toggle scheduling/cancelling id 2 with the desktop/web guard; reminder is (re)scheduled idempotently on app start (CLOSE-01 reminder).
</success_criteria>

<output>
Create `.planning/phases/10-close-the-day/10-03-SUMMARY.md` when done.
</output>
