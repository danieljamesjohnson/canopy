---
phase: 24-where-am-i
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - lib/screens/today/today_screen.dart
  - test/screens/today_screen_test.dart
  - .planning/phases/24-where-am-i/24-VALIDATION.md
autonomous: true
requirements: [NOW-01, NOW-02]

must_haves:
  truths:
    - "A visible Now row appears in the rendered TodayScreen for PreStart, GapBeforeNext and DayComplete"
    - "No Now row appears when the live card is showing an Active chunk"
    - "The marker's gutter time is the same clock sample the edge-state header line came from"
    - "The rendered day list no longer shows 'Free until 8:00 AM' at 10:47 AM"
    - "The rendered day list still shows 'Free until 8:00 AM' at 6:00 AM"
    - "build() still contains exactly one _nowFn() call"
  artifacts:
    - path: "lib/screens/today/today_screen.dart"
      provides: "nowMinutes computed from the existing nowDt sample, threaded to buildTimeline; the fourth switch case rendering NowMarker"
      contains: "case NowMarkerRow"
    - path: "test/screens/today_screen_test.dart"
      provides: "Screen-level NOW-01 marker tests and the corrected NOW-02 assertions"
      contains: "NowMarker"
    - path: ".planning/phases/24-where-am-i/24-VALIDATION.md"
      provides: "Filled Per-Task Verification Map and nyquist_compliant: true"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "lib/screens/today/today_screen.dart build()"
      to: "buildTimeline"
      via: "nowMinutes derived from the existing nowDt local"
      pattern: "nowMinutes: nowMinutes"
    - from: "lib/screens/today/today_screen.dart _buildTimelineRow"
      to: "lib/screens/today/widgets/now_marker.dart"
      via: "the fourth exhaustive-switch case"
      pattern: "child: const NowMarker\\(\\)"
    - from: "lib/screens/today/today_screen.dart _buildTimelineRow"
      to: "screen readers"
      via: "Semantics label at the call site"
      pattern: "label: 'Now — \\$\\{formatMinutes\\(minutes\\)\\}'"
---

<objective>
Wire plan 24-01's row model to the screen: compute `nowMinutes` from the clock sample `build()`
already takes, thread it into `buildTimeline`, add the one new case to the codebase's single
exhaustive `TimelineRow` switch, and prove the whole thing at screen level — including correcting
the existing test that currently pins the NOW-02 bug as expected behaviour.

**Locked decisions this plan implements** (from `24-CONTEXT.md` → `### Locked`, referenced as
D-01/D-02 for traceability):

- **D-01 — position, not a second detector.** `nowMinutes` MUST be derived from the existing
  `nowDt` local at `today_screen.dart:931`. `build()` must still contain exactly one `_nowFn()`
  call after this plan; no `DateTime.now()` may be added anywhere. This is the discipline WR-01
  (commit `48af6bf`) established for `_liveSecondsRemaining` and that Phases 17/22 plus a 22 code
  review spent real effort defending — three competing detectors have already been killed here.
- **D-02 — show the marker when it adds information.** Rendering follows whatever `buildTimeline`
  emitted; no suppression logic is duplicated in the widget layer.

**Explicitly out of scope, do not touch** (`24-CONTEXT.md` `## Phase Boundary`): the live row's own
styling (`_buildLiveRow`, `_liveKicker`, `LiveRowCard`, settled in Phase 23 and signed off by Dan),
the mood chip, `ScheduleProgressBar`, the PreStart/GapBeforeNext/Overdue/DayComplete header copy
(~lines 417-449), and `resolveNowState`'s classification logic.

Purpose: this is the plan that actually makes Dan's screen answer "where am I".
Output: a rendered now-marker, a truthful leading free row, and a green full suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-where-am-i/24-CONTEXT.md
@.planning/phases/24-where-am-i/24-UI-SPEC.md
@.planning/phases/24-where-am-i/24-PATTERNS.md
@.planning/phases/24-where-am-i/24-VALIDATION.md
@.planning/phases/24-where-am-i/24-01-SUMMARY.md
@CLAUDE.md
</context>

<environment>
`flutter` and `dart` are NOT on the default PATH in non-login shells. Every command below assumes
`export PATH="$PATH:/home/dan/development/flutter/bin"` (or the absolute
`/home/dan/development/flutter/bin/flutter`). Run all commands from `/home/dan/CodeProjects/canopy`.

Quick command (24-VALIDATION.md):
`flutter test test/screens/today_timeline_model_test.dart test/screens/today_screen_test.dart test/screens/today_row_widgets_test.dart`
</environment>

<tasks>

<task type="auto">
  <name>Task 1: Thread nowMinutes from the single clock sample and render the marker</name>

  <files>lib/screens/today/today_screen.dart</files>

  <read_first>
    - `lib/screens/today/today_screen.dart` lines 543-600 — the `// ── The day — row dispatch ──`
      comment block, `_buildTimelineRow`'s signature (550-555), and the existing
      `LeadingFreeRow` / `GapFreeRow` / `ChunkRow` cases (556-590) showing the
      `TimelineRowTile`-wrapping convention.
    - `lib/screens/today/today_screen.dart` lines 915-970 — the clock-threading comment block
      (922-930), `final nowDt = _nowFn();` (931), `resolveNowState` (932),
      `_liveSecondsRemaining` (933), `_syncFastTimer` (938-940), the `buildTimeline` call
      (941-944), and the `hasLiveRow` centre-on-open block (951-968).
    - `lib/screens/today/today_screen.dart` lines 1-32 — the import block; `../../utils/time_format.dart`
      is already imported at line 21 (so `minutesOfDay` and `formatMinutes` need no new import),
      and the `widgets/` imports show where `widgets/now_marker.dart` belongs alphabetically.
    - `lib/screens/today/timeline.dart` (post-24-01) — `NowMarkerRow`'s exact field name and
      `buildTimeline`'s new parameter name.
    - `lib/screens/today/widgets/now_marker.dart` (created in 24-01) — confirms `NowMarker` is a
      const-constructible zero-arg widget.
    - `lib/screens/today/widgets/timeline_row_tile.dart` — confirms passing a non-null
      `startMinutes` is what renders the compact gutter time.
    - `.planning/phases/24-where-am-i/24-UI-SPEC.md` — the `Semantics` row of the Visual treatment
      table and the Copywriting Contract's locked semantics string.
    - `.planning/phases/24-where-am-i/24-PATTERNS.md` § 2 — the verified current text of both
      blocks being edited.
  </read_first>

  <action>
1. In `build()`, immediately after `final nowDt = _nowFn();` (line 931), add
   `final nowMinutes = minutesOfDay(nowDt);`. It MUST read the existing `nowDt` local (D-01). Do
   NOT add a second `_nowFn()` call, do NOT call `DateTime.now()`, and do NOT recompute the formula
   inline — `minutesOfDay` comes from `../../utils/time_format.dart`, already imported at line 21.

2. Update the comment block at lines 922-930. It currently opens with
   `The only two "what is happening now" calls on this screen` and describes `nowDt` being threaded
   into two consumers. Change it to name **three** consumers of the one sample — `resolveNowState`,
   `_liveSecondsRemaining`, and `nowMinutes` → `buildTimeline` — and keep the WR-01 rationale
   sentence ("a single clock read per build, so the consumers can never straddle a second/minute
   boundary and disagree"). Add that the marker is a *position* derived from that same sample and
   never a second opinion about which chunk is current (D-01).

3. Pass the new argument to the existing `buildTimeline` call at lines 941-944, so it reads
   `buildTimeline(chunks: schedule.chunks, nowState: nowState, nowMinutes: nowMinutes)`.

4. Add exactly one new `case` to `_buildTimelineRow`'s switch at line 556 — the only exhaustive
   switch over `TimelineRow` in the entire tree, so this is the whole render cost. Place it after
   the `GapFreeRow` case and before the `ChunkRow` case:

   `case NowMarkerRow(:final minutes):` returning
   `TimelineRowTile(startMinutes: minutes, child: Semantics(label: 'Now — ${formatMinutes(minutes)}', excludeSemantics: true, child: const NowMarker()))`.

   Details that are locked, not discretionary:
   - `startMinutes: minutes` (NOT `null`) — this is what makes `TimelineRowTile` render the current
     compact time in the 52dp gutter, which is why `NowMarker`'s own visible label carries no time.
   - The `Semantics` node lives **here at the call site**, not inside `NowMarker` (24-UI-SPEC.md).
   - The separator in the label is an em dash `—` (U+2014) with a single space either side, exactly
     `'Now — ${formatMinutes(minutes)}'`, e.g. `Now — 12:34 PM`. Not a hyphen, not an en dash.
   - `excludeSemantics: true` so the row announces once instead of also reading the bare `Now`
     `Text` underneath.

5. Add `import 'widgets/now_marker.dart';` to the `widgets/` import group, in alphabetical position
   (after `widgets/live_row_card.dart`, before `widgets/review_banner.dart` — confirm against the
   actual block when you read it).

6. Do NOT add a `default:` branch to the switch, do NOT add an `if (row is NowMarkerRow)` check
   anywhere else in the file, and do NOT change `hasLiveRow` at line 951 — `NowMarkerRow` is not a
   `ChunkRow`, so that predicate and the centre-on-open one-shot are unaffected by construction.

7. Change nothing in `_buildLiveRow`, `_liveKicker`, `_liveSecondsRemaining`, `_syncFastTimer`, the
   mood chip, `ScheduleProgressBar`, or the edge-state header copy around lines 417-449 — all
   locked out of scope by `24-CONTEXT.md`.

**Expected intermediate state:** after this task, exactly one test fails —
`test/screens/today_screen_test.dart`'s `'"Free until 8:00 AM" precedes the first activity'` (line
364), which asserts a closed 8:00 AM window is still advertised at a 10:47 AM clock. That failure
is NOW-02's own proof, not a regression; Task 2 corrects it. Do not "fix" it by weakening the
`timeline.dart` guard.
  </action>

  <verify>
    <automated>flutter analyze &amp;&amp; flutter test test/screens/today_timeline_model_test.dart test/screens/today_row_widgets_test.dart</automated>
  </verify>

  <acceptance_criteria>
    - `flutter analyze` prints `No issues found!`.
    - `flutter test test/screens/today_timeline_model_test.dart test/screens/today_row_widgets_test.dart` exits 0.
    - `git diff lib/screens/today/today_screen.dart | grep -c '^+.*_nowFn()'` returns `0`
      (no new clock read added — D-01).
    - `git diff lib/screens/today/today_screen.dart | grep -c '^+.*DateTime.now()'` returns `0`.
    - `git diff lib/screens/today/today_screen.dart | grep -c '^+.*default:'` returns `0`
      (the switch stays exhaustive with no default arm).
    - `grep -c "case NowMarkerRow" lib/screens/today/today_screen.dart` returns `1`.
    - `grep -c "nowMinutes: nowMinutes" lib/screens/today/today_screen.dart` returns `1`.
    - `grep -c "minutesOfDay(nowDt)" lib/screens/today/today_screen.dart` returns `1`.
    - `grep -c "child: const NowMarker()" lib/screens/today/today_screen.dart` returns `1`.
    - `grep -c "excludeSemantics: true" lib/screens/today/today_screen.dart` returns `1`.
    - `grep -c "Now — " lib/screens/today/today_screen.dart` returns `1` (em dash, U+2014).
    - `flutter test test/screens/today_screen_test.dart` fails on exactly one test, whose name
      contains `Free until 8:00 AM` — recorded as expected, corrected in Task 2.
  </acceptance_criteria>

  <done>
`TodayScreen` computes `nowMinutes` from its single existing clock sample, passes it to
`buildTimeline`, and renders `NowMarkerRow` through `TimelineRowTile` + `Semantics` + `NowMarker`;
`flutter analyze` is clean and the only failing test in the tree is the known stale NOW-02 assertion.
  </done>
</task>

<task type="auto">
  <name>Task 2: Screen-level marker tests and correction of the stale NOW-02 assertion</name>

  <files>test/screens/today_screen_test.dart</files>

  <read_first>
    - `test/screens/today_screen_test.dart` lines 140-195 — `_todayYmd()` and the shared
      `_pumpTodayScreen(tester, {required scheduleNotifier, DateTime Function()? now,
      RestorativesNotifier? restorativesNotifier})` helper. Every new test must go through this
      helper, not a hand-rolled `pumpWidget`.
    - `test/screens/today_screen_test.dart` lines 285-380 — the
      `Task 2 — the day as one list…` group: `buildDayFixture()` (c1 480 completed, c2 540 skipped,
      c3 645/5min, c4 650/25min, c5 780 commitment), the `pumpDay` helper hardcoded to
      `DateTime(2026, 8, 7, 10, 47)` at line 341, the sibling test at 346-352 proving c3 is the live
      chunk at 10:47, and **the stale test at 364-370** that must be corrected.
    - `test/screens/today_screen_test.dart` lines 575-660 — the correctly-shaped `pre-start`,
      `gap-before-next` and `day-complete` tests, whose fixtures and clocks the new marker tests
      reuse verbatim.
    - `lib/utils/time_format.dart` `formatMinutesCompact` — the gutter strings asserted below
      (360 → `6:00`, 570 → `9:30`, 1080 → `6:00p`).
    - `.planning/phases/24-where-am-i/24-VALIDATION.md` — the behavior contract these tests satisfy.
    - `.planning/phases/24-where-am-i/24-PATTERNS.md` § 6 — the landmine analysis of the stale test.
  </read_first>

  <action>
Add `import 'package:canopy/screens/today/widgets/now_marker.dart';` to the import block. Modify no
existing test other than the one named in step 1.

**1 — Correct the stale NOW-02 test (lines 364-370).** Do not delete it; split it into two tests in
the same group, both using `buildDayFixture()`:

  - `'"Free until 8:00 AM" precedes the first activity while that window is still open'` — calls
    `_pumpTodayScreen` **directly** (not `pumpDay`, which hardcodes 10:47) with
    `scheduleNotifier: _FakeScheduleNotifierWithSchedule(schedule)` built from `buildDayFixture()`
    and `now: () => DateTime(2026, 8, 7, 6, 0)`; asserts
    `find.textContaining('Free until 8:00 AM')` finds one widget.
  - `'the leading "Free until" row is gone once its window has closed (NOW-02)'` — calls
    `pumpDay(tester)` (10:47) and asserts `find.textContaining('Free until 8:00 AM')` finds nothing.

  **Do not add a marker-presence assertion at 10:47 with this fixture.** At 10:47 c3's 10:45–10:50
  window is open, so `resolveNowState` returns `Active` — the correct assertion for that clock/
  fixture pair is marker *absence*. Add `expect(find.byType(NowMarker), findsNothing);` to the
  second test as the screen-level proof of D-02's Active suppression.

**2 — Add a new group `Phase 24 — now-marker (NOW-01)`** placed after the existing `day-complete`
test (~line 659). Each test builds its own `DailySchedule(dateYmd: _todayYmd(), moodIndex: 3,
chunks: [...])` and pumps once via `_pumpTodayScreen`:

  - **PreStart shows the marker** — reuse the pre-start fixture and clock verbatim from lines
    575-599 (one `_workChunk(syntheticStartMinutes: 480, durationMinutes: 60)`,
    `now: () => DateTime(2026, 8, 7, 6, 0)`). Assert `find.byType(NowMarker)` finds one widget,
    `find.text('Now')` finds one widget, and `find.text('6:00')` finds one widget (the gutter time
    the marker's own `startMinutes` produced).
  - **GapBeforeNext shows the marker** — reuse the gap-before-next fixture and clock verbatim from
    lines 601-632 (c1 at 540/25min completed, c2 at 600/25min, `now: () => DateTime(2026, 8, 7, 9,
    30)`). Assert `find.byType(NowMarker)` finds one widget and `find.text('9:30')` finds one
    widget. This is the exact state from Dan's evidence screenshot.
  - **DayComplete shows the marker, last** — reuse the day-complete fixture and clock verbatim from
    lines 634-659 (one chunk at 480/60min, `now: () => DateTime(2026, 8, 7, 18, 0)`). Assert
    `find.byType(NowMarker)` finds one widget and `find.text('6:00p')` finds one widget.
  - **Active suppresses the marker** — `pumpDay(tester)` (10:47, the `Active` case) →
    `find.byType(NowMarker)` finds nothing while `find.byType(LiveRowCard)` finds one widget. This
    pins "the loud card answers it, so the quiet marker stands down" (D-02).
  - **Single-sample agreement (D-01)** — one pump of the GapBeforeNext fixture at
    `DateTime(2026, 8, 7, 9, 30)`, asserting in the same pump that `find.text('Up next')` (the
    header line the shared `nowState` produced) and `find.text('9:30')` (the marker's gutter,
    produced by `nowMinutes` from the same `nowDt`) are both present. One clock sample, two
    consumers, no disagreement possible.

**3 — Multi-clock discipline.** If any single `testWidgets` body pumps two different `now:` closures,
it must first force a full unmount with `await tester.pumpWidget(const SizedBox.shrink());` between
them. `TodayScreenState._nowFn` is `late final`, set once in `initState`, so a second
`_pumpTodayScreen` with a different clock is otherwise silently ignored (Phase 23-03 decision,
recorded in STATE.md). Prefer one clock per `testWidgets` to sidestep this entirely.

**4 — Do not** add assertions about the live row's styling, the mood chip, the progress bar or the
header copy beyond the `'Up next'` presence check named above — those surfaces are locked out of
scope for this phase.
  </action>

  <verify>
    <automated>flutter test test/screens/today_timeline_model_test.dart test/screens/today_screen_test.dart test/screens/today_row_widgets_test.dart</automated>
  </verify>

  <acceptance_criteria>
    - `flutter test test/screens/today_screen_test.dart` exits 0 with zero failures.
    - The quick command
      `flutter test test/screens/today_timeline_model_test.dart test/screens/today_screen_test.dart test/screens/today_row_widgets_test.dart`
      exits 0.
    - `grep -c "Free until 8:00 AM" test/screens/today_screen_test.dart` returns `2` — one
      window-open assertion and one window-closed assertion.
    - `grep -c "NOW-02" test/screens/today_screen_test.dart` is `>= 1`.
    - `grep -c "find.byType(NowMarker)" test/screens/today_screen_test.dart` is `>= 5`.
    - `grep -c "DateTime(2026, 8, 7, 10, 47)" test/screens/today_screen_test.dart` returns `1`
      (`pumpDay` remains the only 10:47 site — no duplicated clock literal drifted in).
    - `grep -c "Phase 24 — now-marker (NOW-01)" test/screens/today_screen_test.dart` returns `1`.
    - `git diff test/screens/today_screen_test.dart | grep -c "^-"` is `<= 12` — the only removals
      are the stale test's 7 lines plus the diff header, proving no other test was touched.
    - `flutter analyze` prints `No issues found!`.
  </acceptance_criteria>

  <done>
`TodayScreen` renders exactly one `NowMarker` for `PreStart`, `GapBeforeNext` and `DayComplete` and
none for `Active`; the marker's gutter time and the edge-state header agree in a single pump; the
leading free row is proven present before its window opens and absent after it closes; and the whole
quick command is green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — full suite, analyze, format, and close out 24-VALIDATION.md</name>

  <files>.planning/phases/24-where-am-i/24-VALIDATION.md</files>

  <read_first>
    - `.planning/phases/24-where-am-i/24-VALIDATION.md` (100 lines, full) — the frontmatter
      (`nyquist_compliant: false` at line 5), the `## Per-Task Verification Map` table at lines
      44-50 with its `TBD` Task ID / Plan cells and `⬜ pending` Status cells, the
      `## Manual-Only Verifications` table at 84-86, and the `## Validation Sign-Off` checklist at
      90-97.
    - `CLAUDE.md` § Commands — the canonical `flutter test` / `flutter analyze` / `dart format lib/`
      invocations for this repo.
  </read_first>

  <action>
Run the phase gate `24-VALIDATION.md` requires before `/gsd-verify-work`:

1. `flutter test` (full suite, ~90s) — must be fully green.
2. `flutter analyze` — must report `No issues found!`.
3. `dart format lib/` — if it reformats any of the four files this phase touched
   (`lib/utils/time_format.dart`, `lib/screens/today/now_state.dart`,
   `lib/screens/today/timeline.dart`, `lib/screens/today/widgets/now_marker.dart`,
   `lib/screens/today/today_screen.dart`), keep the reformat and re-run step 1. Do not reformat
   files this phase did not touch — revert any incidental reformatting elsewhere in `lib/`.

Then update `.planning/phases/24-where-am-i/24-VALIDATION.md`:

  - In the `## Per-Task Verification Map` table, replace each `TBD` in the `Task ID` and `Plan`
    columns with the real plan-and-task identifiers from this phase (`24-01 / Task 2`,
    `24-01 / Task 3`, `24-02 / Task 1`, `24-02 / Task 2`), correct each `Wave` cell to the plan's
    actual wave (24-01 → 1, 24-02 → 2), and flip every `| ⬜ pending |` status cell to
    `| ✅ green |`. Leave the `*Status: ⬜ pending · ✅ green · …*` legend line beneath the table
    unchanged — it is a key, not a row.
  - Tick the `## Validation Sign-Off` checkboxes that are now genuinely true: every task has an
    `<automated>` verify, no 3-consecutive-task gap without automated verification, Wave 0 N/A, no
    watch-mode flags, feedback latency under 20s.
  - Set `nyquist_compliant: true` in the frontmatter.
  - **Leave the `## Manual-Only Verifications` row untouched** — the "does the marker actually
    answer 'where am I' at a glance" judgement is plan 24-03's job and is not satisfied by any test
    run here. Do not mark it done.
  </action>

  <verify>
    <automated>flutter test &amp;&amp; flutter analyze</automated>
  </verify>

  <acceptance_criteria>
    - `flutter test` exits 0 with zero failing tests across the whole suite.
    - `flutter analyze` exits 0 and prints `No issues found!`.
    - `dart format --output=none --set-exit-if-changed lib/` exits 0.
    - `grep -c "TBD" .planning/phases/24-where-am-i/24-VALIDATION.md` returns `0`.
    - `grep -c "| ⬜ pending |" .planning/phases/24-where-am-i/24-VALIDATION.md` returns `0`.
    - `grep -c "nyquist_compliant: true" .planning/phases/24-where-am-i/24-VALIDATION.md` returns `1`.
    - `grep -c "Manual-Only Verifications" .planning/phases/24-where-am-i/24-VALIDATION.md` returns `1`
      and that section still describes the check as outstanding (24-03 has not run yet).
  </acceptance_criteria>

  <done>
Full `flutter test` green, `flutter analyze` clean, `lib/` formatted, and `24-VALIDATION.md` reflects
the real task IDs with every automated row marked green and `nyquist_compliant: true` — with the
manual perceptual check still open for plan 24-03.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local system clock → screen render | The only external input. Sampled exactly once per `build()` via the injectable `_nowFn` seam; not attacker-controlled (single-user local app, no network). |
| (none other) | This plan performs no I/O, accepts no user input, persists nothing, and installs no packages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Tampering (state integrity) | `TodayScreen.build()` clock sampling | mitigate | A second `_nowFn()`/`DateTime.now()` call would let the marker and the header straddle a minute boundary and disagree — the exact class of bug three prior detector-deletions targeted. Mitigated by deriving `nowMinutes` from the existing `nowDt` local, enforced by the Task 1 diff-grep criteria (`^+.*_nowFn()` and `^+.*DateTime.now()` both `0`) and by Task 2's single-pump agreement test. |
| T-24-05 | Tampering (render dispatch) | `_buildTimelineRow` switch | mitigate | Adding a `default:` arm would silently swallow a future fifth subtype. Mitigated by keeping the switch exhaustive with no default, enforced by the Task 1 `^+.*default:` criterion. |
| T-24-06 | Information disclosure | `Semantics(label: 'Now — …')` | accept | Announces only the local wall-clock time already rendered visibly in the gutter of every row. No PII, no new data surface. |
| T-24-SC | Tampering | supply chain (pub/npm/pip/cargo) | accept | No package is installed by this plan — `pubspec.yaml` is untouched. No install task exists, so no legitimacy checkpoint is required. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 24-02 portion)

| Symbol | Kind | Location | Notes |
|--------|------|----------|-------|
| `nowMinutes` (local) | local variable (new) | `TodayScreen.build()` in `lib/screens/today/today_screen.dart` | `final nowMinutes = minutesOfDay(nowDt);` — third consumer of the single `nowDt` sample |
| `case NowMarkerRow(:final minutes)` | switch arm (new) | `_buildTimelineRow` in `lib/screens/today/today_screen.dart` | Fourth and final arm of the codebase's only exhaustive `TimelineRow` switch |
| `'Now — <formatMinutes(minutes)>'` | semantics label string (new, locked) | `lib/screens/today/today_screen.dart` | Em dash U+2014; e.g. `Now — 12:34 PM` |
| `'"Free until 8:00 AM" precedes the first activity while that window is still open'` | test (new, replaces stale) | `test/screens/today_screen_test.dart` | Replaces the assertion that pinned the NOW-02 bug |
| `'the leading "Free until" row is gone once its window has closed (NOW-02)'` | test (new) | `test/screens/today_screen_test.dart` | The NOW-02 proof |
| `Phase 24 — now-marker (NOW-01)` | test group (new) | `test/screens/today_screen_test.dart` | 5 screen-level cases |

Phase-wide symbol list (24-01 + 24-02):
`minutesOfDay`, `NowMarkerRow`, `NowMarkerRow.minutes`, `buildTimeline`'s `nowMinutes` parameter,
`NowMarker`, `lib/screens/today/widgets/now_marker.dart` (new file), the `case NowMarkerRow` switch
arm, and the `Now — <time>` semantics label.
</artifacts_this_phase_produces>

<verification>
Run from `/home/dan/CodeProjects/canopy` with
`export PATH="$PATH:/home/dan/development/flutter/bin"`:

1. `flutter test` — full suite green.
2. `flutter analyze` — `No issues found!`.
3. `dart format --output=none --set-exit-if-changed lib/` — exits 0.
4. `git diff lib/screens/today/today_screen.dart | grep -c '^+.*_nowFn()'` returns `0` — D-01 held.
5. `grep -rn "resolveNowState" lib/ | wc -l` returns `2` (one definition, one call site) — still a
   single now-detector; a third occurrence means a parallel detector crept back in.
6. `git diff --stat lib/screens/today/widgets/live_row_card.dart lib/screens/schedule/widgets/schedule_progress_bar.dart`
   prints nothing — the Phase 23 live-row styling and the progress bar were not touched.
</verification>

<success_criteria>
- `TodayScreen` renders a `NowMarker` row for `PreStart`, `GapBeforeNext` and `DayComplete`, and
  none for `Active`.
- The marker's gutter shows the current compact time, sourced from the same `nowDt` sample the
  edge-state header used — proven in a single pump.
- `build()` contains exactly one `_nowFn()` call; no `DateTime.now()` was added.
- `_buildTimelineRow`'s switch has four arms and no `default:`.
- A leading "Free until 8:00 AM" row renders at 6:00 AM and does not render at 10:47 AM.
- Full `flutter test` green, `flutter analyze` clean, `lib/` formatted.
- `24-VALIDATION.md` has real task IDs, all automated rows green, `nyquist_compliant: true`, and the
  manual row still open.
</success_criteria>

<output>
Create `.planning/phases/24-where-am-i/24-02-SUMMARY.md` when done.
</output>
